# 121. Tab 0 느린 착지 오버슈팅 모션 작업로그

## 완료 내용

- 착지 구간을 760ms로 늘렸다.
- 이동 방향으로 미세하게 오버슈팅한 뒤, DOT별 위상·진폭이 다른 느린 감쇠 흔들림을 적용했다.
- 마지막 프레임에서는 흔들림 값이 0이 되어 정확한 배치 좌표에 정지한다.

## 확인

- `test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 파일의 CRLF 경고만 출력)
