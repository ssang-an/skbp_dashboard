# 112. Tab 0 불규칙 직사각형 노드 분포 작업 로그

## 완료 내용

- G6 Grid 레이아웃 대신 각 node에 직접 좌표를 지정했다.
- 카드 비율 기반의 밀도는 유지하면서 각 slot 안에서 결정론적 jitter를 적용해 불규칙한 분포를 만든다.
- 위치는 card inset으로 clamp하여 node가 직사각형 card 밖으로 나가지 않는다.
- force layout과 animation은 계속 사용하지 않아 대량 Listing node에서도 정지 상태로 가볍게 표시된다.

## 확인

- `DashboardInformationArchitectureTests.test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 vault의 CRLF 경고만 출력)
