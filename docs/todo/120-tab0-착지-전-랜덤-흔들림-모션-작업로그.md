# 120. Tab 0 착지 전 랜덤 흔들림 모션 작업로그

## 완료 내용

- 각 DOT에 결정론적 위상과 미세 진폭을 부여했다.
- 주 확산 모션 뒤 220ms 동안 감쇠하는 settle motion을 적용하고, 종료 시에는 정확한 최종 좌표에 고정했다.
- 무한 애니메이션이나 추가 렌더 루프는 만들지 않았다.

## 확인

- `test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 파일의 CRLF 경고만 출력)
