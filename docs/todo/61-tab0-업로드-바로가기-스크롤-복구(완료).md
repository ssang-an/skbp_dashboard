# 61. Tab 0 업로드 바로가기 스크롤 복구 (완료)

- Tab 0 활성 상태에서는 별도 후보 목록 Upload 패널(`#step0UploadPanel`)과 입력칸(`#step0PasteInput`)을 대상으로 이동하도록 수정했다.
- 기존 Tab 1·2 입력 패널도 명시적 ID로 선택하도록 정리했다.
- `scrollIntoView` 뒤 고정 헤더 높이를 제외한 `window.scrollTo` 보정을 추가해, 화면이 움직이지 않는 상황을 방지했다.
- 브라우저 캐시 무효화 버전을 갱신하고 정적 회귀 테스트를 추가했다.
