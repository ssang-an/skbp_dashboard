# 107. Tab 0 원형 노드 군집 정렬 작업 로그

- 각 단계 카드의 G6 렌더 영역을 카드 내부 최대 정사각형으로 계산했다.
- 정사각형 canvas를 카드 중앙에 배치하고, force layout도 같은 가로·세로 크기를 사용하도록 변경했다.
- 대량 Listing node를 포함한 모든 단계 cloud가 가로로 늘어지지 않고 원형에 가까운 군집으로 움직인다.
- JavaScript 문법, 대상 IA 회귀 테스트, diff whitespace 검사를 통과했다.
