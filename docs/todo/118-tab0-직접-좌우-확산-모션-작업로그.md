# 118. Tab 0 직접 좌우 확산 모션 작업 로그

## 완료 내용

- d3-force layout을 제거해 DOT이 왼쪽에 뭉치거나 최종 view가 축소돼 보이던 문제를 없앴다.
- G6 node의 entry/target 좌표를 직접 update하며, 460ms 동안 좌측 entry 영역에서 card 전역의 불규칙 target 위치로 이동한다.
- 짧은 rebound와 Listing → Fast Triage → Full Scout → Shortlisting의 300ms 순차 delay는 유지한다.
- 재렌더링·필터 변경 시 timeout과 requestAnimationFrame을 모두 취소한다.

## 확인

- `DashboardInformationArchitectureTests.test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 vault의 CRLF 경고만 출력)
