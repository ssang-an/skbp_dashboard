# 22. Tab0 GPT 지침 복사 pill 아이콘·명도 정리

## 목표

- Tab0 진척 현황의 GPT 지침 복사 pill을 밝고 조용한 Excel export 계열 UI로 조정한다.
- 콘텐츠 순서를 `복사 아이콘 → GPT → 지침 복사`로 정리한다.

## 작업 순서

1. 버튼 HTML의 아이콘 및 GPT label 순서를 변경한다.
2. 기본·hover 색상을 밝은 slate-blue 톤으로 조정하고 복사 아이콘 가독성을 확인한다.
3. JavaScript 문법과 diff를 확인하고 완료 로그·커밋을 남긴다.
