# 126. Tab 0 좌측 5% 출발과 카운트업 작업로그

## 완료 내용

- 각 카드의 DOT 시작 폭을 좌측 5%로 줄였다.
- Summary Dashboard의 Listing·Fast Triage·Full Scout·Shortlisting 총 건수 및 최근 증가 배지가 1→4 순서로 카운트업된다.
- 재렌더 시 이전 timeout과 animation frame을 취소해 중복 애니메이션을 방지한다.

## 확인

- `test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 파일의 CRLF 경고만 출력)
