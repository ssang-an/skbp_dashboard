# 114. Tab 0 워크플로우 노드 드래그 이동 작업 로그

## 완료 내용

- 각 Summary Dashboard 단계 node graph에 G6 `drag-element` behavior를 추가했다.
- 사용자는 개별 Pipeline dot을 drag해 화면 안에서 임시로 위치를 변경할 수 있다.
- force simulation은 사용하지 않으므로 drag가 다른 dot의 위치나 렌더링 부하를 유발하지 않는다.

## 확인

- `DashboardInformationArchitectureTests.test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 vault의 CRLF 경고만 출력)
