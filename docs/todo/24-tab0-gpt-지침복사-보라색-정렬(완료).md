# 24. Tab0 GPT 지침 복사 보라색 정렬 (완료)

## 반영 내용

- Tab0 Pipeline Table의 `GPT 지침 복사` pill을 header `GPT 지침 1`과 같은 차분한 lavender 계열로 바꿨다.
- 기본·hover·GPT 배지에 동일한 색상 계열을 적용했고, 복사 아이콘·문구·기능은 유지했다.

## 검증

- JavaScript 구문 검사와 변경분 공백 검사를 통과했다.
