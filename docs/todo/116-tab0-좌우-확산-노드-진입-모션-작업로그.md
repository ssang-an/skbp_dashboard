# 116. Tab 0 좌우 확산 노드 진입 모션 작업 로그

## 완료 내용

- 각 node는 card 좌측 22% 영역의 결정론적 entry 좌표에서 시작한다.
- d3-force의 anchor는 기존 불규칙 직사각형 최종 위치를 사용해 DOT이 왼쪽에서 오른쪽으로 빠르게 확산된다.
- 기존 stage 순차 render·약한 repulsion·종료 후 정지 규칙은 유지했다.

## 확인

- `DashboardInformationArchitectureTests.test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 vault의 CRLF 경고만 출력)
