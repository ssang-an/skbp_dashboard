# 113. Tab 0 단계별 순차 노드 등장 애니메이션 작업 로그

## 완료 내용

- Summary Dashboard의 Listing, Fast Triage, Full Scout, Shortlisting graph canvas가 좌에서 우로 300ms 간격으로 나타난다.
- 각 canvas는 220ms 동안 opacity와 scale만 전환하며, node force simulation이나 반복 animation은 사용하지 않는다.
- 운영체제의 reduced-motion 설정에서는 animation을 표시하지 않는다.

## 확인

- `DashboardInformationArchitectureTests.test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 vault의 CRLF 경고만 출력)
