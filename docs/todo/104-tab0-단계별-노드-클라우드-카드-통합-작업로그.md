# 104. Tab 0 단계별 노드 클라우드 카드 통합 작업 로그

- Listing · Fast Triage · Full Scout · Shortlisting 카드 각각에 독립 G6 캔버스를 배치했다.
- 하단 통합 그래프 컨테이너를 제거하고, 각 캔버스의 높이를 174px로 고정해 카드 내부 넘침을 차단했다.
- 단계별 node 크기·충돌 반경·반발력을 조정했다. Listing은 4px node로 700건 이상을 고밀도로 표시한다.
- x/y force와 강한 중앙 힘으로 노드 클라우드를 세로·가로 중앙에 유지한다.
- `node --check src/app.js`, 대상 IA 회귀 테스트, `git diff --check`를 통과했다.
