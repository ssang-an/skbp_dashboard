# 24. Tab0 GPT 지침 복사 보라색 정렬

## 목표

- Tab0 Pipeline Table의 `GPT 지침 복사` pill을 상단 `GPT 지침 1`과 같은 차분한 보라색 계열로 맞춘다.
- 버튼 구조와 복사 기능은 변경하지 않는다.

## 작업 순서

1. 기본·hover·GPT 배지의 slate-blue 색상을 header GPT action과 조화되는 lavender 색상으로 조정한다.
2. JavaScript 구문과 CSS 변경분을 확인한다.
