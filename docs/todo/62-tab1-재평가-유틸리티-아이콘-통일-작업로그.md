# 62. Tab 1 재평가 유틸리티 아이콘 통일 작업로그

## 구현

- `rubricReevaluationButton()`에 Tab 2 상단 새로고침과 동일한 path의 SVG를 적용했다.
- `triageFullScoutCopyButton()`도 같은 행 유틸리티 SVG class를 사용하게 했다.
- 공통 아이콘의 선 두께, 크기, hover 확대, 재평가 중 회전 스타일을 하나의 규칙으로 관리한다.

## 검증

- `python -m unittest tests.test_dashboard_ia.DashboardInformationArchitectureTests.test_triage_reassessment_uses_the_same_svg_utility_icon_system_as_refresh -v`
- `node --check src/app.js`
- `git diff --check`
