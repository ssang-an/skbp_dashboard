# 125. Tab 0 단계 전환 완화와 좌측 흐름 강조 작업로그

## 완료 내용

- 네 워크플로 카드의 시작 간격을 420ms로 조정하고 카드 진입 transition을 360ms로 완화했다.
- DOT 주 이동 시간을 720ms로 늘리고, 카드 좌측 10% 영역에서 출발해 오른쪽으로 이동하도록 조정했다.
- 개별 DOT 출발 지연을 240ms 범위로 넓혀 주루룩 생성되는 흐름을 강화했다.

## 확인

- `test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 파일의 CRLF 경고만 출력)
