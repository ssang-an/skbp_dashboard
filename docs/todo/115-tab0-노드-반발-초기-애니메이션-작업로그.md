# 115. Tab 0 노드 반발 초기 애니메이션 작업 로그

## 완료 내용

- 각 node의 불규칙 직사각형 기준 좌표를 d3-force의 X/Y anchor로 저장했다.
- Listing부터 우측 단계 순서로 graph render를 지연하고, 각 단계는 26회 이하의 약한 repulsion·collision animation 후 정지한다.
- 중앙 흡인력은 0으로 유지해 원형 군집을 만들지 않는다.
- 재렌더링 시 남아 있는 지연 timer를 clear해 이전 graph가 늦게 나타나는 문제를 막았다.

## 확인

- `DashboardInformationArchitectureTests.test_step0_workflow_map_uses_filtered_rows_and_g6_stage_nodes` 통과
- `node --check src/app.js` 통과
- `git diff --check` 통과 (기존 생성 vault의 CRLF 경고만 출력)
