## Changes

- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: combined the Value Up In-vivo and In-vitro requirements into one comma-separated non-wrapping line; refreshed stylesheet cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: changed the Tab 1 and Tab 2 Target Relevance indication-chip labels to regular text weight while preserving their pill styling; refreshed stylesheet cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: reused the Tab 3 indication chip styling for Unknown keywords and changed both chip sets to regular text weight; refreshed stylesheet cache keys and regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: shortened the Tab 3 Unknown keyword from Stage and Platform Attractiveness to Platform Attractiveness so all three keyword pills fit more compactly; updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: unified the Instruction 2 Parameter Guide MoA Validity summary with the shared Target/mechanism and functional, independent, asset-specific evidence wording; added regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: removed the Data Maturity card from the Tab 2 Parameter Guide and updated card-count and ordering regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: further compacted the shared Full Scout Final Status summary slot and bullet gap so all three aligned lists sit closer to their summaries; refreshed stylesheet cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: moved all three Full Scout Final Status bullet lists slightly upward together while preserving their shared baseline; refreshed stylesheet cache keys and regression coverage.
- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: changed Full Scout Final Status summary backgrounds to fit their text while retaining equal-height invisible alignment slots for a shared bullet baseline; refreshed stylesheet cache keys and regression coverage.
- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: simplified the Tab 3 Unknown explanation and replaced missing-value examples with highlighted modality, ADMET information, and Stage/Platform Attractiveness keyword pills; refreshed stylesheet cache keys and regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: removed the redundant Tab 2 Evidence & Record Rules note about keeping criteria descriptions only in rubric and updated regression coverage.
- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: made Competitive Landscape and Marketability full-width horizontal Parameter Guide cards with right-side Keywords panels, applied filled pill styling to Tab 2 keywords, refreshed stylesheet cache keys, and added regression coverage.
- `index.html`, `triage_detail.html`, `config/scoring_criteria/v3_1_display.md`, `config/scoring_criteria/v3_2_display.md`, and `tests/test_dashboard_ia.py`: removed the three redundant Fast Triage scoring reminder sentences from judgment-basis displays and added regression coverage.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: removed the redundant `파이차트` wording from the Indication and Modality summary subtitles in both initial and dynamically rendered states, refreshed the dashboard script cache key, and added regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: matched the Tab 3 OI Partnership cards to the Final Status green/amber/red palette and exact 29px circular icon treatment, aligned heading and helper typography and 78px helper rows, retained responsive mobile sizing, refreshed shared CSS cache keys, and expanded regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: restyled the three Tab 3 OI Partnership Type cards to match the Tab 1/2 Final Status hierarchy with neutral outer cards, equal-height tinted helper boxes, regular-weight condition bullets, responsive height reset, refreshed shared CSS cache keys, and regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: removed the entire Tab 3 `Review & Recalculation` explanatory section, including its AUTO, Evidence Record, HUMAN, and Recalculate cards, and updated the decision-guide regression coverage from four steps to three.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: expanded the Tab 2 Target Relevance parameter card across both grid columns, restored its three indication/theme groups to a horizontal layout, retained a single-column mobile layout, refreshed shared CSS cache keys, and updated regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: renamed the Shortlisting priority metric from `Action Required` to `F/U Action`, removed `Action 우선순위` from its subtitle, refreshed the dashboard script cache key, and updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: changed the Focus Management investment constraint from `canonical stage` wording to `Pipeline이 정확히 IND-enabling인 경우만 해당`, and updated regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: corrected the Tab 2 Full Scout Total Score alignment override by fixing all three summary boxes to a border-box 96px desktop row and adding a shared 16px gap before their bullet lists; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: aligned the three Tab 2 Full Scout Total Score rule starting positions on one desktop baseline while retaining automatic mobile heights, refreshed shared CSS cache keys, and added regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: added matching subtle frames around the three Target Relevance indication/theme groups in both Tab 1 and Tab 2 Parameter Guides, refreshed shared CSS cache keys, and added regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: removed the forced line break from the Tab 2 Evidence & Record Rules introduction so the sentence displays on one line when space permits, and added regression coverage.
- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: converted all three Tab 1 Evidence Level card descriptions from line breaks to semantic bullet lists, added consistent bullet spacing and accent markers, refreshed shared CSS cache keys, and added regression coverage.
- `detail.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: aligned the Tab 1 record-detail judgment and delete controls with the dashboard pill language, enforced circular 22px icon containers, replaced the delete glyph with a trash SVG, refreshed shared CSS cache keys, and added regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: enlarged the Data Upload GPT response placeholder to 16px, strengthened its weight and contrast, increased line spacing, refreshed the shared stylesheet cache key, and updated regression coverage.
- `src/styles.css` and `index.html`: unified TAB1 Parameter Guide and Evidence Level inner cards with midpoint tint `#F1F9F8`, explicitly matched Evidence card row/column spacing at 16px, and refreshed the stylesheet cache key.
- `src/styles.css` and `index.html`: reverted the TAB1 judgment-basis request and repaired encoding damage from the unintended full-file rewrite, including CSS strings, symbols, quotes, BOM, and the original stylesheet cache key.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: stretched the Tab 1 Parameter Guide cards within their grid row so MoA Validity and Data Maturity share the same height and alignment; refreshed the stylesheet cache key and added regression coverage.
- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: changed the three Tab 1 Evidence Level cards from plain white to a consistent subtle 8% teal-accent tint while preserving equal spacing, borders, shadows, and readable contrast; refreshed the shared stylesheet cache key and regression assertion.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: normalized the three Tab 1 Evidence Level cards to an exact 16px horizontal/vertical gap, reset their outer margins consistently, and synchronized the shared stylesheet cache key across every page; updated regression coverage.

- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: removed the remaining border and inset edge from all Tab 1/2 Final Status cards, leaving only their status-tinted shaded backgrounds; refreshed the stylesheet cache key and tightened regression coverage.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: strengthened only the shaded background of the Tab 1/2 `Final Status` cards to match the visible box treatment under `Evidence & Record Rules`, refreshed the stylesheet cache key, and added regression coverage.
- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: enabled the same subtle accent-tinted row hover used by the Full Scout Scoring Table on the Fast Triage Scoring Table, preserving existing zebra/score-column backgrounds; refreshed the shared stylesheet cache key and added focused regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: aligned the OI Partnership Type and related option boxes with the GPT response input field using a uniform thin neutral border, 12px radius, subtle inset depth, and neutral surface instead of colored left-edge borders; refreshed the shared stylesheet cache key and added focused regression coverage.

- `index.html` and `tests/test_dashboard_ia.py`: corrected both Target Relevance guide sentences from `洹??섏쐞 Cluster???곹빀?? to `洹??섏쐞 Cluster????곹빀?? and updated regression coverage.

- `index.html`, `src/app.js`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: added right-aligned `Filter 1`, `Filter 2`, and `Filter 3` labels to the workflow information panel for Fast Triage, Full Scout, and Shortlisting, using the Pipeline Table's 12px Tahoma/Malgun Gothic typography; refreshed CSS/JS cache keys and updated regression coverage.

- `main.py`, `src/app.js`, `index.html`, `tests/test_dashboard_summary.py`, and `tests/test_dashboard_ia.py`: confirmed Priority Pipeline ordering as score-first with recency only breaking equal scores, and applied the same five-item cap concept to Action Required while preserving action urgency/date precedence and using latest focus update or search time only for equal-priority ties; refreshed the dashboard script cache key and added backend/frontend regression coverage.

- `main.py`, `index.html`, `src/app.js`, `src/styles.css`, all five page HTML files, `tests/test_dashboard_summary.py`, and `tests/test_dashboard_ia.py`: simplified Priority Pipeline to the five highest-scoring entries, changed equal-score ordering to newest completed search first, and removed the 16-point filter, more button, and detail modal; refreshed CSS/JS cache keys and added backend/frontend regression coverage.

- `index.html`, `src/app.js`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: limited Full Scout Priority Watch to pipelines scoring at least 16, added a counted `?뚯씠?꾨씪???붾낫湲? action, and added a responsive detail modal listing every qualifying pipeline with score, decision, company, indication, Data, and TR context plus detail-page navigation; refreshed CSS/JS cache keys and added regression coverage.

- `index.html`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: aligned the Tab 1 Fast Triage scoring table with the Tab 2 scoring-table format by using plain score and criterion headers plus the same neutral header, zebra rows, borders, 10px radius, spacing, and typography; refreshed the shared stylesheet cache key and regression coverage.

- `index.html`, `detail.html`, `triage_detail.html`, and `tests/test_dashboard_ia.py`: revised the Target Relevance Parameter Guide copy to describe Cluster explicitly as subordinate to the R&D Theme across dashboard/detail criteria views, and added focused regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: reverted the immediately preceding icon-only neutral treatment for Fast Triage status and Parameter Guide cards, restoring their prior tinted borders/backgrounds, circular status icons, and Target Parameter accent; refreshed the shared stylesheet cache key and removed the superseded regression test.

- `index.html`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: highlighted the Fast Triage scoring footnote's `UNVERIFIED` label with the dashboard status-badge treatment and expanded zero-score guidance to include inactive and discontinued development cases; refreshed the shared stylesheet cache key and added regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: aligned Fast Triage SELECT/REJECT/UNVERIFIED and Parameter Guide cards with the Evidence Level presentation by using neutral card borders/backgrounds and retaining saturated status/teal color only on unboxed SVG symbols and headings; refreshed the shared stylesheet cache key and added focused regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: reverted the immediately preceding Office-style SELECT/REJECT/UNVERIFIED card cosmetics, restoring the prior colored top accents, circular status icons, tinted card surfaces, and original status-heading treatment; refreshed the shared stylesheet cache key and removed the superseded regression test.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: restyled the Fast Triage scoring table with plain formal score headers instead of circular number badges, a compact 4px table radius, stronger neutral outer and cell borders, and a two-pixel header divider; refreshed the shared stylesheet cache key and added regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: restyled the judgment-criteria SELECT/REJECT/UNVERIFIED cards with neutral Office-style surfaces, uniform borders, subtle elevation, compact rectangular status labels, and squared icon containers while removing the decorative colored top bars and circular treatment; refreshed the shared stylesheet cache key and added focused regression coverage.

- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: precision-aligned Summary Dashboard cards with a shared 64px header baseline, 332px desktop height, and fixed 144px donut diameter; compacted the priority list to fit, removed internal scrollbars, preserved natural mobile height, refreshed the stylesheet cache key, and added regression coverage.
- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: aligned the account-menu sign-out button typography with the user-management action using the same inherited font, 13px size, 800 weight, normal line height, and centered alignment; refreshed the shared stylesheet cache key and added regression coverage.

- `index.html` and `tests/test_dashboard_ia.py`: labeled E/I Balance and Neuroimmune as `R&D Theme 1` and `R&D Theme 2` in the Judgment Criteria Tab 1/2 Parameter Guides, explicitly identified their keyword lists as `Cluster`, and added regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: replaced the workflow-description title colon with a centered middle dot for a softer visual separation; refreshed the dashboard script cache key and regression coverage.

- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: increased the GPT response input placeholder from 13px to 14px, explicitly set it to normal weight, refreshed the stylesheet cache key, and updated regression coverage.
- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: squared the workflow-description panel's two top corners while retaining 11px rounding only on the bottom corners so it aligns cleanly below the workflow tabs; refreshed the shared stylesheet cache key and regression assertion.

- `index.html` and `tests/test_dashboard_ia.py`: hid the duplicate Fast Triage Target Relevance and E/I Balance/Neuroimmune theme explanation sections below the Parameter Guide while retaining the guide's scoring content and evidence guidance; added focused regression coverage.

- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: changed both GPT execution-guide mode instructions from `High` to `High ?댁긽`; refreshed the dashboard script cache key and regression assertion.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: changed the three workflow tab markers to 22px circular bordered icons matching the header GPT instruction-button icon treatment; refreshed the shared stylesheet cache key and regression coverage.

- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: shortened both Data Upload input labels to `GPT 吏移?1 ?꾩껜 ?묐떟` / `GPT 吏移?2 ?꾩껜 ?묐떟` by removing the `Markdown + JSON` suffix; refreshed the dashboard script cache key and added focused regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: removed the collapsed Summary Dashboard border entirely and constrained its neutral hover elevation below the bar with negative-spread shadows so no top line appears; refreshed the stylesheet cache key and updated focused regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: removed the visible border from the collapsed Summary Dashboard bar and replaced it with subtle neutral-gray elevation in light/dark and hover states; refreshed the shared stylesheet cache key and added focused regression coverage.

- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: recombined the Fast Triage 50-asset limit and 10??0 asset recommendation into one placeholder paragraph while retaining separation from the opening instruction; refreshed the dashboard script cache key and updated regression coverage.

- `index.html`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: restyled the Data Upload review and save actions with the same capsule height, spacing, circular icon treatment, and hover lift used by the header instruction buttons; refreshed the shared stylesheet cache key and regression coverage.

- `src/app.js`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: replaced the Fast Triage Data Upload placeholder with the requested guide-first and 10??0 asset recommendation copy, added paragraph spacing to both workflow placeholders, styled placeholder text as regular-weight 13px copy with a roomier line height, refreshed CSS/JS cache keys, and added focused regression coverage.

- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: clarified that an optional NCDP file can be attached together with GPT 吏移?1 during GPT execution; refreshed the dashboard script cache key and regression assertion.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: changed the GPT execution guide's numbered step markers from neutral rounded squares to pale-green circular number badges, refreshed the shared stylesheet cache key, and updated regression coverage.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: changed the GPT search-mode and Pipeline Table Excel pills from neutral gray to a restrained pale-green surface with green border/text, kept disabled Excel visibly inactive without losing all color, refreshed the stylesheet cache key, and updated focused regression coverage.

- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: shortened the Full Scout response-field instruction from ?쏮arkdown + JSON ?꾩껜 ?묐떟??to ?쒖쟾泥??묐떟??and refreshed its script cache key and regression assertion.

- `src/app.js` and `tests/test_dashboard_ia.py`: removed the redundant `GPT 寃?됱갹?? wording from both `吏移?諛?????낅젰` guide steps and updated regression coverage.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: revised the Full Scout response-field guidance to direct users through the right-side execution guide, paste the complete Markdown + JSON output, and optionally attach an NCDP file; refreshed the dashboard script cache key and regression assertion.

- `src/sk-butterfly.svg` and all five page HTML files: replaced the page-specific letter favicon with one shared, minimal orange SK butterfly favicon across the dashboard, detail, triage, wiki, and user-admin pages.

- `src/styles.css` and all five page HTML files: restyled the Pipeline Table Excel export control as a compact green icon pill matching the Data Upload search-mode recommendation badge's height, rounded shape, typography, and spacing; refreshed the shared stylesheet cache key.

- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: replaced dashboard filter focus lines with neutral gray shadow-only feedback, applied the same focus depth to the GPT result Data Upload field, aligned Data Upload text (including Korean) with the Pipeline Table/dashboard Tahoma and Malgun Gothic stack at 12px for input content, refreshed the shared stylesheet cache key, and updated focused regression coverage.

- `main.py`, `src/auth.js`, `user_admin.html`, `src/user-admin.js`, `src/styles.css`, page entry files, and `tests/test_auth_admin.py`: made login mandatory on initial page entry, simplified signup guidance, restricted user administration to `joowon.jung@sk.com` on the server, recorded page access history, and added a searchable/sortable user table with CSV export, per-user history, and account activation controls. Verification: Python compile, JavaScript syntax checks, and 4 focused authorization tests passed.

- Summary Dashboard ?먯닔瑜???퀎 ?됯퇏 珥앹젏?쇰줈 紐낇솗???쒖떆?섍퀬 hover ?덈궡? ?묎렐???쇰꺼??異붽???(`src/app.js`).
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: removed the explanatory sentence directly beneath the `Data Upload` heading; retained workflow-specific instructions only in the input placeholder and right-side quick guide, and removed the now-unused description binding and styling.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: clarified both Data Upload recommendation badges and first guide steps from the ambiguous `High ?댁긽` wording to `GPT 寃??Mode: High ?댁긽`, preserved the Tab1/Tab2 batch-size guidance, and refreshed the dashboard script cache key.
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: rewrote the Full Scout Quick Guide target-input sentence to prioritize one Asset name and make company name recommended, then rendered `GPT 吏移?2` as a compact GPT-icon copy chip linked to the existing prompt-copy action; added copied-state feedback, refreshed CSS/JS cache keys, and extended regression coverage.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: standardized both Fast Triage and Full Scout Quick Guides with compact `GPT 紐⑤뱶 High` badges, GPT-icon copy chips for 吏移?1/2, and matching icon action chips for `?낅젰 寃?? and `寃利??????; connected every guide chip to its existing dashboard action, refreshed CSS/JS cache keys, and expanded regression coverage.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: reduced repeated green accents by restyling Excel export, GPT mode badges, guide step markers, and non-primary Quick Guide chips with neutral gray/white surfaces; retained a restrained green treatment only for the guide's actual save action, refreshed the stylesheet cache key, and added regression coverage.
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: converted the Fast Triage, Full Scout, and Shortlisting descriptions into a dedicated information panel with an icon and clearer type hierarchy; joined the selected tab underline to the panel's teal top border, aligned the panel with dashboard content, added responsive/dark-mode styling, refreshed cache keys, and added regression coverage.
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: reorganized Data Upload actions into a left-aligned ghost clear control and right-aligned secondary review/primary save flow, made the save button visually neutral until validation succeeds, changed the waiting status to `?묐떟 遺숈뿬?ｊ린 ?湲?, exposed validating state through `aria-busy`, added responsive handling, refreshed cache keys, and added regression coverage.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: removed workflow-tab divider bars, active underlines, connector stems, and the guidance panel's teal top rule; retained a clear selected state through a subtle rounded background and refreshed regression coverage/cache keys.
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: cosmetically refined Data Upload without changing its copy, validation, or button flow; separated the input and Quick Guide into polished cards, added a defined rounded textarea, consistent SVG icons, state-aware badges, mode-specific teal/blue accents, clearer examples/actions, responsive guide ordering, refreshed cache keys, and added regression coverage.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: unified the Tab1/Tab2/Tab3 judgment-guide cosmetics without changing any criteria content; standardized section surfaces, spacing, radii, title hierarchy, cards, notes, tables, dark-mode headers, and responsive padding while limiting each workflow color to restrained accent lines; refreshed the stylesheet cache key and added regression coverage.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: rounded the Data Upload `?낅젰 寃?? and `寃利?????? icon containers into compact circles matching the visual language of the `吏移?1쨌2` chips, while leaving button behavior and the clear action unchanged; refreshed the stylesheet cache key and regression coverage.
- `src/app.js`, `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: synchronized the Quick Guide `?낅젰 寃?? and `寃利?????? icons with their actual workspace buttons, including the same shield-check/save glyphs, 22px circular containers, 14px icon size, and stroke weight; refreshed cache keys and regression coverage without changing actions.
- `index.html` and `tests/test_dashboard_ia.py`: removed the obsolete Fast Triage `Target Relevance ??Theme / Cluster 洹쇨굅` and E/I Balance/Neuroimmune detail blocks instead of retaining them as hidden markup; updated regression coverage.
- `src/app.js`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: added visible hover/focus popups to grouped Summary Dashboard legend items, clarifying that CGT combines Cell/Gene Therapy and Others can include Other/Unknown/N/A; refreshed the shared stylesheet cache key and added regression coverage.
- `src/app.js`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: removed duplicate native legend tooltips, restyled grouped-label explanations as standard surface cards, and changed the judgment guide from a side drawer to a centered large accessible modal with focus restoration; refreshed the shared stylesheet cache key and added regression coverage.
- `src/app.js`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: removed the oversized custom Summary Dashboard legend popup and retained only the compact native browser tooltip for grouped-label explanations, preventing tooltip-induced chart scrolling; refreshed the shared stylesheet cache key and regression coverage.
- `index.html`, `detail.html`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: converted Target Relevance priority indications and E/I Balance/Neuroimmune cluster lists into responsive rounded keyword chips in the judgment guide; refreshed the shared stylesheet cache key and added regression coverage.
- `src/detail.js`, `detail.html`, and `tests/test_dashboard_ia.py`: removed Theme/Cluster from the shared GPT source-report header subtitle so every Full Scout pipeline displays only its Target; left persisted records and modification timestamps unchanged, refreshed the detail script cache key, and added regression coverage.
- `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: removed the residual accent-colored top edge from the Full Scout Competitive Landscape, Platform Attractiveness, and Data Maturity parameter cards while retaining their neutral card outlines; refreshed the shared stylesheet cache key and added regression coverage.
- `index.html`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: renamed step 4 to `Evidence 湲곕줉 洹쒖튃` and nested the Evidence Type table plus record-field principles as two responsive subcards within that numbered section; refreshed the shared stylesheet cache key and added regression coverage.
- `index.html`, `src/styles.css`, all five page HTML files, and `tests/test_dashboard_ia.py`: restored the step 4 title to `Evidence & Record Rules` and applied one shared framed-card treatment to numbered step 1 and step 4 sections; refreshed the shared stylesheet cache key and updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: shortened the Evidence & Record Rules introduction to define only Evidence Type and score-traceability notes, removing the competitive-similarity sentence; updated regression coverage.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: made the actual Data Upload `?낅젰 寃?? and `寃利?????? controls fully pill-shaped like the `吏移?1쨌2` chips and locked their icon containers to true 22px circles with no internal padding; refreshed the stylesheet cache key and regression coverage.
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: assigned the actual Data Upload buttons and their Quick Guide counterparts the same shared action-icon class and token, synchronizing glyph, 22px circle, border, tint, 14px size, and stroke weight across both locations; refreshed cache keys and regression coverage without changing behavior.
- `index.html`, `src/app.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: rebuilt the Fast Triage judgment modal as a dashboard-native scoring and decision reference without changing criteria content or logic; added a mode/version/purpose header, balanced vector-icon status cards, a restrained 0?? progression matrix, clearer parameter hierarchy, provenance-focused Evidence cards, sticky-accessible close treatment, responsive table/card layouts, refreshed cache keys, and regression coverage.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: reorganized all three judgment-guide tabs with Quick Guide-style circular number badges and consistent vector-icon section headers; gave Fast Triage a clear Final Status ??Scoring ??Parameter ??Evidence flow, added compact scoring helpers and MoA/Data checkpoints, and applied corresponding Full Scout and Shortlisting section flows without changing criteria text or scoring logic.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: simplified judgment section headers to one circular number marker followed by inline `title 쨌 description` copy, removed the redundant adjacent section glyphs, and replaced colored status-card top rules/tints with neutral gray surfaces, borders, and dashboard-style soft shadows across all judgment tabs.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: tightened the gap below judgment Final Status headers, restored the prior status icon colors and soft card tints while keeping top color rules removed, and aligned judgment-card borders, radii, surfaces, and shadows with the Data Upload GPT response workspace treatment.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: unified Parameter Guide criteria cards and Evidence Level source/rule cards with the SELECT/REJECT/UNVERIFIED card family, using the same neutral outline, 12px radius, subtle workflow tint, inset depth, padding, and circular accent icons without reintroducing colored top borders.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: separated the SELECT/REJECT/UNVERIFIED summary sentences from their condition bullets with compact, softly status-tinted helper panels matching the Fast Triage scoring guidance treatment; preserved all wording and status logic.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: aligned the Full Scout Scoring Table with the Fast Triage display system by splitting its existing scope note into two compact helper cards, adding consistent vector icons and short labels to all seven criteria rows, and matching table spacing, header treatment, hover state, subtle score-column tints, and responsive overflow without changing score definitions.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: reduced excess vertical whitespace below the Full Scout `Evidence & Record Rules` heading by tightening the heading margin/divider padding, removing the following paragraph's default margin, and reducing the gap before its record-card grid.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: restyled MoA Validity and Data Maturity checkpoint labels as the same fully rounded chips used for Target Relevance priority indications, matching height, border/tint, padding, type size, and weight while removing the redundant leading dot marker.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: removed the outlines from all three Fast Triage status-summary helper panels while retaining their soft state tints, padding, and separation from condition bullets.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: standardized the content gap beneath every numbered Tab 1/Tab 2 judgment-section title to 12px with consistent divider padding, and removed the status-grid row gap that previously compounded the space below Final Status.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: increased the borderless SELECT/REJECT/UNVERIFIED summary-panel tint from 6% to 11% so each summary is visibly separated from its 4%-tinted parent card while retaining a soft, natural state color.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: standardized Tab 1 Evidence Level spacing with a shared 12px token, applying the same distance between the `?ъ슜???낅젰?뺣낫` and `怨듦컻?먮즺` cards and between those source cards and the following evidence-rule panel.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: replaced the oversized numbered Tab 1 Parameter Guide titles with the Scoring Table's compact icon + `TR`/`MoA`/`Data` label + criterion-name hierarchy, matching its 9px abbreviation and 12px title typography without changing parameter content.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: changed the borderless SELECT/REJECT/UNVERIFIED description helpers from darker state tints to brighter elevated surfaces with subtle shadow depth, plus a dedicated lighter dark-mode surface, so the explanatory copy reads as an inner helper distinct from each parent status card.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: made the Tab 1 Evidence source-card separation visibly apparent by increasing the shared source/rule gap from 12px to 16px and placing the borderless source/rule cards on brighter surfaces with subtle shadow depth instead of a nearly identical parent tint.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: softened the SELECT/REJECT/UNVERIFIED description helpers from bright elevated white panels to borderless, shadow-free 2% status-tinted surfaces, keeping them only slightly lighter than their 4%-tinted parent cards.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: changed the Tab 1 Evidence Level source cards from a two-column row to a single-column stack, giving `사용자 입력정보`, `공개자료`, and the evidence-rule card equal full widths and the same 16px vertical separation so the first two can no longer appear joined.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: corrected the still-collapsed gap between Tab 1 `사용자 입력정보` and `공개자료` cards by replacing the legacy-overridden definition-list grid spacing with an enforced vertical flex stack and an explicit 16px adjacent-sibling margin.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: unified Target Relevance Theme/indication keywords and both MoA/Data checkpoint lists under one explicit white pill rule, matching their 27px height, 999px radius, accent outline, 6px gap, padding, typography, and subtle depth.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: restored the original accent-tinted Target Relevance priority-indication and Theme/Cluster keyword chips by removing them from the later white-pill override, while retaining the requested white pill treatment only for MoA/Data checkpoints.
- `index.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: removed the optional MoA Validity and Data Maturity checkpoint-summary lists and their now-unused pill CSS, leaving the original criterion definition sentences and all scoring rules unchanged.
- `index.html` and `tests/test_dashboard_ia.py`: removed the standalone `寃쎌웳???좎궗??湲곕줉` guidance block from the Tab2 Full Scout judgment drawer without changing competitive scoring logic or persisted report data; added regression coverage for the removed UI copy.
- `src/styles.css`, `index.html`, and `tests/test_dashboard_ia.py`: restored Evidence Level subcards to a borderless tinted treatment and applied the same color-only card language to Parameter Guide criterion/formula cards and both Fast Triage and Full Scout Final Status cards; removed nested status-summary borders and inset shadows, refreshed the stylesheet cache key, and added regression coverage.
- `src/styles.css` and all four page HTML files: restyled Tab 1 Evidence Level as a compact `4. Evidence Level` card matching the simple numbered Tab 2 parameter-card format, flattened its nested evidence blocks and footnote, and refreshed shared CSS cache keys.
- `index.html`, `triage_detail.html`, `src/styles.css`, all shared page cache keys, and `tests/test_dashboard_ia.py`: reverted the compact parameter-card interpretation and instead matched Tab 1 Evidence Level to Tab 2 `4. Evidence & Record Rules` with the same framed section container and compact heading/content spacing; updated regression coverage.
- `src/styles.css`, all four page HTML files, and `tests/test_dashboard_ia.py`: removed colored borders from all three Tab 1 Evidence Level inner boxes and normalized both the two-column gap and third-box top spacing to 10px; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: placed each Tab 1 Parameter Guide abbreviation and criterion name in one horizontal title row to the right of its icon, kept TR/MoA/Data labels from wrapping below the icon, refreshed shared CSS cache keys, and added focused regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: applied the Quick Guide step-card surface (subtle neutral border, 11px radius, near-white background, no shadow) to Tab 1 status, scoring-helper, parameter, and evidence cards while retaining semantic title and icon colors; refreshed shared CSS cache keys and regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: replaced the three Tab 1 Parameter Guide summary lines with the requested concise TR, MoA, and Data evaluation descriptions while leaving keyword lists and scoring logic unchanged; added scoped copy regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: restored the original green accent border and background only for the Tab 1 `각 항목은 독립적으로 평가합니다.` scoring reminder while retaining the neutral Quick Guide surface on the adjacent cards; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: extended the restored green accent treatment from the first Tab 1 scoring reminder to all three adjacent scoring-guide cards, preserving their shared Quick Guide border shape; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: aligned the SELECT, REJECT, and UNVERIFIED bullet-list starting positions in the three-column Tab 1 status layout by reserving a shared summary row and removing bottom-pushed list positioning; preserved the existing stacked responsive flow and refreshed cache/test coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: removed borders from all three Tab 1 scoring-guide cards and replaced their neutral surfaces with a consistent light-green background while preserving icon/text colors and rounded geometry; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: strengthened the three borderless Tab 1 Final Status helper backgrounds using their semantic status colors—soft green for SELECT, soft red for REJECT, and soft amber for UNVERIFIED—while preserving existing copy, sizing, and alignment; refreshed shared CSS cache keys and regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: updated the Tab 1 Evidence Level copy to distinguish user-provided and web-verified evidence and present the three scoring principles in the existing card layout; added focused copy regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: made the Tab 1 Final Status helper panels size to their sentence content instead of stretching to the reserved alignment row, removing internal empty space while retaining the shared bullet-list baseline; refreshed shared CSS cache keys and regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: converted Tab 1 Evidence Level into a horizontal three-card row with `평가 원칙` presented as a standard card title without brackets, and rebuilt Tab 2 Evidence & Record Rules as a concise three-card Evidence Type, score-record, and competitor-similarity summary with a rubric-only note; added responsive stacking and focused regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: changed the final Tab 1 Evidence Level evaluation-principle line to the complete sentence `출처 개수 자체로 점수를 결정하지 않습니다.` and updated copy regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: moved the source-count rule out of the Tab 1 evaluation-principle card and combined it with verified-source and Unknown handling into one continuous Evidence Level introduction; kept the sentence on one desktop line with responsive wrapping and updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: clarified the Tab 1 TR evidence rule as `사용자 입력정보 및 공개자료 모두 사용 가능.` and shortened the Tab 2 competitor-similarity description to the noun phrase `공통·차별 데이터와 출처 기록`; updated copy regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: removed the Full Scout Section 2 preliminary-application note and its dedicated spacing style so the `Scoring Table · 7개 criteria의 0–3점 평가 기준` header flows directly into the table; refreshed cache keys and added regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: added the requested PASS, REVIEW, and FAIL one-line summaries between each Full Scout status heading and its unchanged decision bullets, using the existing check/search/x icon family and semantic green/amber/red soft tints; preserved thresholds and rule copy, refreshed cache keys, and added regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: matched Tab 2 Final Status to Tab 1 by using neutral Quick Guide surfaces for the outer PASS/REVIEW/FAIL cards while keeping their helper panels and icon/title accents status-colored green, amber, and red; preserved all status copy and rules, refreshed cache keys, and extended regression coverage.
- `triage_detail.html`, `src/triage-detail.js`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: restyled Fast Triage detail `판단근거` and `삭제` actions with the dashboard instruction-pill and circular-icon system, replaced the text delete glyph with the shared trash vector, and synchronized the detail criteria drawer from the current home Dashboard Tab 1 criteria DOM on load/open; refreshed caches and added regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: replaced the Full Scout PASS hard-blocker shorthand with the requested user-facing exclusion wording and aligned PASS/REVIEW/FAIL cards to the Fast Triage header-summary-bullets rhythm, including shared summary height, darker body text, consistent bullet spacing, and 3/2/1-column responsive behavior; preserved REVIEW/FAIL rules and backend logic.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: removed the trailing period from the Tab 1 TR evidence principle and rebalanced the three desktop Evidence Level cards so the user-input and public-source cards are slightly narrower while the longer evaluation-principle card has more room; retained the existing single-column mobile layout and refreshed regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: completed the Full Scout Final Status typography pass by rendering the unchanged PASS thresholds with the requested `≥` notation and using the Dashboard body-text token for all condition bullets; retained the aligned header-summary-bullet grid, status colors, REVIEW/FAIL copy, and backend rules.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: rebuilt Full Scout Section 3 with the Fast Triage parameter-card system—one full-width Target Relevance card, six responsive two-column cards, seven matching vector-icon/abbreviation headers, neutral readable surfaces, existing Target chips, compact Competitive record-field chips, and a vertical Marketability formula list—while preserving parameter order, definitions, scoring content, and backend/data structures.
- `index.html`, `src/app.js`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: rebuilt the Tab 3 Shortlisting decision guide into the requested four-step Eligibility/Input → OI Partnership Type → Priority/Exceptions → Review/Recalculation flow, with shared numbered headers, vector-icon cards, exact Filter 3 formulas and IND-enabling exclusions, target-indication chips, Unknown/N/A comparison, AUTO/HUMAN rules, Action date note, and 3/2/1-column responsiveness; left Filter 3 classification, override, persistence, and backend logic unchanged.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: restyled Tab 3 investment, Value Up, and joint-research decisions to match the Tab 1/2 Final Status hierarchy by keeping status-colored helper summaries, replacing inline `AND` formula chips with aligned condition bullets, separating each result into a compact outcome badge, and retaining the exact IND-enabling interpretation notes; refreshed caches and regression coverage without changing Filter 3 logic.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: removed the Full Scout Competitive Landscape keyword panel and its six technical labels, reorganized Target/Competitive, MoA/Platform, and Expansion/Data into consistent two-card rows, made only Marketability span both columns, removed the misleading Marketability `KEYWORDS` label while retaining its A/B/C formulas, and preserved the mobile single-column layout.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: reduced Full Scout Final Status condition lists to the same muted regular-weight typography as Tab 1, removed the bold first-condition exception, refreshed stylesheet cache keys, and added regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: removed the forced 88px Full Scout status-summary height so REVIEW and adjacent summary boxes fit their copy without empty space; refreshed stylesheet cache keys and added regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: removed the redundant Shortlisting Unknown/N/A comparison strip and Action date auto-classification note while preserving their detailed cards and classification behavior; updated regression coverage.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: reorganized the right-side Marketability A/B/C formulas into the same titled group-box and rounded keyword-chip pattern as Target Relevance, preserving every formula and calculation order while making each factor and multiplication step easier to scan; refreshed stylesheet caches.
- `index.html` and `tests/test_dashboard_ia.py`: removed the cross-asset platform-evidence sentence from the Full Scout Parameter Guide while preserving the remaining Platform Attractiveness definition; added regression coverage.
- `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: changed the Marketability A/B/C formula-factor pills from semibold to regular weight while retaining the emphasized formula titles, grouping, and calculation order; refreshed stylesheet caches.
- `index.html` and `tests/test_dashboard_ia.py`: removed three redundant IND-enabling explanatory lines and the `충족 시 → 투자` footer from the Shortlisting investment guide card while preserving its actual stage conditions and classification logic; updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: removed the remaining `충족 시 → Value Up` and `충족 시 → 공동연구` footers from the Shortlisting decision cards while preserving their conditions and classification logic; updated regression coverage.
- `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: made the Tab 3 OI Partnership helper tint boxes fit their actual summary copy instead of filling the shared alignment row, while retaining the 78px grid track that keeps the first condition bullet aligned across investment, Value Up, and joint-research cards; refreshed stylesheet caches.
- `index.html`, `src/app.js`, `src/styles.css`, shared asset cache keys, and `tests/test_dashboard_ia.py`: added the active workflow mode to the connected description panel and gave only the Shortlisting `3차 집중 관리` left information icon a soft favorite-like amber border, background, and glyph color; left the other workflow icons and description text unchanged.
- `src/app.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: made the Tab 3 pipeline table proportionally compress below its prior minimum-width sum and disabled horizontal overflow only in Shortlisting mode; preserved other table modes, stored widths, and classification behavior while refreshing asset cache keys and adding regression coverage.
