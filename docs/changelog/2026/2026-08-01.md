# Work Log — 2026-08-01

## Changes

- `src/app.js`, `main.py`, `index.html`, `triage_detail.html`, `detail.html`, `src/triage-detail.js`, `src/detail.js`, `src/styles.css`, `json/drug-valuation.schema.json`, `json/schema.md`, `config/category-synonyms.json`, `config/scoring_criteria/v3_2_triage.md`, `config/scoring_criteria/v3_3_{full,display}.md`, `scripts/export_pipeline_wiki.py`, `tests/test_rubric_v32_v33.py`: advanced Fast Triage to instruction/rubric/schema v3.2 and Full Scout to instruction/rubric v3.3 while retaining Full Scout schema v3.2; shared the evidence-discipline and TR/MoA/Data rules, introduced SELECT/REJECT/UNVERIFIED plus criterion-level evidence basis and verified-source validation, standardized conservative development-stage mapping, removed Theme/Cluster-only Full Scout FAIL behavior, refreshed both criteria views and Fast result evidence labels, and added acceptance/regression coverage without migrating historical records.
- `main.py`, `src/app.js`, `src/triage-detail.js`, `tests/test_rubric_v32_v33.py`: aligned final save/display safeguards with the new contracts — current Fast records count only explicitly verified public URLs, `user_input_only` summaries reject unsupported target/MoA/cell/data claims, Markdown Triage rows reject legacy `N/A` and must match JSON statuses, and Full Scout rubric refresh treats lifecycle stages/flags such as `Discontinued / inactive` or `terminated` as hard blockers.
- `main.py`, `detail.html`, `src/detail.js`, `src/styles.css`, `json/drug-valuation.schema.json`, `json/schema.md`: added a single-record "Score 기준 갱신" button (Full Scout only) next to the Filter 2/Rubric badge — `POST /api/records/{id}/refresh-rubric` sends the record's GPT report + uploaded attachments + the current `v3.2` rubric text to OpenRouter, asking whether there's clear, unambiguous evidence to update *this record's* scores; it reuses `apply_ai_revision_scores`/`recalculate_total_score`/`append_source_report_revision` (the same primitives `apply-ai-revision` already uses) rather than new scoring-mutation code. On a genuine update it stamps a new `meta.rescored_rubric_version` (chained via the previously-dead-code `next_minor_version` helper, e.g. 3.1→3.2→3.3) plus `rescored_at`/`rescored_by`/`rubric_refresh_history`, and `renderMetaInfoBar` shows "원본 스코어링 지침 v{original} · Score recalculated with rubric v{rescored}"; `meta.rubric_version` itself is never touched. Unclear evidence, conflicting sources, or an API failure all leave the record byte-for-byte unchanged with a matching transient message via the existing `setCollaborationStatus`. v1 scope, confirmed with the user: single record only — no bulk rescore of other saved pipelines, and no auto-written `config/scoring_criteria/v3_x_*.md` files (a human formalizes actual rubric wording later from the logged reasons).
- `src/styles.css`: unified the top header title row (eyebrow label + main h1) across `index.html`, `detail.html`, `triage_detail.html`, and `wiki_view.html` — laid out eyebrow and h1 inline on one thin baseline-aligned line (separated by a small "·") instead of stacked block-level lines, shrunk the h1 font-size from clamp(26-36px)/clamp(22-34px) down to a single consistent clamp(15-18px, 700 weight) everywhere (including the mobile <720px breakpoint, now clamp(16-19px) instead of clamp(22-28px)), and forced `white-space: nowrap` + ellipsis so long asset/company titles truncate on one line rather than wrapping to two.
- `index.html`, `detail.html`, `triage_detail.html`, `wiki_view.html`, `src/styles.css`: made the top-left header title block (eyebrow + h1) a clickable link back to `/` on all four pages (wrapped in `<a class="topbar-home-link">`, replacing the old plain `<div>`/`.triage-title-block`), with a hover-dim state; updated the CSS selectors that previously targeted `.topbar > div:first-child` to target `.topbar-home-link` instead.
- `AGENTS.md`, `CLAUDE.md`, `.claude/skills/post-edit-log/SKILL.md`, `docs/CHANGELOG.md`, `docs/changelog/`: required a final `변경 요약`, changed the durable work log to one file per date, and reorganized the existing changelog entries into dated year folders.
- `main.py`, `src/detail.js`, `src/styles.css`, `json/drug-valuation.schema.json`, `json/schema.md`, `config/qualitative_review_criteria.md`: added cross-record custom qualitative-criterion recommendations and one-click definition import on the detail page, with duplicate prevention, usage/source context, and import provenance while excluding prior opinions.
- `index.html`, `detail.html`, `triage_detail.html`, `wiki_view.html`, `src/app.js`, `src/detail.js`, `src/triage-detail.js`, `src/wiki_view.js`, `src/floating-agent.js`, `src/styles.css`, `main.py`, `.gitignore`: completed an end-to-end UI/UX and functional regression pass covering mode-aware filters/KPIs/charts, responsive tables and detail layouts, floating Agent/modal behavior, unified GPT response parsing, attachment/wiki navigation, safer record and rubric-refresh validation, cache busting, and page icons; verified 33 saved records, core HTTP/API routes, desktop and 390px Chromium interaction with no runtime errors or page overflow, JS/Python syntax, CSS balance, DOM IDs/selectors, and `git diff --check`.
- `main.py`, `index.html`, `src/app.js`, `src/styles.css`, `tests/test_dashboard_summary.py`, `tests/test_dashboard_ia.py`: reorganized the main Dashboard around a standalone Fast Triage / Full Scout / Shortlisting workflow navigation, mode-specific descriptions and KPI cards, and an unfiltered three-card Summary (result donut, six SKBP-interest indication bars, and priority/action queue). Added the read-only `/api/dashboard-summary` aggregation contract using stable unique-asset identities, persisted HUMAN-over-AUTO partnership values, and deterministic action ordering; limited Search/Main indication/Modality/Theme/Cluster/Country/Filter 1·2·3 to each tab's table state; removed Stage/Due Date from the common filter bar; added accessible inline country flags, filtered Excel-compatible export UI, filtered/total unique-asset counts, table empty states, and responsive 3/2/1-column behavior. GPT instructions, scoring, Filter 3 classification, stored records, and detail criteria pages were not changed by this Dashboard IA work. Verification: Python compile, JavaScript syntax, DOM ID parity, live summary endpoint serialization, and full unit discovery `45/45` passed; an interactive browser backend was unavailable for the final visual pass.
