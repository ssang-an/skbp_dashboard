# 2026-09-08

## Changes

- 커스텀 Shortlisting Project의 "분류" 판단이 판단근거(criteria drawer)에만 표시되고 Pipeline Table에는 전혀 노출되지 않던 문제 수정. OIC의 Filter 3(`partnershipEditSelect`, `.partnership-edit-select`)와 동일한 시각 스타일(둥근 색상 pill `<select>`)을 재사용하는 `classificationEditSelect(row, project)`를 신설해, Custom Review 탭에서 비-OIC Project를 볼 때 "CUSTOM REVIEW" 그룹의 지표 컬럼들 바로 왼쪽에 "분류" 컬럼을 추가(`renderFocusTable`의 colgroup/헤더/바디 셀, `focusTableColumnKeys()`, `FOCUS_DEFAULT_COLUMN_WIDTHS`/`FOCUS_MIN_COLUMN_WIDTHS`에 `classification` 항목 추가). 값은 레코드별 수동 선택(자동 분류 로직 없음 — OIC의 규칙 기반 `partnership_type`과 달리, 이 Project가 정의한 `classification_columns` 중 리뷰어가 직접 고르는 방식)이며, 선택 안 함(`미분류`)을 포함해 4가지 OIC 톤 컬러(투자/Value Up/공동연구/N-A 색상)를 옵션 순서대로 순환 적용해 여러 분류를 시각적으로 구분. 백엔드에 `PATCH /api/records/{id}/shortlisting-projects/{project_id}`의 `field: "classification"` 케이스를 추가해 `classification_columns`에 존재하는 id인지 검증 후 `meta.shortlisting_projects.<project_id>.classification`에 저장(`main.py`), `json/schema.md`에 데이터 형태 문서화. 구현 중 "분류 관리"에서 분류를 추가/삭제해도 그 즉시 Pipeline Table의 select 옵션 목록이 새로고침 전까지 갱신되지 않던 버그를 발견해 수정 — `submitShortlistingClassificationModal`/`deleteShortlistingClassification`에 (지표 추가/삭제와 동일하게) `renderTable()` 호출을 추가.
- 분류 판단근거의 영문(EN) 번역을 OpenRouter로 자동 생성. Project 설정 → 분류 관리에서 새 분류를 추가하면(`POST /api/shortlisting/projects/{project_id}/classifications`), 서버가 `call_openrouter_translate_ko_to_en()`(신설, 기존 `openrouter_models_to_try()`/`post_openrouter()` 재사용)로 분류명·설명을 OpenRouter에 보내 리터럴 영어 번역을 요청하고 결과를 `label_en`/`description_en`로 함께 저장 — 번역은 어디까지나 부가 기능이라 `OPENROUTER_API_KEY` 미설정이나 호출 실패 시에도 분류 생성 자체는 그대로 성공하고 두 필드는 빈 문자열로 남는다(그 경우 판단근거의 EN 토글은 한국어 원문으로 대체 표시). 대시보드(`src/app.js`)와 상세 페이지(`src/detail.js`) 양쪽의 `renderCriteriaDrawerCustomProjectContent`/`updateCriteriaDrawerScope`가 판단근거 언어 토글(KO/EN) 상태에 따라 `label_en`/`description_en` 우선 사용·없으면 한국어 fallback으로 렌더링하도록 수정(스코프 라벨·부제도 함께 이중언어화). 브라우저에서 "test" Project에 분류("적극 검토"/"자료등록 완료 건")를 추가 → OpenRouter가 실제로 "Active Review"/"Data registration completed case"로 번역해 저장한 것을 JSON으로 확인 → Pipeline Table의 분류 select에서 선택 시 파란색(investment 톤) pill로 즉시 반영 → 판단근거를 EN으로 전환해 번역된 영문이 정확히 표시됨을 확인, 콘솔 에러 없음. 테스트 데이터(추가한 분류·레코드의 classification 값)는 JSON 파일 직접 롤백으로 정리.
- 백엔드 회귀 테스트 4건 신규 추가(`tests/test_shortlisting_project_permissions.py`): write 멤버의 분류 추가 403, owner의 분류 추가·삭제 성공(OpenRouter 응답 mock), OIC 기본 Project의 분류 추가 차단(400), 존재하지 않는 classification id로 레코드 업데이트 시 404. `pytest tests/test_dashboard_ia.py tests/test_shortlisting_project_permissions.py tests/test_shortlisting_tracking_status.py` 전체 재실행 결과 신규 실패 0건(`test_dashboard_ia.py`의 기존 무관 실패 45건은 diff 없이 동일, 신규 테스트 4건 포함 총 151건 통과).
- OIC Custom Review 탭의 D·Link(Disease Linkage) 자동 판정에 MoA Validity 점수 기반 결정론적 규칙 추가: v3.8 rubric상 MoA 3점만 "제안된 MoA 관련 직접 근거 확인됨"이고 2점은 target/pathway 수준의 간접·독립 검증에 그치므로, MoA 2점 레코드는 investigation_note 내용과 무관하게 OpenRouter LLM 호출 없이 D·Link를 자동 "X"로 확정(신규 `DISEASE_LINKAGE_CONFIRMED_MOA_SCORE = 3` 상수, `classify_record_disease_linkage()`에 `score < DISEASE_LINKAGE_CONFIRMED_MOA_SCORE` 분기 추가) — MoA 3점 레코드만 기존처럼 LLM이 note를 읽고 O/X/NA를 판정하며, 이 규칙으로 "O"는 MoA 3점에서만 나올 수 있게 됨. 기존 `disease_linkage_eligible()`(MoA 2점 이상만 분류 대상)·수동 오버라이드(`disease_linkage_status_source: "manual"`) 경로는 그대로 유지, `main.py`만 수정. `load_records`/`save_records`를 mock한 격리 함수 호출로 MoA 2점→"score_below_confirmed"/X/저장 1회(재호출 시 캐시로 저장 0회), MoA 3점·API 키 없음→기존 "unavailable" 경로 그대로임을 확인. `pytest tests/` 전체 재실행 결과 신규 실패 0건(기존 무관 실패 63건과 동일 목록, disease_linkage 관련 테스트는 기존에도 없음).
- Custom Review Pipeline Table에 Number/Date/Text 타입 커스텀 지표(예: "년수")를 추가하면 그 입력칸이 왼쪽 Filter 2 pill보다 눈에 띄게 커 보이던 문제 수정. `.evidence-edit`(boolean/list)·`.table-edit-select`(Filter 2)·`.partnership-edit-select`(Filter 3/분류)는 전부 `height: 30px` 고정인데, 같은 셀에서 쓰이는 `.focus-due-input`(number/date/text 지표 및 Action Date 공용 클래스)만 높이 지정이 없어 브라우저 기본 `<input>` 높이로 더 크게 렌더링되던 것이 원인. `.focus-status-cell .focus-due-input`(지표 셀에만 적용, Action Date 컬럼과 상세 페이지 등 다른 `.focus-due-input` 사용처는 영향 없음)에 `height/min-height/max-height: 30px`를 지정해 같은 행의 다른 컬럼들과 높이를 맞춤(`src/styles.css`; `index.html`/`detail.html`의 `styles.css` 버전 쿼리스트링을 `20260908-metric-input-size-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 동기화). 브라우저에서 "test" Project에 Number 타입 지표("년수")를 추가해 Filter 2·분류·자료등록여부·년수 네 칸의 높이가 픽셀 단위로 일치함을 확인 후 테스트 지표 삭제로 정리, 콘솔 에러 없음. `pytest tests/` 전체 재실행 결과 신규 실패 0건(기존 무관 실패 63건과 동일 목록).
- 상세 페이지 "TEAM REVIEW WORKSPACE" 상단 우측 즐겨찾기(별) 버튼(`#detailFocusToggle`)이 Home Dashboard Pipeline Table의 별표 버튼과 동작이 어긋나 있던 것을 동일한 규칙으로 통일. 기존에는 이 버튼이 항상 OIC(`focus_management`) 하나만 대상으로 동작해, 다른 Shortlisting Project에 쓰기 권한이 있어도 그 Project들은 전혀 반영되지 않았음. 이제 Pipeline Table의 `focusActionButton()`/`focusProjectPickerButton()`과 동일한 분기를 적용: 현재 계정이 쓸 수 있는 Project가 (1) 0개면 버튼을 숨기고, (2) 정확히 1개면 기존과 같은 3단계 순환(미등록→Priority(노란 별)→Stationary(검정 별)→제거, `saveDetailFocus`/`saveDetailShortlistingProjectTracking`로 OIC/커스텀 Project 어느 쪽이든 동일하게 처리)을 그 Project 하나에 대해 수행하고, (3) 2개 이상이면 클릭 시 Pipeline Table과 완전히 같은 마크업(`table-focus-project-picker*` 클래스 재사용, CSS 추가 없음)의 드롭다운을 새로 열어 Project별로 체크(★)/해제(☆) 가능하게 함(`renderDetailFocusToggle`/`toggleDetailFocusProjectPicker`/`renderDetailFocusProjectPickerPopover`/`pickDetailFocusProjectFromPicker` 신설, `src/detail.js`). 팝오버는 `document.body`에 동적으로 붙고 트리거 버튼 기준으로 위치를 계산하며, 바깥 클릭·Escape·버튼 재클릭으로 닫힘(Pipeline Table의 동작과 동일). `detail.html`의 `detail.js` 버전 쿼리스트링을 `20260908-focus-project-picker-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 3건 동기화. 브라우저에서 3-Project 트래킹 레코드(OIC+test+test2)로 드롭다운을 열어 전체 옵션·체크 상태 확인 → "test" 옵션 클릭으로 제거 → API로 `is_tracked:false` 확인 → 다시 클릭해 원복(`is_tracked:true` 확인) → 바깥 클릭/Escape/버튼 재클릭 3가지 방식으로 팝오버가 정상적으로 닫힘을 확인. 미등록 레코드로도 별표 클릭 시 즉시 드롭다운이 뜨는 것을 확인(추가 조작 없이 종료해 데이터 변경 없음). 3단계 순환 로직 자체(`FOCUS_TRACKING_COPY`/`saveDetailFocus`)는 이번에 새로 만든 것이 아니라 기존 코드가 이미 정확했음을 실제 클릭 3회로 재확인(초기 재현 시도에서 "한 번 눌러도 반응이 없다"는 결과가 나온 것은 자동화 브라우저 탭이 백그라운드 상태라 `setTimeout` 대기가 실제 저장 완료보다 먼저 끝나버린 테스트 쪽 레이스였고, 앱 코드 문제는 아니었음). `pytest tests/test_dashboard_ia.py` 수정 전/후 실패 목록을 diff한 결과 신규 실패 0건(기존 무관 실패 45건과 동일).
- 사이트 전역 권한 모델을 `user < admin < developer` 3단계에서 사실상 `user`/`developer` 2단계로 재편. `admin` role은 인프라(enum·`INITIAL_ADMIN_IDENTITIES`·`is_auth_admin`)는 남겨뒀지만 코드 어디에서도 더 이상 다르게 취급하지 않는다. **(1) main.py 24개 엔드포인트**에서 `require_auth_admin`/`is_auth_admin` 게이트를 제거하고 `require_authenticated_user`(로그인 여부만 확인)로 교체: Tab0 일괄업로드/Listing·Comment·Contact 편집(`import_candidate_queue`, `update_candidate_queue_listing_details`, `update_candidate_pipeline_metadata`, `delete_candidate_queue_entry`), Filter 2 수동 판정(`update_manual_review`, `update_manual_review_history_reason`), Rubric/OI 재분류(`refresh_record_rubric`, `recalculate_record_oi_partnership`), 레코드/첨부 삭제(`delete_records`, `delete_record_attachment`), Qualitative Review 기준 추가·삭제·AI생성, 레코드 저장(`PUT /api/records/{id}`, `POST /api/records`의 기존 리포트 덮어쓰기 확인 분기 포함). 예외 없이 전부 개방(삭제류는 프론트 `window.confirm()`이 이미 안전장치 역할). **단, `PUT /api/records`(전체 데이터셋 일괄 교체)는 그대로 `require_auth_developer` 유지** — 이 엔드포인트는 프론트 어디서도 호출하지 않아(확인 팝업 등 안전장치가 전혀 없음) API 전체를 통틀어 가장 파급력이 큰 쓰기 작업이라 예외적으로 developer 전용을 유지. **(2) Tab0 Comment/Contact History 편집 권한 개방**: `can_edit_listing_comment`/`can_edit_listing_contact`의 `team_review_import`(일괄업로드) 분기를 `is_auth_admin`→`bool(account)`(로그인만 하면 가능)로 변경, 직접 작성한 글(`admin_listing_post`/`admin_contact_post`)은 기존처럼 작성자 본인만 수정 가능. **(3) Tab2/Custom 상세페이지 댓글·Contact History 삭제**: 기존에 이미 구현돼 있던 확인 팝업(`confirmDetailCommentDelete`, "로딩중입니다" 계열과 같은 `.operation-modal` 공용 컴포넌트 재사용)을 그대로 활용해, `system_import`(Tab0 일괄업로드 동기화) 댓글의 관리자 전용 삭제 제한을 제거하고 로그인한 모든 사용자가 삭제 가능하도록 변경(`main.py`의 `delete_record_comment` 403 분기 삭제, `src/detail.js`의 `renderCommentNode` canDelete 조건에서 `is_admin` 제거). 이 경우 팝업 문구를 "Tab 0 팀 일괄 업로드로 동기화된 코멘트입니다. 삭제하면 동기화된 모든 페이지에서 이 코멘트 기록이 함께 삭제되며, 복구할 수 없습니다."로 분기(일반 본인 댓글은 기존 문구 유지); Contact History는 댓글과 동일한 `collaboration.comments` 배열·렌더 함수를 공유해 별도 구현 없이 함께 적용됨. Tab1→Tab2 위임 댓글("원작성자만 삭제 가능")은 별개 기능이라 그대로 유지. **(4) 프론트엔드 `is_admin` 게이트 약 40곳 제거**(`src/app.js`, `src/detail.js`, `src/triage-detail.js`): Partner Materials/DD Report 업로드·삭제, 테이블 셀 수동 편집(Company/Asset/Location/Modality/Target/Main indication/Pipeline Stage, Tab0 Listing 필드), Rubric/OI 재분류 버튼, Pipeline Website 편집, Triage 점수 인라인 편집·최종 코멘트, `step0ListingCommentCanEdit`/`step0ContactHistoryCanEdit` — 전부 로그인 여부만으로 개방. Topic note/Qualitative 의견 삭제는 admin 요건만 제거하고 **"본인이 쓴 것만 수정·삭제 가능"(id/email 매칭) 조건은 그대로 유지**(백엔드 `can_manage_topic_note`/`can_delete_topic_note`도 동일하게 ownership-only로 변경, AI가 생성한 Qualitative 의견은 작성자가 없으므로 `is_ai` 플래그로 누구나 삭제 가능하도록 처리). **(5) JSON 원본 편집기는 developer 전용으로 명확화**: `src/detail.js`의 `openEditDrawer()`가 `is_admin` 대신 `is_developer`를 확인하도록 수정(버튼 노출 자체는 이미 `data-auth-admin`→`auth.js`가 `is_developer` 기준으로 처리 중이었는데 클릭 핸들러만 admin 기준으로 어긋나 있던 기존 버그를 바로잡음). 이 드로어가 저장 시 쓰는 `PUT /api/records/{id}`는 AI 채팅 어시스턴트의 "수정 초안 적용" 기능도 같은 엔드포인트를 공유하므로 backend 자체는 로그인 사용자에게 열어두고, "JSON 원본 접근"은 UI 레벨(버튼 노출 + 클릭 핸들러)에서만 developer로 제한. **(6) Custom Review Project의 "site admin=모든 프로젝트 owner" 자동 바이패스를 developer 전용으로 좁힘**: `main.py`에 `is_auth_developer()` 헬퍼 신설, `shortlisting_project_role_for_account()`의 바이패스 조건을 `is_auth_admin`→`is_auth_developer`로 변경 — 이제 프로젝트 `members[]`에 명시적으로 등록되지 않은 한 admin이어도 기본은 read이며, developer(정주원)만 모든 프로젝트에서 자동 owner. 프로젝트 자체의 owner(설정·지표·분류·멤버 관리)/write(그 프로젝트 소속 레코드 값 편집)/read(조회만, 로그인 없이도 전체 프로젝트 목록·데이터 조회 가능) 3단계 모델과 각 역할이 할 수 있는 일은 기존 그대로 변경 없음 — 프로젝트 간 권한은 서로 완전히 독립적(한 프로젝트의 owner/writer라고 다른 프로젝트 권한이 생기지 않음). **(7) 예전 admin 6명(주연주/허정환/이정태/유택상/서지영/정영찬)을 OIC(`oic_default`) Project의 `write` 멤버로 전환**(`json/shortlisting-projects.json`, 다른 프로젝트에는 자동 추가되지 않음). 검증: `py_compile main.py` 통과, `unittest.mock.patch.object`로 격리한 함수 호출로 (a) `shortlisting_project_role_for_account`가 admin→read, developer→owner를 반환, (b) `can_edit_listing_comment`/`can_edit_listing_contact`가 일반 사용자에게도 True, (c) `can_manage_topic_note`가 ownership만으로 판정, (d) 일반 사용자 계정으로 `delete_record_comment`가 system_import 댓글을 실제로 삭제(저장 호출까지 확인)함을 각각 확인. `pytest tests/` 전체 재실행: 이번 변경으로 새로 깨진 테스트(주로 `require_auth_admin`을 직접 mock하던 테스트, admin bypass를 가정한 `test_shortlisting_project_permissions.py`/`test_shortlisting_tracking_status.py`, "admin만 삭제 가능" 구식 동작을 검증하던 `test_topic_notes_reupload.py`/`test_step0_pipeline_metadata.py`)을 전부 새 동작(로그인 사용자 개방·developer bypass·ownership-only)에 맞게 업데이트해 신규 실패 0건으로 정리(63건 남은 실패는 이번 변경 이전부터 있던, 다른 세션의 진행 중인 UI/텍스트 작업발 무관 실패와 동일 목록). 브라우저에서 페이지 새로고침 후 콘솔 에러 없음, `/api/auth/me`로 현재 계정 역할 확인, JSON 편집 버튼이 developer 계정에는 정상 노출됨을 확인.
