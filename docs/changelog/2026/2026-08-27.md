# 2026-08-27

## Changes

- Activated Graphify across the local development workflow: added its user bin directory to the persistent PATH, installed post-commit/post-checkout graph refresh hooks and the merge driver, installed Codex graph-first instructions plus its compatible hook config, and refreshed `graphify-out/` from the current repository.
- Extended the shared 0–3 score filter to the Full Scout-only Comp, Plat, Exp, and Market columns (Total remains sort-only because it is out of 21). Matched every new score-header trigger's muted color, font size, and weight to the existing Main indication, Pipeline Stage, and Filter 2 header controls.
- Restored the Pipeline Table's unobtrusive score-sort affordance: the TR/MoA/Data sort area is blank until sorting is active, then shows only the current up/down arrow; header text continues to open the new score filter. Made Full Scout aliases in Fast Triage use the same rectangular status/score treatment as native triage rows, and shortened the alias navigation notice to one non-redundant sentence.
- Added separate score-filter and sort controls to the Pipeline Table's TR, MoA, and Data headers for Fast Triage and Full Scout. Score popovers support exact or minimum-score selection (including typed expressions such as `>=2`, `>1`, and `=2`), OR within a criterion and AND across criteria; selected filters update the Dashboard summary to the filtered asset count.
- Removed the duplicate five-pill Partner Materials status row from the Full Scout Team Review Workspace Shortlisting card; source-status visibility remains in Partner Materials and Due Diligence. Reduced collapsed workspace padding, card gaps, and the Filter 2 score-panel reservation for a shorter Show/Less summary on all responsive detail layouts.
- Aligned dashboard-owned totals and final statuses at paste validation, AI second parsing, and the server save boundary. GPT criterion scores/evidence remain unchanged, while stale derived totals or status labels are recalculated under the current Fast Triage/Full Scout rules instead of blocking a valid save; added regression coverage and documented the maintenance rule. Verified the DM3159 pasted report after the existing safe bare-`source_url` repair: `15/21`, `PASS`.

- Aligned the English Full Scout Target Area Relevance parameter card with the Fast Triage card, including the shared title, scope explanation, and priority/broad-scope evidence list. Updated Fast Triage and Full Scout fallback guide labels to use the same display name.
- Matched the English Evidence Level cards to the Korean guide's icon, pill, and bullet-list structure; aligned the Full Scout Data table with the complete stage-appropriate two-domain and program-progression wording.
- Emphasized the six-priority-indication and quantitative-data-domain thresholds in Fast Triage and Full Scout judgment-guide tables, including the English guide.
- Added a KR/EN toggle to the Dashboard judgment guide, with a complete English Fast Triage, Full Scout, and OI partnership guide; retained Korean as the default and persisted the selection locally. Reworded MoA 3 display guidance to make clear that target engagement is one of three alternative asset-specific direct-validation paths, not a mandatory requirement.
- Expanded the rubric release-maintenance guide to distinguish instruction-only operational changes from scoring, schema, and display-only changes, including when backend changes are not required.

- Aligned the legacy rubric regression fixtures with Fast Triage v3.4 and Full Scout v3.6, including the current TR, status-gate, country, and development-stage behavior; corrected the backend TR helper to use indication scope only. Added the rubric release-maintenance guide and manifest-level display-version tracking.
- Clarified Platform 3 as quantitative advantage reproduced across multiple conditions or platform-derived assets with independent/external validation or use, or an officially linked platform asset reaching First Patient Dosed.
- Revised Fast Triage to use SELECT / REJECT / INSUFFICIENT. INSUFFICIENT now covers identity or terminal-lifecycle early stops and completed assessments with a TR, MoA, or Data score of 0; REJECT covers identified non-terminal candidates that do not meet SELECT. Updated the prompt, validator, dashboard compatibility mapping, judgment guides, and schema together without a rubric-version bump. Target Relevance now scores strategic indication scope only: six priority indications (3), broader neurological/psychiatric/neuroimmune/neurodegenerative/pain scope (2), and confirmed assets outside that scope (1).
- Clarified Fast Triage judgment-guide copy for REJECT and identity/lifecycle early-stop score display.
- Simplified identity/lifecycle early-stop guidance and clarified that a zero core score after identity verification is an evaluated INSUFFICIENT result.
- Aligned pre-release Fast Triage and Full Scout criteria across active rubric/display documents, copied GPT instructions, judgment-guide screens, schema guidance, and regression coverage; removed the identity-source helper line from the visible Fast Triage guide.
- Simplified the visible Full Scout REVIEW and FAIL judgment-guide language without changing scoring or early-stop rules.
- Removed the topic-marker particle from the Fast Triage REJECT judgment-guide bullet ("TR·MoA·Data는 모두 1점 이상이지만" → "TR·MoA·Data 모두 1점 이상이지만") on the Dashboard and Fast Triage detail page.
- Reworded the Fast Triage REJECT judgment-guide subtitle on the Dashboard and Fast Triage detail page ("현재로서 공개 근거 확인을 통해서는 추가 Full Scout 조사 대상은 아니지만, 추후 추가 공개 근거 확인 후 재평가 대상입니다." → "현재 공개 근거를 기반으로 조사한 결과 Full Scout 조사 대상은 아니지만, 추후 공개 근거 확인 후 재평가 대상입니다.").
- Reworded the Fast Triage INSUFFICIENT judgment-guide subtitle on the Dashboard and Fast Triage detail page ("Full Scout로 넘기기 위한 최소 확인 근거가 부족하거나, 조사 조기 종료 사유가 확인됐습니다." → "조사 조기 종료 사유가 확인됐거나, 추가 조사를 수행할 수 있는 최소 확인 근거가 확인되지 않았습니다.").
- Trimmed the redundant "Full Scout 조사에" lead-in from the Full Scout FAIL judgment-guide summary on the Dashboard ("Full Scout 조사에 필요한 핵심 정보가 부족하거나, ..." → "필요한 핵심 정보가 부족하거나, ...").
- Combined the MoA Validity and Data Maturity PASS-gate bullets into one line ("MoA Validity = 3, Data Maturity = 3") on the Dashboard Full Scout judgment guide to save vertical space; verified `tests/test_rubric_release_manifest.py` still passes since it only checks for the substrings.
- Reworded the Fast Triage REJECT judgment-guide subtitle again on the Dashboard and Fast Triage detail page for smoother phrasing ("현재 공개 근거를 기반으로 조사한 결과 Full Scout 조사 대상은 아니지만, ..." → "현재 공개 근거의 한계로 Full Scout 조사 대상은 아니지만, ...").
- Fixed the Dashboard Full Scout PASS/REVIEW/FAIL judgment-guide cards: the fixed-height summary slot (78px, desktop) wasn't filled by the colored summary box itself, so shorter-text cards (e.g. PASS, FAIL) left a large uncolored gap before the bullet list below. Made `.criteria-full-status-summary` stretch to fill its slot and lowered the bullet-list top margin from 12px/8px to a consistent 8px. Verified via layout measurement in a headless browser that all three cards now have zero empty space inside the slot and an identical 8px gap to the list.
- Further trimmed the Fast Triage judgment guide on the Dashboard and Fast Triage detail page: dropped "확인" from the INSUFFICIENT subtitle ("최소 확인 근거가 확인되지 않았습니다" → "최소 근거가 확인되지 않았습니다"), removed the redundant "추가 공개 근거 또는 active status 확인 후 재평가" bullet from the REJECT card, combined the SELECT card's MoA/Data gate bullets into one line ("MoA 1점 이상, Data 2점 이상"), and removed the "(identity 미확인 시 조사 조기 종료)" aside from the Fast Triage Target Area Relevance 0-point scoring-table cell (the identical Full Scout scoring-table cell on `index.html`/`detail.html` was left unchanged). Verified `tests/test_rubric_release_manifest.py` still passes; confirmed the remaining `test_dashboard_ia.py` failures pre-date this change (stale UNVERIFIED-era and v3.3-badge assertions from earlier rubric renames) and are unrelated to these edits.
- Reordered the Full Scout criterion presentation so the three Fast Triage-shared criteria (Target Relevance, MoA Validity, Data Maturity) lead, followed by Competitive Landscape, Platform Attractiveness, Expansion Potential, and Marketability — matching the Pipeline Table's existing column order. This is a pure rearrangement of unchanged content, so no rubric version bump: updated in place on the current v3.6 instruction only. Scope: (1) `src/app.js`'s GPT instruction 2 prompt template — the "## 3) Scorecard Summary" table and "## 4) Criterion Detail Pages" subsections (renumbered 4.1–4.7) — plus `COMPACT_FULL_SCOUT_JSON_TEMPLATE`'s `scoring.criteria` key order, so future GPT Full Scout reports/JSON follow the new order; (2) `src/detail.js`'s `reviewScoreOrder` (Team Review Workspace score chips) and `scoringLabels` (Score Evidence cards), which reorders the display for every existing and future record immediately since it's render-time only — no stored JSON/Markdown was touched or needs migration. Verified `node --check` on both files and re-ran `tests/test_rubric_v32_v33.py` (62 tests) plus `tests/test_rubric_release_manifest.py` — all pass; `tests/test_dashboard_ia.py`'s pre-existing 34 failures/4 errors are unchanged (unrelated, pre-dating this change).
- Extended the same Full Scout criterion reorder to the remaining static display surfaces: the Dashboard and Full Scout detail-page 판단근거 scoring tables (`index.html`, `detail.html`) and their Parameter Guide card grids (`index.html`, `detail.html`), all reordered to Target Relevance, MoA Validity, Data Maturity, Competitive Landscape, Platform Attractiveness, Expansion Potential, Marketability, with the Parameter Guide's numbered headings (`detail.html`'s "1.–7.") renumbered to match. Updated `tests/test_dashboard_ia.py::test_full_scout_parameter_guide_uses_shared_icon_card_system`'s expected card order to match, since it was order-sensitive and pinned to the old sequence; re-ran the full `test_dashboard_ia.py` suite afterward and confirmed the failure/error count returned to the pre-existing baseline (34/4), i.e. no new regressions.
- **Rubric-tracking audit and fixes (instruction-only; no rubric/schema version change; treated as a pre-deployment consolidation into the existing v3.6 release per `docs/rubric-release-maintenance.md` §1's explicit exception for unshipped work, with the user's approval):**
  - **Bug fix:** `config/scoring_criteria/v3_6_full.md` was a 35-line "amendment" that only said it "inherits" the seven criterion definitions from `v3_5_full.md`, with no code resolving that inheritance. `main.py`'s `build_rubric_refresh_prompt()` — the only runtime reader of this file, used by the Pipeline Table's per-record rubric-refresh ("새로고침") icon — sent this incomplete excerpt to OpenRouter as "the current rubric," missing the Evidence Type ladder, Evidence Discipline, canonical taxonomy, and the Competitive Landscape and MoA Validity scoring definitions entirely (both absent from the amendment's partial restatement). Rewrote `v3_6_full.md` as a complete, self-contained document (full v3.5 content plus v3.6's Hard Filter pre-research-gate framing and the early-stop `—` display rule), so rubric-refresh re-scoring now receives the full rubric. Fast Triage's `v3_4_triage.md` did not have this problem (already a complete 135-line standalone doc).
  - Rewrote `config/scoring_criteria/v3_6_display.md` the same way: it was also a thin delta missing a Competitive Landscape reminder entirely and using a third, inconsistent criterion order. Now a complete, self-contained document in the new TR/MoA/Data-first order. This endpoint (`GET /api/scoring-criteria`) has no current frontend caller, so this was inert today but is now correct for when it is wired up.
  - Reordered `main.py`'s `CRITERION_IDS`/`RULE_PREFIXES` and `record_storage.py`'s `FULL_CRITERION_IDS` to the same TR/MoA/Data-first order, for consistency with every display surface (previously the only place the old order still existed). Verified via a temporary revert/re-apply of just these three constants that the full test suite's failure count is identical before and after (53 failures/9 errors baseline across all 413 pre-existing tests) — order-independent (name-keyed) usage confirmed, no regressions.
  - Found and fixed the same stale ordering in `src/criteria-guide-i18n.js` (the English judgment-guide toggle added earlier today), which still listed Full Scout criteria in the old order — a new EN/KO mismatch that the Korean-only reorder above had introduced.
  - Added two regression tests to `tests/test_rubric_release_manifest.py`: `test_full_scout_rubric_and_display_docs_are_complete_not_thin_deltas` (fails if a future version's rubric/display doc drops a criterion's actual rule text) and `test_full_scout_criterion_order_is_shared_across_backend_and_display` (fails if the backend constants or any of the rubric/display/i18n surfaces drift out of the shared order).
  - Verified: full test suite (415 tests including the 2 new ones) at the pre-existing baseline (53 failures/9 errors, all pre-dating and unrelated to this work); `node --check` on `src/app.js`, `src/detail.js`, `src/criteria-guide-i18n.js`; `python -m py_compile main.py` — matching `docs/rubric-release-maintenance.md` §4's release-check list.
- **Doc fix (`docs/rubric-release-maintenance.md`), no code change:** found that its change-type table told authors they could bump `instruction_version` alone while leaving `rubric_version` unchanged for procedural-only prompt edits, but `main.py`'s manifest loader hard-enforces `instruction_version == rubric_version` at startup (raises `RuntimeError` otherwise) — that scenario has never been executable. Rewrote the guide to state the two versions always move together (per explicit user direction: fix the guide to match the code, not relax the code constraint), while keeping the score-meaning-vs-procedure distinction as the criterion for whether `main.py`'s calculator/validator also needs to change. Also added two previously-undocumented no-version-bump categories to the change-type table and changelog-label list: (1) technical bug fixes in an instruction/prompt file (e.g. today's `src/app.js` backtick-escape fix) that don't change research scope or scoring meaning, and (2) the existing `config/instruction_warnings.json` auto-appended caution-note mechanism (`main.py`'s `append_instruction_warning`/`load_instruction_warnings`, `app.js`'s `copyPromptToClipboard`), which was already live in production data but entirely unmentioned in the guide.
- Added a confirmation popup before navigating from a Full-Scout-completed row shown on the Fast Triage (Tab 1) Pipeline Table to its Tab 2 detail page, reusing the existing shared `step0EditLockedModal` component (same modal already used for the Listing-tab comment/official-field "navigate to source" flows) instead of building a new one. Clicking such a row (`data-full-scout-alias="true"`, e.g. `Illimis Therapeutics ILM01`) now calls `openStep0EditLockedModal('full', { recordId, fullScoutAlias: true })` — new title "Full Scout 원문 리포트는 Tab 2에서 확인합니다", message explaining the row is a read-only Full Scout alias, and a "Tab 2 상세 페이지로 이동" button — instead of navigating immediately; the button then does the same `/detail?...&tab=full` navigation the row used to do directly. `src/app.js` only; no HTML/CSS changes since the modal markup/styling is reused as-is. Verified in a headless browser end-to-end (row click opens the modal with the expected title/message/button text, close button dismisses without navigating, Go button navigates to the correct Full Scout detail URL) and confirmed via `node --check` and the full test suite that no test covers or was broken by this change (no existing test referenced `fullScoutAlias`/`data-full-scout-alias`).
- Renamed the Step 0 진척 현황 (Tab 0) Listing Comment popover's label from "Listing Comment Post" to "Comment" (`src/app.js`'s `openStep0MetadataPopover`, `label`/`postLabel`), affecting the popover header, the edit-mode header/placeholder, and the "add/edit" button text; the delete-button aria-label inherits the same change since it reuses `postLabel`. Contact History's label is unchanged. Verified with `node --check src/app.js`.
- Restricted editing of bulk-imported (Excel `team_review_import`) Tab 0 Comment/Contact History entries to developer-role accounts only; previously any admin could edit them once posted, same as a directly-authored Listing Comment/Contact History post. Changed `main.py`'s `can_edit_listing_comment`/`can_edit_listing_contact` (used by `PATCH /api/candidate-queue/metadata`, which already required admin auth via `require_auth_admin`) to require `has_auth_role(account, ROLE_DEVELOPER)` for `team_review_import`-sourced entries instead of unconditionally allowing any admin, and mirrored the same check client-side in `src/app.js`'s `step0ListingCommentCanEdit`/`step0ContactHistoryCanEdit` (`user?.is_developer`) so non-developer admins no longer see an edit affordance on bulk-imported entries. The "일괄 업로드: ..." source label itself is unchanged and intentional — confirmed against `docs/todo/87-tab0-comment-출처와-시간-가독성-정비(완료).md` and commit `58597dc7` ("refine: label bulk listing comments"), which deliberately distinguish Excel bulk-import posts from admin-authored ones in the feed. Verified with `python -m py_compile main.py` and `node --check src/app.js`.
- Fixed inconsistent judgment-guide status "pill" sizing/spacing between the Full Scout PASS/REVIEW/FAIL cards and the Fast Triage SELECT/REJECT/INSUFFICIENT cards (`src/styles.css`). Both cards' colored summary box was being forced into a fixed-height slot at desktop widths (Full Scout: 78px via `grid-template-rows: auto 78px 1fr` + a flex-filled `.criteria-full-status-summary-slot`; Fast Triage: a `minmax(74px, auto)` grid row) so text-hugging text left blank space inside the box before the bullet list — and the two mechanisms produced different actual gaps (8px vs ~18.8px measured in a headless browser at 1440px width), which is what read as "different pill size and spacing." Removed both fixed-height mechanisms entirely (box now sizes to its own content at every breakpoint) and made the bullet-list top margin (`8px 0 0`) the single, unconditional source of the gap for both tabs, replacing Fast Triage's stray `margin-bottom: 12px` on the summary box and its media-query-only `margin: 0` on the list (which combined to the inconsistent gap). Verified in a headless browser: both sections now measure an identical 8px gap and content-sized (not artificially stretched) box heights at 500px/800px/1440px viewport widths. Updated two now-stale `tests/test_dashboard_ia.py` tests that had asserted the old fixed-height CSS (`test_full_status_total_score_rules_share_a_desktop_baseline` → `test_full_status_summary_hugs_its_content_instead_of_a_fixed_slot`; `test_triage_status_bullet_lists_share_vertical_start` → `test_triage_status_summary_hugs_its_content_instead_of_a_fixed_row`) to assert the new behavior instead; re-ran the full suite and confirmed the failure/error count returned to the pre-existing baseline.
- Pulled one new origin/main commit (`8346f5f7 more assets 27`) that only touched data files (`json/pipeline-records.json`, `json/candidate-queue.json`, `config/instruction_warnings.json` — asset/Step 0 진척 현황/detail-page Tab 2 메모 content). Verified no overlap with the day's locally uncommitted code edits before pulling; fast-forwarded cleanly, no conflicts.
- Added `.claude/skills/safe-pull/SKILL.md`: a new skill that, before any `git pull` with local uncommitted changes present, diffs the remote's new commits' changed files against `git status`'s local-touched files and only pulls once confirmed there's no file-level overlap (otherwise reports the overlapping file(s) and asks how to proceed instead of pulling blindly).
- Installed the `graphifyy` CLI (`uv tool install graphifyy`) and wired up Graphify for this repo so Claude Code routes file exploration through a knowledge graph instead of raw grep/read. `graphify claude install` appended a `## graphify` section to `CLAUDE.md` (query/path/explain-first workflow, `graphify update .` after edits) and registered a `PreToolUse` hook in `.claude/settings.json` (matchers `Bash|Grep` and `Read|Glob`) that nudges toward `graphify query`/`explain`/`path` before raw file reads or searches. Built the initial graph with `graphify extract . --code-only` (AST-only, no LLM cost) followed by `graphify cluster-only . --no-label`, producing `graphify-out/graph.json` (3267 nodes, 7776 edges, 146 communities), `GRAPH_REPORT.md`, and `graph.html`; community labels are left as placeholders (`--no-label`) pending an optional LLM-backed `graphify label .` pass. Also ran `graphify install --platform claude`, which installed the `/graphify` skill and a separate CLAUDE.md at the user level (`~/.claude/`), outside this repo. Added `graphify-out/` and `.claude/settings.json.graphify-bak` to `.gitignore` — the graph is regenerable local tooling state (like `.pytest_cache/`), not a deliverable vault like `obsidian/`/`skbp_pipeline_wiki/`.
- Removed the faint 1px divider between the header's Shortlisting tab and Wiki Map link (`src/styles.css`'s `.app-shell:not(...) > .topbar .pipeline-table-tabs button + .workflow-knowledge-map-link` rule, `border-left: 1px solid rgba(255, 255, 255, 0.11)` → `none`). No test covered this rule; verified via headless browser that `#knowledgeMapTab`'s computed `border-left` is now `0px none`.
- Fixed the Wiki Map header tab not showing the same gray "selected" shading as the other tabs when active: `src/styles.css`'s dark-header override `.app-shell:not(...) > .topbar .workflow-knowledge-map-link.active` was hardcoding `background: transparent; border-color: transparent; box-shadow: none`, overriding the base purple-tinted active style and never matching `.topbar-top .pipeline-table-tabs button.active`'s gray background (the other tabs are `<button>`s, so that rule never applied to this `<a>` link). Changed it to the same `rgba(255, 255, 255, 0.11)` background / `rgba(255, 255, 255, 0.14)` border / inset box-shadow the button tabs use. Verified via headless browser at `/?tab=map`: `#knowledgeMapTab.active`'s computed background is now `rgba(255, 255, 255, 0.11)`, matching the other tabs.
- Strengthened the Wiki Map header tab's hover shading (`.workflow-knowledge-map-link:hover` background `rgba(255,255,255,0.045)` → `0.1`) to match the other tabs' real hovered background, and rewrote its `data-tooltip` copy ("Pipeline과 Company·Target·MoA·근거의 연결을 탐색합니다." → "저장된 Pipeline을 Company·Target·MoA·근거 중심의 지식 그래프로 탐색합니다."). Also bumped `index.html`'s `styles.css` cache-busting query (`?v=20260827-score-header-filter-1` → `?v=20260827-wiki-map-tab-parity-1`) so browsers that had already cached today's earlier stylesheet edits (the border and active-background fixes above) actually pick up all three CSS changes — likely why the hover/tooltip fix wasn't visible without this. Verified via headless browser (fresh load, no cache) that hovering `#knowledgeMapTab` now sets `background: rgba(255, 255, 255, 0.1)` and shows the updated tooltip text with `opacity: 1`.
- Fixed the Pipeline Table's colored score/status badges (`src/styles.css`) rendering too bright in dark mode: the PASS/REVIEW/FAIL and SELECT/REJECT/INSUFFICIENT status pill, the seven per-criterion 0–3 score cells, the Total score circle, the evidence-level pills, and the OI Partnership type pill are all editable `<select>`/`<input>` elements (`table-edit-select.status-edit.*`, `table-edit-select.score-edit.*`, `total-score-edit-circle.*`, `evidence-edit.*`, `partnership-edit-select.*`) that had no `html[data-theme="dark"]` background override — they kept their light-mode opaque pastel backgrounds (`#e1f6eb`/`#fff2d7`/`#fae7e7`/etc.) while their text color switched to the brighter dark-mode `--green`/`--yellow`/`--red`, producing a bright pastel chip glowing against the near-black table. Extended the existing dark-mode translucent-background rules (already applied to the read-only `.score.high/mid/low` and `.pill.*` badges) to cover all of these editable variants, plus added new tinted-background rules for the Partnership pill's investment/value-up/joint-research tones, so every badge now uses a dark-tinted, semi-transparent background with the same bright text color for contrast. Light mode is untouched — same selectors only take effect under `html[data-theme="dark"]`. Bumped `index.html`'s `styles.css` cache-busting query (`-wiki-map-tab-parity-2` → `-dark-table-badge-contrast-1`). Verified visually in a headless browser: Full Scout table screenshots before/after in dark mode show the badges going from solid light pastel blocks to dark-tinted chips consistent with the rest of the dark UI, and a light-mode screenshot after the change confirms the original pastel colors are unchanged.
- Gave the four header tab buttons' leading icons (the `0`/`1`/`2` step badges and the Shortlisting star) a hover-grow affordance: added `transform: scale(1.12)` on hover plus `transition: transform 150ms ease` to `src/styles.css`'s shared `.tab-step` rule (`.topbar-top .workflow-navigation-shell .pipeline-table-tabs button:hover .tab-step`). Initially also applied the same treatment to the Wiki Map sparkle `✦` (`.knowledge-map-icon`), which required adding `display: inline-block` — but that overrode the `display: inline-grid; place-items: center` centering it inherits (as a `.tab-step`-classed element) from the shared rule, so the enlarged glyph rendered top/left-anchored instead of centered and visually "flew" out of its 22px badge on hover. Reverted per user feedback: removed `.knowledge-map-icon` from the hover-scale selector and dropped the `display`/`transition` additions from its own rule, restoring its original (non-scaling) hover behavior while keeping the new grow effect on the other four tabs. Verified via headless browser: `#knowledgeMapTab .knowledge-map-icon`'s bounding box and `transform` are now unchanged on hover (22×22, `none`), while `#focusManagementTableTab .tab-step` still grows centered (22×22 → 25×25, `scale(1.12)`). Bumped `index.html`'s `styles.css` cache-busting query again (`?v=20260827-wiki-map-tab-parity-1` → `-2`) for this revert.
