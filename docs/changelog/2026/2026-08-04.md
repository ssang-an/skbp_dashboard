## Changes

- `src/app.js` and `tests/test_rubric_v32_v33.py`: changed GPT Instruction 2 Full Scout from separate Markdown and JSON fenced blocks to one `text` fenced block containing the Markdown report, the exact `--- JSON DATA ---` separator, and the raw JSON object; aligned normal and identity-not-verified output contracts with Instruction 1 and added focused prompt-contract coverage while retaining the existing shared combined-response parser and schema/rubric versions.
- `index.html` and `tests/test_dashboard_ia.py`: renamed the TAB2 Marketability formula's final `Global` row to `D. Global` so the four calculation steps consistently follow A/B/C/D labeling; added a focused regression assertion without changing the formula itself.
- `detail.html`, `src/detail.js`, `src/styles.css`, shared HTML cache keys, and `tests/test_dashboard_ia.py`: restyled the attachment-viewer `GPT 원문으로`, `원본 다운로드`, and `원본 파일 열기` actions as one horizontal set of matching 30px circular dashboard icons; added icon-only hover tooltips and ARIA labels, reused the AI answer full-view corner icon for opening the original/preview file, and refreshed focused regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, shared HTML cache keys, and `tests/test_dashboard_ia.py`: moved attachment `원본 다운로드` and `원본 파일 열기` links from the preview body into a dedicated action group directly below `← GPT 원문으로`; replaced text arrows with dashboard-style download/external-open SVG buttons, added responsive header wrapping, preserved original-file download and converted-preview opening targets, and added focused regression coverage.
- `src/app.js` and `tests/test_rubric_v32_v33.py`: changed GPT Instruction 1 Fast Triage from two separately copyable Markdown/JSON fenced blocks to one `text` fenced block containing Markdown, an exact `--- JSON DATA ---` separator, and the JSON array; enhanced unified-response parsing so JSON nested inside that single block is extracted while `source_report.raw_markdown` retains only the Markdown portion; added focused prompt-contract and parser regression coverage without changing Instruction 2.
- `index.html` and `tests/test_dashboard_ia.py`: simplified the TAB2 Full Scout judgment guide by removing the newly added COMP similarity/check/search list, Platform evidence/FPD list, Expansion program/scope/exclusion list, Data evidence-domain list, Marketability assessment/benchmark/pricing/external-forecast list, and Evidence Origin/judgment rows; retained each criterion's concise description, the Data Maturity card, scoring table, and Marketability formulas, and updated focused regression expectations.
- `index.html` and `tests/test_dashboard_ia.py`: updated only the TAB2 Full Scout judgment guide content to match the current Instruction 2 rules—15-point PASS with TR/MoA/Data gates and active identity/program checks, 9–14 REVIEW and explicit lifecycle FAIL, criterion-specific scoring copy, competitive-search sufficiency, reusable-platform and asset-specific expansion/data guidance, calculation-or-external-forecast Marketability with one-time US-to-Global ×1.5 conversion, and separated Evidence Type/Origin records—while preserving the existing component order, classes, styling, and responsive layout; added focused regression coverage.
- `src/app.js`: revised only GPT Instruction 2 Full Scout and its backward-compatible JSON template without changing schema/instruction/rubric version values; added attachment evidence origin/nature handling, the canonical 15-point PASS gate and lifecycle failures, criterion-specific Competitive/Platform/Expansion/Data rules, calculation-or-external-forecast Marketability scoring, optional competitive/forecast/attachment audit fields, and final consistency checks; removed conflicting legacy thresholds/formulas and collapsed the unused compact prompt copy to the canonical prompt.
- `src/detail.js`, `src/styles.css`, `detail.html`, `index.html`, `triage_detail.html`, `wiki_view.html`, and `tests/test_dashboard_ia.py`: when previewing a converted PowerPoint attachment in Partner Materials, replaced the single ambiguous "원본 파일 열기" link (which actually pointed at the converted PDF) with two explicit `PDF 다운로드` / `PPT 다운로드` buttons that download the converted PDF and the original PPT/PPTX file respectively with proper filenames; non-slide attachments keep the original single-link behavior unchanged. Added an 8px gap to the shared toolbar to fit the second button, refreshed shared stylesheet/detail script cache keys, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: moved the `PDF 다운로드` / `PPT 다운로드` buttons out of the in-viewer toolbar and into a new `detailAttachmentDownloadActions` group stacked directly below the `← GPT 원문으로` back button in the report panel header, so they no longer overlap the PDF iframe; non-slide attachments are unaffected, refreshed shared cache keys again, and updated regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: reverted the header-group relocation back to the original in-viewer toolbar and removed the separate `PDF 다운로드` control (the browser's native PDF viewer already offers its own download), replacing it with a consistent rule applied to every attachment type: real `.pdf` attachments keep only the single `원본 파일 열기` link, and every non-PDF attachment (PPT/PPTX, DOC/DOCX, XLS/XLSX, TXT) now shows both `원본 다운로드` (forces a save of the actual original file) and `원본 파일 열기` (opens whatever is being previewed); removed the now-unused `detailAttachmentDownloadActions` element and `.detail-viewer-back-group` styling, refreshed shared cache keys, and rewrote the regression test.
