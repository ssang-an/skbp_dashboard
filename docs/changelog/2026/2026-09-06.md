# 2026-09-06

## Changes

- Shortlisting를 OIC 전용에서 전사 플랫폼으로 확장: 새 "Project" 개념 추가. 각 팀이 독립적인 Shortlisting 시트(Project)를 만들고, 그 안에서 자유롭게 커스텀 지표 컬럼(이름/설명/리턴 값 타입: Boolean·List·Integer·Score·Text)을 정의해 관리할 수 있다. 기존 OIC 컬럼(Filter3/DD/In-vivo/In-vitro/ADMET/D·Link/Action date)은 로직 변경 없이 새 시스템의 "기본 Project"(`oic_default`)로 편입되었고, 레코드별 상태도 계속 `meta.focus_management`에 저장되어 기존 데이터 마이그레이션이 전혀 필요 없다. 새 Project의 레코드별 상태는 별도 네임스페이스 `meta.shortlisting_projects.<project_id>`에 저장되며, 트래킹 대상 레코드 목록도 Project별로 완전히 독립적이다 (`main.py`, `src/app.js`, `index.html`, `src/styles.css`, `json/schema.md`).
  - 신규 스토어 `json/shortlisting-projects.json` (기존 `candidate-queue.json`과 동일한 lazy-seed 패턴). Project CRUD: `GET/POST /api/shortlisting/projects`, `PATCH/DELETE /api/shortlisting/projects/{id}` — 생성/수정은 admin이 아닌 로그인 유저 누구나 가능(전사 확산 목적). 지표 컬럼 CRUD: `POST/DELETE /api/shortlisting/projects/{id}/columns` (label ≤60자, description ≤400자, `list` 타입 옵션 1-20개, `score` 타입 `max_value` 1-100, Project당 커스텀 컬럼 최대 20개).
  - 레코드별 Project 트래킹/필드 수정은 신규 `PATCH /api/records/{id}/shortlisting-projects/{project_id}` (로그인 전용, `oic_default`는 404로 거부해 기존 admin 전용 `/focus-management` 엔드포인트를 그대로 사용하도록 강제)로 분리 — 기존 OIC 엔드포인트는 `custom_score` 필드 케이스 하나만 추가되었을 뿐 그 외 로직은 손대지 않음.
  - Shortlisting 테이블에 새 컬럼 3개 추가: 기존 "Total Score"(Full Scout 0-21 미러) 컬럼은 "Scout Score"로 이름만 변경(위치·로직 불변), Filter 2 바로 앞에 신규 "Total Score"(Scout Score + Custom Score, 0-30) 컬럼 추가, 관리 컬럼 바로 앞에 신규 "Custom Score"(0-9, 사람이 입력) 컬럼 추가 — 기본/커스텀 Project 공통 적용.
  - Full Scout 탭의 "즐겨찾기" 별표 버튼이 현재 선택된 Shortlisting Project를 대상으로 트래킹하도록 일반화되어(`focusActionButton`), 다른 팀이 자기 Project에 레코드를 추가할 수 있는 유일한 진입점이 됨. 기본값이 `oic_default`라 기존 OIC 사용자 동작은 완전히 동일하게 유지됨.
  - 브라우저에서 Project 생성 → 지표 추가(boolean/list) → Full Scout에서 레코드 트래킹 → 지표 값 편집 → Custom Score 입력 → Total Score 합산 확인까지 end-to-end 검증, 기존 OIC Shortlisting 워크플로(D·Link, Filter3, DD, ADMET 등) 회귀 없음 확인.
- Shortlisting Project 선택 UI를 Pipeline Table 패널 헤더에서 상단 헤더바/필터바로 재배치: "+ 프로젝트 추가" 버튼은 헤더 상단(판단근거~유저 프로필 사이)으로 이동해 새로고침/판단근거/PIPELINE 업로드와 동일한 pill 버튼 스타일을 적용받도록 `#shortlistingProjectSwitchButton`/`#addShortlistingProjectButton`을 기존 ID 허용목록 CSS 규칙에 추가했고, Project 선택은 네이티브 `<select>` 대신 같은 pill 스타일의 "현재 Project명" 버튼을 눌러 드롭다운(`.shortlisting-project-switch-menu`)에서 고르는 방식으로 교체했다. 테이블 필터 행(Filter 3 왼쪽)에 있던 Project 필터는 제거 — Full Scout에서 즐겨찾기를 누르면 여기서 선택된 Project로 계속 추가된다 (`index.html`, `src/app.js`, `src/styles.css`).
- "+ 지표 추가"(1개씩 모달로 추가)를 "지표 관리"로 개편: 버튼을 누르면 현재 Project의 모든 커스텀 지표를 지표·설명·입력 3열 테이블로 한눈에 보여주고, 각 행 맨 우측에서 바로 삭제할 수 있으며, 표 아래 한 줄짜리 입력 행(이름/설명/타입/+ 추가)에서 새 지표를 추가하면 모달을 닫지 않고 목록에 바로 반영된다. 지표 리턴 값 타입을 Boolean/List/Integer/Score/Text 5종에서 Boolean/List/**Number**/**Date**/Text로 재정리 — Integer와 Score(0-N)를 하나의 Number 타입으로 통합하고(최대값은 선택 입력, 지정 시에만 0~최대값으로 제한) Date 타입을 새로 추가했다. 기본 제공 ADMET 지표의 표시 타입도 number로 갱신(로직 변경 없음). 값 저장/검증(`update_shortlisting_project_record_state`)과 테이블 셀 렌더러(`shortlistingMetricEditControl`)도 함께 갱신해 Date는 `<input type=date>`, Number는 소수 허용 숫자 입력으로 렌더링한다 (`main.py`, `src/app.js`, `index.html`, `src/styles.css`). 브라우저에서 지표 목록 조회·타입별 추가(boolean/date/number+max)·삭제까지 end-to-end 검증.
- Shortlisting 헤더 컨트롤이 진척 현황(Step0)/Wiki Map 탭까지 새어 들어오던 버그 수정: `renderShortlistingProjectControl()`이 `activeTableMode()`만 보고 focus 여부를 판단했는데, `state.tableMode`는 Step0/Knowledge Map 패널로 전환해도 바뀌지 않는 값이라 직전에 Shortlisting을 보고 있었으면 그 값이 그대로 남아 다른 패널에서도 Shortlisting 헤더가 노출됐다 — 이제 Step0/Knowledge Map 패널이 실제로 보이는지 직접 확인해 그 경우엔 무조건 숨긴다. 근본 원인은 `activatePipelineTab()`이 전환 전후 테이블 모드가 같으면(`setTableMode`가 내부에서 조기 반환) 헤더 재계산 자체를 건너뛰는 것이었다 — Knowledge Map/Step0에서 원래 보던 것과 같은 탭(Shortlisting)으로 되돌아오면 `setTableMode`가 아무 것도 안 해서 헤더가 갱신되지 않았다. `activatePipelineTab()` 끝에서 `syncTopDataActionsForVisibleTab()`/`renderShortlistingProjectControl()`을 무조건 다시 호출하도록 수정. 모든 탭 전환 조합(Shortlisting↔Step0↔Knowledge Map↔Full Scout↔Fast Triage)을 브라우저에서 검증.
- 상단 "새로고침" 버튼 제거: Knowledge Wiki Map을 다시 만드는 용도 외에는 `window.location.reload()`와 동일해 불필요하다고 판단, Wiki Map 탭에서만 계속 보이고 그 외 모든 탭(진척 현황/Fast Triage/Full Scout/Shortlisting)에서는 숨긴다(`syncTopDataActionsForVisibleTab`, `src/app.js`).
- Shortlisting 테이블 컬럼 정리: "Custom Score" 입력란을 항상 보이는 숫자 인풋에서 Scout/Total Score와 동일한 원형 배지(`total-score-edit-circle`)로 바꾸고, 더블클릭하면 0~9 입력창으로 전환되어(`customScoreCircle`/`openCustomScoreEdit`/`saveCustomScoreEdit`) 기존 저장 경로(`saveFocusManagement`/`saveShortlistingProjectField`)를 그대로 사용한다. "Scout Score"/"Custom Score" 컬럼 헤더 문구를 각각 "Score"로 통일(툴팁에는 구분을 위해 "Score (Full Scout)"/"Score (Shortlisting)" 유지). Action date 컬럼을 `oic_default` 전용에서 모든 Shortlisting Project 테이블에 상시 노출로 확장 — 커스텀 Project에서도 지표 컬럼 옆에 Action date가 추가되며, 어떤 Project 화면에서 입력하든 항상 동일한 `meta.focus_management.due_date`에 저장되므로 백엔드의 Action Date 알림 메일 스케줄러(`run_action_date_reminders`)는 코드 변경 없이 모든 Project를 계속 커버한다(`src/app.js`).
- 대시보드 UI 워딩 통일: Fast Triage/Full Scout/Shortlisting 명칭을 탭·헤더 등 좁은 공간에서는 Simple/Advanced/Custom으로, 툴팁·팝업·판단근거 드로어·테이블 첫 행 헤더처럼 풀어 쓸 수 있는 곳에서는 Simple Research/Advanced Research/Custom Review로 전면 교체(`index.html`, `triage_detail.html`, `detail.html`, `onboarding.html`, `src/app.js`, `src/detail.js`, `src/triage-detail.js`, `src/criteria-guide-i18n.js`, `src/sigma_graph.js`의 Knowledge Wiki Map 범례 포함). GPT 복사 프롬프트 본문, JSON 붙여넣기 검증 오류 메시지, `main.py`가 기록/파싱하는 영구 저장 데이터 값(comment source 라벨, `parser_note`, `final_insight.recommendation` 등)은 하위 호환을 위해 기존 표기를 그대로 유지 — 브라우저에서 탭 전환·판단근거 드로어(KR/EN)·진척 현황 테이블까지 렌더링 확인.
- Custom Review(Shortlisting) 탭 테이블 컬럼 레이아웃 최적화: "Total Score"(Score+Custom Score 합산, 0-30) 컬럼을 Pipeline Stage 뒤에서 Custom Review 컬럼 그룹과 "관리" 컬럼 사이로 이동 — Advanced Research의 Score(0-21)와 Custom Review의 Score(0-9) 두 서브 점수를 먼저 보고 그 오른쪽 끝에서 합계를 확인하는 흐름이 되도록 함(`renderFocusTable`, `FOCUS_TABLE_FIXED_LEFT_COLUMN_KEYS`/`FOCUS_TABLE_COLUMN_KEYS`). 두 서브 점수 컬럼(Score/Score)과 합계 컬럼(Total Score) 전체에 세로로 옅은 틴트를 넣어(서브 점수는 약하게, 합계는 진하게) 구분이 한눈에 들어오도록 CSS 추가(`.score-subtotal-col`/`.score-grandtotal-col`, 라이트/다크 테마 모두 `color-mix` 알파 블렌딩이라 별도 다크 오버라이드 불필요). 세 컬럼 width가 이미 기본값 92px로 동일했으나, 과거 드래그로 컬럼폭을 조정해둔 로컬 저장값이 남아있을 수 있어 `FOCUS_COLUMN_WIDTH_STORAGE_KEY`를 v7→v8로 올려 새 레이아웃 기준 폭으로 리셋(`src/app.js`, `src/styles.css`). 캐시 무효화를 위해 `index.html`의 `app.js`/`styles.css` 버전 쿼리스트링도 갱신. 브라우저에서 라이트/다크 테마 모두 컬럼 순서·틴트·컬럼폭(92px 동일) 확인, 콘솔 에러 없음, Simple/Advanced 탭 회귀 없음 확인.
- 위 Custom Review 레이아웃 후속 보정 3건: (1) Score/Score/Total Score 세 컬럼을 92px→64px(최소폭 76px→52px)로 더 압축하고 `FOCUS_COLUMN_WIDTH_STORAGE_KEY`를 v8→v9로 올려 기존 로컬 저장폭을 리셋. (2) 틴트가 너무 진하다는 피드백에 따라 `color-mix` 알파를 서브 점수 7%→4%, 합계 16%→9%로 낮춤. (3) "Total Score" 헤더가 "Total"/"Score" 두 줄로 표시되면서 가운데 정렬이 빠져 있던 문제 수정 — 이 헤더가 rowspan 2로 첫 번째 헤더 행(`.pipeline-group-row`)에 있어 두 번째 행 전용 가운데 정렬 규칙(`.pipeline-score-row th button`)을 못 받고 있었음이 원인. 공용 `sortableHeader()` 대신 이 컬럼만 버튼 마크업을 직접 작성해 `Total<br />Score`로 줄바꿈하고 `data-sort-label="Total Score"`는 그대로 유지해 정렬 접근성 문구(`updateSortIndicators`)에 영향 없게 하면서 `.score-grandtotal-col button { text-align:center }` CSS로 정렬만 별도 적용(`src/app.js`, `src/styles.css`, `index.html` 버전 쿼리스트링 갱신). 브라우저에서 컬럼폭(64px 동일)·완화된 틴트·2줄 가운데 정렬 헤더 확인, 콘솔 에러 없음.
- 위 틴트 적용 범위 추가 수정: `.score-subtotal-col`/`.score-grandtotal-col` 배경 규칙이 `th`(헤더)에도 걸려 있어 "Total Score" 헤더만 다른 헤더보다 진한 색으로 튀어 보이던 문제 수정 — 두 클래스의 배경 규칙을 `td`(데이터 셀)에만 적용되도록 좁혀, 헤더 행은 모두 기존 공통 헤더 배경(`.score-group-head`/`.pipeline-score-row th`)으로 통일되고 틴트는 그 아래 데이터 셀에만 남도록 함(`src/styles.css`, `index.html` `styles.css` 버전 쿼리스트링 갱신). 브라우저에서 헤더 배경색 전부 동일(`rgba(248, 250, 252, 0.94)`)해진 것과 데이터 셀 틴트(서브 4%/합계 9%)는 그대로 유지되는 것을 computed style로 확인.
- Total Score 데이터 셀 틴트가 여전히 진하다는 피드백에 따라 `color-mix` 알파를 9%→6%로 한 단계 더 낮춤(서브 점수 컬럼 4%는 그대로 유지, `src/styles.css`, `index.html` `styles.css` 버전 쿼리스트링 갱신). 브라우저 computed style로 6% 적용 확인.
- Shortlisting(Custom Review) Project별 이메일 기반 write/owner 권한 시스템 추가. 비-기본 Project 객체에 `members: [{email, role, added_by, added_at}]` 필드를 신설(`role`은 `owner`|`write`만 저장, 목록에 없는 이메일은 암묵적 read) — `oic_default`는 대상에서 제외되며 계속 `is_admin` 단일 게이트를 사용한다. `load_shortlisting_projects()`가 이 필드가 없는 기존 Project(`test`/`test2`/`CNS Discovery Team`)에 `created_by_email`을 owner로 자동 시딩(lazy migration, 기존 default-Project 시딩과 동일 패턴). `shortlisting_project_role_for_account()`가 유효 권한(`owner`/`write`/`read`)을 계산하며 사이트 관리자(`is_admin`)는 멤버 등록 없이 모든 Project에서 자동 owner로 취급된다. 이 권한으로 Project 이름 변경/보관(`PATCH`/`DELETE /api/shortlisting/projects/{id}`)과 커스텀 지표 컬럼 추가/삭제(`POST`/`DELETE .../columns[...]`)를 owner 전용으로, 레코드별 트래킹·필드 수정(`PATCH /api/records/{id}/shortlisting-projects/{project_id}`, 즐겨찾기 추가/해제 및 커스텀 점수·지표값·Action date 입력 포함)을 write 이상 전용으로 새로 제한했다(이전에는 로그인만 하면 누구나 가능했음). 멤버 관리는 신규 `POST`/`DELETE /api/shortlisting/projects/{id}/members[/{email}]`(owner 전용, 마지막 남은 owner 제거·강등은 400으로 거부)로 처리하며, `GET /api/shortlisting/projects` 응답에 저장되지 않는 `current_user_role`을 함께 내려줘 프론트가 재조회 없이 UI를 게이팅할 수 있게 했다. 프론트에서는 Project 스위처 드롭다운에서 owner 권한이 있는 Project 옆에만 설정(⚙) 아이콘을 표시하고, 클릭하면 "멤버 관리"/"지표 관리" 두 탭으로 구성된 Project 설정 모달이 열린다 — 기존 독립 "지표 관리" 툴바 버튼은 완전히 제거하고 이 모달(owner 전용 진입점)로 일원화했다(`#shortlistingMetricModal` 재사용). 즐겨찾기 별표, 커스텀 지표 입력, Custom Score, Action date 컨트롤은 활성 Project에서 read 권한뿐이면 비활성화되도록 렌더링을 변경했다. 이 작업 도중 Action date가 모든 Project에서 `meta.focus_management.due_date` 하나만 공유하도록 되어 있던(직전 커밋에서 메일 알림 유지를 위해 임시로 그렇게 만들었던) 부분을 Project별 완전 분리(`meta.shortlisting_projects.<id>.due_date`)로 되돌리고, `run_action_date_reminders()`가 `focus_management`뿐 아니라 모든 `shortlisting_projects.<id>` 버킷을 각자 독립적으로 스캔·발송·이력 기록하도록 확장해 메일 알림 기능은 계속 모든 Project를 커버한다(`main.py`, `src/app.js`, `index.html`, `src/styles.css`). 신규 단위 테스트 `tests/test_shortlisting_project_permissions.py`(21건, 역할 판정/기록 권한/컬럼 권한/멤버 관리/last-owner 가드/lazy migration/Project별 알림 통과) 및 기존 `tests/test_shortlisting_tracking_status.py` 회귀 없음 확인. 브라우저 + 실제 로그인 세션(owner/신규 write 멤버/미등록 사용자) 기준으로 설정 모달 열기·멤버 추가/삭제·last-owner 차단·write 권한 없는 사용자의 트래킹/컬럼 관리 API 403 응답까지 end-to-end 검증(`json/schema.md` 문서도 함께 갱신).
- Custom Review In-vivo/In-vitro 자동 판정(O/X/N-A) 새로고침 오탐 점검 및 부분 수정: 사용자가 보고한 "1ST Biotherapeutics 새로고침 후 O→X" 건을 계기로 Tab3에 트래킹 중인 16개 Advanced Research record 전부에 대해 OI Partnership 새로고침(`recalculate_record_oi_partnership`/`apply_auto_detected_evidence`/`classify_evidence_presence`)을 재실행하고, 값이 바뀐 7건은 각 record의 원문(raw_markdown)만 근거로 `/api/chat`(OpenRouter)에 독립적으로 재질의해 교차검증했다. 결과: 1ST Biotherapeutics_1ST-104(사용자가 보고한 건)·Lynk_LNK01006·Regeners_RG111의 O→X 전환은 원문이 실제로 in-vivo/in-vitro 데이터 미완료를 명시하고 있어 정상 동작으로 확인. 반면 Aliad Biopharma_AB-AD-002(in_vivo)와 Orgasis_OGS-101(in_vitro)는 원문에 명시된 긍정 결과가 있음에도 X/N-A로 오탐된 것을 확인 — 원인은 `EVIDENCE_POSITIVE_CUES` 정규식의 한국어 키워드 목록에 영어 쪽 `confirm(ed)`에 대응하는 한국어 "확인/관찰/재현" 계열 단어가 빠져 있었기 때문(`main.py`). 해당 키워드 3종을 추가해 Orgasis는 정상값(O/O)으로 즉시 복구 확인, 이후 16건 전체를 다시 새로고침해 HTTP 오류 0건 확인.
  - 미해결로 남긴 한계 2건(코드 미변경, 리포트별 수동 확인 필요): Aliad Biopharma의 in_vivo는 "in vivo efficacy가 다수 보이지만 PK 연결은 충분하지 않다"처럼 무관한 caveat성 부정 표현이 같은 140자 탐색 윈도우 안에 함께 있어 부정 판정이 우선 적용되는 문장-스코핑 한계로 여전히 X로 남는다(다른 15건 정상 케이스에 대한 회귀 위험 때문에 이번엔 윈도우/우선순위 로직 자체는 손대지 않음). RudaCure_RCI002·CorestemChemon_CE223BR17은 원문의 실제 효능 서술이 "in vivo"/"in vitro" 리터럴 표현 없이(예: "pain model") 작성돼 있어 패턴 앵커 자체가 매치되지 않아 N/A로 남는다 — 두 record 모두 대시보드에서 수동으로 O로 override하면 `*_status_source`가 `manual`로 저장되어 이후 자동 새로고침이 덮어쓰지 않는다.
- Shortlisting Project 설정 모달의 "멤버 관리" 탭에서 이메일을 직접 타이핑하던 구성원 추가 방식을 가입된 계정 체크박스 다중 선택 방식으로 교체. 신규 `GET /api/users/directory`(로그인 전용, admin/developer 게이트 없음)가 활성 계정의 이름/이메일만 최소 노출로 반환하고, 프론트는 이를 현재 Project의 기존 멤버를 제외한 체크박스 목록으로 렌더링한다(`renderShortlistingMemberPicker`) — 권한(Write/Owner)을 한 번 선택하고 "구성원 추가" 버튼을 누르면 체크된 계정 전원을 기존 단건 멤버 추가 엔드포인트(`POST /api/shortlisting/projects/{id}/members`)에 순차 호출해 한 번에 추가하며, 일부 실패 시에도 성공/실패를 구분해 상태 메시지로 표시한다(`main.py`, `src/app.js`, `index.html`, `src/styles.css`). 신규 테스트 계정 2개로 브라우저에서 체크박스 다중 선택 → 일괄 추가 → 목록 갱신 → 삭제까지 end-to-end 검증, 콘솔 에러 없음.
- Advanced Research(Tab2) 탭의 즐겨찾기(★) 버튼 동작 변경: 지금까지는 클릭 시 항상 Custom Review 탭에서 마지막으로 선택해둔 Project(`state.activeShortlistingProjectId`, 화면 어디에도 명시적으로 드러나지 않는 값)에 조용히 추가/제거되어, write 권한이 있는 Project가 2개 이상이면 사용자가 의도하지 않은 Project에 등록될 수 있었다. 이제 로그인 계정이 write 이상 권한을 가진 Project(기본 Project `oic_default` + 커스텀 Project 중 role이 read가 아닌 것, `writableFocusProjectOptions()`)가 2개 이상이면 별표 클릭 시 각 Project명과 현재 트래킹 여부(★/☆)를 보여주는 드롭다운이 뜨고, 그중 하나를 클릭하면 그 Project에 한해 추가/제거된다(`focusProjectPickerButton`/`toggleFocusProjectPicker`/`pickFocusProjectFromPicker`, 팝오버는 기존 점수 필터 팝오버와 동일한 fixed-position 싱글턴 패턴 재사용). 별표 아이콘 자체는 "하나라도 트래킹된 Project가 있는지"를 표시하도록 집계했다. write 가능한 Project가 1개뿐이면(대다수 사용자의 기본 상태) 기존과 동일하게 클릭 한 번으로 바로 추가/Stationary 전환/제거가 순환하는 동작을 그대로 유지 — Custom Review 탭 내부의 별표(이미 어느 Project를 보고 있는지 명확함)는 이번 변경 대상이 아니며 계속 3단계 순환 그대로 동작한다(`src/app.js`, `src/styles.css`, `index.html` 버전 쿼리스트링 갱신). 브라우저에서 Project 3개(Open Innovation Center/test/test2) 보유 계정으로 드롭다운 오픈 → 특정 Project만 해제 → 재등록 시 `custom_score`/`metric_values` 등 기존 값이 그대로 복원되는 것을 `json/pipeline-records.json` 조회로 확인, 콘솔 에러 없음.
- 위 즐겨찾기 드롭다운/버튼의 write 권한 판정 버그 수정: `oic_default`(Open Innovation Center)는 `members[]`/`current_user_role`이 없어 지금까지 프론트에서 무조건 write 가능한 것으로 취급했는데(`isDefaultProject ||`로 항상 true), 실제 `/focus-management` 엔드포인트는 site admin에게만 write를 허용(`require_auth_admin`)하므로 admin이 아닌 로그인 사용자에게는 여전히 별이 활성화된 채로 보이다가 클릭 시 403으로 조용히 실패하는 불일치가 있었다. 신규 `canWriteFocusProject(projectId)` 헬퍼가 `oic_default`는 `getCurrentUser()?.is_admin`으로, 그 외 커스텀 Project는 기존 `shortlistingRoleFor(projectId) !== 'read'`로 판정하도록 통일해 `writableFocusProjectOptions()`(드롭다운 후보 목록)와 개별 별표 버튼의 `canWrite`/`disabled` 계산에 모두 적용했다 — 이제 write 권한이 없는 Project는 드롭다운 후보에서 아예 빠지고(`아무 Project도 write 불가하면 별 자체를 렌더링하지 않음`), write 가능한 Project가 정확히 1개일 때는 그 Project를 명시적으로 대상으로 삼도록 고쳐(이전에는 여전히 `activeShortlistingProjectId`를 그대로 썼음) 사용자가 read 권한만 있는 Project가 우연히 선택돼 있어도 엉뚱한 곳에 등록/실패하지 않는다(`src/app.js`, `index.html` 버전 쿼리스트링 갱신). 브라우저에서 admin 세션 기준 회귀 없음(Project 3개 그대로 노출) 확인, 콘솔 에러 없음.
- OIC(Open Innovation Center) Project에도 커스텀 Project와 동일한 `members[]` 기반 write/owner 권한을 확장 적용. `default_shortlisting_project()`가 빈 `members: []`를 갖도록 변경(생성자 이메일이 없어 초기엔 site admin만 owner로 동작하며, admin이 Project 설정에서 명시적으로 이메일을 owner/write로 등록하면 그 계정도 사용 가능). `load_shortlisting_projects()`의 lazy migration에서 `oic_default`를 건너뛰던 예외를 제거해 동일한 백필 로직을 타도록 통일. `serialize_shortlisting_project()`의 OIC 전용 분기(admin 여부만 확인)를 제거하고 `shortlisting_project_role_for_account()`를 모든 Project에 동일하게 적용. 핵심 변경은 `PATCH /api/records/{id}/focus-management`(즐겨찾기·Filter3·In-vivo/In-vitro·ADMET·D-Link·Action date·Custom Score 등 OIC의 모든 레코드별 수정)의 게이트를 `require_auth_admin` 단일 체크에서 `oic_default`의 `members[]`를 조회해 write 이상 여부를 확인하는 방식으로 교체 — 이제 admin이 아니어도 OIC에 write로 등록된 계정은 정상적으로 저장할 수 있다(main.py). `POST`/`DELETE /api/shortlisting/projects/{id}/members[/{email}]`에 있던 "기본 Project는 멤버를 관리하지 않습니다" 차단을 제거해 OIC도 멤버 추가/삭제가 가능해졌다. 다만 `POST /api/shortlisting/projects/{id}/columns`는 `oic_default`일 때 role과 무관하게 400으로 차단 유지 — OIC의 7개 컬럼(Filter3/DD/In-vivo/In-vitro/ADMET/D·Link/Action date)은 하드코딩된 값이라 지금은 지표 추가 대상이 아니기 때문. 프론트에서는 Project 스위처의 설정(⚙) 아이콘 표시 조건에서 `oic_default` 제외 규칙을 삭제해 OIC도 owner에게 톱니바퀴가 뜨도록 했고, 설정 모달의 "지표 관리" 탭은 `oic_default`일 때 추가 폼을 숨기고 "OIC 기본 Project의 지표는 하드코딩되어 있어 현재 추가/삭제할 수 없습니다" 안내문을 대신 보여준다(`resetShortlistingMetricAddForm`, `#shortlistingMetricLockedNote`/`#shortlistingMetricAddRow`). `focusActionButton`/`customScoreCircle`/`openCustomScoreEdit`/`actionDateCell`와 OIC 전용 편집 컨트롤(`partnershipEditSelect`/`evidenceEditSelect`/`admetEditSelect`/`diseaseLinkageEditSelect`)에 남아있던 "OIC는 항상 쓰기 가능"류 예외를 모두 제거하고 `shortlistingRoleFor(DEFAULT_SHORTLISTING_PROJECT_ID)`로 통일 게이팅했다(`main.py`, `src/app.js`, `index.html`). 기존 `tests/test_shortlisting_tracking_status.py`가 옛 `require_auth_admin` 패치에 의존하고 있어 이번 게이트 교체로 401 회귀가 발생한 것을 확인하고 `require_authenticated_user`/`load_shortlisting_projects` patch로 갱신, `tests/test_shortlisting_project_permissions.py`에 OIC 전용 신규 테스트 5건(write 멤버 사용 가능/미등록 사용자 403/admin 계속 동작/컬럼 추가 owner도 차단/멤버 관리 가능)과 기존 "members 없음" 단언을 "빈 members 배열" 단언으로 교체, 총 28건 전체 통과 확인(`json/schema.md` 동반 갱신).
- Custom Review 테이블의 "DD"(Due Diligence) 컬럼을 OIC(Open Innovation Center) Project 테이블에서만 우선 숨김 처리. `FOCUS_TABLE_OIC_METRIC_COLUMN_KEYS`에서 `'dd'` 키를 제거하고, 헤더 셀(`focusFilterHeader('DD', ...)`)과 데이터 셀(`dueDiligenceStatusBadge(row)`), colgroup의 `dd` 컬럼, `shortlistingGroupColspan` 계산(6→5)을 함께 제거/조정했다 — 내부 `row.ddStatus` 데이터와 Full Scout 탭의 DD 필터 자체는 그대로 유지하고, Custom Review 테이블에 보이는 컬럼만 제거했다(`src/app.js`).
- Shortlisting Project 설정 모달의 "구성원 추가" 영역 개선: (1) 후보 목록에 이름+이메일을 함께 표시하고 목록 위에 검색창을 하나 추가해(`#shortlistingMemberSearchInput`) 이름 또는 이메일로 실시간 필터링할 수 있게 했다 — 대시보드 다른 테이블의 단순 검색창과 동일한 느낌의 단일 입력창 UI를 재사용. (2) 기존 멤버 목록 테이블에도 "이름" 컬럼을 추가해 이메일뿐 아니라 가입자 이름도 함께 보이도록 했다(`userDirectoryNameFor()`가 캐시된 `state.userDirectory`에서 이메일로 이름을 역조회)(`main.py`는 변경 없음, `src/app.js`, `index.html`, `src/styles.css`).
