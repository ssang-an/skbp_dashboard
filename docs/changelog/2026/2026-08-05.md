## Changes

- `index.html`, `src/app.js`, `main.py`, `tests/test_dashboard_ia.py`, and `tests/test_auth_admin.py`: removed the dashboard reviewer-ID modal; TAB1/TAB2 manual table edits now use the authenticated user's name, and the API derives the audit actor from the login session instead of trusting request data.
- `index.html`, `src/app.js`, `json/schema.md`, `tests/test_dashboard_ia.py`, and `tests/test_rubric_v32_v33.py`: replaced nested-fragment JSON recovery with exact `--- JSON DATA ---` splitting and one-pass top-level parsing, added precise syntax locations, locked TAB1/TAB2 to their own Triage/Full Scout contracts with separate drafts, retained legacy two-fence compatibility, documented the ingestion contract, and strengthened both GPT instructions' JSON output checks.
- `src/compact-ingestion.js`, `src/app.js`, `index.html`, `json/schema.md`, `tests/test_compact_ingestion.py`, `tests/test_dashboard_ia.py`, and `tests/test_rubric_v32_v33.py`: introduced Compact JSON v1 for both Filter 1 and Filter 2 instructions, centralized source objects behind `source_ids`, and expanded compact payloads back into the existing persisted dashboard/export shape before validation while retaining verbose-input compatibility.
- `main.py`, `src/app.js`, `json/schema.md`, and `tests/test_rubric_ai_refresh.py`: unified TAB2 home and detail rubric refresh through the latest-rubric AI review of the preserved GPT report and partner-uploaded evidence; successful changes now update only structured scoring, Total, Filter 2, and audit metadata without rewriting the GPT source Markdown.
- `main.py`, `src/app.js`, `.env.example`, `json/schema.md`, and `tests/test_rubric_ai_refresh.py`: extended guarded AI rubric refresh to TAB1's TR/MoA/Data scores and Filter 1 decision while preserving the batch GPT report; reduced the OpenRouter output ceiling from the local 100,000-token override to 1,600 and aligned the documented default.
- `main.py`, `detail.html`, `src/detail.js`, `src/styles.css`, `json/schema.md`, and `tests/test_topic_notes_reupload.py`: added authenticated Topic-level inline notes stored outside GPT Markdown, heading-key remapping and unmatched-note retention across confirmed Full Scout reuploads, recoverable pre-reupload report/data snapshots, a detail-page combined Markdown/Compact JSON reupload workflow, and an admin-only floating JSON editor protected by both UI and API authorization.
- `src/styles.css`: removed the visible border from the Tab 3 (Focus/판단근거) keyword pills — `.criteria-focus-chip-list > span`, `.criteria-focus-keyword-list > li`, `.criteria-focus-formula > span`, and `.criteria-focus-priority-formula` chips (공동연구/투자 등) — and darkened their tint (6%→12% accent mix) so they match the borderless pill style already used by Tab 1/2's `.criteria-keyword-list` (e.g. Alzheimer's disease, Total Patient Pool).
- `src/detail.js`, `src/styles.css`, `detail.html`, and `tests/test_topic_notes_reupload.py`: restyled inline Topic notes to match the report update callout's restrained green tone, removed the yellow card/double-border treatment, and simplified the add-note textarea and actions with a soft focus highlight.
- `src/styles.css`, `detail.html`, and `tests/test_topic_notes_reupload.py`: aligned the Full Scout reupload control with the detail header's dashboard/judgment pill system, including identical inline-flex alignment, glass treatment, icon spacing, sizing, and hover behavior.
- `src/detail.js`, `src/styles.css`, `detail.html`, and `tests/test_topic_notes_reupload.py`: replaced rounded Topic-note cards with a minimal thin-gray section divider; the add-note action now appears only on hover/focus (and remains visible on touch devices), while existing notes and the editor use restrained square-edged neutral styling.
- `src/styles.css`, `detail.html`, and `tests/test_topic_notes_reupload.py`: recolored the detail reupload pill to the same teal-to-blue treatment as the home Pipeline upload action and added an explicit higher-specificity hover/focus state so its highlight is no longer masked by the base ID style.
- `detail.html` and `tests/test_topic_notes_reupload.py`: replaced the detail reupload document-plus glyph with the exact upload-arrow/tray SVG used by the home Pipeline upload action.
- `src/styles.css`, `detail.html`, and `tests/test_topic_notes_reupload.py`: aligned the report fullscreen/copy controls with the Review Workspace favorite and count badge on one 30px header action axis, including matching top offset, box sizing, and SVG centering.
- `main.py`, `src/triage-detail.js`, `triage_detail.html`, `json/drug-valuation.schema.json`, `json/schema.md`, and `tests/test_rubric_ai_refresh.py`: persisted successful latest-rubric reviews even when TAB1 scores do not change, preserved the original rubric version, distinguished review-only from actual recalculation metadata, and exposed recalculation/review time plus the applied rubric version in Fast Triage Quick scan.
- `detail.html`, `src/styles.css`, and `tests/test_topic_notes_reupload.py`: simplified the Full Scout reupload instruction to request the complete latest GPT response and restyled its textarea to match the home Data Upload field with one neutral border, no duplicate green outline, and a restrained focus shadow.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: replaced the report and review panel's two-line labels with the single-line `GPT ORIGINAL REPORT` and `TEAM REVIEW WORKSPACE` titles, sized above the table-of-contents label and aligned to the shared 30px action-icon axis.
