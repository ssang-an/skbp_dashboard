# 2026-08-30

## Changes

- Made the dashboard-header Wiki Map star permanently light purple in normal, hover, and active states (`src/styles.css`).
- Unified lifecycle handling across Fast Triage and Full Scout: only canonical `development_stage = Discontinued / inactive` is a lifecycle status gate; lifecycle wording in notes/flags no longer independently changes status. `suspended`/`halted` remain pause signals, retain a confirmed stage or use `Unknown`, and continue assessment. Updated Full Scout prompt/rubric, server/browser calculation, compact-ingestion defaults (Fast Triage v3.5; Full Scout v3.7), legacy `UNVERIFIED` normalization, and regression coverage.
- Increased the Fast Triage Filter 1 default and enforced minimum column width so the full `INSUFFICIENT` status pill remains visible even when an older narrower table width is stored locally (`src/app.js`).
- Rendered Fast Triage and Full Scout early-stop score placeholders (`—`) with the existing rounded-rectangle status treatment instead of circular score badges (`src/app.js`).
- Reduced the Fast Triage Filter 1 width while retaining room for the complete `INSUFFICIENT` pill. Made the Pipeline Table's viewport header mirror explicitly refresh after every table render, preventing its first header row from disappearing after table updates; bumped dashboard asset cache versions (`src/app.js`, `index.html`).
- Released Fast Triage v3.5 and Full Scout v3.7: SELECT/PASS now require Target Relevance 3 (one of the six SKBP priority indications), while retaining Fast Triage MoA >=1/Data >=2 and Full Scout Total >=14/MoA 3/Data 3 gates. Updated the versioned rubrics, release manifest, GPT prompts/templates, server and browser recalculation paths, Korean/English judgment guides, schema guidance, and regression fixtures together.
- **rubric (instruction/rubric change, no version bump — Fast Triage, consolidated into the same-day, not-yet-deployed v3.5 release above per the user's explicit approval and `docs/rubric-release-maintenance.md`'s pre-deployment consolidation exception; verified zero saved records exist under `instruction_version: "3.5"` before merging):** removed the separate `triage.active_asset` tri-state field (`true`/`false`/`null`) from the Fast Triage SELECT/REJECT/INSUFFICIENT gate. It was redundant with `structured_table.development_stage` (which already has a canonical `Discontinued / inactive` value) and its "confirmed inactive" bar was undefined in evidentiary/recency terms — discussed and decided with the user across several turns, including tracing the pre-existing gap to commit `07252ec4`. `development_stage` is now the sole activity signal: `Discontinued / inactive` is a confirmed early stop (`INSUFFICIENT`); every other canonical stage, including `Unknown`, proceeds to normal TR/MoA/Data scoring with SELECT/REJECT determined purely by score thresholds (previously `active_asset=null` forced `REJECT` even with qualifying scores — that gate is gone). Per the user's request, also fully retired the parallel `hard_filter.flags` keyword-based lifecycle scan (`fast_triage_record_has_hard_blocker`/`fast_triage_lifecycle_text_has_hard_blocker` in `main.py`, `hasAffirmedLifecycleBlocker` in `src/app.js`) — it duplicated the same signal and is now unified into the single `development_stage` check. Scope was intentionally limited to Fast Triage; Full Scout's own separate terminal-lifecycle FAIL gate (`full_scout_has_hard_blocker`) is untouched.
  - `config/scoring_criteria/v3_5_triage.md`: rewrote the Final Status table and Canonical Development Stage section to development_stage-only wording; removed the `active_asset` paragraph.
  - `main.py`: `calculate_fast_triage_status()` dropped the `active_asset`/`hard_blocker` parameters in favor of `development_stage`; updated its 5 call sites; removed the now-required-nowhere `active_asset` validation block and the two now-dead hard-blocker functions; kept `active_asset` as an optional, unvalidated-as-required field in `reject_extra_keys`'s allow-list and the lenient type check so pre-v3.5 saved records stay valid without migration.
  - `json/drug-valuation.schema.json`: moved `triage.active_asset` out of `required` (kept in `properties` — schema has `additionalProperties: false`, so old records with the field would otherwise fail validation). `json/schema.md` rewritten to match.
  - `src/app.js`: removed `active_asset` from `COMPACT_TRIAGE_JSON_TEMPLATE` and the Legacy Fast Triage prompt's inline example JSON; rewrote the "Triage status rule" instruction text to development_stage-only wording; updated both client-side status-recalculation mirrors (`synchronizeDashboardOwnedInputFields`, and the compact-ingestion input-issue validator) to the same gate — the latter already used `TR >= 3`, but the former was still stale at `TR >= 2` (a separate, pre-existing drift from the v3.5 gate bump above; fixed here since the line was already being rewritten). Removed the now-unused "Active asset" Pipeline Table extra column and its formatter branch.
  - `src/compact-ingestion.js`: `expandCompactInputRecord()` no longer synthesizes `active_asset: null`/passthrough on freshly-expanded records (still preserved via object spread if an old-format paste explicitly includes it).
  - Tests: rewrote `tests/test_rubric_v32_v33.py`'s `test_fast_triage_select_formula_and_identity_gate` for the new signature (including the `Unknown` stage → `SELECT`-eligible behavior change) and added `test_fast_triage_and_full_scout_share_the_cluster_taxonomy`-adjacent coverage — `test_current_contract_no_longer_requires_active_asset`, `test_unknown_development_stage_does_not_block_select`, `test_discontinued_development_stage_forces_insufficient`, `test_hard_filter_flags_text_no_longer_gates_fast_triage_status` (replacing the retired `test_current_contract_requires_explicit_active_asset`, `test_unknown_activity_is_explicit_null_and_cannot_select`, `test_lifecycle_flag_matching_is_phrase_aware_and_negation_safe`). Updated `tests/test_record_storage.py`'s `_recalculated_fast_status` helper and `tests/test_compact_ingestion.py`'s expected `triage` key set.
  - Verified: `tests.test_rubric_v32_v33`, `tests.test_record_storage`, `tests.test_rubric_release_manifest`, `tests.test_rubric_ai_refresh`, `tests.test_compact_ingestion`, `tests.test_render_compatibility` (137 tests) — the only remaining failures/errors are pre-existing and unrelated: 2 real records still carrying the legacy `UNVERIFIED` status (predates this change, confirmed via `git stash`), a `json/pipeline-records.json` worktree-vs-git-baseline drift, `src/compact-ingestion.js`'s hardcoded `instruction_version: '3.4'` defaults left stale by the earlier same-day v3.4→v3.5 bump above (not touched — out of scope for this change), and two unrelated `test_render_compatibility.py` DOM-stub errors. `node --check` on `src/app.js`/`src/triage-detail.js`/`src/detail.js`/`src/compact-ingestion.js` and `python -m py_compile main.py record_storage.py` all pass. Also added a new no-version-bump category, "비-scoring 메타데이터 canonical 값 정합성 보완", to `docs/rubric-release-maintenance.md` in an earlier change today — this active_asset removal itself was a scoring/status-gate change (not that category) and got a normal in-place rubric edit per the pre-deployment exception instead.
