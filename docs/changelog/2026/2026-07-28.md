# Work Log — 2026-07-28

## Changes

- `src/app.js`: fixed mojibake in the table count label ("Full Scout: N items · N rows/page").
- `index.html`, `src/styles.css`: renamed TAB1/2/3 to "Fast Triage / Full Scout / 집중 관리" with eyebrow labels, and redesigned the tab bar with numbered step badges.
- `src/app.js`, `src/styles.css`, `detail.html`, `src/detail.js`: replaced the "TAB3 추가" text button with a favorite-style icon toggle (☆ add / ★ tracked), then simplified it to icon-only.
- `src/detail.js`: fixed a separate mojibake string in the source report empty-state message.
- `index.html`, `src/styles.css`: moved the tab selector out of a centered/floating position into its own left-aligned row directly under the panel title, snug above the table.
