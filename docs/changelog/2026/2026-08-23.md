# 2026-08-23

## Changes

- Optimized Tab 0 Summary Dashboard for dark mode: replaced the bright gray metric pill with a quiet navy readability layer, aligned surrounding Summary surfaces, and simplified recent-growth indicators to use only the up-triangle plus value (`index.html`, `src/app.js`, `src/styles.css`).

- Changed Knowledge Map's default and reset connection range from 3 hops to 1 hop, including the fallback expansion depth (`index.html`, `src/sigma_graph.js`).

- Refined the Knowledge Map dark theme as one graph palette: subdued blue-slate relationship lines, lower default edge weight, restrained entity/stage colors, clearer selected relationships, and tuned canvas, legend, and label contrast (`src/sigma_graph.js`, `src/styles.css`, `index.html`).

- Added a dedicated dark-mode surface, border, text, and hover treatment for the Knowledge Map reset button so it no longer falls back to a white control (`src/styles.css`).
- Corrected Knowledge Map selection dimming in dark mode: inactive nodes now fade to a deep navy instead of a bright light-gray tint, preserving contrast for the selected node and its connections (`src/sigma_graph.js`).
- Set explicit Sigma node-label colors for Knowledge Map themes so graph wording stays light and readable in dark mode instead of using the default black label color (`src/sigma_graph.js`).
- Tuned Knowledge Map dark-mode contrast: brightened entity node colors, made base and selected links readable on the dark canvas, and added dedicated dark graph/control/Selected Node surfaces (`src/sigma_graph.js`, `src/styles.css`).
- Added a temporary Knowledge Map exploration starting point: the default Overview now renders the 36 most connected pipelines and their direct context, with an option to return to the complete map; grouped the legend into stage color and entity visibility concepts for comparison (`index.html`, `src/sigma_graph.js`, `src/styles.css`).
- Styled the Knowledge Map `✦` workflow marker as an indigo insight star with a subtle glow, keeping it visually distinct from the Shortlisting gold star (`index.html`, `src/styles.css`).
- Updated the Knowledge Map workflow header marker from `◉` to the AI/insight-style `✦` icon (`index.html`).
- Replaced the Knowledge Map workflow header's numeric step marker with a node-style `◉` icon (`index.html`).
- Made Knowledge Map legend chips interactive filters: clicking a workflow stage or entity type now hides/shows its nodes and automatically removes/restores the corresponding links (`src/sigma_graph.js`, `src/styles.css`).
- Widened the entire Knowledge Map Selected Node overlay to up to 640px, including its empty-state guidance card (`index.html`, `src/styles.css`).
- Increased the Knowledge Map Selected Node overlay width to 520px so its Markdown content and connected-node list are easier to read (`src/styles.css`).

- Made the Tab 0 Summary Dashboard scope note describe the active dot-color basis instead of a generic filter state: it now shows workflow-stage color by default, or the active attribute and selection such as Country color / China (`index.html`, `src/app.js`).

- Synchronized Tab 0 Summary Dashboard count motion with dot creation: workflow dots now render before the card counts begin, each count starts with its card's dot wave and rises over the staggered arrival, and the top-right 15-day increase animates from zero rather than jumping ahead (`src/app.js`, `index.html`).

- Refined multi-country Country presentation: retain no more than two explicitly named countries as slash-separated compact codes (for example `CN/US`), while displaying only the first/main country's flag. Comma-separated address text still resolves to the right-most country unless both parts are explicit country labels (`main.py`, `src/app.js`, `index.html`, `json/schema.md`).

- Restored Tab 0 Summary Dashboard dot colouring for both “전체” and selected filter values. The active/most-recently used Tab 0 filter now drives the DOM dot renderer's category colors; without an active attribute filter, dots retain their workflow-stage colors. Multi-country rows also participate in either matching Country filter (`src/app.js`, `index.html`).

- Added multi-country Country handling: explicitly named countries such as `China / United States operations` are stored and filtered as `China / United States`, show both compact flags/codes (`CN/US`), and count under either Country filter. Tab 1–2 Country editing now accepts free text; recognized wording is canonicalized while unrecognized wording remains visible as entered (`main.py`, `src/app.js`, `index.html`, `json/schema.md`).

- Added standalone knowledge-graph comparison pages without changing `main.py`: a G6 exploration view and a Sigma.js + Graphology WebGL view with D3 force physics, draggable/pinnable nodes, core filters/search, and connected-node inspection (`src/knowledge_graph.*`, `src/sigma_graph.*`).
- Refined the Sigma Physics Graph into the primary compact exploration view: full-width canvas, overlay inspector, command-bar controls, and selected-node connection highlighting with non-related nodes and edges muted (`src/sigma_graph.*`).
- Fixed Sigma Physics Graph filtering so force-layout processing no longer mutates the source edge data; blank-stage clicks now clear the selected-node highlight and inspector (`src/sigma_graph.js`).
- Expanded Sigma Physics Graph search and filters into visible subgraphs: filtered root nodes now include 1–3 configurable hops of linked nodes (default 3), preserving relationship context rather than rendering isolated matches (`src/sigma_graph.html`, `src/sigma_graph.js`).
- Added workflow-stage color encoding for Asset nodes in the Sigma Physics Graph: Listing, Fast Triage, Full Scout, and Shortlisting now use distinct primary colors while knowledge entity nodes retain type colors (`src/sigma_graph.js`).
- Fixed the Sigma Physics Graph legend's Set-to-array conversion and the inspector score fallback string, preventing its runtime and syntax errors (`src/sigma_graph.js`).
- Restored the Sigma Physics Graph selected-node helper message to UTF-8 Korean (`src/sigma_graph.js`).
- Rebuilt Sigma Physics Graph status and interaction messages as UTF-8 Korean, including initial, filtered, layout-complete, node-release, and graph-load-error states (`src/sigma_graph.js`).
- Added a Physics Graph header agent icon that opens the existing dashboard All Pipelines Agent with a graph-analysis starter prompt (`src/sigma_graph.html`, `src/app.js`).
- Moved the Physics Graph agent control to the dashboard-matched floating launcher and added three dynamic, selected-node-aware Agent prompts with connected-node context (`src/sigma_graph.html`, `src/sigma_graph.js`).
- Corrected Asset workflow-stage colors to use each Asset's direct Review relationship (and explicit Shortlist recommendation), restoring visible Fast Triage grouping (`src/sigma_graph.js`).
- Removed the obsolete standalone G6 knowledge-graph page; the shared graph stylesheet remains in use by the Sigma Physics Graph (`src/knowledge_graph.html`).
- Added Knowledge Map as the fifth workflow navigation destination and rebuilt the Sigma map page header around the dashboard's shared brand/workflow-navigation visual language (`index.html`, `src/sigma_graph.html`, `src/styles.css`).
- Restored the Progress Summary Dashboard to its original four-column graph layout; the Knowledge Map navigation remains separate from summary-card layout (`src/styles.css`).
- Restored the Step 0 Summary Dashboard's `step0-stat-strip` to four columns, removing the unintended empty fifth card slot (`src/styles.css`).
- Widened the five-item workflow navigation area and matched Knowledge Map's header-tab treatment to prevent label overlap without changing Summary Dashboard cards (`src/styles.css`).
- Integrated Knowledge Map into the main dashboard as the fifth workflow tab, retaining the shared header and existing floating Agent drawer; removed the standalone Sigma HTML page (`index.html`, `src/app.js`, `src/sigma_graph.js`, `src/styles.css`, `src/sigma_graph.html`).
- Expanded the shared workflow navigation width and normalized the Knowledge Map label to the existing single-line 13px tab typography (`src/styles.css`).
- Further expanded and left-shifted the desktop workflow navigation to prevent all five tab labels from colliding (`src/styles.css`).
- Increased desktop spacing between the five workflow tabs for clearer visual separation (`src/styles.css`).
- Set desktop workflow-tab spacing to 35px (`src/styles.css`).
- Set desktop workflow-tab spacing to 60px (`src/styles.css`).
- Replaced fixed desktop workflow-tab gaps with equal `space-between` distribution so each tab-to-tab interval is consistent while labels keep their natural widths (`src/styles.css`).
- Fixed workflow-tab navigation after leaving Knowledge Map by restoring the hidden main pipeline panel before returning to Fast Triage, Full Scout, or Shortlisting (`src/app.js`).
- Removed the duplicate Knowledge Map Agent launcher and connected selected graph-node context to the existing dashboard Agent drawer, displaying the active node and automatically supplying its direct-link context for responses (`index.html`, `src/app.js`, `src/sigma_graph.js`, `src/styles.css`).
- Restarted Knowledge Map force physics whenever its tab opens, moved the shared dashboard footer after the map content, and added the shared `i`-style workflow description panel for Knowledge Map (`index.html`, `src/app.js`, `src/sigma_graph.js`).
- Removed the purple active-tab underline from Knowledge Map while leaving existing workflow-tab indicators unchanged (`src/styles.css`).
- Aligned the Knowledge Map description panel directly beneath the shared header at the same full width as other workflow descriptions, while retaining graph-content gutters (`src/styles.css`).
- Removed the redundant Sigma force-physics command bar, moved Node/Link counts beside the reset control, and normalized the reset-control height with the filter row (`index.html`, `src/styles.css`).
- Applied identical calculated widths and gutters to the Knowledge Map filter bar and graph container (`src/styles.css`).
- Moved the Knowledge Map Selected Node overlay to the left side of the graph, retaining the mobile stacked layout (`src/styles.css`).
- Restored matching filter-bar gutters after Sigma-specific CSS had overridden them, aligning filter and graph box edges (`src/styles.css`).
- Moved selected-node Agent prompts into the existing Agent drawer and upgraded the Selected Node panel to fetch and render the linked Markdown note with a full-note link (`index.html`, `src/app.js`, `src/sigma_graph.js`, `src/styles.css`).
- Removed the faint Shortlisting-to-Knowledge Map header divider (`src/styles.css`).
- Widened the Selected Node Markdown panel and changed its full-note action into a draggable graph-overlay Markdown modal (`index.html`, `src/sigma_graph.js`, `src/styles.css`).
- Restored a clickable direct-neighbor list beneath each Selected Node Markdown note (`src/sigma_graph.js`, `src/styles.css`).

- Restored a compact red `▲ 최근 15일간 +N 증가` indicator immediately left of Tab 0 Summary Dashboard's collapse control. It counts distinct pipelines with a Listing, Fast Triage, Full Scout, or Shortlisting update in the last 15 days within the active Tab 0 filters; the scope note now matches Tabs 1–2 as `현재 Tab · Filter 기준` (`index.html`, `src/app.js`, `src/styles.css`).

- Clarified Tab 3 Shortlisting ownership: Filter 2, Total Score, Filter 3, In-vivo, In-vitro, ADMET, and Action date remain directly manageable there; Company, Country, Asset, Modality, Target, Main indication, and Stage now open a shared Full Scout read-only guidance modal on administrator double-click, with a direct link to the matched Tab 2 detail page (`src/app.js`, `src/styles.css`).

- Added administrator double-click Country editing in Tab 1 and Tab 2 Pipeline Tables, using a canonical Country dropdown with compact table codes (for example CN · China and US · United States) and audit-logged server validation.

- Restored Tab 0’s independent v0.7 collapsed Summary cards by explicitly overriding the expanded minimum card height; cross-tab size matching now applies only while expanded.

- Restored the Tab 0 dot motion to the v0.7 timing model: staged 720ms left-to-right arrival followed by one varied 1.5–5 second damped settle, rather than continuous drifting.

- Restored Tab 0 Summary dot motion with staggered left-to-right entry and restrained ambient drift, and bound Tab 0 plus Tabs 1–3 to the same expanded Summary heading/card/padding dimensions.

- Finalized Tab 0 Listing identity resolution: reviewed connections now automatically retain both Asset/Company labels as aliases, offer a representative-name choice only for Listing-only targets, support separate-new or exclude decisions, and merge more specific Listing descriptions without changing Fast Triage/Full Scout official fields. Added the operator guide in `docs/listing-import-guide.md`.

- Matched Tab 0’s expanded Summary Dashboard frame to Tabs 1–3 (50px heading plus 332px cards), placed metric text above the dot field, and retained whole-card stage filtering while dots remain inspectable.

- Restored Tab 0 Summary dot fields with direct DOM rendering instead of the externally loaded G6 graph dependency, so Pipeline dots render reliably after server restarts and on networks that block the CDN.

- Aligned the User Guide title block in User Management to the left, matching Access History.

- Added a short retry for Tab 0’s deferred G6 workflow graph so dots render even when the graph library finishes loading after the initial dashboard render; reduced the summary count scale and removed the white count pill in collapsed cards (`src/app.js`, `src/styles.css`, `index.html`).
- Restored the Tab 0 v0.7 dot-layer stacking so expanded workflow dots remain visible, and enlarged collapsed metric cards so their count, label, and recent-upload text retain the same scale as the expanded state (`src/styles.css`, `index.html`).
- Made numbered inline Evidence citations in Fast Triage and Full Scout reports navigable: hovering `[1]` now reveals its internal-link affordance and clicking scrolls to the matching reference definition with a subtle target highlight (`src/detail.js`, `src/triage-detail.js`, `src/styles.css`, `detail.html`, `triage_detail.html`).
- Removed the Tab 0 Summary metric stripe: only the large number now carries a small translucent white emphasis, the recent-upload marker no longer has a dividing bar, and the dot canvas again receives hover events over the entire card. Added subtle long-lived ambient dot motion and refreshed the asset hover layer (`src/styles.css`, `src/app.js`, `index.html`).
- Returned Tab 0 Summary metrics to the lighter v0.7 visual language: replaced the navy gradient with a subtle translucent white stripe, restored dark type and number-first/label-second hierarchy in both states, and softened the collapse motion while retaining the aligned Summary layout (`src/styles.css`, `index.html`).
- Unified Summary Dashboard geometry across Tabs 0–2: corrected Tab 0’s oversized heading box model, aligned desktop summaries to a four-card row with shared responsive breakpoints, and added a readable full-width navy metric stripe over Tab 0’s node field. Collapsed metrics now settle into centered cards with a short upward animation and the expanded recent-upload marker is larger (`src/styles.css`, `index.html`).
- Made Tab 0 import/identity-resolution metadata additive for Contact History as well as Comments: identical text remains idempotent, new text is appended even when existing Listing fields are retained, and cross-workflow sync carries the accumulated note to the Full Scout workspace first or Fast Triage when no Full Scout exists. Direct post editing still replaces only the edited Contact post (`main.py`, `tests/test_canonical_fields.py`).
- Aligned Tab 0 Summary Dashboard with Tabs 1–2: expanded workflow cards now share their 292px height and begin directly under the Summary heading; collapsed view restores four quiet boxed summaries, keeps the recent-15-day guide beside the title, and uses a restrained red upward upload marker (`index.html`, `src/app.js`, `src/styles.css`).
- Added loss-aware canonical fields for Fast Triage and Full Scout: original Stage/Modality wording is retained alongside a compact primary value, unmapped wording remains visible/filterable rather than collapsing to `Unknown`/`Other`, and composite modalities such as AAV-delivered ASO retain `RNA therapy` as primary while also matching the `Gene therapy` filter tag. Added Stage/Modality regression tests and documented the conservative planned-stage policy plus Registration meaning (`main.py`, `src/app.js`, `json/drug-valuation.schema.json`, `json/schema.md`, `tests/test_canonical_fields.py`, `index.html`).

- Made Tab 0 Listing canonicalization loss-aware: unclassified Modality, Indication, and Stage values now remain visible and filterable as their original source wording instead of collapsing to `Other`/`Unknown`; partially recognized Indication cells retain their unmatched items alongside canonical labels; comma-separated Country values now prefer a rightmost exact country component such as `Cambridge, UK` → `Europe/UK` (`src/app.js`, `index.html`).
- Refined Tab 0 Summary Dashboard to v0.8: the primary count, recent-15-day upload change, and workflow name now sit centrally inside the flat dot field; an active stage highlights the entire card with a green outline only; and the collapsed view becomes one bright, divider-led summary strip rather than four gray cards (`src/app.js`, `src/styles.css`, `index.html`).
- Completed shared checkbox drag selection across Tab 0–3: dragging from an unchecked row selects each crossed row, dragging from a checked row clears them, header checkboxes still select/clear the visible page, and Tab 0 now uses the same selected-row highlight as the other Pipeline Tables (`src/app.js`, `src/styles.css`, `index.html`).
- Kept Tab 1–3 multi-select filter menus open while values are checked or unchecked, added a compact `완료` close action, and retained live table/summary filtering throughout selection (`src/app.js`, `src/styles.css`, `index.html`).
- Restricted GPT original-report Topic memo creation/editing/deletion and Partner Materials/DD Report upload/deletion to administrators (developers inherit this access). The detail UI now hides restricted upload/memo controls for users while retaining read-only material/report access; server routes enforce the same rule, and a maintained user/admin/developer permission matrix was added to the README and `docs/access-control.md` (`main.py`, `detail.html`, `src/detail.js`, `src/styles.css`, `README.md`, `docs/access-control.md`).
- Refined executive snapshot chart density and alignment: 100-day usage and signup data is now grouped into weekly totals, all three cards share a fixed chart baseline and explanatory timespan row, and the to-date Pipeline cumulative chart now uses weekly buckets after two weeks of history and true calendar-month buckets after two years (`main.py`, `user_admin.html`, `src/user-admin.js`, `src/styles.css`).
- Filled the Tab 2 DUE DILIGENCE heading's empty space with compact Efficacy, Commercial, and Dev. & Partnership Risk opinion-status pills. Each pill lights up and shows its saved-entry count, expands the qualitative panel on click, then scrolls to and focuses that criterion's composer with a brief highlight (`detail.html`, `src/detail.js`, `src/styles.css`).
- Replaced the redundant Pipeline Coverage card in the developer executive snapshot with a to-date cumulative Pipeline registration chart. Usage and signup charts now cover the most recent 100 days; the Pipeline chart begins at the first saved report and automatically compresses its date buckets from daily to weekly/monthly as the service history grows, while retaining the cumulative report count (`main.py`, `user_admin.html`, `src/user-admin.js`, `src/styles.css`).
- Added Excel-style viewport header freezing to every dashboard Pipeline Table (Tab 0 진척 현황 and Tab 1–3). The header mirrors beneath the persistent top navigation only while two or more body rows remain visible, stays aligned with horizontal table scrolling, and disappears near the final lone row to preserve reading space (`src/table-header-freeze.js`, `src/app.js`, `src/styles.css`, `index.html`).
- Added an executive snapshot above the developer-only Account Directory: 14-day active-time and new-signup bar charts, plus current Full Scout/Fast Triage/Listing coverage. Added a concise USER GUIDE below Access History that explains account status, valid sessions, event count, active time, and recommended executive-reporting KPIs; the backend now returns only aggregate, privacy-conscious chart data (`main.py`, `user_admin.html`, `src/user-admin.js`, `src/styles.css`).
- Refined the developer-only User Management page: CSV export and refresh now share the same secondary-button treatment, table headers use the dashboard's lighter heading weight, and the account/session labels clarify login eligibility versus valid login tokens. Added per-user 30-day active time, 30-day active-user/time summaries, sortable CSV export fields, and privacy-conscious activity heartbeats that count visible, recently interacted-with dashboard time rather than passive open-tab time (`main.py`, `src/auth.js`, `src/user-admin.js`, `user_admin.html`, `src/styles.css`).
- Restored Tab 0 (진척 현황) as the default home view when the dashboard URL has no `?tab=` parameter; explicit tab URLs continue to open their requested view (`src/app.js`).
- Restyled the page-jump orb to the AI-chatbot treatment: a restrained pale-lavender surface with a deep-purple outline and compact arrows, with the internal divider removed for a calmer single-icon appearance (`src/styles.css`).
- Enriched Tab 0 Quick Guide step 2 with a compact Excel selection-to-Listing Input illustration and a rounded representative Header mapping card (including Drug Name/Pipeline Code → Asset and Geography/Location → Country) (`src/app.js`, `src/styles.css`).
- Refined the page-jump controls into one 58px AI-launcher-matched circular orb with vertically split up/down arrow zones, eliminating the mismatched pair of smaller blue circles (`src/page-jump.js`, `src/styles.css`).
- Added shared floating page-jump controls across the dashboard and record-detail screens: restrained AI-launcher-style up/down icons appear only when a page can scroll farther, expand on hover, and move smoothly to the page top or bottom without overlapping the AI launcher (`src/page-jump.js`, `src/app.js`, `src/detail.js`, `src/triage-detail.js`, `src/styles.css`).
- Allowed a reviewed report reupload to keep the existing Pipeline while still storing the incoming Asset/Company labels as searchable aliases; the shared waiting modal now explains whether research is being overwritten or retained, and reiterates the Tab 0 Comment/Contact destination rule (`main.py`, `src/app.js`).
- Made Tab 0 Excel Listing Comment and Contact History synchronize to one canonical research workspace only: Full Scout (Tab 2) takes priority, otherwise Fast Triage (Tab 1); superseded system copies are removed while original user-written posts remain intact (`main.py`).
- Consolidated the duplicate VASTHERA VTA-27 Full Scout records into the 2026-08-12 record; migrated the 2026-08-06 attachment and preserved its report as reupload history before removing the duplicate listing (`json/pipeline-records.json`, regenerated `obsidian/`, `skbp_pipeline_wiki/`).
- Added a two-step Tab 0 Listing import: a cancellable pre-save check, then a shared-style overwrite review for similar Asset/Company labels; exact identities still merge automatically, while parenthetical aliases such as `NEOK001 (ABL206)` require an explicit overwrite or new-Listing choice (`main.py`, `index.html`, `src/app.js`).
- Extended the same explicit parenthetical-alias review to Full Scout/Fast Triage reuploads; an approved replacement retains the existing Team Workspace metadata, including comments, Contact History, attachments, and change history, while replacing the report-derived fields (`main.py`, `src/app.js`).
- Renamed Tab 0 review actions from ambiguous “merge” wording to “overwrite,” added a compact alias-search guidance note after choosing overwrite, and made the existing processing modal explicitly explain that a confirmed Full Scout/Fast Triage overwrite refreshes web-researched findings while retaining existing files, Comments, and Contact History (`index.html`, `src/app.js`, `src/styles.css`).
- Added an overwrite-only “유사 Asset 이름도 함께 저장” control: selected old/current and parenthetical Asset codes are retained as search-only aliases while the Pipeline Table keeps the incoming report's main Asset label (`main.py`, `src/app.js`, `src/styles.css`, `json/drug-valuation.schema.json`, `json/schema.md`).
- Made the optional Asset-alias save control visible for every confirmed overwrite, not only Asset labels containing commas or parentheses (`src/app.js`).
- Expanded the overwrite-only alias control to retain both prior/current Asset and Company names as search-only metadata, while keeping the incoming report's primary names in all Pipeline Tables (`main.py`, `src/app.js`, `json/drug-valuation.schema.json`, `json/schema.md`).
- Made Full Scout the canonical Tab 1 view whenever both workflows exist: Tab 1 renders the Full Scout TR/MoA/Data summary and links to its Tab 2 detail, Tab 0's Fast Triage status link follows the same destination, and Fast Triage post comments/Contact History now synchronize into the Full Scout Team Workspace with source-labelled authorship (`main.py`, `src/app.js`).
- Extended the Tab 1/2 canonical-view match to use stored Asset and Company aliases as well as the displayed names, so a reviewer-approved renamed/aliased Full Scout also replaces its prior Fast Triage view (`src/app.js`).
- Standardized Excel-imported Tab 0 Listing Comment and Contact History authors as `Team` (rather than an administrator), including their propagated Tab 1/2 source-labelled posts (`main.py`, `src/app.js`).
- Restored the Tab 1 and Tab 2 Stage filter trigger to the standard dropdown height, using single-line truncation for consistent filter-row alignment.
- Added dedicated Contact History inputs to Tab 1 and Tab 2, storing posts as categorized operational metadata and surfacing them in the Tab 0 Contact feed.
- Clarified the Tab 2 comment and Contact History sections, removed the redundant active-author bar, and aligned Contact History styling to the shared green comment accent.
- Moved Tab 2 Qualitative Review above Team Workspace Comments and gave it an independent Show/Less control.
- Replaced the Tab 1 Contact History heading and guidance with a compact icon beside the Final Comment action.
- Changed the Tab 0 Contact icon to open its history feed first; posting now starts only from the Contact History Post action.
- Made Fast Triage Final Comments individual author-owned posts, added the labelled Contact History action, and added progress feedback while comments are saved, edited, or deleted.
- Removed the technical Internal pipeline metadata block from the Tab 2 Team Workspace; Tab 0 Comment and Contact sync there as source-labelled read-only posts instead.
- Made Team Workspace DD and Contact upload controls compact title-adjacent pills, restored file-picker activation, and tightened collapsed-section scrolling and composer hints.
- Added a Partner Materials upload-modal note explaining that the file will be used as supporting evidence for AI Agent responses.
- Added the same AI-evidence notice to dedicated DD Report uploads while preserving Contact History attachments as AI-excluded operational files.
- Removed the gray helper text under Tab 2's Qualitative Review, Comments, and Contact History section headings, and renamed the Qualitative Review title to "DUE DILIGENCE" (`detail.html`, `src/detail.js`).
- Moved the Tab 2 "AI 생성" (✨) button to sit directly beside the DUE DILIGENCE title (always visible, not just when expanded), with a hover tooltip explaining it generates AI answers for the qualitative review items below (`detail.html`, `src/detail.js`, `src/styles.css`).
- Moved the Tab 2 Comment input to sit directly under the Comments thread and above the Contact History section (previously pinned as a sticky bar at the very bottom, below Contact History), boxed to match the Contact History composer, and changed its placeholder to end with a period instead of an ellipsis (`detail.html`, `src/styles.css`).
- Changed the Filter 2 column sort to a fixed PASS/REVIEW/FAIL severity order (ascending PASS→REVIEW→FAIL, descending FAIL→REVIEW→PASS) instead of alphabetical, which previously put REVIEW out of order (`src/app.js`).
- Unified the Tab 2 Comment composer border to the same plain top-border line as the Contact History composer (dropped its rounded box style), and reworded the Contact History placeholder to "미팅, 통화, 이메일 등 연락 여부를 기록하세요." on both the Full Scout and Fast Triage detail pages (`src/styles.css`, `detail.html`, `src/triage-detail.js`).
- Changed the Tab 1 Filter 1 column sort to a fixed SELECT/REJECT/UNVERIFIED severity order (ascending SELECT→REJECT→UNVERIFIED, descending UNVERIFIED→REJECT→SELECT) instead of alphabetical, matching the Filter 2 sort fix (`src/app.js`).
- Reworded the Contact History placeholder again to "미팅, 통화, 이메일 등 연락 여부 및 연락 내용을 기록하세요." (Full Scout and Fast Triage detail pages), and removed the green-tinted top border on the Contact History composer so its separator line matches the plain gray one above the Comment composer (`detail.html`, `src/triage-detail.js`, `src/styles.css`).
- Changed the home dashboard's default landing tab from Full Scout to Tab 0 진척 현황: root URL (no `?tab=` param) now boots into the Step 0 panel instead of the Full Scout table, while `?tab=triage|full|focus` links still open that specific tab as before; also fixed a tab-highlight bug where the Full Scout tab button could stay visually "active" while the Step 0 panel was actually showing (`index.html`, `src/app.js`).
- Fixed the Tab 2 Team Review Workspace's Comments and Contact History post cards, where the delete (×) button overlapped the timestamp in the top-right corner; added right padding to the meta row so the timestamp no longer runs under the button, matching the same `:has()`-based spacing fix already used on the Fast Triage detail page's note cards (`src/styles.css`).
- Renamed the Tab 0 intro banner's right-side label from "Listing" to "진행 단계" (`index.html`).
- Fixed the Tab 0 Summary Dashboard collapse/expand toggle: re-expanding now rebuilds the workflow stat counters and stage circle-node graphs from scratch, replaying the same staggered fade-in entry animation used on first Tab 0 load, instead of leaving the graphs in whatever stale mid-animation state they were left in before collapsing (`src/app.js`).
- Restyled the Tab 0 Summary Dashboard's four stage cards: the number/label box now keeps its own fixed gray border and background at all times (collapsed or expanded), and the circle-dot graph beneath it dropped its enclosing box/fill so the dots render as plain open space instead of "a box inside a box"; the graph area also got more room, with each card's total height set to 292px to match the Tab 1/2 Summary Dashboard panel height so switching tabs no longer changes the dashboard's footprint (`src/styles.css`).
- Added a trailing "·" to the Tab 2 Full Scout Team Review Workspace's "Comments" heading (`detail.html`).
- Added a source breadcrumb (e.g. "Tab 1 · Fast Triage · Contact History", "Tab 1 · Fast Triage · Target Area Relevance", "Tab 2 · Full Scout · Comments") in front of the author name on every Fast Triage and Full Scout comment/note card, replacing the name-only byline; Tab 1's Final Comment and Contact History posts now also render in distinct blue/green accent colors instead of the same yellow (`detail.html`, `src/detail.js`, `src/triage-detail.js`, `src/styles.css`).
- Fixed Fast Triage → Full Scout comment sync overwriting the real author's name with a fake one (e.g. "Fast Triage · Target Area Relevance") when criterion notes and the legacy final-comment field were copied into the linked Full Scout record's Comments thread; the sync now preserves the real author and carries the origin breadcrumb as a separate `label` field so Tab 2 can render it correctly (`main.py`). Also reworded the Tab 0 progress feed's comment-type label from a generic "Team Review Comment" to category-specific "Comment"/"Comments"/"Contact History" (`main.py`).
- Fixed the Tab 2 pipeline table's Stage column truncating labels like "IND-enabling" and "Lead Optimization" for admins: the always-visible native `<select>` (which can't wrap its closed-state text) is replaced with a plain wrappable text span that swaps into the select only while actively editing (double-click), matching the existing Modality edit pattern; also widened the Stage column's default/min width across the Tab 1/2 and Shortlisting tables so long labels wrap cleanly onto two lines instead of clipping (`src/app.js`).
- Added Show/Less toggle buttons to the Tab 2 Team Review Workspace's Comments and Contact History headings, matching the existing DUE DILIGENCE toggle so all three sections look and behave consistently (collapsed by default, composer stays visible while collapsed) (`detail.html`, `src/detail.js`).
- Added a meeting-note upload dropzone under Tab 2's Contact History composer: uploaded files are stored via the existing Partner Materials attachment pipeline (tagged `source: contact_history` so they're excluded from the Partner Materials list and don't trigger OI partnership auto-classification), then posted as a Contact History entry with the standard "Tab 2 · Full Scout · Contact History" breadcrumb, author, and date; the filename renders as a clickable chip that opens the existing attachment preview pane on the left (`main.py`, `detail.html`, `src/detail.js`, `src/styles.css`).
- Removed the trailing "·" that had been added to the Tab 2 Team Review Workspace's "Comments" heading — it read as a stray/misplaced dot next to the title (`detail.html`).
- Reworded the Tab 2 (Full Scout) Contact History composer placeholder to "미팅, 통화, 이메일 연락 여부 및 내용을 남겨주세요." (`detail.html`).
- Fixed Tab 0 Contact History Posts never propagating into the matching Fast Triage/Full Scout record's Contact History thread: `synchronize_cross_workflow_comments` only synced the Tab 0 "Comment" field, never "Contact"; added the missing sync (tagged `category: contact_history`, labelled "Tab 0 · Contact History Post") and backfilled every already-posted Tab 0 Contact History value into its record's Team Review Workspace (`main.py`).
- Replaced Tab 2's large "회의록 업로드" dropzone under the Contact History composer with a compact upload icon next to the Contact History title (left of the Show/Less toggle, tooltip "회의록 파일 업로드 (최대 30MB)"), and added a matching upload icon next to the DUE DILIGENCE title for DD Report files (tooltip "DD 리포트 파일 업로드 (최대 30MB)", uploaded under the existing DD Report Partner Materials category). Uploaded files now render as a clean list directly above each section's thread/input instead of inline as thread posts, reusing the existing Partner Materials preview viewer on click. Contact History file uploads (any file type, including PDF/PPT/PPTX) no longer run text extraction/caching — they're stored for viewing only and are excluded from AI chat, rubric-refresh, and qualitative-review context, matching the existing rule that Contact History text posts are operational-only and never become AI evidence (`main.py`, `detail.html`, `src/detail.js`, `src/styles.css`).
- Reordered the Tab 2 DUE DILIGENCE heading's action icons so the upload icon sits immediately next to the title (matching Contact History's layout), with the AI-generate (✨) icon and Show/Less toggle grouped together at the far right edge — unifying where the Show button lands across DUE DILIGENCE, Comments, and Contact History (`detail.html`).
- Moved the "Enter 전송 · Shift+Enter 줄바꿈" hint on Tab 2's Comments and Contact History composers from a separate line below the input box to inline, right-aligned inside the textarea on the same line as the placeholder text; reserved right padding on the textarea so typed text never runs under it, and removed the now-unused standalone hint styling (`detail.html`, `src/styles.css`).
- Removed the "아직 등록된 의견이 없습니다." placeholder text under each Tab 2 DUE DILIGENCE criterion when it has no opinions yet; an empty criterion now takes no extra vertical space instead of padding out the scroll length across all criteria (`src/detail.js`, `src/styles.css`).
- Fixed the DUE DILIGENCE/Contact History heading layout: the upload icon, AI-generate (✨), and Show/Less toggle were all bunched together right after the title instead of the Show/Less control sitting at the section's far right edge. Grouped the title with its upload icon in one flex cluster and the remaining controls in another, then spread the two clusters across the full heading width, so the upload icon now stays snug next to the title while ✨/Show land flush right — matching Comments' existing right-aligned Show button (`detail.html`, `src/styles.css`).
- Unified DD Report file storage: DD Report attachments (whether uploaded via the new DUE DILIGENCE icon or the Partner Materials "+" picker with DD Report chosen as the category) no longer show in the Partner Materials list — they now surface only in the DUE DILIGENCE file list. The Partner Materials "자료 보유" DD flag still lights up either way, since it's detected independently from the record's full attachment set (`src/detail.js`).
- Changed the home dashboard's default landing tab (no `?tab=` param) from Tab 0 진척 현황 to Shortlisting, per updated preference; explicit `?tab=step0|triage|full|focus` links all continue to open that specific tab as before (`src/app.js`).
- Fixed a critical missing-comma syntax error in the Tab 0 Listing overwrite-review save flow (`runBlockingOperation({...})` object literal) that was throwing on page load and breaking the entire dashboard — no tab, table, or button worked until this was fixed (`src/app.js`).
- Removed the unintended bold weight on the "Enter 전송 · Shift+Enter 줄바꿈" inline composer hint (it was inheriting a `label > span` bold rule meant for a different label caption); added the same hint to each Tab 2 DUE DILIGENCE criterion's opinion textarea for consistency with Comments/Contact History, and reworded that textarea's placeholder from "의견을 추가하세요…" to "의견을 입력해 주세요." (`src/detail.js`, `src/styles.css`).
- Reworked the Tab 0 "붙여넣기" paste-result message from a single dense run-on sentence into the same structured badge + bulleted "입력 검토" list already used by Tab 1/2's upload review and Tab 0's own Import summary: a status badge and row/column/field summary line, then separate labelled rows for the data actually entered, header mapping, skipped/duplicate headers, and any blank rows kept — instead of one paragraph running all of that together (`index.html`, `src/app.js`, `src/styles.css`).
- Simplified the Tab 0 Quick Guide's step 1 body text: replaced the long "Country · Modality · Target · Main indication · Stage · Comment · Contact는 선택 입력이며, 아래 표의 첫 칸에서 Excel 열을 그대로 붙여넣을 수 있습니다..." sentence with "그 외 추가 정보가 있다면 입력해주세요. Contact의 경우 O, 날짜가 있을 경우 체크되며, X·-·빈칸은 연락 이력 없음으로 표시됩니다." (`src/app.js`).
- Reworded the Tab 0 Quick Guide's step 2 ("Excel처럼 여러 행·열을 한 번에 붙여넣기") body text from the fixed-column-order explanation to "Listing input에 해당하는 내용을 포함한 엑셀 표의 첫 행의 Column Title과 함께 그대로 복사 붙여넣기 하면, 각 셀에 맞는 정보로 매칭되어 입력됩니다. 입력 후 내용이 잘 입력되었는지 확인해주세요." (`src/app.js`).
- Renamed the Tab 0 Quick Guide's step 4 title from "Listing 항목 선택 후 지침 복사" to "조사 대기 항목 선택 후 지침 복사", and updated the body's opening from "Listing 중인 파이프라인을" to "조사 대기중인 파이프라인을" (rest of the sentence unchanged) (`src/app.js`).
- Added a line break in the Tab 0 Quick Guide's step 1 body so "Contact의 경우 O, 날짜가 있을 경우 체크되며..." starts on its own line instead of running on from the previous sentence; guide step bodies now convert `\n` to `<br>` generally, so future guide text can use the same pattern (`src/app.js`).
- Renamed the Tab 0 Quick Guide's step 2 title from "Excel처럼 여러 행·열을 한 번에 붙여넣기" to "Listing Input 관련 Excel 정보 여러 행·열을 한 번에 붙여넣기", and reworded its body to "엑셀 표의 첫 행의 Column Title과 함께 Pipeline List 모든 내용을 그대로 복사 붙여넣기 하면, 각 셀에 맞는 정보로 매칭되어 입력됩니다. 입력 내용이 Listing Input 표에 잘 입력되었는지 확인해주세요." (`src/app.js`).
- Renamed the Tab 0 Quick Guide's step 1 title from "표의 각 칸에 Listing 정보를 입력" to "Listing Input 관련 정보를 Collect" (`src/app.js`).
- Reworded three more Tab 0 Quick Guide text bits: step 1 body's "…있다면 입력해주세요." → "…있다면 Collect하여 엑셀 표에 정리해 주세요."; step 2 body's closing "입력 내용이 Listing Input 표에 잘 입력되었는지 확인해주세요." → "Listing Input 표에 잘 매칭되었는지 확인해 주세요."; step 4 title "조사 대기 항목 선택 후 지침 복사" → "조사 대기 항목 GPT 1 Fast Triage 웹서칭 준비" (`src/app.js`).
- Removed "Collect하여" from the Tab 0 Quick Guide's step 1 body: "…있다면 Collect하여 엑셀 표에 정리해 주세요." → "…있다면 엑셀 표에 정리해 주세요." (`src/app.js`).
- Restyled Tab 2's "Enter 전송 · Shift+Enter 줄바꿈" composer hint from italic gray to a small bold teal-green caption (matching the size/weight of the "Filter 2" label), and applied the same fix that removed an accidental bold on it from a stray `label > span` rule. Normalized Team Review Workspace vertical spacing so it's even across the board: DUE DILIGENCE→Comments and Comments→Contact History section gaps are both 14px, and the Comments/Contact History title-to-input-box gaps are both 9px (the empty DD Report/Contact History status line now collapses via `:empty` instead of reserving dead space, and DUE DILIGENCE's heading no longer gets an extra margin the other two don't have). Added a small colored file-count badge next to the DD Report and Contact History upload icons (same style as Partner Materials' count), shown only once a file is uploaded and hidden entirely at zero (`detail.html`, `src/detail.js`, `src/styles.css`).
- Reworded the Tab 0 Quick Guide's step 1 title from "Listing Input 관련 정보를 Collect" to "Listing input 관련 정보 Collect" (`src/app.js`).
- Added a new line to the Tab 0 Quick Guide's step 2 body: "엑셀 표 첫 행은 Listing input table의 Column Title 내용을 포함해 주세요. (예: Company, Country, Asset, Modality 등)" (`src/app.js`).
- Removed the standalone "---" section dividers from the GPT Full Scout instruction template (between Company Profile / Pipeline Snapshot / Scorecard Summary / Criterion Detail Pages / Validation Notes / Final Take / References) — they rendered as literal unstyled dashes in the GPT 원문 리포트 viewer, which now looks redundant since headings and the Topic 메모 panels already provide visual separation. The functional `--- JSON DATA ---` delimiter is untouched. Also made the Markdown renderer skip any standalone `---`/`***`/`___` divider line so already-saved reports render cleanly too, without needing re-upload (`src/app.js`, `src/detail.js`). This is a formatting-only prompt change, not a scoring/rubric change, so `config/rubric-release.json` (instruction_version/rubric_version) was intentionally left unchanged.
- Reordered the Tab 0 Quick Guide's step 2 body: the Column Title note now comes first ("엑셀 표 첫 행은 Listing input table의 Column Title 내용을 포함해 주세요. (예: Company, Country, Asset, Modality 등)"), followed by a line break and the paste/matching explanation, reworded to "엑셀 표 첫 행 Column Title과 함께 Pipeline list 모든 내용을 그대로 복사 붙여넣기 하면, Listing input의 각 셀에 맞는 정보로 매칭되어 입력됩니다. Listing Input 표에 잘 매칭되었는지 확인해 주세요." (`src/app.js`).
- Reworded the Tab 0 Quick Guide's step 3 body's middle sentence: "가져오는 중 특정 Pipeline이 이미 Fast Triage나 Full Scout가 수행된 Pipeline이라면 기존 Pipeline 기록을 유지하며, Comment·Contact History는 따로 입력됩니다." → "가져오는 중 특정 Pipeline이 이미 Fast Triage나 Full Scout가 수행되었다면, 기존 서칭 기록을 유지합니다. Comment·Contact History는 따로 입력됩니다." (`src/app.js`).
- Simplified the Tab 0 Quick Guide's step 2 second line: "엑셀 표 첫 행 Column Title과 함께 Pipeline list 모든 내용을 그대로 복사 붙여넣기 하면, Listing input의 각 셀에 맞는 정보로 매칭되어 입력됩니다. Listing Input 표에 잘 매칭되었는지 확인해 주세요." → "엑셀 표 첫 행(Column Title)과 함께 Pipeline list 표 전체를 그대로 복사 붙여넣기 하면, Listing input의 각 셀에 맞는 정보로 매칭됩니다." (dropped the trailing confirm-visually sentence) (`src/app.js`).
- Restyled the admin-only User Management page's "새로고침" button from an unstyled bare `<button>` to match the "CSV 내보내기" button next to it: same `secondary-button` pill class, plus a small refresh icon matching the app's existing refresh-icon convention (`user_admin.html`, `src/styles.css`).
- Fixed the Tab 0 Summary Dashboard's "Listing" stat: it now counts every pipeline in the Pipeline Table (Fast Triage/Full Scout/Shortlisting-completed pipelines included), instead of only the "조사 대기" pending subset it showed before; clicking the card still filters the table down to "조사 대기" pipelines only. Updated its hover tooltip to "클릭 시 Listing 완료된 Pipeline List 중 조사 대기 중인 Pipeline을 표시합니다." This also fixes an existing mismatch where the workflow node-graph dots under the card already showed all pipelines while the number above only showed the pending count (`src/app.js`, `index.html`).
- Removed the trailing sentence "Tab 0의 보조 정보는 Listing 관리용이며, Fast Triage·Full Scout의 공식 조사 값이나 점수를 변경하지 않습니다." from the Tab 0 Quick Guide's "가져오기" step body (`src/app.js`).
- Moved the Tab 0 Quick Guide's "Contact의 경우 O, 날짜가 있을 경우 체크되며..." sentence to below the step's "입력 형식 예시" example row instead of above it, so the example comes right after the intro sentence and the Contact display-rule note follows (`src/app.js`).
- Restyled the Tab 0 Quick Guide's Excel-illustration "drag · copy · paste" caption to match the blue/bold "입력 형식 예시" label styling instead of tiny gray text, and let it wrap onto multiple lines within its fixed-width column so the longer bold text no longer clips against the adjacent "LISTING INPUT" preview box (`src/styles.css`).
- Unified the Tab 0 Quick Guide's two example boxes' background/border: the "입력 형식 예시" `<pre>` block used a slightly different tint (`--data-upload-accent` 3% onto `--surface-soft`, 12% border) than the Excel-illustration box (4% onto `--surface`, 20% border); both now use the same values so the two boxes read as one consistent style (`src/styles.css`).
- Made the Tab 0 Summary Dashboard's "Listing" stat card swap its label to "조사 대기" while its click-filter is active (and back to "Listing" when cleared or another card takes over), fixing a related bug where the card's `active` highlight state never applied because the click handler stores the filter as `investigation_pending` while the highlight check compared against `pending` (`src/app.js`).
- Reworded the Tab 0 Quick Guide's "가져오기" step body's closing sentence: "Comment·Contact History는 따로 입력됩니다." → "Comment·Contact History는 따로 추가됩니다." (`src/app.js`).
- Fixed the scroll-to-top/bottom "page jump" orb's position on pages with no floating AI agent (Fast Triage detail): it now drops down into the AI launcher's own bottom-right spot (`right:24px; bottom:24px`, `right:16px; bottom:16px` on mobile) instead of floating above an empty gap where the agent would have been, so the corner position is consistent across all pages (`src/page-jump.js`, `src/styles.css`).
- Made the "page jump" orb's positioning reactive to the AI agent's per-tab visibility on the main dashboard, not just a one-time check at page load: the agent launcher stays in the DOM and only toggles `hidden` when switching tabs (e.g. it's hidden on Tab 1 Fast Triage), so the previous load-time-only check never dropped the orb down there. A MutationObserver on the launcher's `hidden`/class now keeps the orb correctly stacked right above the agent whenever it's visible (Tab 0/Tab 2/Shortlisting) and dropped into the agent's own spot whenever it isn't (Tab 1) (`src/page-jump.js`).
- Made the Tab 0 "Listing" stat card's filter toggle off on a second click of the already-active card (reverting to the full unfiltered Listing count) instead of only ever narrowing further; its hover tooltip now also swaps to match state — the unfiltered "Listing" state keeps the existing "클릭 시 Listing 완료된 Pipeline List 중 조사 대기 중인 Pipeline을 표시합니다." tooltip, while the filtered "조사 대기" state now shows "클릭 시 현재 Tab0에 업로드된 모든 Pipeline List 수를 나타냅니다." (`src/app.js`).
- Widened the gap between the Tab 0 Quick Guide's "입력 형식 예시" box and the "Contact의 경우 O, 날짜가 있을 경우..." note that follows it: it had 0px (touching) while the Excel-illustration box keeps ~10px before its own outro note; matched it to 10px for consistency (`src/app.js`, `src/styles.css`).
- Unified the Korean "파이프라인" wording in hover tooltips (`data-tooltip`) to the English "Pipeline" used elsewhere in the same tooltips, so a tooltip no longer mixes both within one sentence: the refresh button, the Fast Triage/Full Scout/Shortlisting Summary Dashboard stat cards, and the "선택 삭제" button (`index.html`).
- Fixed the Tab 2 DUE DILIGENCE heading layout after the new Efficacy/Commercial/Dev. & Risk opinion-status pill row was added: the pill row's implicit CSS Grid placement had also pushed the ✨/Show action cluster down and off the flush-right position it shares with the Comments/Contact History headings. Gave the title and actions clusters explicit `grid-row: 1` (so Show stays flush right on the title row) and the pill row explicit `grid-row: 2` (so it reliably spans the full width below), and made each status pill `flex: 1 1 0` so the three pills stretch to fill that row at roughly the same width as the Comments/Contact History input boxes below, instead of staying shrink-wrapped to their label text (`src/styles.css`).
- Fixed the User Management page's header, which was taller than and misaligned with the main dashboard's Tab 1/2/3 header: its title link and right-side actions (대시보드 link, theme toggle, account icon) were direct children of `.topbar`, which defaults to a stacked column layout meant for pages with many header buttons (record detail/wiki), so they rendered as two separate rows instead of one. Wrapped them in the same `.topbar-top` row container the main dashboard already uses, matching its single 72px-tall row with the title on the left and all action icons vertically centered flush right (`user_admin.html`).
