# Work Log — 2026-07-29

## Changes

- `.claude/skills/post-edit-log/SKILL.md`, `docs/CHANGELOG.md`: added a repo-wide skill so any agent session logs a short change summary here after coding, instead of only in chat.
- `src/app.js`: unified the pipeline table column format across Fast Triage, Full Scout, and 집중 관리 tabs — Company/Country/Asset/Modality/Target/Main indication now render identically in all three, including the Modality/Target hover tooltip (Theme · Cluster · Description) that was previously Full-Scout-only.
- `index.html`, `src/app.js`: redesigned the 6-card dashboard summary grid — replaced Target Relevance 분포 / Pipeline Filter donut / Score Profile with Theme·Indication·Modality 분포 cards (top 3 + "Others" rollup, 4 lines each), moved 국가별 후보군 to row 2, scoped Priority Watch to REVIEW-status pipelines only, and added a new Due Date Watch card (top 3 집중 관리 items by nearest action date, showing the saved action-item note and due date instead of a score).
- `detail.html`, `src/detail.js`, `main.py`, `json/drug-valuation.schema.json`, `json/schema.md`, `config/qualitative_review_criteria.md`: added a "파일 추가" attachment dropzone to the detail page's raw-report panel (`POST`/`DELETE /api/records/{id}/attachments`, files stored under gitignored `attachments/`) and a qualitative-review panel with 3 temporary criteria (scientific_rigor, commercial_appeal, execution_risk), each auto-appending a rule-based placeholder AI reply on submit (no OpenRouter call).
- `src/app.js`, `src/styles.css`: added a "Filter 3" dropdown column (N/A / V-Up / Co-dev / 투자) to the TAB3 (집중 관리) table, wired to the existing `partnership_type` field on `PATCH /api/records/{id}/focus-management`.
- `src/styles.css`: changed the topbar background from a navy gradient to a solid `var(--navy)`, removing the duplicate gradient overrides that had been controlling it.
- `src/app.js`, `src/styles.css`: replaced the 4 bar-chart distribution cards (Theme/Indication/Modality/Country) with compact multi-segment donut charts (pure-CSS conic-gradient, no library) to cut vertical space and restore at-a-glance scannability; bumped each from top-3+Others to top-5+Others (top-7 for Country) since donuts handle more slices without adding height; fixed `.visual-grid` so panels flow into one row on wide screens again (a later unconditional `repeat(3, ...)` rule had been permanently overriding the responsive `auto-fit` layout and media queries).
- `src/app.js`, `src/styles.css`: rebuilt the 4 donut charts from a flat CSS conic-gradient into per-segment SVG arcs so each wedge is hoverable — hovering a segment now swaps the center number/label to that segment's count instead of the static total (33) shown on every chart; on no-hover, Theme 분포 now defaults to total minus N/A and Unknown counts (28) while the other three still show the plain total.
- `src/app.js`, `src/styles.css`: replaced the donut palette (which reused the app's reserved PASS/REVIEW/FAIL status hues — `var(--green)`/`var(--yellow)` — plus two arbitrary hex) with a dedicated 4-hue categorical set (`--chart-1..4`, blue/orange/aqua/violet, light+dark steps) validated with the dataviz skill's `validate_palette.js` (clears CVD/contrast/lightness-band checks in both modes, avoiding collision with the status colors used elsewhere on the same dashboard); dropped each donut from top-5-with-Others back to top-4-with-Others so every chart only ever needs the 4 validated hues.
- `index.html`, `src/app.js`, `src/styles.css`: split the "판단근거" drawer into per-tab scopes instead of showing all criteria at once — Tab1 (Fast Triage) now shows only the SELECT/REJECT/N/A rule and Target Relevance Theme/Cluster note; Tab2 (Full Scout) shows the PASS/REVIEW/FAIL rule plus the full scoring table, parameter guide, and evidence-recording rules; Tab3 (집중 관리) shows a placeholder for the not-yet-defined OI Partnership Type (Filter 3) criteria. Sections are tagged `data-criteria-tab`, toggled via `updateCriteriaDrawerScope()` on drawer open and on tab switch, with the drawer's eyebrow label reflecting the active tab.
- `main.py`, `requirements.txt`, `json/drug-valuation.schema.json`, `json/schema.md`, `src/app.js`, `src/styles.css`: replaced the TAB3 table's wide "Team comments / note" textarea column with three compact score-style columns — In-vivo, In-vitro (O/X/N-A), and ADMET (N/50) — under `meta.focus_management.{in_vivo_status,in_vitro_status,admet_completed}`. Each is auto-computed by a keyword heuristic (`apply_auto_detected_evidence` / `classify_evidence_presence` / `count_admet_completed`) over the GPT report plus extracted attachment text (new `xlsx_text_preview`/`pdf_text_preview` via openpyxl/pypdf, reusing the existing `openxml_text_preview` for .docx/.pptx), recomputed on TAB3 add and after every attachment upload/delete, and always human-overridable via the table dropdown (a per-field `_source: "manual"` sibling then exempts it from further auto-recompute) — same auto-then-overridable pattern as the score columns.
- `detail.html`, `src/detail.js`, `src/styles.css`, `main.py`, `json/drug-valuation.schema.json`, `json/schema.md`: fixed Full Scout CDP/NCDP/ADMET material indicators so a reviewer click creates an explicit on/off override that takes precedence over filename auto-detection; delegated click handling, immediate visual feedback, and a cache-version bump make the controls reliably clickable and persistent.
- `main.py`, `src/app.js`, `src/detail.js`, `index.html`, `detail.html`, `src/styles.css`, `config/oi_partnership_criteria.md`, `json/drug-valuation.schema.json`, `json/schema.md`: implemented Tab3 OI Partnership v1.0 automatic classification from structured Full Scout data, explicit in-vivo/in-vitro outcomes, ADMET score, Platform Attractiveness, and uploaded evidence; added investment/Value Up/joint research/N-A/Unknown outcomes, overlap priority, notes and evidence-source storage, human override with Auto reset, and matching Tab3/detail guidance UI.
- `src/styles.css`: made the detail page's right-side "Review workspace" panel scroll as one continuous unit instead of only scrolling the middle `정성 평가 기준`/comment-thread strip — `.collaboration-panel` now owns the scroll (`overflow-y: auto`), and `.collaboration-scroll` no longer clips its own content, so the Filter2/Action date header and the comment composer scroll with everything else, giving the qualitative-review cards more visible room.
- `src/app.js`, `src/detail.js`, `detail.html`: made the detail page's Filter 2 (Decision/score) block and OI Partnership block mutually exclusive based on which dashboard tab the record was opened from, instead of always showing Filter 2 and showing OI Partnership whenever the record is favorited. Every navigation path to `/detail` now appends `&tab=full` or `&tab=focus` (table row clicks via `activeTableMode()`, and the dashboard's Priority Watch/Due Date Watch cards); `detail.js` reads it as `viewTab` and shows OI Partnership only when `viewTab === 'focus'` and the record is tracked, Filter 2 otherwise (so a stale focus-origin link on an untracked record still shows something). Action date/owner/plan, comments, and qualitative review stay unconditionally shared across both. In-page URL rewrites (record rename via AI edit, manual JSON save) now preserve the `tab` param via a new `detailUrlForCurrentRecord()` helper instead of dropping it.
- `src/app.js`: removed the redundant "Decision" (Filter 2 PASS/REVIEW/FAIL) column from the TAB3 (집중 관리) table — once an asset is watchlisted, Filter 3 (OI Partnership Type) is the relevant decision, not the already-settled Filter 2 status.
- `detail.html`, `src/detail.js`: scoped the detail page's "판단근거" drawer to match the record's context instead of always showing every criteria section — deleted the Fast-Triage-only sections entirely (this page never shows triage records), and now toggles between the Full Scout (PASS/REVIEW/FAIL + scoring table) section and the OI Partnership (Filter 3) section based on `meta.focus_management.is_tracked`, via `updateCriteriaDrawerScope()` called on drawer open. Same `data-criteria-tab` pattern as the dashboard drawer.
- `src/styles.css`: redesigned the "OI Partnership 자동 분류 v1.0" criteria card (`.oi-partnership-guide`) — bumped heading/body/card text sizes (12→17px for category labels, 11→14-15px for descriptions), widened the classification grid to 3 columns with a color-coded left border per outcome (투자/Value Up/공동연구/중복/Unknown/N/A), and boxed the footnote — so it reads at a glance without scrolling now that it's the only section shown in the Tab3 criteria scope.
- `src/styles.css`, `src/detail.js`: shrank the OI Partnership rationale textarea (`.oi-note-shell .review-reason-input`, `resizeOiPartnershipNoteInput()`) from 52px to 34px so it matches the height of the adjacent classification dropdown instead of towering over it; also added the missing `color: #b9616b` (the existing "manual override" rose-red already used for Filter 2/3 manual edits) to the shared `.review-reason-shell.is-human .review-reason-input` rule, so a manually-edited rationale note now shows in that color instead of looking identical to the auto-generated one — fixes both the OI Partnership note and Filter 2's rationale box, which shared the same incomplete rule.
- `src/detail.js`: changed the qualitative-review comment textareas' placeholder text from short generic hints to each criterion's actual title + subtitle (e.g. "Efficacy (효능 및 유효성)" / "% Reversal(정상군 대비 회복율) 및 SoC 대비 통계적 유의성(p-value) 있는 개선 우위 확인"), so the gray hint text shown before typing matches the header already shown above the entries instead of a separate shorter phrase; dropped the now-unused per-criterion `placeholder` field.
- `src/detail.js`: removed the barely-visible description `<p>` under each qualitative-review criterion's title (it was rendered too small to read), and dropped the title line from the comment textarea placeholder — placeholder now shows only the description text, matching what's actually meant to guide the reviewer.
- `src/detail.js`, `src/styles.css`: removed the "아직 의견이 없습니다" empty-state line from qualitative-review criteria (applies to all 3: Efficacy/Commercial/Risk, shared template) and the now-dead `.qualitative-empty`/`.qualitative-criterion header p` CSS rules left over from the earlier subtitle removal.
- `src/detail.js`, `detail.html`: switched the comment-author identity (`skbp.detail.commentAuthor`) from `localStorage` to `sessionStorage` — previously set-once-per-browser forever; now it's asked again each time the tab/browser is closed and reopened, while still persisting across reloads/navigation within the same session. Updated the identity modal's hint text to match.
- `src/detail.js`: dropped the "v1" from the qualitative-review panel heading ("정성 평가 기준 (임시 v1)" → "정성 평가 기준 (임시)") since that panel isn't part of the actual versioned-rubric system (unlike Fast Triage v3.1 / Full Scout v3.2 / OI Partnership v1.0, which each have a real backend version constant driving per-record tracking) — labeling it with a version number implied tracking that doesn't exist there.
- `src/detail.js`, `main.py`, `config/qualitative_review_criteria.md`: dropped the Korean parenthetical from the qualitative-review criterion titles ("Efficacy (효능 및 유효성)" → "Efficacy", "Commercial (파트너링 매력도 고려)" → "Commercial") and renamed "Risk (실행 및 개발관점)" to "Dev. & Partnership Risk"; updated all three synced copies per the file's own "keep in sync" convention.
- `detail.html`, `src/detail.js`, `src/styles.css`: reworked the detail page's review-workspace top section from a toggle (only Filter 2 OR only OI Partnership, based on which dashboard tab the record was opened from) into two side-by-side cards always shown together — "Full Scout" (Filter 2 dropdown + Total Score, now on the same line, + the 7 criterion chips wrapping below + rationale) on the left, "집중 관리" (Filter 3/OI Partnership + CDP/NCDP/ADMET material flags + Action Date, moved out of its own separate row into this card) on the right. The 집중 관리 card only renders when the record is actually tracked (`focus_management.is_tracked`); when it's hidden, the Full Scout card expands to the full panel width via a `:has()` selector instead of leaving blank space. Removed the now-dead `.action-info-row`-scoped CSS left over from moving Action Date.
- `src/detail.js`, `src/styles.css`: made the Filter 2 rationale box (`#detailReviewSummary`, left "Full Scout" card) auto-grow to fit its content (52px-160px) instead of staying fixed at 52px with an internal scrollbar — most rationale text now shows in full without needing to scroll, matching the more generously-sized boxes elsewhere in the panel. The OI Partnership note box stays fixed at 34px as before (unaffected, more specific rule).
- `detail.html`, `src/detail.js`, `src/styles.css`: made the "집중 관리" card's label symmetric with "Full Scout" — "OI partnership / Favorite only" → "Filter 3 / OI Partnership v{version}" (now dynamic, mirrors Filter 2's "Filter 2 / Rubric v{version}" pattern, showing "Human decision" when manually overridden). This also fixed the two cards' label rows landing at different heights (the old "OI partnership" text was long enough to behave differently from "Filter 2"). Added `align-content: center` to the Full Scout card so its content is vertically centered within the grid-stretched card height instead of top-anchored with dead space collecting at the bottom when its rationale is short.
