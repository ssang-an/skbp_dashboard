# 2026-09-09

## Changes

- Custom Review(BD/test2 등) Pipeline Table의 지표 컬럼(예: "유효 CoM(년)", "Direct 경쟁 약물(개)", "CMC Ready")이 자유 텍스트용 기본 폭(180px, `extra`)을 그대로 물려받아 OIC의 In-vivo/In-vitro/ADMET(56-84px)보다 눈에 띄게 넓던 문제 수정. `metric:<id>` 컬럼 폭을 해당 지표의 `return_type`(boolean/number/list/date/text)별 폭 테이블(`METRIC_COLUMN_TYPE_DEFAULT_WIDTHS`/`_MIN_WIDTHS`, `metricColumnTypeWidth()`)로 계산하도록 `defaultColumnWidth()`/`minColumnWidth()` 수정(`src/app.js`).
- Custom Review 지표·분류 셀에 OIC의 pass/review/fail/na 색상 팔레트를 재사용한 색상 코딩 추가. `classificationLabelTone()`(신설)이 분류 라벨 텍스트에서 "Tier 1/2/3" → 초록/노랑/빨강, "PASS/합격/승인/우수/적합" → 초록, "FAIL/불합격/반려/부적합/탈락" → 빨강 패턴을 감지하고, 그 외(자유 텍스트 분류, 미분류)는 연한 회색(`na`)으로 통일(`classificationToneClass()` 재작성). Boolean 지표(CMC Ready 등)는 Pass=초록/Fail=빨강, list 지표는 같은 라벨 패턴 감지, number/text/date 지표는 균일한 연한 회색으로 렌더링(`shortlistingMetricToneClass()`/`shortlistingMetricEditControl()` 신설·수정). CSS에 `.partnership-edit-select.pass/.fail/.review`, `.metric-value-edit.pass/.fail/.review/.na` 및 다크모드 대응 규칙 추가(`src/styles.css`).
- Pipeline Table 상단 "Filter 3"(구 "분류") 컬럼 헤더 라벨을 OIC의 기존 Filter 3 컬럼과 동일하게 통일. 검색바의 최상단 "Filter 3" 드롭다운·헤더 클릭 팝오버가 Custom Project를 봐도 항상 OIC 전용 옵션(투자/Value Up/공동연구/Unknown/N·A)만 보여주던 버그를 수정 — `activeStatusFilterOptions()`가 활성 Project의 `classification_columns`를 반환하도록 하고, `getVisibleRows()`의 실제 필터링도 `meta.shortlisting_projects.<id>.classification` 값을 읽도록 수정(이전에는 이 필터가 OIC 전용 `row.filter3` 필드만 비교해 Custom Project에서는 사실상 동작하지 않았음).
- Summary Dashboard(KPI·도넛차트·F/U Action)가 활성 Custom Project와 무관하게 서버가 미리 계산한 "shortlisting" 전역 집계(OIC 필드 기준)를 그대로 보여주던 문제 수정. `activeTabSummary()`가 기본 Project(OIC)가 아닐 때는 항상 `getVisibleRows()` 기반 클라이언트 재계산(`fallbackTabSummary`)을 쓰도록 하고, `fallbackTabSummary`의 focus 분기가 OIC 전용 `row.filter3` 대신 활성 Project의 `classification_columns`/`meta.shortlisting_projects.<id>.classification`을 읽어 분포·F/U Action 라벨을 계산하도록 재작성. "OI Partnership 분포" 도넛 패널은 Custom Project에서 "Filter 3 분포"로 제목이 바뀌고 그 Project 자신의 분류 라벨(Tier 1/2/3 등)을 범례로 표시하도록 수정(`renderCharts()`가 OIC 전용 4-키 고정 매핑 `partnershipSummaryDistribution()`을 OIC에서만 사용하도록 분기).
- (추가 수정) 위 Summary Dashboard 분리가 페이지 새로고침 시에는 정상 동작했지만, 상단 nav의 Project 전환 드롭다운(Open Innovation Center/BD/test2)으로 **같은 세션에서 프로젝트만 바꾸면 Summary Dashboard가 갱신되지 않던 실제 버그**를 발견해 수정. `setActiveShortlistingProjectId()`가 `renderTable()`만 호출하고 `renderCharts()`/`renderFilters()`는 호출하지 않아, Project를 전환해도 Pipeline Table만 바뀌고 Summary Dashboard(F/U Action·도넛차트·KPI)와 검색바의 Filter 3 드롭다운 옵션은 마지막으로 전체 렌더된 Project(보통 최초 로드 시 OIC) 상태 그대로 남아 있었음 — BD 페이지를 보면서도 F/U Action에 OIC 근거 라벨이 뜨던 원인. `renderFilters()`와 `renderFilteredDashboard()`를 호출하도록 수정하고, Project 전환 시 이전 Project의 Filter 3 선택값(`state.pass`, 예: OIC의 `investment`)이 새 Project에서 의미 없는 채로 남지 않도록 초기화(`src/app.js`). 추가로 `renderWorkflowPriorityList()`의 F/U Action 항목 라벨이 이미 올바르게 계산된 `item.partnership_label`보다 OIC 전용 `PARTNERSHIP_LABELS[item.partnership_type]`를 우선 조회해, Custom Project의 `partnership_type`이 우연히 `'unknown'`과 같은 OIC 키와 겹치면 그 프로젝트 고유 라벨 대신 OIC 라벨("Unknown")이 표시되던 우선순위 버그도 함께 수정. 브라우저에서 새로고침 없이 OIC→BD→OIC로 실시간 전환하며 레코드 수 배지(18건/4건)·F/U Action 라벨(Investment vs 미분류)·도넛 제목(OI Partnership 분포 vs Filter 3 분포)이 매번 즉시 올바르게 바뀌는 것을 `document.querySelector(...).click()` 기반으로 확인.
- (추가 수정) Pipeline Table에서 "Filter 3"/"유효 CoM(년)"/"Direct 경쟁 약물(개)"/"CMC Ready" 등 커스텀 컬럼 헤더 글자색이 검정에 가깝게 보여 옆 컬럼(Company/Filter 2/Total Score 등, 회색 `var(--muted)`)과 어긋나 보이던 문제 수정. 다른 헤더는 `sortableHeader()`가 만드는 `<button>`에 걸린 `th button { color: var(--muted) }` 규칙을 받는데, 이 커스텀 헤더들은 `plainHeader()`가 `<span>`으로 렌더링해 그 규칙을 못 받고 기본 텍스트색으로 보이던 것이 원인 — `.extra-column-head span`에 동일한 `color: var(--muted)`/`font-weight: 900`을 추가해 통일(`src/styles.css`). 브라우저에서 `getComputedStyle()`로 두 헤더 그룹의 색상값이 `rgb(102, 115, 134)`로 동일함을 확인.
- `src/app.js`/`src/styles.css` 변경에 맞춰 `index.html`/`detail.html`의 캐시버스팅 버전 쿼리스트링을 `20260909-filter3-colorcoding-1`로 갱신하고, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 2건 동기화. 브라우저에서 BD(Custom) 페이지와 Open Innovation Center 페이지를 라이트/다크 모드 각각으로 열어 Filter 3 드롭다운 옵션(Tier 1/2/3 vs 투자/Value Up/공동연구), 지표 컬럼 폭·색상, Summary Dashboard 패널이 올바르게 분리 표시됨을 확인(테스트로 지정한 분류 값은 되돌려 원상복구). `pytest tests/test_dashboard_ia.py -k "version or 20260908 or 20260909 or metric_input or classification"` 재실행 결과 신규 실패 0건(기존에도 무관한 사유로 실패하던 `test_review_workspace_uses_compact_version_refresh_pills_and_owner_meta` 1건만 동일하게 남음 — 템플릿 리터럴로 조립되는 토스트 문구를 리터럴 문자열로 검사하는 이번 변경과 무관한 기존 assertion).
- Custom Project(BD 등)의 `metric_columns`에는 없던 자동 영문 번역(`label_en`/`description_en`)을 `classification_columns`와 동일하게 생성 시점에 채우도록 `create_shortlisting_metric_column`을 수정(`main.py`). 또한 번역 기능이 생기기 전에 이미 만들어진 기존 지표/분류 항목(예: BD Project의 Tier 1/2/3, 유효 CoM(년) 등)에는 `label_en`이 아예 없어 판단근거 EN 토글 시 번역이 표시되지 않던 문제를 해결하기 위해, 공용 헬퍼 `retranslate_shortlisting_item()`과 소유자 전용 재번역 엔드포인트 `POST /api/shortlisting/projects/{project_id}/columns/{column_id}/translate`, `POST /api/shortlisting/projects/{project_id}/classifications/{classification_id}/translate`를 신설(`main.py`). Project 설정 모달의 지표 관리·분류 관리 표에 항목별 "번역"/"재번역" 버튼과 EN 미리보기를 추가해 위 엔드포인트를 호출하도록 배선(`src/app.js`).
- 판단근거(Criteria) 드로어의 Custom Project 섹션(`renderCriteriaDrawerCustomProjectContent`)이 그동안 `classification_columns`(분류 기준)만 보여주고 `metric_columns`(지표)는 전혀 노출하지 않던 것을 수정 — "Metrics/지표" 섹션을 "Classification/분류" 섹션 위에 추가해 두 섹션 모두 동일한 카드 UI(`.criteria-focus-info-card`)로 렌더링하고, `return_type`(boolean/list/number/date/text)에 맞는 값 힌트를 보여주는 `shortlistingMetricReturnTypeHint()` 헬퍼를 신설(`src/app.js`, `src/detail.js` 각각 동일 로직 중복 구현 — 두 페이지가 별도 스크립트라 공유 모듈 없음). 대시보드/상세페이지 모두 기존과 동일하게 현재 보고 있는 Project 컨텍스트에 맞는 지표·분류만 표시.
- 위 작업 중 발견한 CSS 버그 수정: 새로 추가한 "지표"/"분류" 섹션 제목에 아이콘 열을 가정하는 3열 그리드 클래스(`criteria-focus-section-heading`, `grid-template-columns: 24px 30px minmax(0,1fr)`)를 실제로는 자식이 2개(번호+텍스트)뿐인 마크업에 그대로 재사용해, 텍스트가 30px 폭의 좁은 열로 자동 배치되며 세로로 한 글자씩 줄바꿈되던 문제 수정 — 해당 클래스를 제거하고 2열 그리드 기본 클래스(`criteria-guide-section-heading`)만 사용하도록 변경(`src/app.js`, `src/detail.js`).
- `src/app.js`/`src/detail.js`/`src/styles.css` 변경에 맞춰 `index.html`의 `app.js?v=`를 `20260909-criteria-drawer-metrics-3`, `detail.html`의 `detail.js?v=`를 `20260909-criteria-drawer-metrics-2`로 갱신하고 `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 동기화. 브라우저에서 BD Project 판단근거 드로어를 열어 지표(유효 CoM/Direct 경쟁 약물/CMC Ready) 카드와 분류(Tier 1/2/3) 카드가 모두 정상적인 가로 텍스트로 렌더링되는지, KR/EN 토글 시 번역이 채워진 항목(CMC Ready, Tier 1/2/3)은 영문으로, 미번역 항목(유효 CoM/Direct 경쟁 약물)은 "No description provided." 플레이스홀더로 정상 대체(graceful degradation)되는지 스크린샷으로 확인.
- Custom Project(BD 등)의 Filter 3 검색바 드롭다운을 열면 "Available values"에 정상적으로 뜬 옵션(Tier 1/2/3)이 "Others" 그룹에도 그대로 중복되던 문제 수정. 원인은 이번 작업으로 새로 생긴 게 아니라 `multiFilterMenuMarkup()`의 기존 로직 — Theme/Modality처럼 `category-synonyms.json`에 등록된 필드는 "정식 값 목록(canonical)"과 "그 외 값"으로 나눠 보여주도록 설계돼 있는데, Filter 3(`pass`)처럼 애초에 그런 사전이 없는 키는 `canonicalFilterOrder()`가 빈 배열을 반환해 "정식 값 목록에 없음" 판정 기준(`canonicalValues`)이 빈 Set이 되고, 그 결과 모든 옵션이 "목록에 없다"고 판정되어 이미 "Available values"에 표시된 옵션이 "Others"에도 다시 나열됐던 것. `hasCanonicalLibrary`가 false인 경우 "Others" 목록을 아예 비우도록 수정(`src/app.js`). `index.html`의 `app.js?v=`를 `20260909-criteria-drawer-metrics-4`로 갱신. 브라우저에서 BD Project의 Filter 3 드롭다운 DOM(`#passFilterMenu`)을 직접 검사해 "Available values" 그룹 하나에만 Tier 1/2/3이 있고 "Others" 그룹은 더 이상 렌더링되지 않음을 확인.
- 판단근거 드로어의 Custom Project 섹션에서 "지표"(Metrics) 섹션이 "분류"(Classification) 섹션보다 먼저 보이던 순서를 요청에 따라 뒤바꿈 — "분류"를 1번, "지표"를 2번으로 재배치하고, 지표 섹션의 안내 문구도 "위 지표를 종합해"에서 "아래 지표를 종합해"로 맞춰 수정(`renderCriteriaDrawerCustomProjectContent()`, `src/app.js`·`src/detail.js` 동일 적용). `index.html`의 `app.js?v=`를 `20260909-criteria-drawer-metrics-5`, `detail.html`의 `detail.js?v=`를 `20260909-criteria-drawer-metrics-3`으로 갱신하고 `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 3건 동기화. `pytest tests/test_dashboard_ia.py -k "version or criteria"` 재실행 결과 신규 실패 0건(기존에도 무관한 사유로 실패하던 `test_review_workspace_uses_compact_version_refresh_pills_and_owner_meta` 1건만 동일하게 남음). 브라우저에서 BD Project 판단근거 드로어를 KR/EN 각각으로 열어 "1. 분류/Classification" → "2. 지표/Metrics" 순서로 정상 표시됨을 스크린샷으로 확인.
- 판단근거 드로어의 Custom Project "분류"/"지표" 섹션 사이 간격이 너무 좁아 카드가 바로 붙어 보이던 문제 수정. 원인은 OIC의 Filter 3 판단근거(Tab 3)에서 각 섹션에 박스형 테두리·배경·padding·그림자를 입히는 CSS 규칙(`.criteria-drawer-body .criteria-focus-section[data-criteria-tab="focus"]`)이 `data-criteria-tab="focus"` 속성 존재를 전제로 하는데, Custom Project 섹션은 그 속성 없이 렌더링돼 박스 스타일을 전혀 받지 못했고, 동시에 대시보드(`src/app.js`)는 이 섹션들을 `.criteria-drawer-body`(자체 `display:grid; gap:16px`로 카드 사이 간격을 주는 컨테이너)의 직계 자식이 아니라 그 안에 새로 끼워 넣은 래퍼 div(`#criteriaDrawerCustomProjectPanel`, 자체 gap 없음) 안에 렌더링해 간격 자체가 아예 적용되지 않았던 것. 두 섹션에 `data-criteria-tab="focus"`를 추가해 OIC와 동일한 박스(테두리·배경·그림자)를 입히고, `.criteria-custom-project-panel`에 `display:grid; gap:16px; --criteria-tab-accent:#7657c9`를 추가해 대시보드에서도 OIC와 같은 간격·강조색을 갖도록 통일(`src/app.js`, `src/detail.js`, `src/styles.css`) — 지표/분류 항목이 앞으로 추가·수정되어도 항상 이 공통 클래스/속성을 따르므로 별도 유지보수 없이 동일한 디자인이 유지됨. `index.html`의 `app.js?v=`를 `20260909-criteria-drawer-metrics-6`·`styles.css?v=`를 `-2`, `detail.html`의 `detail.js?v=`를 `-4`·`styles.css?v=`를 `-2`로 갱신하고 `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 5건 동기화. `pytest tests/test_dashboard_ia.py -k "version or criteria"` 재실행 결과 신규 실패 0건(기존과 동일하게 무관한 `test_review_workspace_uses_compact_version_refresh_pills_and_owner_meta` 1건만 남음). 브라우저에서 대시보드(BD Project)와 상세페이지(`/detail?id=...&tab=focus`, Custom 패널에서 BD 선택) 양쪽 모두 판단근거 드로어를 열어 "분류"·"지표" 섹션이 OIC 페이지와 동일하게 카드형 박스와 자연스러운 간격으로 표시됨을 스크린샷으로 확인.
- Step 0 진척현황의 Main indication Canonical Library 정비. `json/candidate-queue.json`(1,349건) + `json/pipeline-records.json`(182건)의 `listing_details.main_indication` 원문 176종(633회 노출)을 실제 `main.canonicalize_dictionary_category('indication', ...)` 매칭 로직으로 전수 재현해 64종(164회)이 미매칭 상태임을 확인, 이 중 반복 출현하며 단일 질환을 명확히 지칭하는 항목만 선별해 `config/category-synonyms.json`(v1.4→v1.5) `indication` 배열에 반영: **신규 canonical 4개** `Vascular dementia`("Dementia, cerebrovascular" 8건 등), `Progressive supranuclear palsy / atypical parkinsonism`(Tauopathies/PSP 계열 ~6건), `Charcot-Marie-Tooth disease / hereditary neuropathy`(CMT/CMT1/CMT2C 계열 5건), `Ataxia (spinocerebellar / Friedreich)`(3건) 및 **기존 canonical에 synonym 추가** `Alzheimer's disease`+"Mild cognitive impairment"(일반형, "MCI due to AD"는 기존에 이미 등록돼 있었음), `Parkinson's disease`+"Morning akinesia", `Multiple sclerosis / neuroinflammatory disease`+"Neuromyelitis optica"/"NMO", `Stroke`+"Subarachnoid hemorrhage"/"Brain hemorrhage". **의도적으로 승격하지 않은 것**: "Other neurological indications"(40건, 단일 질환이 아닌 원본 템플릿의 기타 placeholder), "Neurodegenerative Diseases"(14건), "Unspecified Central Nervous System Disorders"(10건) 등 상위 카테고리성 placeholder 문구 — `docs/listing-import-guide.md`에 이미 명시된 승격 기준("동일 원문이 서로 다른 3개 이상 Pipeline에서 확인되거나 운영상 별도 집계가 필요할 때만 검토")에 따라 특정 질환을 지칭하지 않는 값은 일괄 매핑 대상이 아니라 자산별 원문 재확인이 필요한 개별 검토 대상으로 남겨둠; `Alcohol Dependence`/`Dystonia`/`Anosmia`/`Traumatic Brain Injury`처럼 1건뿐인 항목도 3건 임계값 미달로 보류(캐노니컬 없음 확인, 향후 재등장 시 재검토). `docs/listing-import-guide.md`에 `Others`/원문표시/`Unknown`/hover 원문의 정확한 노출 범위를 명문화하는 문단 추가: `Others`라는 문자열 자체는 Summary Dashboard 집계 차트에서만 쓰이는 라벨이고(6대 우선 적응증 외 전부를 하나로 합산), Pipeline Table 셀·Tab 0 표·상세페이지·wiki/Obsidian 마크다운 export에는 이 문자열이 노출되지 않으며 항상 (a)canonical 대표값 (b)미매칭 시 원문 그대로 (c)원문 없을 때만 `Unknown` 중 하나가 보이고, canonical 대표값이 보이는 셀도 hover 시 항상 원문이 함께 노출됨을 코드(`indicationDisplay`/`canonicalDisplayWithRawFallback`/`indicationFullHoverTitle`, `scripts/export_pipeline_wiki.py`의 raw `indication` 사용, `src/app.js`의 "적응증 6개 · Others" 차트 라벨)로 직접 확인 후 반영. `pytest tests/test_rubric_v32_v33.py -k Canonicalization`(8 passed) 및 `pytest tests/`(신규 실패 0건, 기존 무관 실패와 동일 목록 — `test_canonical_fields.py`의 modality 병합 관련 실패 1건은 이번 indication 변경과 무관함을 격리 실행으로 확인) 통과.
- Custom Review 탭 상단 "3차 집중 관리" 안내문("즐겨찾기로 등록한 Advanced Research 후보의 OI Partnership Type과 후속 Action을 관리합니다.")이 OIC뿐 아니라 모든 Custom Project(BD/test2 등)에서 그대로 노출되던 문제 수정 — "OI Partnership Type"은 OIC 전용 개념이라 다른 Project에서는 부정확한 문구였음. `WORKFLOW_COPY.focus`에 Project-무관 문구인 `descriptionCustomProject`("즐겨찾기로 등록한 Advanced Research 후보의 팀 내 추가 고려 사항, 추가 가산점 및 후속 Action을 기록할 수 있습니다.")를 추가하고, `renderWorkflowMode()`가 `state.activeShortlistingProjectId`가 OIC(`DEFAULT_SHORTLISTING_PROJECT_ID`)가 아닐 때는 이 문구를 쓰도록 분기(`src/app.js`) — 오늘 앞선 작업에서 Summary Dashboard 제목("OI Partnership 분포" vs "Filter 3 분포")에 이미 적용한 것과 동일한 패턴. `index.html`의 `app.js?v=`를 `20260909-focus-description-custom-1`로 갱신, `tests/test_dashboard_ia.py`의 `test_workflow_description_is_a_connected_information_panel`이 옛 `escapeHtml(copy.description)` 리터럴을 검사하던 것을 새 지역 변수 `description`과 `copy.descriptionCustomProject` 존재 여부로 갱신. 브라우저에서 BD Project(`#workflowModeDescription`)로는 새 문구가, Open Innovation Center로 전환하면 기존 OI Partnership Type 문구가 즉시 표시됨을 확인. `pytest tests/test_dashboard_ia.py -k "version or workflow"` 재실행 결과 신규 실패 0건(기존 무관 실패 2건만 동일하게 남음).
