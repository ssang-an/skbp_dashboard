# 2026-09-10

## Changes

- (되돌림) 이전에 상단 헤더 "판단근거" 버튼(`#criteriaDrawerButton`)의 "?" 아이콘 스팬을 제거했던 것을 원복 — 요청 의도는 아이콘만 지우는 게 아니라 Tab 0(진척 현황)에서 버튼 자체를 없애는 것이었음(`index.html`).
- Tab 0(진척 현황)에서는 "판단근거" 버튼 전체를 숨김 처리해 클릭 자체가 불가능하도록 수정. 모든 탭 전환 경로가 공통으로 거치는 `syncTopDataActionsForVisibleTab()`에 `elements.criteriaDrawerButton.hidden = isStep0Visible` 분기를 추가하고, 이 함수보다 먼저 return하는 Knowledge Map 분기에도 동일하게 버튼을 다시 보이도록 추가해 Tab 0 → 다른 탭/Wiki Map 전환 시 버튼이 정상적으로 다시 나타나도록 함(`src/app.js`). Tab 0은 아직 스코어링 이전 단계라 SKBP 판단기준을 보여줄 대상이 없다는 점에서 아이콘 제거가 아닌 버튼 전체 숨김이 맞는 처리.
- `index.html`의 `app.js?v=`를 `20260910-hide-criteria-button-step0-1`로 갱신.
- 검증: `pytest tests/test_dashboard_ia.py` 전체 실행 결과 45 failed/119 passed로, 변경 전(같은 파일들을 `git stash`로 되돌린 상태) 베이스라인과 정확히 동일한 실패 수임을 확인 — 이번 변경으로 인한 신규 실패 없음(45건은 모두 이번 변경과 무관한 기존 실패, 예: `step0FilteredSortedRows`의 `searchTerms.some` 관련 오래된 assertion). Chrome 확장이 이 세션에 연결되지 않아 브라우저 클릭 확인은 하지 못했고, 로컬에 이미 떠 있던 dev 서버(:8000)에서 curl로 반영 상태만 확인함.
- Custom page(Shortlisting) 상단 프로젝트 전환 메뉴의 프로젝트 설정 톱니바퀴 버튼을 눈에 잘 안 띈다는 피드백에 따라 유니코드 `⚙` 대신 더 굵은 선의 SVG 기어 아이콘으로 교체하고, 상시 옅은 테두리/배경을 줘 hover 전에도 클릭 가능한 버튼임이 드러나도록 함(`src/app.js`의 `renderShortlistingProjectControl()`, `src/styles.css`의 `.shortlisting-project-settings-button`). hover 시 뜨는 title/aria-label 텍스트를 "Project 권한, 지표, 분류 기준 수정"으로 변경. 코드 확인 결과 이 버튼은 기존부터 `project.current_user_role === 'owner'`일 때만 렌더링되어 owner에게만 노출되는 것을 재확인(신규 게이팅 로직 추가는 아님).
- `json/shortlisting-projects.json`에 멤버 추가: Open Innovation Center(`oic_default`)에 신해인(write)을 `haein.shin@sk.com`/`haein.shin@skbp.com` 두 도메인 계정 모두로 추가, BD(`proj_08b16e1526`)에 이인규·송승우·조용민·나경은을 owner로 각각 `@sk.com`/`@skbp.com` 두 도메인 계정으로 추가 — 프로젝트 멤버 매칭이 이메일 문자열 완전일치(`shortlisting_project_role_for_account` in `main.py`)라서 두 도메인 어느 쪽으로 로그인해도 동일 권한이 인식되도록 기존 `INITIAL_ADMIN_IDENTITIES`(같은 사람에 sk.com/skbp.com 두 항목)와 동일한 패턴을 적용함. 각 프로젝트의 `updated_at`도 함께 갱신.
- `index.html`의 `app.js?v=`/`styles.css?v=` 캐시버스터를 `20260910-project-settings-gear-1`로 갱신.
- 검증: `python -c "import json; json.load(open('json/shortlisting-projects.json', encoding='utf-8'))"`로 JSON 유효성 확인, 갱신된 멤버 목록을 출력해 이메일/역할이 의도대로 반영됐는지 확인. 이번 세션에 Chrome 확장이 연결되지 않아 브라우저에서 실제 hover/버튼 렌더링 확인은 하지 못함.
- 헤더의 "PIPELINE 업로드" 버튼(`#dataUploadShortcutButton`) 라벨 왼쪽에 있던 원형 아이콘 배지(`<span class="data-utility-icon">` + 업로드 SVG, 22px 원형 배경)를 요청에 따라 제거 — 텍스트("PIPELINE 업로드"/축약형 "업로드")만 남김(`index.html`). 해당 스팬을 참조하는 JS 셀렉터가 없음을 `src/app.js`에서 확인. curl로 서빙된 페이지에 반영됨을 확인.
- 상세페이지 Team Review Workspace의 Custom(Shortlisting) 패널에서, OIC가 아닌 Custom Project(예: BD)를 볼 때 그 Project의 `classification_columns`(Tier 1/2/3 등)가 설명 카드로 나열만 되고 실제로 이 asset이 어느 분류에 해당하는지 고르는 dropdown이 없던 문제 수정 — Pipeline Table(대시보드)의 Filter 3 dropdown(OIC의 투자/Value Up/공동연구 등)과 동일한 패턴으로, "미분류" + 각 분류 라벨을 보여주는 `<select class="partnership-edit-select classification-edit-select">`를 추가(`customProjectClassificationSummaryMarkup()`, `src/detail.js`). 현재 값은 `record.meta.shortlisting_projects.<projectId>.classification`에서 읽고(`customProjectClassificationValue()`, 비어있으면 "미분류"), 변경 시 대시보드와 동일한 `PATCH /api/records/{id}/shortlisting-projects/{project_id}` (`field: "classification"`)를 호출해 저장(`saveDetailClassificationSelect()` → 기존 `saveDetailShortlistingProjectField()` 재사용, 백엔드 변경 없음). Tier 1/2/3, pass/fail류 라벨의 초록/노랑/빨강 톤 컬러링도 대시보드의 `classificationLabelTone()`/`classificationToneClass()` 로직을 `src/detail.js`에 그대로 포팅해 동일하게 적용. 레이아웃은 OIC Filter 3 select와 같은 `.oi-filter3-control-row` 구조를 재사용하고, 고정폭 규칙만 `.classification-edit-select`용으로 추가(`src/styles.css`, 520px 이하 반응형 규칙 포함).
- `index.html`/`detail.html`의 `styles.css?v=`/`detail.js?v=`를 `20260910-custom-classification-dropdown-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 5건 동기화.
- 검증: `pytest tests/test_shortlisting_project_permissions.py`(32 passed, 백엔드 classification 필드 검증·권한 로직은 이번에 손대지 않았음을 재확인) 및 `pytest tests/test_dashboard_ia.py` 전체(45 failed/119 passed, 이번 변경 전과 정확히 동일한 실패 수 — 버전 문자열 동기화로 신규 실패 없음) 통과. `node --check src/detail.js`로 구문 오류 없음 확인. `json/pipeline-records.json`에서 BD Project(`proj_08b16e1526`)에 추적 중이면서 `classification: ""`(미분류)로 저장된 실제 레코드(Shaperon_HY209_NuCerin_20260622)를 찾아 API 응답 구조(`record.meta.shortlisting_projects.proj_08b16e1526.classification`)가 새 코드가 읽는 경로와 일치함을 curl로 확인. curl로 서빙된 `detail.js`에 새 함수·클래스가 반영됨을 확인. Chrome 확장이 이 세션에 연결되지 않아 실제 dropdown 클릭·저장 동작은 브라우저에서 확인하지 못함 — 실사용 전 한 번은 화면에서 선택 후 저장/새로고침 유지 여부를 확인해볼 것을 권장.
- 프로젝트 설정 톱니바퀴 버튼이 "너무 튄다"는 피드백에 따라 상시 테두리/배경(청록 틴트)을 제거하고 원래의 무배경·연회색(`var(--muted)`) 스타일로 되돌림. 다만 아이콘 자체는 이전 유니코드 `⚙` 대신 이번에 넣은 굵은 선 SVG 기어 모양을 그대로 유지 — 배경 없이도 형태가 또렷해 가독성 목적은 유지하면서 튀지 않도록 함(`src/styles.css`의 `.shortlisting-project-settings-button`).
- `index.html`의 `styles.css?v=`를 `20260910-project-settings-gear-2`로 갱신.
- 검증: 관련 버전 문자열 assertion이 `tests/`에 없음을 grep으로 확인. Chrome에서 Custom 탭 프로젝트 전환 드롭다운을 열어 Open Innovation Center/BD/test2 세 항목 모두 배경·테두리 없는 연회색 기어 아이콘으로 렌더링됨을 스크린샷으로 직접 확인.
- GPT Instruction 2(Full Scout v3.8, `buildGptInstructionPromptLegacy()`, `src/app.js`)에 요청받은 최소 변경만 추가: "## 2) Pipeline Snapshot"과 "## 3) Scorecard Summary" 사이에 새 Markdown 전용 섹션 "## 2A) IP & Launch Outlook"(Core Patent/CoM, Earliest Priority Date, Expected Patent Expiry/PTE, Expected Launch Year, Patent Runway at Launch 5행 표)을 삽입하고, 이를 뒷받침하는 최소한의 조사 규칙("IP & Launch Outlook investigation rules (Section 2A)")을 기존 "Investigation-note requirements" 블록 바로 뒤·"Marketability method and score" 블록 바로 앞에 추가. Compact v2 JSON 스키마·7개 채점 기준·Hard Filter·구분선(`--- JSON DATA ---`)·기존 섹션 번호(3~6, References)는 전혀 건드리지 않음 — 새 섹션은 그 구분선보다 한참 앞, 순수 Markdown 영역에만 존재. 좌측 Table of Contents(`renderDetailOutline()`, `src/detail.js`)는 렌더링된 Markdown의 h1/h2/h3를 그대로 순서대로 읽어 자동 생성되는 방식이라 코드 변경 없이 새 섹션이 "2) Pipeline Snapshot" 바로 아래에 자동으로 나타남.
- `index.html`의 `app.js?v=`를 `20260910-full-scout-ip-launch-outlook-1`로 갱신(detail.js/styles.css는 이 변경과 무관해 그대로 둠).
- 검증: `node --input-type=module`로 `buildGptInstructionPrompt()`를 실제 실행해 최종 렌더 결과(모든 `${SHARED_...}` 치환·Compact JSON 템플릿 적용 후)를 확인 — "## 2A) IP & Launch Outlook"이 "## 2)"와 "## 3)" 사이, `--- JSON DATA ---` 구분선보다 훨씬 앞에 위치함을 라인 번호로 확인. "2A"/"IP & Launch Outlook" 문자열이 새 헤딩 1회·조사 규칙 1회, 총 의도한 2회만 등장하고 중복이 없음을 grep으로 확인. `pytest tests/test_compact_ingestion.py tests/test_rubric_v32_v33.py`(4 failed/94 passed) 실행 결과가 변경 전 `git stash` 베이스라인과 동일한 4건(모두 이번 변경과 무관한 기존 실패: modality canonicalization 테스트 2건, parameter guide 테스트 2건)임을 확인 — 신규 실패 없음. 렌더링된 전체 프롬프트 텍스트를 파일로 사용자에게 전달.
- Custom Review Project(BD 등)의 Project 설정 → 지표 관리/분류 관리 표에 삭제·번역만 있고 수정이 없던 문제 수정. 각 행에 "수정" 버튼을 추가(`renderShortlistingMetricManagerTable`/`renderShortlistingClassificationManagerTable`, `src/app.js`) — 클릭하면 기존 추가-폼(라벨/설명/입력 타입/옵션/최대값)에 현재 값이 채워지고 저장 버튼 라벨이 "지표(분류) 수정 저장"으로 바뀌며 새 "취소" 링크(`#shortlistingMetric(Classification)ModalEditCancel`)가 나타남; 저장 시 `state.editingMetricColumnId`/`editingClassificationId`가 세팅돼 있으면 기존 `POST` 대신 신규 `PATCH /api/shortlisting/projects/{project_id}/columns/{column_id}` / `PATCH .../classifications/{classification_id}`를 호출(`main.py`, owner 권한·이름 60자/설명 400자·return_type/list-options/number-max_value 검증, 자기 자신을 제외한 중복 이름 검사는 create와 동일 로직 재사용, 기본 제공(builtin_*/OIC 하드코딩) 항목은 여전히 수정 불가). 라벨/설명이 실제로 바뀐 경우에만 기존 `label_en`/`description_en`을 비우고 `call_openrouter_translate_ko_to_en()`으로 재번역을 시도(내용이 그대로면 기존 번역 유지, API 키 없거나 실패해도 저장 자체는 성공 — 이 경우 "번역"/"재번역" 버튼으로 수동 재시도 가능). 모달을 닫거나(`closeShortlistingProjectSettingsModal`) 취소를 누르면 편집 상태가 초기화되어 다음에 열 때 "추가" 모드로 돌아옴. OIC를 포함한 모든 Project가 이 공용 모달 하나를 재사용하므로 별도 분기 없이 모든 Custom Project에 동일하게 적용됨.
- (질문에 대한 답, 코드 변경 없음) "번역" 버튼을 눌러야 판단근거 EN이 갱신되는지 확인 — 맞다. 지표/분류 생성 시 `OPENROUTER_API_KEY`가 설정돼 있으면 자동으로 1회 번역을 시도하지만, 그 시점에 키가 없었거나 실패한 기존 항목은 `label_en`이 빈 값으로 남아 판단근거 EN 토글에서 한국어로 대체 표시됨 — "번역"/"재번역" 버튼(`translateShortlistingMetricColumn`/`translateShortlistingClassification`)을 누르면 즉시 `POST .../translate`를 호출해 `label_en`/`description_en`을 채우고, 응답으로 받은 최신 project 객체를 `applyShortlistingProjectUpdate()`가 `state.shortlistingProjects`에 그대로 반영하므로 새로고침 없이 다음에 판단근거 드로어를 열면 바로 영문이 보임(페이지를 새로고침해야만 반영되는 게 아님).
- `index.html`의 `app.js?v=`를 `20260910-shortlisting-metric-edit-1`로 갱신.
- 검증: `py_compile main.py`/`node --check src/app.js` 통과. `unittest.mock.patch.object`로 격리한 직접 함수 호출로 (a) 정상 수정 시 필드·`max_value`가 갱신되고 재번역이 반영됨, (b) write 권한 계정은 403, (c) 자기 자신을 제외한 이름 중복은 409, (d) `builtin_*` 항목 수정 시도는 400, (e) 라벨/설명이 그대로면 재번역이 호출되지 않고 기존 `label_en`이 유지됨을 각각 확인. `pytest tests/` 전체 재실행 결과 신규 실패 0건(기존 무관 실패 65건과 동일 목록, `test_shortlisting_*` 관련 실패 2건도 이번 변경 이전부터 있던 무관 사유임을 격리 실행으로 재확인). 이번 세션엔 Chrome 확장이 연결되지 않아 브라우저 클릭 확인은 시도했으나(headless browser-automation 스킬) 앱의 로그인 벽 때문에 실제 화면 클릭 확인은 하지 못함 — 백엔드 API·프론트 렌더/상태 전이 로직만 코드 레벨로 검증했으므로, 실사용 전 한 번은 화면에서 지표/분류 수정 → 저장 → 판단근거 반영까지 직접 확인해볼 것을 권장.
- (되돌림) 상단 헤더 "PIPELINE 업로드" 버튼(`#dataUploadShortcutButton`)의 아이콘을 앞서 제거했던 것을 원복 — 아이콘 자체가 불필요한 게 아니라 tab1/tab2에서도 보이던 정상 표시였다는 피드백에 따라, 라벨 왼쪽에 원래 있던 업로드 화살표 SVG 아이콘 배지(`<span class="data-utility-icon">`)를 그대로 복원(`index.html`). index.html 마크업만 되돌린 것이라 `app.js`/`styles.css` 내용은 변경되지 않아 캐시버스팅 버전은 갱신하지 않음. curl로 서빙된 페이지에 아이콘이 다시 나타남을 확인.
- Project 설정 모달의 지표 관리/분류 관리 표에서 새로 추가된 "수정" 버튼이 옆의 "삭제"/"번역" 버튼과 스타일이 달라(CSS 규칙이 아예 없어 브라우저 기본 버튼으로 렌더링) 통일감이 없다는 피드백에 따라 수정. `.shortlisting-metric-manager-edit-button`/`-translate-button`/`-delete-button` 세 클래스에 공통 pill 모양(`border-radius: 999px`, 작은 패딩/폰트, `inline-flex` 정렬)을 주는 공유 셀렉터를 추가하고, 각 클래스는 색상만 다르게 유지(수정=`var(--blue)`, 번역/재번역=`var(--accent)`, 삭제=`var(--red)`, hover 시 옅은 배경 틴트는 기존과 동일)(`src/styles.css`). 마크업(`src/app.js`)은 변경하지 않고 기존 클래스명을 그대로 재사용했으므로, 같은 `.shortlisting-metric-manager-delete-button` 클래스를 쓰는 구성원 관리(멤버) 표의 "삭제" 버튼도 자동으로 동일한 pill 스타일을 적용받음 — OIC를 포함한 모든 Project가 이 공용 모달을 재사용하므로 전 Project에 동일하게 적용됨.
- `index.html`의 `styles.css?v=`를 `20260910-shortlisting-row-action-pill-1`로 갱신.
- 검증: `pytest tests/` 전체 실행 결과 65 failed/485 passed로 이번 세션에서 확립한 기존 무관 실패 베이스라인(약 63-65건)과 일치 — 신규 실패 없음. 변경한 버튼 클래스명을 `tests/`에서 grep했으나 하드코딩된 assertion 없음을 확인. 이번 세션에도 Chrome 확장이 연결되지 않아 브라우저 클릭/렌더링 확인은 하지 못함 — 실사용 전 한 번은 화면에서 지표/분류/구성원 표의 버튼 모양을 직접 확인해볼 것을 권장.
- 상세페이지 Custom 카드(Team Review Workspace) 3건 정리, 모두 브라우저로 직접 확인:
  1) BD 등 Custom Project에서 Classification(Tier 1/2/3) 전체 서술이 "Hide" 상태에서도 계속 보이던 문제 수정 — OIC의 `.oi-classification-summary`(OI Partnership 근거 textarea)는 `.review-info-stack.is-collapsed` 시 숨겨지는데, 이전 세션에서 추가한 `.custom-classification-summary`(Tier 목록)에는 동일 규칙이 빠져 있었음. `.review-info-stack.is-collapsed .custom-classification-summary { display: none; }`를 같은 규칙 블록에 추가(`src/styles.css`). "Show" 상태에서는 기존대로 전체 서술이 계속 보임.
  2) Custom Project의 "미분류" Classification select 너비를 OIC Filter 3 select와 동일한 126px로 통일(`src/styles.css`의 `.oi-filter3-control-row .classification-edit-select`, 기존 160px) — 두 select는 이미 같은 `.oi-filter3-control-row` flex 구조를 공유해 같은 행·같은 높이였고 너비만 달랐음.
  3) OIC 카드에는 DD/In-vivo/In-vitro/ADMET/D·Link 5개 builtin 기준이 Custom Project의 "Criteria Scores" pill 행처럼 표시되지 않던 문제 — `detail.html`에 `#detailOicMetricPills`(`.filter-score-panel`) 컨테이너를 Filter 3 행과 OI Partnership 근거 textarea 사이에 추가하고, `renderOicMetricPills()`(`src/detail.js`)가 `record.meta.focus_management`(in_vivo_status/in_vitro_status/admet_completed/disease_linkage_status)와 `hasDueDiligenceAttachment()` 로직을 포팅한 `oicDueDiligenceStatus()`로 값을 읽어 BD와 동일한 `score-chip` pill 스타일로 렌더링. `renderCustomProjectPanel()`의 `isDefault` 분기에서 매 렌더마다 호출.
  - 새 Project 생성 시 공통 규칙이 잘 적용되는지 확인: `renderCustomProjectPanel()`은 project id가 `oic_default`가 아니면 전부 동일한 `else` 분기(`renderCustomMetricTable()`/`customProjectClassificationSummaryMarkup()`)를 타므로, test2를 포함한 어떤 신규 Project도 코드 수정 없이 BD와 동일한 Classification select + Tier 서술 + Criteria Scores pill 패턴을 자동으로 받는 구조였음(OIC만 레거시 하드코딩 카드라 예외였고, 이번에 pill 추가로 그 격차를 메움) — 정상 동작 확인.
  - `detail.html`의 `styles.css?v=`/`detail.js?v=`를 `20260910-oic-criteria-pills-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 5건 동기화.
  - 검증: `pytest tests/test_dashboard_ia.py`(45 failed/119 passed, 기존 베이스라인과 동일) 및 `pytest tests/test_shortlisting_project_permissions.py`(32 passed) 통과. Chrome으로 실제 레코드(Shaperon_HY209_NuCerin_20260622, BD·OIC 양쪽 실데이터 보유)를 열어 JS로 DOM을 직접 조회 — collapsed 상태에서 `.custom-classification-summary`의 `getComputedStyle().display`가 `none`→`grid`로 토글됨을 확인, BD/OIC select의 `getBoundingClientRect()`가 동일 행·동일 높이·동일 우측 정렬임을 확인, OIC pill 행이 실제 DD/In-vivo/In-vitro/ADMET/D·Link 값을 정확히 렌더링함을 확인. 스크린샷으로 Show/Hide 토글 시 패널 높이가 OIC·BD 양쪽 모두 비슷하게 컴팩트함을 육안 확인.
- OIC Criteria Scores pill에서 DD 제거 — Pipeline Table(대시보드) 헤더에도 DD 컬럼이 없음을 `focusFilterHeader()` 호출 목록(`src/app.js`, In-vivo/In-vitro/ADMET/D·Link만 존재)으로 확인하고 요청대로 detail 페이지 pill과 대시보드 컬럼 구성을 일치시킴. `renderOicMetricPills()`에서 DD pill과 이를 위해서만 쓰이던 `oicDueDiligenceStatus()` 헬퍼를 제거(`src/detail.js`). 나머지 In-vivo/In-vitro/ADMET/D·Link 4개 pill은 그대로 유지.
- `detail.html`의 `detail.js?v=`를 `20260910-oic-criteria-pills-2`로 갱신(styles.css는 이번 변경과 무관해 그대로 둠), `tests/test_dashboard_ia.py`의 관련 `detail.js?v=` assertion 3건 동기화.
- 검증: Chrome에서 실제 레코드(Shaperon_HY209_NuCerin_20260622)의 OIC 카드를 열어 `#detailOicMetricPills`의 텍스트가 DD 없이 In-vivo/In-vitro/ADMET/D·Link만 나오는 것을 JS로 직접 확인. `node --check src/detail.js` 통과. `pytest tests/test_dashboard_ia.py`(45 failed/119 passed, 기존 베이스라인과 동일 — 신규 실패 없음).
