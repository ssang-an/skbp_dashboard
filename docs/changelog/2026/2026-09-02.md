# 2026-09-02

## Changes

- Source-hover wording extension: all pipeline-table source hovers now explicitly use the `… 원문:` label. Main indication shows `Indication 전체 원문:`, and Modality plus Pipeline Stage now show their own raw stored values with `Modality 원문:` and `Pipeline Stage 원문:` respectively. This is applied consistently to Tabs 0–3 where the linked Full Scout source is available (`src/app.js`, `index.html`, `tests/test_dashboard_ia.py`).

- Main indication hover clarity: Tab 0 Listing, Tab 1 Fast Triage, Tab 2 Full Scout, and Tab 3 Shortlisting tables continue to show one concise canonical Main indication, while hovering it now shows `Indication 전체:` followed by the full stored source wording. This makes a secondary-indication filter match understandable without widening table columns; administrators see the same source text together with their edit guidance (`src/app.js`, `index.html`, `tests/test_dashboard_ia.py`).

- Listing Comment import history: re-importing a recognized Pipeline now retains every distinct Tab 0 Comment as its own timestamped `일괄 업로드: Tab 0 · Comment` card rather than collapsing new text into one display card. Exact comments (ignoring cosmetic whitespace/case) are not added twice; the legacy aggregate remains for compatibility, and the corresponding Team Review system posts stay separated by import event (`main.py`, `src/app.js`, `json/drug-valuation.schema.json`, `json/schema.md`, `tests/test_step0_pipeline_metadata.py`).

- Exact Listing identity display precedence: when both Asset and Company identify the same Pipeline, the newest non-missing Listing Target, indication, stage, modality, location, and website now become the Tab 0 display overlay; prior Listing and official research values fill only blank/Unknown/N/A incoming fields. This cannot overwrite Fast Triage/Full Scout scientific data, reports, or scores (`main.py`, `json/drug-valuation.schema.json`, `json/schema.md`, `tests/test_step0_pipeline_metadata.py`).

- Manual score synchronization: a reviewer’s criterion-score dropdown change now persists the human override and has the backend derive the effective Total Score and Filter 1/2 status from all effective criterion scores; the Dashboard/Filter 1 detail confirms the synchronized fields in the existing three-second toast and records each derived update in history. Total Score is now display-only, so it cannot conflict with the criterion sum. Filter 3 remains its own OI criteria classification, but tracked Full Scout records re-run that classifier when the manual Platform Attractiveness score changes (`main.py`, `src/app.js`, `src/triage-detail.js`, `tests/test_rubric_ai_refresh.py`).

- Provenance wording: Fast Triage now uses `원문 기반 마지막 재평가` only for original-report reassessment provenance, rather than presenting historical stored-score calculations as a report re-evaluation. Full Scout now shows the same explicit label in the Team Review Filter 2 origin when that provenance exists; otherwise it retains the neutral score-basis label (`src/triage-detail.js`, `src/detail.js`, `tests/test_rubric_ai_refresh.py`, `tests/test_dashboard_ia.py`).

- Original-report refresh contract: made every public Filter 1/2 refresh URL, including the legacy `recalculate-rubric` endpoint, delegate to stored-original-report reassessment. A public refresh now re-evaluates eligible criterion scores against the active rubric, recomputes the Total and Filter result, and records its provenance/history; it cannot expose stored-score-only recalculation. Retained the prior stored-score function as a private data-repair helper only, removed the Codex-invoked OpenRouter batch runner so provider calls remain limited to an interactive dashboard reassessment, and documented this release gate (`main.py`, `docs/rubric-release-maintenance.md`, `tests/test_rubric_ai_refresh.py`).

- Original-report reassessment batch utility — added an explicit, dry-run-by-default sequential runner for the same OpenRouter reassessment used by Filter 1/2 controls. It selects the current dashboard surface (omitting Fast Triage identities superseded by Full Scout), skips only records with a same-version AI reassessment, and supports bounded execution with `--execute --limit`; a partial 2026-09-02 run completed four Full Scout assessments before being stopped for long per-request latency, leaving the remaining records untouched for a controlled resume (`scripts/batch_reassess_latest_rubrics.py`, `json/pipeline-records.json`).

- Interactive score refresh behavior — Tab 1/2 dashboard-row and detail controls now call the original-report OpenRouter reassessment route for stale Fast Triage or Full Scout evaluations. It re-evaluates eligible criterion scores, recomputes Total and Filter 1/2, and records the audit while preserving the raw report. A prior deterministic stored-score refresh no longer suppresses that AI reassessment; only a same-version AI reassessment does. Current records avoid a duplicate model call and only restore an active manual override when needed (`main.py`, `src/app.js`, `src/detail.js`, `src/triage-detail.js`, `tests/test_rubric_ai_refresh.py`, `tests/test_dashboard_ia.py`).

- Fast Triage provenance display — the right-hand Quick Summary now separates the preserved original research (`GPT instruction` version plus generation date) from the current score basis and the latest deterministic recalculation, AI score re-evaluation, or rubric review (version plus time). This applies uniformly to every existing and future Fast Triage record without rewriting the original report (`src/triage-detail.js`, `tests/test_dashboard_ia.py`, `tests/test_rubric_ai_refresh.py`).

- Regression coverage — rubric refresh provenance: added Fast Triage and Full Scout OpenRouter refresh tests for a legacy `instruction_version: 3.5` record. They verify that `generated_at` and the original instruction version remain unchanged while the separately stored rescored rubric uses the active release (`tests/test_rubric_ai_refresh.py`).

- Data repair / OpenRouter refresh display safety — Fast Triage: corrected 21 corrupted Target Relevance score-3 `why_not_higher` display strings, including KINE-501D and KINE-501E. The repair changed no score, status, source report, original generation metadata, or review history. Added regression coverage that both Fast Triage and Full Scout legacy OpenRouter score refreshes preserve criterion display prose, and that current criterion display fields contain no malformed replacement or repeated-question-mark characters (`json/pipeline-records.json`, `tests/test_rubric_ai_refresh.py`).

- Rubric / instruction release — Fast Triage v3.6: promoted today’s Fast Triage criteria and all active prompt, compact-ingestion, display, manifest, schema-guide, and detail-page references from v3.5 to v3.6 while preserving the v3.5 document and original generated-report metadata. Retagged only the 148 records re-evaluated in today’s batch as recalculated/reviewed under v3.6; their preserved `source_report.raw_markdown`, original `generated_at`, and original instruction version remain unchanged, and no new change-history entry was added. Full Scout’s stored-score refresh now uses the shared existing processing modal, three-second success toast, and confirmation-style retry failure modal with the same retained-score/history notice as the table control; verified release alignment, AI score-only preservation, UI refresh coverage, JS syntax, Python compile, JSON validation, and whitespace checks (`config/rubric-release.json`, `config/scoring_criteria/v3_6_triage.md`, `src/app.js`, `src/compact-ingestion.js`, `src/detail.js`, `src/triage-detail.js`, `index.html`, `triage_detail.html`, `json/pipeline-records.json`, `tests/`).
- Fixed Dashboard record replacement after a score refresh to target the stable persisted record ID rather than the rendered table index, so Full Scout-derived Fast Triage aliases cannot overwrite a different raw record. Extended Tab 1 to Tab 2 Team Review synchronization to carry reviewer-authored general Comments as well as Final Comments, criterion notes, and Contact History; Final Comment saves now trigger that synchronization immediately, while source labels, original author/time, and delegated source editing are preserved (`src/app.js`, `main.py`, `tests/test_step0_pipeline_metadata.py`).
- Metadata-consistency/documentation fix — Full Scout, version change 없음: aligned release documentation with the active manifest. The persisted-schema guide now identifies Full Scout v3.8, and the rubric-release checklist now consistently requires coupled `rubric_version`/`instruction_version` changes for operational-principle changes while allowing backend/schema preservation when their contracts are unchanged. Added manifest-backed regression coverage for those schema-guide versions; verified with the rubric release test set, JS syntax checks, Python compile, and whitespace validation (`json/schema.md`, `docs/rubric-release-maintenance.md`, `tests/test_rubric_release_manifest.py`).
- Data recalculation — Fast Triage v3.5: user-requested batch recalculation applied the stored-score Filter 1 logic to every Fast Triage source record not covered by a Full Scout identity group (148 records; 6 Full Scout-covered Fast Triage source rows excluded). The batch synchronized `triage.status` and `hard_filter.status`, wrote v3.5 recalculation provenance and visible change history for each processed record, and retained the explicit per-record refresh audit contract. Verified all 148 target records are current at v3.5 with matching Filter 1 statuses (`json/pipeline-records.json`).
- Reordered the Shortlisting criteria guide's 투자 (Investment) auto-classification condition list to All modality → Non-small molecule preferred → Pipeline Stage ≥ IND-enabling → IND filed/cleared 및 Phase 1 이상 포함 (previously grouped the last two conditions into one note line in a different order), and swapped the display order of the "OI Partnership Type" and "Eligibility & Input Basis" sections so Partnership Type now shows first — content and step-number labels moved together, no wording or classification logic changed. Updated the matching structural test's expected step numbers/order (`index.html`, `tests/test_dashboard_ia.py`).
- Metadata-consistency fix — Fast Triage, version change 없음: corrected `config/rubric-release.json`'s `fast_triage.display_version` from a stale `3.7` to `3.5`, matching `docs/rubric-release-maintenance.md`'s own release table and the visible `v3.5` text in `index.html`/`triage_detail.html`. The field is not read anywhere in `main.py` or `src/app.js` (only checked for non-empty by tests), so this had no behavioral effect; verified with `tests/test_rubric_release_manifest.py` (`config/rubric-release.json`).
- Refresh audit and persistence hardening — no rubric-version change: distinguish a deterministic stored-score Filter 1/2 recalculation from an AI score update or AI no-change review in audit labels and completion toasts. Migrated the just-run 148-record Fast Triage batch history to the explicit Filter 1 recalculation label, and its earlier 35 AI score updates / 51 AI no-change reviews to their explicit AI labels. JSON persistence now retries transient Windows `PermissionError` locks during the final atomic replace (five attempts with short linear backoff), while preserving the all-or-nothing write; removed four stale failed-write temporary JSON files and added regression coverage (`main.py`, `src/app.js`, `src/detail.js`, `src/triage-detail.js`, `json/pipeline-records.json`, `tests/test_rubric_ai_refresh.py`, `tests/test_dashboard_ia.py`).
- Refresh failure feedback — Dashboard row refresh now keeps the blocking “updating” modal while the request is in progress, shows a three-second completion toast on every successful outcome, and presents the existing confirmation-style failure modal for both API and network/save failures. The failure copy explicitly confirms that the existing score/history was retained and directs the reviewer to retry after closing it (`src/app.js`, `tests/test_dashboard_ia.py`).
- Full Scout v3.8 stored-score batch recalculation — processed all 21 Full Scout records without calling OpenRouter: 18 stale records received current v3.8 Total/Filter 2 recalculation provenance and visible audit history, while 3 already-current records were left without a redundant history entry. Resulting Filter 2 states are PASS 5, REVIEW 9, FAIL 7 (`json/pipeline-records.json`).
- Legacy OpenRouter rubric-refresh safety — that score-only prompt no longer overwrites criterion display prose (`main_line_summary`, `investigation_note`, `why_not_higher`, or uncertainty) with a short score response. It now changes only eligible scores, derived decision fields, and rubric-review provenance. Normalized the prior Fast Triage batch's 36 generated summary strings and corrected the 21 affected Target Relevance 3-point `why_not_higher` notes to state that 3 is the maximum (`main.py`, `json/pipeline-records.json`, `tests/test_rubric_ai_refresh.py`).
- Backfilled the audit-label wording gap the earlier same-day "distinguish deterministic recalculation from AI score update" migration left for Full Scout: two `Illimis_Therapeutics_ILM01_20260621` `meta.edit_history` entries (2026-08-31T23:48:51Z and 2026-09-01T01:20:56Z, both genuine GPT-driven `competitive_landscape` score changes under then-current Rubric v3.7) still read the generic `Score recalculated by Full Scout Rubric v3.7`; relabeled both to `Scores updated by AI review with Full Scout Rubric v3.7` to match the now-standard AI-update wording. Only the label string changed — no score, version, or history-ordering fields were touched. Lynk_Pharmaceuticals_LNK01006_20260621's earlier (2026-08-19) AI update has no corresponding `edit_history` entry to relabel (a prior development-history-reset maintenance pass already pruned it; its machine-log `rubric_refresh_history` entry has no `audit_label` field) (`json/pipeline-records.json`).
- Non-scoring metadata canonicalization fixes (Fast Triage/Full Scout/Listing, no rubric/schema/version change — canonical grouping only affects dashboard filter/display buckets, not scoring, hard_filter, or status derivation): added `CN` as a China synonym; added `?`, bare `-`, `Undisclosed`/`undisclosed`, and (for country/main indication) `정보 없음` as Unknown-equivalent placeholders across country, modality, theme, stage, and main-indication canonicalization; fixed `Small Moledule` (typo), Korean `줄기세포`/`MSC`, `Grabody`, and `rProtein` to map into their existing canonical modality buckets; added two new canonical modalities, `Natural product` (그룹화된 천연물기반/Natural Product wording) and `Exosome / EV Therapy`; added `Kennedy's disease` as an ALS/motor-neuron-disease synonym, and added two new canonical indications, `Spinal cord injury` and `Spinal muscular atrophy`. Updated the GPT prompt's canonical-value enumeration text and the closed `CANONICAL_MODALITIES` validator (`config/category-synonyms.json`, `main.py`, `src/app.js`) so newly-generated records can also use the added canonical values; existing stored records are unaffected unless individually re-canonicalized. Verified with the rubric/compact-ingestion/OI-partnership/release-manifest test set, JS syntax check, and Python compile.
