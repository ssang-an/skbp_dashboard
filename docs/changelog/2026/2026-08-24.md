# 2026-08-24

## Changes

- Shortened composite Asset, Competitor, and Scorecard Wiki filenames with stable hashed slug segments so generated vault paths remain safe for standard Windows Git checkouts; stabilized repeated local exports and trimmed generated note whitespace (`scripts/export_pipeline_wiki.py`).

- Clarified the grey Stationary Shortlisting star as a Partnership-review hold, and renamed the Wiki Map hop control to `Node 연결 범위` with a concise filter-root and direct-connection tooltip (`src/app.js`, `index.html`, `src/sigma_graph.js`).

- Reframed the Map Display tooltip around finding more or fewer nodes rather than comparison, clarifying its visual-density purpose for node selection (`index.html`, `src/sigma_graph.js`).

- Replaced the ambiguous Target/MoA example phrase `근거 수준` with the practical diligence question `차별화 근거와 추가 확인 항목` in the Wiki Map Quick Guide (`index.html`).

- Reworded the Map Display tooltip in more direct user language and explicitly stretched the desktop Knowledge Wiki Map card with the Quick Guide row so either panel can set their shared height (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Reframed the Wiki Map hop guide as `Knowledge 탐색 범위를 설정하세요` and clarified that it sets how many connection steps are shown from a selected node (`index.html`).

- Aligned the desktop Knowledge Wiki Map card and Quick Guide to the same flexible row height, allowing the graph to grow with the guide instead of capping it independently; updated the Map Display tooltip to frame core view as a Node-selection aid (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Refined Wiki Map wording: changed the Agent prompt to the natural `해 보세요`, renamed the display-only control to `Map Display 조정`, and updated its single shared tooltip with concise core-versus-full-map guidance (`index.html`, `src/sigma_graph.js`).

- Removed the native select title that duplicated the dashboard tooltip, leaving one concise shared tooltip; renamed `탐색 시작점` to `Map 표시 범위` to make its graph-display-only role explicit (`index.html`, `src/sigma_graph.js`).

- Renamed the Wiki Map Quick Guide prompt heading to `Agent에게 실무 질문을 해보세요` for a clearer action-oriented instruction (`index.html`).

- Added an explicit `Knowledge Wiki Map 화면 표시 범위만 바꿉니다` boundary to the shared starting-point tooltip so the expanded-map control is not mistaken for an Agent-scope setting (`index.html`, `src/sigma_graph.js`).

- Condensed the Wiki Map node-selection guide to state that selecting an Asset, Company, or Target node prioritizes its connected information for the Agent response (`index.html`).

- Unified the `탐색 시작점` label and select hover guidance into one shared first-use instruction, while removing the duplicate expanded-map sentence from the compact Quick Guide (`index.html`, `src/sigma_graph.js`).

- Clarified in the `전체 지식 맵 · 넓게 보기` tooltip that it expands only the visible graph scope and does not itself change the All Pipelines Agent question scope (`src/sigma_graph.js`, `index.html`).

- Removed the redundant default-starting-point sentence from the compact Wiki Map guide and eliminated its desktop fixed-height internal scroll so the guide displays as one naturally sized panel (`index.html`, `src/styles.css`).

- Reworked the Wiki Map practical-question guidance into one compact `질문 예시` box, and clarified `전체 지식 맵 · 넓게 보기` as the expanded graph mode in both the control and first-use guide (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Matched the Wiki Map search placeholder to the shared Company/Asset/Target format and added a concise hover guide with currently structured MoA examples plus the raw-report-search limitation (`index.html`, `src/sigma_graph.js`).

- Restored `Knowledge Wiki Map` as the graph-card title while retaining the concise `Wiki Map` header label, and made the `탐색 시작점` guidance reliably appear when hovering its native select value, including the `전체 지식 맵` state (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Made the Wiki Map Quick Guide more scannable with three practical Company, Pipeline, and structured Target/MoA question cards; clarified that dropdown criteria are optional narrowing conditions and updated the graph-node search example (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Replaced the Wiki Map Quick Guide's generic example with practical Company, Pipeline, Target, and MoA question prompts aligned to the currently structured graph and report evidence scope (`index.html`).

- Reframed Wiki Map's header guidance so Filter defines the Agent question scope and selecting an item in Knowledge Wiki Map defines the question focus; shortened user-facing tab and card titles to `Wiki Map` while retaining the full name in explanatory prose (`index.html`).

- Moved Wiki Map's Agent-scope note out of the legend body into a compact grey heading-side label, `질문 범위 영향 없음`, while preserving the graph-only visibility explanation (`index.html`, `src/styles.css`).

- Completed the dashboard-wide terminology audit: development values and criteria now use `Pipeline Stage`, while Listing/Fast Triage/Full Scout/Shortlisting filters, color scope, and Wiki Map workflow controls use `조사 진행 단계`; internal field keys and Excel `Stage` import compatibility are unchanged (`index.html`, `detail.html`, `triage_detail.html`, `src/app.js`, `src/detail.js`, `src/sigma_graph.js`).

- Standardized development-stage wording as `Pipeline Stage` across the dashboard while restoring Wiki Map's Listing/Fast Triage/Full Scout/Shortlisting control to `조사 진행 단계`; added the corresponding import-guide distinction and a concise tooltip for the `전체 지식 맵` starting point (`index.html`, `src/sigma_graph.js`, `docs/listing-import-guide.md`).

- Clarified the Quick Guide’s first-use default: keep `탐색 시작점` on `Overview · 핵심 연결`, then narrow the Pipeline range with Theme, Main indication, Pipeline Stage, or keywords (`index.html`).

- Renamed the user-facing Wiki Map workspace to `Knowledge Wiki Map` across navigation, guidance, quick guide, reset affordance, Agent context, and the import guide; internal IDs and graph behavior are unchanged (`index.html`, `src/sigma_graph.js`, `docs/listing-import-guide.md`).

- Made Wiki Map’s primary action explicit in Quick Guide: users select an Asset, Company, or Target `Node` before asking Agent, reinforced with a compact graph-node marker (`index.html`, `src/styles.css`).

- Updated Wiki Map Quick Guide to frame Filters as the Agent-question scope and Legend controls as graph-only visibility aids, with a dedicated in-panel legend note stating that hides do not change Agent scope (`index.html`, `src/styles.css`).

- Added in-view guidance to Wiki Map legend headings and items: colors identify workflow/node types, while clicks only hide or restore those nodes in the graph without deleting data or changing the underlying records (`src/sigma_graph.js`, `src/styles.css`, `index.html`).

- Differentiated the Wiki Map MoA legend visibility toggle from the `표시할 항목` filter, and normalized filter-facing entity labels including `MoA` for consistent exploration and Agent context (`src/sigma_graph.js`, `index.html`).

- Renamed the selected-node modal eyebrow to `SELECTED NODE · FULL REPORT` in the shared green eyebrow style, clarified that Wiki Map search covers graph nodes rather than GPT-report prose, and documented the MoA legend as a visibility toggle rather than a filter (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Refined the Wiki Map Quick Guide example to explicitly direct users to select a company node in Knowledge Map before asking for that company’s priority Pipeline (`index.html`).

- Clarified Wiki Map’s node-selection instruction, renamed its controls to `검색`, `Theme`, and `Pipeline Stage`, and added an in-view tooltip for `탐색 시작점`; standardized the dashboard/detail user-facing development-stage label as `Pipeline Stage` while retaining `진행 단계` exclusively for Listing → Fast Triage → Full Scout → Shortlisting workflow state (`index.html`, `src/app.js`, `src/sigma_graph.js`, `src/styles.css`, `src/detail.js`, `detail.html`).

- Included active Wiki Map filter settings in the context sent with a selected node to All Pipelines Agent, so filter → selection → question now carries the same scoped intent end to end (`src/sigma_graph.js`, `index.html`).

- Added a fourth Wiki Map Quick Guide step with a concrete Theme → node type → hop → Agent-question example, clarifying the intended filter-to-Agent workflow (`index.html`).

- Added a compact right-column Wiki Map Quick Guide with the shared workflow-guide visual pattern, covering filter, hop, node-selection, and Agent-question flow; it moves below the graph on narrower screens (`index.html`, `src/styles.css`).

- Added concise, in-view Wiki Map tooltips for `표시할 항목` and `연결 범위`, and retained the user-facing label as `표시할 항목` during graph rendering (`index.html`, `src/sigma_graph.js`, `src/styles.css`).

- Fixed the final Wiki Map reset-button mismatch: its later-loaded graph stylesheet was overriding the shared button geometry. Tab 4 now explicitly retains the same 42px size, zero padding, 10px rounded rectangle, glyph scale, and muted/accent colors as Tab 0~3 (`src/styles.css`, `index.html`).

- Fixed Tab 0 Summary Dashboard's scope note to remain `현재 Tab·Filter 기준` through data refreshes, stat animation, and expand/collapse rendering instead of being overwritten by the color-scope label (`src/app.js`, `index.html`).

- Made Wiki Map's filter row inherit the shared reset-button alignment and reset scope; the reset clears hidden legend filters too. Tabs 1~4 now retain Tab 0's same full-opacity reset-button appearance even when disabled, so the common `↻` rounded rectangle is visually identical across all tabs (`src/sigma_graph.js`, `src/styles.css`, `index.html`).

- Smoothed Tab 0 Summary Dashboard's outer border across expanded and collapsed states, and aligned its title-side scope note with Tab 1/2 as `현재 Tab·Filter 기준` (`src/styles.css`, `index.html`).

- Centered each half of the Wiki Map graph legend, slightly increased its type scale, renamed `moa` to `MoA`, and added short on-canvas legend tooltips with clear workflow/entity explanations that stay within the dashboard view (`src/sigma_graph.js`, `src/styles.css`, `index.html`).

- Restored Wiki Map's filter reset to the exact shared Tab 0~3 `↻` glyph and rounded-rectangle styling, removing the duplicate icon caused by the prior mask treatment (`src/styles.css`, `index.html`).

- Matched Wiki Map multi-select menu search inputs to the compact Tab 1/2 filter-search height instead of inheriting the taller shared control; explicitly set the menu input's 28px box height, compact padding, and line height to override Atlas control-row sizing (`src/styles.css`, `index.html`).

- Styled the Wiki Map guide's information icon in a dedicated purple palette, including a readable dark-mode variant (`src/styles.css`, `index.html`).

- Renamed the user-facing Atlas workspace to `Wiki Map` across its navigation tab, guidance, filter labels, and search guidance documentation; internal graph IDs and behavior remain unchanged (`index.html`, `docs/listing-import-guide.md`).

- Prevented Tab 0/1/2 workflow shortcuts from leaking into Atlas: the GPT instruction and Pipeline upload header actions are now always hidden while the Atlas tab is active (`src/app.js`, `index.html`).

- Simplified the Tab 0 Contact provenance and related UI labels from `Contact History Post` to `Contact History`, while retaining the same editing and deletion controls (`src/app.js`, `main.py`, `index.html`, `json/schema.md`).

- Relabeled Full Scout per-topic Due Diligence comment provenance from `Qualitative` to `DD`; each fixed or newly added DD criterion continues to display its current topic title after the `DD` label (`main.py`, `json/schema.md`).

- Refined the new shared table pagination controls into quiet dashboard-style rounded rectangles: muted at rest, accent-colored only on hover/focus, lightly framed page status, and softened disabled states (`src/styles.css`, `index.html`).

- Standardized displayed comment provenance: Tab 0 `Listing Comment` and Tab 2 Full Scout `Team Review Comment` now read simply as `Comment`, while `Contact History` remains separately labeled (`main.py`, `src/app.js`, `src/detail.js`).

- Standardized Tab 0~3 Pipeline Table pagination with compact `« ‹ current / total › »` controls: single chevrons move one page, double chevrons jump to the first or last page, and unavailable directions are disabled (`index.html`, `src/app.js`, `src/styles.css`).

- Shortened the Tab 0 bulk Excel Comment provenance label to `일괄 업로드: Tab 0 · Comment · Team` in both the dashboard fallback and API-provided feed entries (`src/app.js`, `main.py`, `index.html`).

- Slimmed the Tab 0 multi-select menu search input to match the denser visual rhythm of the Tab 1 and Tab 2 filter menus, without changing triggers or option-list spacing (`src/styles.css`, `index.html`).
