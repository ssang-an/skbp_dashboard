## Changes

- `src/styles.css`, all four shared page HTML files, and `tests/test_dashboard_ia.py`: fixed the root cause of the missing Filter 3 card bottom edge by scoping the legacy last-row border removal to direct stack rows only, allowing the OI Partnership review column to retain its native full border below the Action Date fields; removed the earlier targeted workaround, refreshed cache keys, and strengthened regression coverage.

- `src/styles.css`, all five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: increased every Quick Guide pill from a 24px to 26px minimum height and added 1px more vertical padding per side, preserving the compact 18px action icons and tab-specific colors while removing the overly flat appearance; refreshed CSS cache keys and added regression coverage.

- `src/styles.css`, all five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: normalized Quick Guide action pills across Fast Triage and Full Scout by reducing the step 3/4 clipboard, review, and save icon circles from 22px to 18px and their vectors from 14px to 12px, matching the compact height and typography of the `지침 1·2` pills while preserving the larger workspace-button hit areas; refreshed CSS cache keys and expanded regression coverage.

- `src/styles.css`, all five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: moved the Quick Guide `검증 후 저장` pill from the global green accent to workflow-specific gradients, preserving Tab 1 teal while giving Tab 2 a blue-to-sky-blue Full Scout treatment with matching hover/focus states; refreshed CSS cache keys and added regression coverage.

- `src/app.js`, `src/styles.css`, all five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: added tab-aware `GPT 지침 1 전체 응답` / `GPT 지침 2 전체 응답` clipboard pills to the Quick Guide paste step, reused the Data Upload input-label clipboard icon and mode accent, made each pill focus and highlight the left response input, refreshed app/CSS cache keys, and added regression coverage.

- `index.html` and `tests/test_dashboard_ia.py`: renamed the dashboard header shortcut from `GPT 결과 업로드` to `PIPELINE 업로드`, aligned its compact label, tooltip, and accessible name with the new wording, and added regression coverage without changing upload behavior.

- `src/styles.css`, all four shared page HTML files, and `tests/test_dashboard_ia.py`: explicitly restored the home workflow tabs to their prior borderless active style (`border: 0`) so Tabs 1, 2, and 3 use only the soft background selection treatment; refreshed shared CSS cache keys and updated regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: unified the GPT source-report full-view and copy controls with the Team Comments header controls using borderless 30px circles, matching 17px/1.7-stroke SVG geometry and accent-family colors; aligned the icon row to the two-line report-title block independently of metadata height, refreshed stylesheet cache keys, and expanded regression coverage.

- `src/styles.css`, all four shared page HTML files, and `tests/test_dashboard_ia.py`: restored the missing bottom border on the pipeline-detail Filter 3 / OI Partnership review card so it matches the side borders in both compact and expanded states; refreshed shared CSS cache keys and added focused regression coverage.

- `detail.html`, `src/detail.js`, `src/styles.css`, all five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: added concise hover guidance to the Filter 2 final-evaluation summary and Filter 3 OI-partnership rationale fields, removed the embedded `Rubric rationale` badge and edit glyphs, reclaimed their input padding, refreshed detail/CSS cache keys, and added regression coverage.

- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: removed the stray empty header pill before the login control on Shortlisting by hiding the now-empty GPT instruction/upload action group only in that tab, while restoring it normally in Fast Triage and Full Scout; refreshed the dashboard script cache key and added regression coverage.

- `detail.html`, `src/detail.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: replaced the Team Comments favorite's text glyph with a cleaner outline/fill SVG star, removed its visible circle border to match the adjacent comment-count badge, retained the 30px circular hit area and tracked-state fill, refreshed shared asset cache keys, and added regression coverage.

- `src/styles.css`, all five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: aligned the detail-page `AI REPORT UPLOADED` / `GPT 원문 리포트` and `Review workspace` / `팀 코멘트` header text to the same top baseline, line height, and internal spacing without changing panel behavior.


- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: restyled the Shortlisting judgment-guide Marketability formula keywords as borderless, softly tinted pills while keeping multiplication signs as neutral separators; refreshed stylesheet cache keys and added regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: removed the visible rounded border and background from both qualitative-review AI generation controls so only the star remains, retained equal 28px hit areas and hover tooltips, added a box-free star highlight for hover/focus, refreshed stylesheet cache keys, and expanded regression coverage.

- `src/detail.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: converted the qualitative-review full and per-item AI generation controls to matching 28px star-only icon buttons, added accessible `전체 AI 생성` / `AI 생성` hover tooltips, kept their geometry stable during generation progress, refreshed shared asset cache keys, and added regression coverage.

- `detail.html`, `src/detail.js`, and `tests/test_dashboard_ia.py`: renamed the pipeline-detail report eyebrow from `AI REPORT` to `AI REPORT UPLOADED` in both the initial markup and dynamic source-report restoration, while preserving the Partner material state, refreshing the detail script cache key, and updating regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: unified the `TABLE OF CONTENTS` and overflowed `PARTNER MATERIALS` file-list scrollbars with the same thin 6px transparent-track and rounded muted-thumb treatment; refreshed shared CSS cache keys and extended regression coverage.

- `detail.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: simplified the detail left-rail headings to `TABLE OF CONTENTS` and `PARTNER MATERIALS`, split the available rail space evenly between both panels on desktop/tablet layouts, preserved internal scrolling and mobile stacking, refreshed shared CSS cache keys, and added regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: rounded all four Team Comments panel corners consistently by clipping the outer card and moving vertical scrolling into the middle comment area; preserved responsive panel behavior, refreshed shared CSS cache keys, and added regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: matched the detail-page `Report uploaded` eyebrow to the same green treatment as `Review workspace`, refreshed shared CSS cache keys, and added regression coverage.

- `src/styles.css`, all four shared page HTML files, and `tests/test_dashboard_ia.py`: raised the muted header-pill palette to softer, brighter pastel blues, teal, mustard, lavender, indigo, cyan, and rose; modestly increased resting/hover visibility to recover the earlier design's clarity without restoring its high saturation, refreshed cache keys, and updated regression coverage.

- `src/styles.css`, all four shared page HTML files, and `tests/test_dashboard_ia.py`: softened the functional header-pill palette to low-saturation slate blue, sage, muted mustard, lavender, indigo, cyan-gray, and dusty rose tones; reduced resting and hover border/background opacity, refreshed shared CSS cache keys, and retained category consistency and regression coverage.

- `src/styles.css` and `tests/test_dashboard_ia.py`: assigned restrained functional colors to dashboard and detail header pills—blue navigation, sky refresh, amber criteria, violet GPT instructions, indigo JSON, cyan theme, rose delete, and the existing teal upload category—and added regression coverage for consistent cross-page mapping.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: removed the visible outer side/bottom outline from the expanded Summary Dashboard card region so its surface flows smoothly from the borderless heading while preserving individual chart-card borders; refreshed shared CSS cache keys and extended regression coverage.

- `index.html`, `detail.html`, `src/app.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: moved the two single-pipeline sensitivity/evidence example questions from the Home Agent to the Detail Due Diligence Agent, gave Home Tab 2 only the requested two-Pipeline Platform Attractiveness comparison example, separated Tab 3 with a Shortlisting-specific example, expanded only the Detail composer to show both moved questions, refreshed shared asset cache keys, and updated regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: changed the Header login/account trigger from a rounded square to an exact 38px circle matching the adjacent theme icon geometry, refreshed shared CSS cache keys, and added regression coverage.

- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: renamed the Home dashboard Agent available on Tabs 2 and 3 from `Pipeline Discovery Agent` to `All Pipelines Agent` across the launcher, window, messages, and full-response modal; refreshed the app cache key and updated regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: simplified header action pills to neutral slate utilities, retained red only for deletion, and differentiated 판단근거 in muted blue from GPT 결과 업로드 in muted violet with softer borders and fills; refreshed shared CSS cache keys and added regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: restyled AI Agent response copy/full-view controls as borderless transparent buttons matching the floating window actions, with muted default icons and accent hover/focus feedback; refreshed shared CSS cache keys and updated regression coverage.

- `src/detail.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: compacted the attachment AI-scope notice into an inline neutral title pill plus adjacent helper text, tightened spacing, shortened `첨부자료` to `첨부`, removed the divider, and updated regression coverage.

- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: softened AI-generated qualitative review cards to a restrained pale-green surface, converted the purple AI badge to the same muted green family, removed the dark 3px left accent line, refreshed shared CSS cache keys, and added regression coverage.
- `src/app.js`, `src/detail.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: standardized Home and detail AI response actions as compact 24px copy/full-view SVG icons aligned at the response card's upper-right, added hover/focus labels and accessible copy-success feedback, refreshed asset cache keys, and expanded regression coverage.

- `main.py`, `index.html`, `detail.html`, `src/app.js`, `src/detail.js`, `src/styles.css`, `json/schema.md`, and reupload/UI tests: added reviewed same-workflow Company+Asset matching across dated GPT uploads, a confirmation modal for updating the latest existing record, server-side identity validation, preserved Dashboard/Team Review metadata, Human score-override reset events against the new official scores, source-update-specific timestamp labels, and made the detail AI Agent Q&A-only by removing its apply controls and public apply routes.
- `src/detail.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: separated the attachment AI-scope notice into a stronger `AI 질의·정성평가` title and muted 9,000-character helper, added a subtle divider and responsive wrapping, refreshed asset cache keys, and added regression coverage.

- `index.html`, `detail.html`, `src/app.js`, `src/styles.css`, shared HTML cache keys, and `tests/test_dashboard_ia.py`: standardized every AI Agent question composer at a compact 68px two-line initial height, kept workflow-specific placeholder text, retained vertical expansion for longer questions, and updated regression coverage.

- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: swapped the Header refresh and judgment-guide pill positions so refresh appears first and judgment guide occupies the right slot, replaced the judgment guide's yellow treatment with neutral gray, refreshed asset cache keys, and added regression coverage.

- `detail.html`, `src/detail.js`, and `tests/test_dashboard_ia.py`: synchronized the pipeline-detail judgment drawer with the current matching Home dashboard Tab 2 or Tab 3 guide on every open, retained the embedded guide as a network-failure fallback, refreshed the detail script cache key, and added regression coverage.

- `detail.html`, `src/detail.js`, and `tests/test_dashboard_ia.py`: removed the redundant Loading/Loaded status pill beside the pipeline-detail delete button, removed its unused JavaScript update, refreshed the detail script cache key, and added regression coverage.

- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: swapped the Header refresh control with the workflow-specific GPT instruction controls so Tab 1 pairs `지침 1` with `GPT 결과 업로드` and Tab 2 pairs `지침 2` with `GPT 결과 업로드`; preserved existing tab visibility behavior, refreshed shared CSS cache keys, and updated regression coverage.
- `src/detail.js`, `src/styles.css`, `detail.html`, and `tests/test_dashboard_ia.py`: replaced the empty partner-materials message with an always-visible AI context notice stating that extracted attachment text is combined up to 9,000 characters for AI Q&A and qualitative review; retained the existing uploaded-file list directly below the notice, refreshed detail-page asset cache keys, and added regression coverage.
- `index.html`, `src/app.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: expanded the Home AI Agent question composer only on Tabs 2 and 3, added the requested two-line Obtainable Peak Sales and minimum-additional-evidence example placeholder, retained the compact Tab 1/detail composer, refreshed shared asset cache keys, and updated regression coverage.
- `src/styles.css`: styled the E0-E4, Summary-Uncertainty, and Similarity-Sources labels in the Full Scout criteria record cards as rounded chips for stronger visual scanning.

- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: made KPI and all five Summary Dashboard panels recalculate from the active Tab's currently filtered visible rows across Tabs 1–3, restored server tab-wide summaries when filters are clear, added filter-aware scope/count subtitles, refreshed the dashboard script cache key, and updated regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: excluded free-text/company search queries from Summary Dashboard aggregation while retaining active Tab and structured Filter recalculation across Tabs 1–3, standardized the scope label to `현재 Tab·Filter 기준`, refreshed the dashboard script cache key, and updated regression coverage.
- `main.py`, `src/app.js`, `index.html`, `tests/test_agent_chat_context.py`, and `tests/test_dashboard_ia.py`: renamed the Tab 3 Home Agent to `집중 관리 Agent`, added bounded focus-management fields and latest Team Review comments to compact chat context, added Shortlisting-specific visible-row fields to dashboard context, treated user-entered context as untrusted evidence, refreshed the dashboard script cache key, and expanded regression coverage.
- `main.py`, `detail.html`, `src/detail.js`, `src/styles.css`, shared HTML cache keys, `json/schema.md`, and AI-apply regression tests: replaced the detail Agent's immediate JSON/report save with an auth-style two-step confirmation and server-generated before/after preview for JSON, GPT report, and Wiki export impact; final apply now checks the preview's base-record hash, rejects stale previews before writing, and runs JSON persistence plus Markdown exports only after explicit confirmation. Also repaired the shared stylesheet links that had been replaced by an invalid literal path.
- `src/app.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: reduced the Home AI Agent response copy and expand controls to compact 24px icon-only buttons, added the shared dashboard tooltip box with `복사` and `전체보기` labels on hover/focus, refreshed shared asset cache keys, and added regression coverage.
- `index.html`, `detail.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: made the Home AI Agent title switch to `후보 선별 Agent` in Tab 1 and `심층 검토 Agent` in Tab 2, retained Pipeline Discovery Agent for Tab 3, and matched the Full Scout detail Agent title and accessibility label to `심층 검토 Agent`; refreshed the dashboard script cache key and updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: shortened the AI Agent Knowledge Mode label to `JSON + Wiki + Files` while retaining the existing uploaded-file context behavior; updated regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: updated the AI Agent Knowledge Mode label from `JSON + Wiki Agent` to `JSON + Wiki + 사용자 업로드 파일` to reflect the already-active uploaded-file excerpt context; updated regression coverage.
- `index.html`, `detail.html`, `src/detail.js`, and `tests/test_dashboard_ia.py`: shortened AI Agent scope labels to `현재 탭·필터 전체` and `현재 파이프라인 1개`, renamed the detail agent to Due Diligence Agent in its title and accessibility label, refreshed the detail script cache key, and updated regression coverage.
- `main.py`, `src/detail.js`, `detail.html`, `json/schema.md`, and score-provenance regression tests: made Full Scout AI Agent score changes use the Human/Team Review override layer with reviewer attribution and an automatically recalculated effective Total, preserved official JSON and the embedded GPT score table while appending a Revision Note without changing the GPT-original modified timestamp, and made successful Score 기준 갱신 clear active Human/Agent score overrides before synchronizing official scores into the report.
- `index.html` and `tests/test_dashboard_ia.py`: replaced the shared home-dashboard AI Agent eyebrow across Tabs 1–3 from `Obsidian + Agentic Search` to `비교 모드 · 현재 탭·필터 전체`, and added regression coverage without changing search behavior.
- `index.html`, `detail.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: replaced floating AI Agent minimize, maximize, and close text glyphs with consistently centered 15px SVG controls, aligned the action group vertically, refreshed shared CSS cache keys, and added regression coverage.
- `main.py`, `src/app.js`, `index.html`, `json/schema.md`, and agent-context regression tests: scoped Home AI Agent Q&A to every pipeline in the active Tab/current filters, selected question-relevant records server-side, and added bounded GPT source-report plus extracted uploaded-file excerpts to both streamed and non-streamed OpenRouter prompts; detail chat remains current-record scoped and questions remain read-only.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: removed `아래에` from both Data Upload placeholder variants so the instruction now reads `생성된 전체 응답을 그대로 붙여넣으세요.`, refreshed the dashboard script cache key, and updated regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: clarified in only the Data Upload placeholder and first Quick Guide step that GPT research runs in a separate browser tab, renamed the first step to `새 GPT 창 열기 및 모드 선택`, added a small external-link vector icon, and refreshed the dashboard script cache key and regression coverage without changing validation or save logic.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: reduced the shared Tab 3 investment, Value Up, and joint-research helper row from 78px to 68px, trimming the blank space below each summary while retaining aligned bullet starts; refreshed stylesheet cache keys and regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: extended the shared `현재 탭 전체 asset · 적응증 6개 · Others` indication-distribution subtitle to Tab 1, so all three workflows now use identical copy while retaining their own aggregation populations; refreshed the dashboard script cache key and regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: standardized the Tab 3 SKBP indication-distribution subtitle with Tab 2 as `현재 탭 전체 asset · 적응증 6개 · Others`, while preserving the shortlisted-only population; refreshed the dashboard script cache key and regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: added an Others-style hover/focus description to the Shortlisting OI Partnership `TBD` legend explaining that partnership classification has not yet been completed; refreshed the dashboard script cache key and regression coverage without changing classification logic.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: standardized the Summary Dashboard heading scope note beside the average score to `현재 Tab 기준` for Fast Triage, Full Scout, and Shortlisting; refreshed the dashboard script cache key and regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: expanded the Shortlisting indication-distribution subtitle to `Shortlisted Pool N개 · SKBP 관심 적응증 6개와 Others`, matching the adjacent Modality subtitle pattern; refreshed the dashboard script cache key and updated regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: corrected the SKBP indication `Others` legend help text to reflect its actual fixed taxonomy—indications outside the six SKBP priority indications plus Unknown—without changing the existing six-bucket grouping logic; refreshed the script cache key and regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: kept indication legend values sorted by count while placing `Others` after the other labels when counts are tied, including the all-zero case; refreshed the dashboard script cache key and added regression coverage.
- `main.py`, `src/app.js`, `index.html`, `tests/test_dashboard_summary.py`, and `tests/test_dashboard_ia.py`: changed Tab 3 indication and modality distributions from the entire Full Scout population to the shortlisted assets shown in the Shortlisting Pipeline Table, updated population labels and the dashboard script cache key, and added backend/frontend regression coverage.
- `src/app.js`, `index.html`, and `tests/test_dashboard_ia.py`: renamed the SKBP indication-distribution subtitle from `Shortlisting 후보 pool` to `Shortlisted Pool`, refreshed the dashboard script cache key, and added regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: also removed the left and right borders of the expanded `Summary Dashboard` heading bar while preserving the card-area frame below, refreshed shared CSS cache keys, and updated regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: reverted the broad Summary card border/hover changes and removed only the top border directly above the expanded `Summary Dashboard` heading, including its hover state; refreshed shared CSS cache keys and updated regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: centered each Summary donut-plus-legend group with matching 144px/190px columns across tabs while preserving the compact left-aligned legend values; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: restored moderate spacing between Summary donut labels and values using a compact 190px two-column legend with left-aligned number columns; refreshed shared CSS cache keys and regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: tightened Summary Dashboard donut legend label-to-value spacing with content-width legend rows while retaining truncation behavior; refreshed shared CSS cache keys and added regression coverage.
- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: upgraded all seven Tab 2 Scoring Table criterion labels to the Tab 1 icon-plus-abbreviation pattern, added subtle accent treatment to the left criterion cells and circular icons, refreshed shared CSS cache keys, and added regression coverage.
- `index.html` and `tests/test_dashboard_ia.py`: shortened the Tab 2 Full Scout REVIEW helper copy to describe candidates as requiring additional confirmation or monitoring; preserved status criteria, layout, and styling.
- `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: changed the Target Relevance R&D Theme/Cluster chips—including Ion Channel through Disease immune modulation—from semibold to regular weight; the indication chips were already regular and the group headings remain emphasized.
- `index.html`, `src/app.js`, `src/auth.js`, `src/styles.css`, shared asset cache keys, and `tests/test_dashboard_ia.py`: added a connected Header data utility with vector-icon refresh, tab-aware GPT result upload shortcut, and passive dynamic loaded-record status; the shortcut is available only in Fast Triage/Full Scout, smoothly scrolls to and focuses the existing Data Upload textarea, briefly highlights the unchanged upload card, keeps Shortlisting free of a disabled action, groups profile/theme controls, compacts labels responsively, and stabilizes workflow navigation at the viewport center without changing upload validation, save, filter, or refresh logic.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: renamed the Header shortcut to `GPT 원본 업로드`, changed refresh/upload/loaded-count segments into individual rounded pills matching the adjacent Guide group, and released the workflow navigation from absolute viewport centering so it centers naturally in the available space between the Pipeline Finder brand and right-side utilities.
- `index.html`, `src/app.js`, `src/styles.css`, shared asset cache keys, and `tests/test_dashboard_ia.py`: made Header instruction pills workflow-specific—Fast Triage shows only 지침 1, Full Scout only 지침 2, and Shortlisting neither—while preserving 판단근거 and the existing Shortlisting upload-shortcut omission; widened the flexible workflow navigation slightly so it recenters cleanly within the newly available Brand-to-utility gap.
- `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: fixed the desktop workflow navigation in a dedicated middle Header grid column, so Fast Triage, Full Scout, and Shortlisting remain at exactly the same position while the tab-specific instruction and upload pills appear or disappear independently in the right utility column; retained the existing centered second-row navigation on narrower screens.
- `index.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: removed the loaded-record database icon/pill from the right utility group, moved the unchanged dynamic `N건 로드됨` status into a small muted-gray pill beside `SKBP Pipeline Finder`, restored the `GPT 결과 업로드` label, and rebalanced the fixed three-column Header tracks so workflow tabs stay stationary with the slimmer right action area.
- `src/app.js`, `index.html` asset cache key, and `tests/test_dashboard_ia.py`: changed the Brand status copy to `총 XX건 로드됨` and sourced XX from the active workflow summary—Filter 1/Fast Triage asset total on Tab 1, Filter 2/Full Scout asset total on Tab 2, and shortlisted pipeline total on Tab 3—instead of the combined raw-record count.
- `index.html`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: upgraded the Tab 2 Evidence Type, score-record, and competitor-similarity cards with contextual circular icons, aligned helper copy, and consistent structured record rows; preserved all existing evidence and score-record content, refreshed stylesheet cache keys, and updated regression coverage.
- `src/app.js`, `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: increased Priority Pipeline and F/U Action lists from five to ten items, labeled both limits explicitly, retained their fixed-height vertical scrolling, refined thin scrollbar styling, refreshed asset cache keys, and updated regression coverage.
- `src/styles.css`, all five shared page HTML files, and `tests/test_dashboard_ia.py`: unified the Tab 1, Tab 2, and Tab 3 judgment-guide drawer canvases with the same subtle neutral-gray background while preserving each workflow's semantic accent colors; refreshed stylesheet cache keys and added regression coverage.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: expanded the Tab 2 SKBP interest-indication distribution subtitle to `현재 탭 전체 asset · SKBP 관심 적응증 6개 · Others`, using middle-dot separators while preserving the Tab 1 and Tab 3 subtitles; refreshed the app cache key and updated regression coverage.
- `index.html`, `src/app.js`, and `tests/test_dashboard_ia.py`: shortened the Tab 2 indication-distribution subtitle from `SKBP 관심 적응증 6개` to `적응증 6개`, preserved the surrounding asset and Others labels, and refreshed the app cache key.
- `index.html`, `src/app.js`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: replaced the AI Agent send glyph and hourglass with a centered paper-plane SVG and circular loading spinner, refined the 42px round button's hover/disabled states, refreshed asset cache keys, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: disabled the legacy AI Agent `disabled::before` spinner that duplicated the SVG loader and applied the same centered paper-plane and single-ring loading states to both the home dashboard and pipeline detail Agent; refreshed asset cache keys and expanded regression coverage.
- `detail.html`, `src/detail.js`, and `tests/test_dashboard_ia.py`: replaced the pipeline-detail Agent's `JSON + Wiki` scope labels with `Asset 검토 모드 · 현재 파이프라인만` in both the window header and completed assistant-message metadata while retaining the home Agent's broader mode label; refreshed the detail script cache key and added regression coverage.
- `index.html`, `detail.html`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: converted both home and pipeline-detail AI Agent composers from multi-row fields to matching 44px single-line inputs and replaced colored focus emphasis with a Data Upload-like neutral gray shadow; removed manual resizing, added shared light/dark focus treatment, refreshed stylesheet caches, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: expanded the existing AI Agent apply preview from count-led diffs to content-led change cards with friendly labels, change-type badges, actual saved content, record locations, before/after values, expandable long text, a visible Report Revision Note preview, folded raw JSON/Report diffs, explicit Wiki regeneration behavior, and a `최종 반영` action; refreshed asset caches and regression coverage.
- `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: restyled the Tab 3 `공동연구 > 투자` priority formula so both decision values use the same outlined pill treatment as the adjacent modality and ADMET keywords while the comparison symbol remains a plain separator; refreshed stylesheet caches and regression coverage.
- `src/styles.css`, all five shared page cache keys, and `tests/test_dashboard_ia.py`: matched the attachment AI-scope notice typography to the upload dropzone's `최대 30MB` helper at 9px, regular weight, and 1.4 line-height; refreshed stylesheet caches and expanded regression coverage.
- `index.html`, `detail.html`, `src/app.js`, `src/detail.js`, and `tests/test_dashboard_ia.py`: removed the Home AI Agent entry point from Fast Triage while retaining one shared Tab 2/3 discovery workflow, renamed the Home surface to `Pipeline Discovery Agent` and the single-record Full Scout surface to `Due Diligence Agent`, closed any open Home Agent when switching to Tab 1, refreshed script cache keys, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: moved the attachment AI-use notice out of the dynamic file list to a fixed soft-tint card directly below the dotted upload dropzone, added a compact vector icon and emphasized the shared 9,000-character extraction limit, hid the empty file-list container, refreshed detail asset cache keys, and updated regression coverage without changing upload or extraction behavior.
- `detail.html` and `tests/test_dashboard_ia.py`: clarified the attachment AI-use notice as `첨부 추출 텍스트 합산 최대 9,000자 활용 가능` and updated its regression assertion.
- `detail.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: matched the attachment AI-use card typography to the dotted upload box, using the same 11px/700 title treatment and retaining the matching 9px/1.4 helper treatment below it; refreshed the detail stylesheet cache key.
- `detail.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: changed the attachment AI-use card, icon, title, and 9,000-character emphasis from teal accents to a light neutral-gray treatment so the dotted upload area remains the stronger visual action; refreshed the detail stylesheet cache key and regression coverage.
- `detail.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: reordered the partner-material area so the AI-use notice follows the file selector and attachment list, keeping uploaded files above the notice as the list grows; tightened list-to-notice spacing, refreshed the detail stylesheet cache key, and updated regression coverage.
- `detail.html`, `src/styles.css`, and `tests/test_dashboard_ia.py`: restyled the computer file selector as a soft-green rounded action with a compact folder-plus vector icon, neutral default text, and clearer green text/border elevation on hover or keyboard focus; refreshed the detail stylesheet cache key and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: reverted the file selector to its original neutral secondary-button appearance and restored the AI-use notice above it; the notice now appears only when no attachments exist and automatically hides after the first upload, returning when all attachments are removed; refreshed the detail stylesheet cache key and regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: replaced the attachment list's plain extension badges with compact dashboard-style vector file tiles for presentation, PDF, spreadsheet, document, text, and generic files; added restrained type accents and hover feedback, adjusted narrow-list alignment, refreshed detail asset cache keys, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, and `tests/test_dashboard_ia.py`: removed decorative file tiles from the narrow attachment list to restore filename width, top-aligned attachment grid rows to prevent single files from stretching vertically, preserved the report Mini TOC during attachment preview, and made TOC clicks return to the GPT report before navigating to the selected section; refreshed detail asset cache keys and regression coverage.
- `detail.html`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: kept the Partner Materials header fixed while moving the upload controls, status, and complete attachment list into one capped internal scroll region; removed the nested list-only scrollbar, matched the thin Mini TOC-style scrollbar, and added regression coverage.
- `src/detail.js`, `detail.html`, and `tests/test_dashboard_ia.py`: removed the redundant first `Company Pipeline Scout Report: Asset` H1 from every pipeline-detail Mini TOC while preserving it in the source report and page header; kept the 14-entry limit for actual report sections, refreshed the detail script cache key, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: aligned the GPT source-report header with the Review Workspace hierarchy using a `Report uploaded` eyebrow above `GPT 원문 리포트`, removed the redundant target subtitle in report mode, converted copy/full-view actions to 30px circular vector-icon controls with hover labels, preserved file-preview metadata, and added regression coverage.
- `detail.html`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: renamed the detail outline header to `GPT Report` / `목차` and matched its label size, title size, weight, letter spacing, and padding to the adjacent `Partner materials` / `파트너사 자료함` header; refreshed stylesheet caches and expanded regression coverage.
- `detail.html` and `tests/test_dashboard_ia.py`: renamed the Partner Materials eyebrow to `PRIVATE DATA` while preserving the `파트너사 자료함` title, layout, and attachment behavior; updated regression coverage.
- `detail.html`, `src/detail.js`, and `tests/test_dashboard_ia.py`: renamed the GPT source-report eyebrow from `Report uploaded` to `AI REPORT` in both initial markup and report-mode rendering, refreshed the detail script cache key, and updated regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: placed middle-dot separators between Rubric/OI versions and their refresh actions, replaced the old Score refresh control with a compact vector-icon pill, added the matching functional OI Partnership v1.0 refresh pill through the existing recalculation API, labeled `Action Date · Set by Asset Owner`, preserved version labels during manual overrides, refreshed asset caches, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: relabeled the review columns as `Full Scout · Key Takeaway` and `Shortlisting · Takeaway`, added a default-collapsed Show/Hide control that retains Filter 2 status/scores, Filter 3 classification, and Action Date while hiding secondary rationale/material/owner/follow-up fields, removed empty collaboration-status space and tightened the Team Review history offset so qualitative review appears higher, refreshed asset caches, and added regression coverage.
- `detail.html`, `src/detail.js`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: replaced the detail-page Filter 2/3 dropdowns' stacked green focus rings with neutral shadow-only depth in light and dark modes, removed the redundant `자동 분류 v1.0` note-origin chip, added breathing room below the Filter 3 select, reclaimed note-input width, refreshed asset caches, and added regression coverage.
- `detail.html`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: simplified the compact Review Workspace column headings from `Full Scout · Key Takeaway` and `Shortlisting · Takeaway` to `Full Scout` and `Shortlisting`, removed the now-unused context styling, refreshed stylesheet caches, and updated regression coverage without changing Show/Hide behavior.
- `detail.html`, `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: placed the Review Workspace Show/Hide control in a dedicated footer row with a full-width top divider matching the surrounding glass border, tightened the button margin, refreshed stylesheet caches, and updated regression coverage.
- `src/styles.css`, the five shared page stylesheet cache keys, and `tests/test_dashboard_ia.py`: matched the detail Action Date field to the Filter 3 dropdown's neutral depth treatment, replacing stacked green hover/focus rings with a subtle hover shade and shadow-only focus in light and dark modes; refreshed stylesheet caches and expanded regression coverage.
