# Work Log — 2026-08-09

## Changes

- `user_admin.html`: fixed 404s on `/admin/users` for `styles.css`, `user-admin.js`, `sk-butterfly.svg` by switching asset references from relative (`./src/...`) to absolute (`/src/...`) paths, since the extra `/users` path segment broke relative resolution.
- `src/app.js`: fixed a `SyntaxError` (unescaped raw newline inside a single-quoted string in `buildCopyText`) that was left uncommitted from an in-progress edit and prevented the entire module from loading (data never loaded, login/profile menu never mounted). Repaired the mojibake Korean text in the same block, and wired the new Fast Triage "복사" (copy) button into the table's actually-live triage action cell (`rubricReevaluationCell`) — the previous attempt (`triageRowActions`/`rubricRefreshButton`) was never called from the render path, so the button never appeared; removed that dead, duplicate code. The button (`triageFullScoutCopyButton` / `copyTriageFullScoutPrompt`, wired via the existing `[data-triage-full-copy]` delegated click handler) copies the current GPT 지침 2 Full Scout prompt together with the row's asset name, company name, and saved Fast Triage research content (`source_report.raw_markdown`, falling back to `triage.why`) to the clipboard, with the existing `execCommand('copy')` textarea fallback.
- Added a new "Step 0 진척 현황" tab (before Fast Triage) as an independent panel, not a real `tableMode`: paste a bulk asset/company list (same row format as the Fast Triage prompt spec), import via `POST /api/candidate-queue/import` which dedups against existing Fast Triage/Full Scout records (`dashboard_identity_groups`) and against the queue itself, and view a unified progress table (`GET /api/candidate-queue/progress`) with independent 조사대기/Fast Triage/Full Scout/Shortlisting status per candidate, clickable through to `/triage-detail` or `/detail`. New persisted store `json/candidate-queue.json` (deliberately outside `dashboard_hybrid_v1`). Up to 10 pending rows can be selected and copied into the Fast Triage GPT prompt via a new `지침 복사` button. `main.py` refactors `dashboard_company_aliases`/`dashboard_asset_aliases` into reusable `company_aliases_from_text`/`asset_aliases_from_text` helpers and adds `parse_candidate_pair_lines`/`find_matching_identity_group`. Also added `DELETE /api/candidate-queue/{queue_id}`. Files: `main.py`, `index.html`, `src/app.js`, `src/styles.css`, `CLAUDE.md`.
- `src/styles.css`: fixed the header workflow tab bar (`.workflow-navigation-shell .pipeline-table-tabs` and its `.topbar-top` ≤720px variant) still being hard-coded to `grid-template-columns: repeat(3, minmax(0, 1fr))`, left over from before the Step 0 tab was added — the 4th tab wrapped onto its own row instead of sitting inline. Both now use `repeat(4, minmax(0, 1fr))`; bumped the ≤720px horizontal-scroll fallback's `min-width` from 550px to 720px to match. Verified via browser automation that all 4 tab buttons now sit on the same row at desktop and mobile widths.
- Restructured the Step 0 진척 현황 panel (`index.html`, `src/app.js`, `src/styles.css`) to match the other 3 tabs' real UI/UX: a `.panel.control-panel` search box (약물명/회사명) plus Company and 진행 단계 filter dropdowns with a reset button (same markup as Tab2/Tab3's `#searchInput`/filters); the progress table now lives inside a real `.panel.pipeline-table-panel` with separate sortable Asset/Company columns (previously merged into one "후보" cell) via new `data-step0-sort` header buttons; the paste/import UI moved to the bottom of the page as a `.panel.paste-panel` in the exact `data-upload-layout` (workspace + `data-upload-guide` aside with numbered steps) structure the Fast Triage/Full Scout upload panels use, including a `data-state`-driven status pill (`setStep0SaveStatus`). Backend candidate identity was already stored as separate `asset_input`/`company_input` fields (`json/candidate-queue.json`); this only changes how the frontend renders and filters them. Verified end-to-end via browser automation: search, Company/진행 단계 filters, column sort, checkbox selection cap, and 지침 복사 all work against the new markup.
- Step 0 follow-up (`index.html`, `src/app.js`, `src/styles.css`): replaced the single-select 진행 단계 `<select>` with a multi-select toggle-pill group (`.step0-status-toggle`, `data-step0-status`, OR-matched against each row's stage `done` flags — clicking multiple pills shows rows matching any selected stage) since a dropdown made picking/comparing stages slower than necessary; fixed a latent bug where the old dropdown's `"shortlisted"` option value didn't match the row data's `shortlisting` key, so that filter silently never matched anything. Added a header "전체 선택" checkbox (`#step0SelectAllRows`, indeterminate-aware like the main table's `#selectPageRows`) that selects all currently visible/filtered 조사 대기 rows up to the existing 10-item cap. Changed the done-stage pill label from the text "완료" to a checkmark (with a `title` tooltip naming the stage) for a more scannable status glance. Verified via browser automation: multi-toggle OR filtering (조사대기 + Full Scout together showed 15 rows, matching 3+12), reset clears toggle state, select-all fills up to the cap and syncs its checked/indeterminate state, and the checkmark pill renders with the expected tooltip.
