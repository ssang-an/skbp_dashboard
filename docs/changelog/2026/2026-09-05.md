# 2026-09-05

## Changes

- Disease Linkage badge follow-up (feature added 2026-09-04): added a free zero-risk shortcut where `investigation_note` containing the rubric-mandated fixed phrase `확인 불가` (config/scoring_criteria/v3_8_full.md ~L99, written verbatim by GPT when evidence is insufficient) short-circuits to `NA` before any OpenRouter call — the same string-match philosophy as the in-vivo/in-vitro regex classifier, applied to the one fixed phrase this rubric actually standardizes (`main.py`). Also reworked the Shortlisting-table badge presentation (later superseded by the editable-select rework below): compact rounded-rectangle pill instead of a circle, plain `-` instead of `—`/`NA`, column header shortened to "Link", column narrowed from ~72-96px to ~40-54px.
- Disease Linkage becomes an editable field, matching ADMET/In-vivo/In-vitro exactly: converted the read-only badge into a real `<select>` reusing `.evidence-edit` styling (same rounded-rectangle shape, O/X/`N/A` options — no separate CSS needed, so the earlier one-off `.disease-linkage-pill` rule was removed). A reviewer can now click it to manually override the AI verdict; picking a value sets `meta.focus_management.disease_linkage_status_source = "manual"` via a new `disease_linkage_status` case in `PATCH /api/records/{id}/focus-management` (validated to O/X/NA, mirroring the existing `in_vivo_status`/`in_vitro_status` case), and `classify_record_disease_linkage()` now returns the manual value untouched instead of overwriting it — same "manual wins forever" contract as the other evidence fields. Double-clicking the select now jumps to `/detail?...&tab=focus&open=moa-note`, which scrolls the GPT original report to the MoA Validity heading and centers it (`navigateToCriterionReportSection`/`scrollReportHeadingIntoView` in `src/detail.js` gained an optional `block` position so this jump can center instead of the default top-align used by the existing per-criterion "GPT 원문 근거로 이동" buttons). Caveat: native `<select>` dblclick can be flaky across browsers once the OS dropdown has opened on the first click — if that proves unreliable in testing, the fallback is a small separate "jump to source" icon instead of overloading dblclick on the select itself. Header text changed to "D·Link" with an explanation tooltip (`main.py`, `src/app.js`, `src/detail.js`, `src/styles.css`).
- Ran a one-off bulk re-classification (not a code change) for the 6 currently-tracked, Full Scout v3.8+, MoA-2-3 records that had never been classified yet (`json/pipeline-records.json` had no `disease_linkage_status` written for any of them - the feature had never actually been exercised in a browser before this). Result: Astrion `AON-PP23` → `X` ("binding site/target engagement... remain to be demonstrated" is an explicit non-confirmation); the other 5 (Illimis `ILM01`, VASTHERA `VTA-27`, Curacle `CU71`, CK Regeon `CKR-492`, Vivozon `VVZ-3416`) → `NA`, on manual review of each `investigation_note` this looks correct rather than a prompt problem — most of those notes answer a different question (score justification, data-provenance/canonicalization caveats) or mix independent target-biology validation with asset-specific evidence still being preclinical, not a clean "confirmed" or "not confirmed" statement, so the fail-safe NA is the right call.
- D·Link header centering: `.disease-linkage-head` (and every other `plainHeader`-based column) rendered as a bare `<span>` with no wrapping button, so it never picked up the `text-align: center` that sortable/filter headers get from their `<button>`/`<div>` wrappers — it inherited the base `th, td { text-align: left; }` reset instead. Added `text-align: center` directly on `.disease-linkage-head` (`src/styles.css`). Also investigated a report that the D·Link rounded-rectangle looked "thinner" than ADMET's: `getComputedStyle` on both `<select>`s in the live page showed byte-identical geometry (height 30px, padding `3px 7px`, border-radius 7px, same border) — both share the exact same `.evidence-edit` class, so there is no sizing difference. The visual impression is almost certainly the pale-gray `na` tone (most rows currently show D·Link as `N/A`) reading as "thinner" next to ADMET's more saturated `review`/`pass` colors on the same rows; not a CSS bug.
- Diagnosed why the D·Link (Disease Linkage) badge showed `N/A` for every row including newly-tracked DigmBio `DM3159`: an orphaned process was squatting on `127.0.0.1:8000` and answering requests with a stale build of `main.py` that predates the `disease-linkage-classify` route (confirmed via `/openapi.json` showing zero `disease-linkage` paths, and a direct `POST .../disease-linkage-classify` returning `405`), so the frontend's per-row background classifier (`scheduleDiseaseLinkageClassification`) never got a usable response and silently left the field unset. This also explains why the 6 records classified earlier today (see above) had reverted to unclassified: the stale process's in-memory `save_records()` calls were overwriting the real worker's writes. Root cause matches the known duplicate-dev-server class of issue. After the user rebooted and restarted a single `uvicorn --reload` process (port 8000 now has exactly one listener), re-ran `POST /api/records/{id}/disease-linkage-classify` for the affected records to restore/verify results (no code changed): DigmBio `DM3159` → `X` (new), Astrion `AON-PP23` → `X`, VASTHERA `VTA-27` → `X` (previously `NA`; investigation_note-based classification is not guaranteed byte-identical across reruns), Curacle `CU71` → `NA`, CK Regeon `CKR-492` → `NA`, Vivozon `VVZ-3416` → `NA`, Illimis `ILM01_20260621` → `NA` (unchanged) (`json/pipeline-records.json`).
- Pipeline save badge no longer overloads the status pill with a second fact: it always reads plain "저장 완료" now, and when the wiki export was deferred (the normal case — see `deferred_markdown_exports()` in `main.py`), a small note in the same muted `.input-validation-summary span` style as the "N건 · 오류 · 경고" line is appended instead: "Wiki Map은 Wiki Map 탭에서 새로고침하면 최신화됩니다" (`renderInputValidation`, `src/app.js`). Bumped `app.js` cache-busting query string (`index.html`).
- Team Review Workspace drag-and-drop uploads: the Contact History and Due Diligence upload icons, the DD status pill (Due Diligence 의견/자료 보유 row), and the four Partner Materials pills (NCDP/CDP/ADMET/IR) now each accept a file dragged directly onto them, reusing each target's existing upload function and "업로드 중…" status/progress-modal handling — no new upload path was added. Added a shared `wireAttachmentDropTarget` helper (`src/detail.js`) that highlights the hovered target with a `.is-dragover` class while a file is dragged over it and dispatches the drop to that target's existing handler; it mirrors each target's own click-time permission gate (disabled state for the Partner Materials pills, an explicit admin check for the DD pill since it has no disabled state) so drag-and-drop can't bypass a restriction the click path already enforces. Added matching `.is-dragover` highlight/brief-enlarge styles for the small icon buttons (`.panel-icon-button`), the DD pill (`.qualitative-criterion-status-pills button`), and the Partner Materials pills (`.oi-material-toggle`, using each pill's existing per-category `--material-color`) (`src/styles.css`). Bumped the shared `styles.css`/`detail.js` cache-busting query strings (`index.html`, `detail.html`, `triage_detail.html`, `tests/test_dashboard_ia.py`).
- Shortlisting table's second header row (Filter 2 / Total Score / Filter 3 / DD / In-vivo / In-vitro / ADMET / Action date, plus D·Link) now has `text-align: center` on the `<th>` itself, not just on the `<button>`/`<div>` wrapper inside it (`.pipeline-score-row th` in `src/styles.css`). The inner elements were already centered, so this is a defensive, explicit guarantee at the cell level rather than a behavior change; verified in a live browser (wide viewport, login overlay removed) that all of these titles render centered under the FULL SCOUT / SHORTLISTING group headers.
- Shortlisting table default sort order changed from "most recently added to Shortlisting" to a two-tier priority sort: rows with the yellow (`priority`) ★ tracking status now always sort above rows with the gray (`stationary`) ★ tracking status, and within each of those two groups rows sort by Total Score descending (cumulative, not reset per group). Implemented as a new synthetic `focusPriority` sort key — `focusPriorityRank` (0=priority, 1=stationary) computed per row (`src/app.js` row-mapping, ~L2849), a dedicated comparator branch in `getVisibleRows()` (rank first, then `focusTotalScore` descending, ignoring the asc/desc toggle since there is no visible column for this key — same pattern as the `focusAddedAt` default it replaces), and updated as the default/fallback sort for the `focus` (Shortlisting) table mode in `initialSort` and `normalizeSortForMode()`. Other column headers remain independently clickable/sortable as before (`src/app.js`). Bumped `app.js` cache-busting query string (`index.html`).
- Fixed a false-positive in the In-vivo/In-vitro auto-classifier's text-fallback path (`classify_evidence_presence`, used when a record has no DeepSeek-analyzed attachment): 1ST Biotherapeutics `1ST-104` showed In-vivo `O` even though its own report text says "in vivo efficacy...가 없으며" (no completed in vivo efficacy data) — the bare word `efficacy` sat on the positive-cue list and fired regardless of the Korean negation next to it, because the negation-cue list only matched exact strings like `없음`/`확인되지` and missed the conjugated forms actually used (`없으며`, `되지 않았고`). Removed ambiguous topic-only nouns (`efficacy`/`activity`/`potency`/bare `확인`/`효과`/`효능`/`활성`) from `EVIDENCE_POSITIVE_CUES` — they name the study, not a result — and broadened `EVIDENCE_NEGATION_CUES` to match Korean negation stems (`없\w*`, `않\w*`, `못\w*`, `미\s*확립/완료/실시/...`, `아직`, `부재`, `예정`) plus English patterns like `still required`, `to be established`, and contractions (`main.py` ~L4189-4204). Re-running `auto_detect_evidence_fields` on the 1ST-104 record now correctly returns In-vivo `X`. Added 3 regression tests covering the exact bug, a "still required" negative phrasing, and a genuine positive-result sentence (`tests/test_admet_canonical25.py`); `test_admet_canonical25.py` + `test_oi_partnership_criteria.py` (34 tests) pass. This only affects records with no completed DeepSeek document analysis (`filter3_document_analyses` empty); records with an analyzed Partner Material already used the structured verdict and were unaffected. Any other Full Scout record already sitting on an auto-classified In-vivo/In-vitro badge should be spot-checked, since a change to this regex can flip badges the next time auto-detection re-runs (e.g. on a Filter 3 criteria refresh) for records that were previously misclassified the same way.
- Fixed D·Link header hover tooltip and added per-value explanations: the previous D·Link filter rework (see entry above) intended to move the descriptive text into the filter header's hover title via `FOCUS_HEADER_FILTERS.diseaseLinkage.description`, but `focusFilterHeader()` never actually read that `description` field — it always fell back to the generic `"D·Link filter"` string regardless. Fixed `focusFilterHeader()` to use `definition.description` as the tooltip/aria-label base (falling back to the old generic string only if a filter entry has no description), and reworded the description to the simpler "MoA가 실제 질환 병리·기능과 연결됐는지 표시". Also added plain-language explanations for the three cell-level values shown under D·Link: `DISEASE_LINKAGE_STATUS_OPTIONS` now carries a `title` per option (O: 확인됨, X: 미확인, N/A: 판단 불가, each with one added clause of context) rendered as native `<option title="...">` attributes so hovering an open dropdown's individual O/X/N/A entries shows its meaning, and `diseaseLinkageStatusMeaning()` (the tooltip for the select's current value) was reworded to lead with the same short label (`src/app.js`). Bumped `app.js` cache-busting query string (`index.html`).
- D·Link header gains the same O/X status filter popup as ADMET/In-vivo/In-vitro/DD: added a `diseaseLinkage` entry to `FOCUS_HEADER_FILTERS` (`kind: 'status'`), switched the header markup from a bare `plainHeader` `<span>` to `focusFilterHeader('D·Link', 'diseaseLinkage', 'diseaseLinkage')`, added `diseaseLinkage: []` to `emptyFocusFilters()`, and added a `statusMatches('diseaseLinkage', row.diseaseLinkageStatus)` clause to `focusFilterMatches()` (`src/app.js`). The long inline tooltip text moved into the filter popup's description line (shown when the filter is opened) instead of a hover title, matching how the other filterable headers already work. Removed the now-unused `.disease-linkage-head`/`.disease-linkage-head span` rules (`src/styles.css`) since the header cell now renders through the shared `.score-filter-header` markup. Verified in a live, logged-in browser session: the popup renders identically to ADMET/In-vitro's ("표시 상태 · 복수 선택 가능", O/X toggle buttons, 전체/완료), and selecting `O` narrowed the Shortlisting table from 10 to 3 rows, all showing D·Link = O.
- Total Score circle's tone thresholds now mirror Filter 2's PASS/REVIEW/FAIL total_score cutoffs (`json/schema.md`): previously `totalScoreEditCircle()` colored the circle relative to `row.maxScore` (21) — green only at the literal max (21), yellow from 12.6 up, red for everything else — so e.g. both a score of 10 and a score of 8 rendered red with nothing distinguishing them. Changed the thresholds to fixed cutoffs matching Filter 2: green (`high`) at `>=14`, yellow (`mid`) at `9-13`, red (`low`) at `<=8` (`src/app.js`). This colors the score purely from the number itself — it does not consult the row's actual Filter 2 value, so a row can legitimately show a green 14 next to a manually-overridden red T·Down Filter 2 badge (verified live: row with score 14 and Filter 2 = FAIL renders `high`/green on the score and `fail`/red on Filter 2) — expected per the request, since Filter 2's real derivation also weighs Target Relevance/MoA/Data/hard-blockers, not total_score alone.
- Shortlisting table's "Filter 2" column header sort arrow now re-applies the yellow-priority/gray-stationary grouped sort (the `focusPriority` key added above) instead of sorting by the Filter 2 PASS/MONITOR/T·Down rank: `sortableHeader('Filter 2', 'filter2', 'filter2')` → `sortableHeader('Filter 2', 'focusPriority', 'filter2')` (`src/app.js`, `renderFocusTable`'s header row). This gives a one-click way back to the default grouped view after sorting by any other column (e.g. Company, Total Score) — clicking it once always snaps to priority-group-desc regardless of the previous sort. Added `focusPriority` to `DESCENDING_FIRST_SORT_KEYS` so the arrow's first click lands on the grouped/score-desc view (matching the default), a second click reverses both the group order and the score order (stationary group on top, ascending score), and a third click clears sorting entirely — the same three-state cycle every other sortable column already has. The Filter 2 status cell/select itself is unaffected; only the header's sort binding changed, so Filter 2 values can no longer be sorted by their own rank from this header (`getVisibleRows()`'s comparator for `focusPriority` was extended to flip both the group-rank and score comparisons together based on `state.sortDirection`) (`src/app.js`). Bumped `app.js` cache-busting query string (`index.html`).
- Filter 2's three statuses are now labeled PASS / MONITOR / T·Down instead of PASS / REVIEW / FAIL everywhere a reviewer sees them — the Shortlisting/Full Scout table's editable status select (and its read-only Fast-Triage-completed variant), the static status pill, the top "Filter 2" multi-select filter menu, the Summary Dashboard KPI cards and donut chart legend (`distributionDisplayLabel`), the "우선순위" workflow list badge, the record detail page's Filter 2 decision `<select>` (`detail.html`) and its "Filter rationale"/"Pipeline Filter" summary strings, and the Team Review edit-history audit trail's before/after value for `filter_status` changes. This is a display-only relabel: `hard_filter.status` stays `PASS`/`REVIEW`/`FAIL` in `json/pipeline-records.json`, the JSON schema, `main.py`'s derivation logic, sort ranks, CSS tone classes (`.pass`/`.review`/`.fail`), and the GPT authoring instructions (copied-to-clipboard prompts) — none of that needed to change, only the text shown to a human. Added a small `filter2StatusLabel(value)` helper (`{ REVIEW: 'MONITOR', FAIL: 'T·Down' }`, passthrough otherwise) duplicated in `src/app.js` and `src/detail.js` (the two are separate script contexts with no shared module) and wired it into every display site above. Also updated the "판단근거" criteria guide's Final Status cards (Korean in `index.html`, English in `src/criteria-guide-i18n.js`) to say MONITOR/T·Down instead of REVIEW/FAIL, keeping the underlying `data-full-status="review"/"fail"` attributes unchanged since nothing reads them for matching. Verified live: table select options render `PASS`/`MONITOR`/`T·Down` while `option.value` stays `PASS`/`REVIEW`/`FAIL`; a row with `filter2=FAIL` shows `T·Down`. Bumped `app.js`/`detail.js` cache-busting query strings (`index.html`, `detail.html`).
- Investigated a report that the Full Scout detail page's 판단근거 (Scoring Guide) only showed Korean with no English toggle, unlike the home dashboard. Live-tested it directly (Threebrooks `3BT-1`, both the Full Scout and Shortlisting scopes): the KR/EN toggle and English content (`src/criteria-guide-i18n.js`, wired into `src/detail.js` since 2026-09-04) both work correctly once the page is freshly loaded — the browser tab used for testing was serving a stale cached copy of `detail.html` from before that day's changes and only showed the current content after a hard reload, which is the most likely explanation for what was seen. While checking, found and fixed two real staleness bugs the PASS/MONITOR/T·Down relabel (see entry above) missed: (1) the 판단근거 button's own hover tooltip still read "PASS/REVIEW/FAIL" on both the dashboard (`index.html`) and detail page (`detail.html`) — updated both to "PASS/MONITOR/T·Down"; (2) `detail.js`'s `CRITERIA_DRAWER_SCOPE_LABELS` eyebrow text was doubly stale — `full` still said `"TAB2 · Full Scout (PASS/REVIEW/FAIL)"` and `focus` still used the pre-rename term `"TAB3 · 집중관리 (OI Partnership Type)"` — updated both to match the dashboard's current wording (`"TAB 2 · FULL SCOUT · SCORING GUIDE"` / `"TAB 3 · SHORTLISTING · DECISION GUIDE"`). Confirmed no other screen (`triage_detail.html`'s Fast Triage guide uses SELECT/REJECT/INSUFFICIENT, unaffected by the relabel) still carries the old wording. Bumped `detail.js`'s cache-busting query string (`detail.html`) so any browser still on a stale cached copy is forced to reload the fix.
- Widened the Shortlisting table's Filter 2 column (`MONITOR` was getting visually clipped in its `<select>`): `FOCUS_DEFAULT_COLUMN_WIDTHS.filter2` 62→82, `FOCUS_MIN_COLUMN_WIDTHS.filter2` 54→68. Also bumped `totalScore` there (62→92 / 52→76) once its header picked up the same two-button (label+sort) popup layout as Filter 2 below — a plain sort button fit "Total Score" fine at 62px, but the trigger+sort-button pair left too little room and clipped the leading "T" (`src/app.js`).
- Added a Total Score filter to both the Full Scout/Fast Triage table (header label "Total") and the Shortlisting table (header label "Total Score") — same popup family as the TAR/MoA/.../Market score filters (`SCORE_HEADER_FILTERS`, `scoreFilterHeader`), not the O/X status-filter one. Total Score spans 0–21 rather than 0–3, so listing every value the way the criteria filters do would be unusable; instead added a `TOTAL_SCORE_FILTER_OPTIONS` list with just the three tiers that already drive the score circle's color (see above): "Total Score ≥ 14", "Total Score 9–13", "Total Score ≤ 8" (values `gte:14`/`range:9:13`/`lte:8`). `renderScoreHeaderFilterPopover()` now branches on `active.key === 'totalScore'` to render that single button group instead of the free-text expression row + "점수 이상"/"정확한 점수" grids the 0–3 criteria use (a `range:a:b` filter value isn't expressible through that expression parser anyway). `scoreFilterMatches()` gained `lte`/`range` operator handling (`gte`/`eq` already existed) — generalized, so it still matches the 0–3 criteria filters unchanged. `scoreFilterHeader()` gained an optional 4th `filterKey` param (defaults to `sortKey`) so the Shortlisting table's Total Score header can keep sorting via its existing `focusTotalScore` key while filtering via the shared `totalScore` entry in `SCORE_HEADER_FILTERS` (`src/app.js`). Verified live: selecting "≥ 14" narrowed Full Scout from 28 to a subset where every visible Total Score is ≥14.
- Added Filter 1 / Filter 2 / Filter 3 header-click status filters, matching the existing ADMET/In-vivo/In-vitro/DD popup UI exactly ("표시 상태 · 복수 선택 가능", O/X-style toggle buttons, 전체/완료). Two different mechanisms, reusing whichever existing filter state each column already had rather than adding a second, out-of-sync copy of the same selection:
  - Filter 1 (Fast Triage table) and Filter 2 (Full Scout table) share one dynamic header column (`activeFilterKey()`/`activeFilterLabel()` pick whichever applies) already backed by `state.pass` — the same state the pre-existing "Filter N" multiselect above the table reads/writes. Added a new popover (`passFilterPopoverElement`/`renderPassHeaderFilterPopover`/`togglePassHeaderFilter`/`commitPassHeaderFilter`, `#passTableFilterPopover`) styled identically to the DD/ADMET one but backed by `state.pass` instead of `state.focusFilters`, with a new `activeStatusFilterOptions()` helper (extracted from the inline options list `renderFilters()` already built) supplying the right option set per mode (SELECT/REJECT/INSUFFICIENT, PASS/MONITOR/T·Down, or the OI Partnership values). The header itself now renders via a new `passFilterHeader(label, sortKey, columnKey, attrs)` in place of `sortableHeader(...)`. Because opening this popup writes back into the exact same `state.pass` array, it stays in sync with the top multiselect in both directions.
  - Shortlisting's own Filter 2 column (separate from Filter 3, since `state.pass` there is already spoken for by Filter 3/OI Partnership) reuses the *existing* DD/ADMET machinery instead: added a `filter2` entry to `FOCUS_HEADER_FILTERS` with its own `options: [PASS, MONITOR, T·Down]`, and generalized `renderFocusHeaderFilterPopover`/`focusFilterSelections`/`focusFilterSummary` to read a per-entry `options` list (falling back to the existing O/X `FOCUS_STATUS_FILTER_OPTIONS`) instead of hardcoding O/X for every "status"-kind filter. `focusFilterHeader()` gained an optional 4th `sortKey` param (defaulting to its old `${filterKey}Status` derivation) so this column's header can keep sorting via the existing `focusPriority` composite sort while filtering via the new `filter2` entry. Added `statusMatches('filter2', row.filter2)` to `focusFilterMatches()`.
  - Filter 3 (Shortlisting, OI Partnership) uses the same `passFilterHeader`/`state.pass` mechanism as Filter 1/2 above, since `activeFilterKey()` already resolves to `'filter3'` in focus mode.
  - All three headers (`Filter 1`, `Filter 2` in the Full Scout/Fast Triage table, `Filter 3`) render through the shared `.score-filter-header`/`.score-filter-header-controls` flex markup, which was already centered (`justify-content: center`) for the DD/ADMET/etc. headers — so converting these to the same markup centers "Filter 1"/"Filter 2" in the Full Scout/Fast Triage table's single header row as a side effect, without touching the still-left-aligned Company/Location/Asset/etc. headers around them (Shortlisting's own Filter 2/Filter 3 were already centered from an earlier entry in this log).
  - Caught mid-implementation that the Full Scout/Fast Triage table's header is actually built by `renderTable()` (called everywhere), not `renderTableLegacy()` (confirmed zero callers — dead code, apparently superseded some time before today and never deleted); the first pass edited the dead copy and had no visible effect (`Filter 1` never appearing in Full Scout, `Filter 2` staying a plain sort button, confirmed by inspecting the live DOM), caught by testing in a real browser rather than trusting the source edit alone. Fixed by editing `renderTable()`'s single dynamic `passFilterHeader(filterLabel, filterKey, filterKey, 'rowspan="2"')` call instead; `renderTableLegacy()`'s now-inconsistent copy was left alone since it has no callers and touching it further is out of scope.
  - `src/app.js` only. Verified live in both directions: Fast Triage's Filter 1 popup lists SELECT/REJECT/INSUFFICIENT, Full Scout's Filter 2 popup lists PASS/MONITOR/T·Down, Shortlisting's Filter 3 popup lists 투자/Value Up/공동연구/Unknown/N/A, and Shortlisting's own Filter 2 popup also lists PASS/MONITOR/T·Down; selecting a value in each narrowed the table to matching rows only, and all three now read as centered rowspan cells in the Full Scout/Fast Triage table's header row.
- Followed up on the earlier 판단근거 caching investigation with a full audit of every remaining PASS/REVIEW/FAIL string and every 판단근거-serving page, per a direct request to check for more of the same:
  - **Root-caused the caching class of bug instead of only bumping one version string.** `index.html`, `detail.html`, `triage_detail.html`, `onboarding.html`, `wiki_view.html`, and `user_admin.html` are all served via bare `FileResponse(...)` in `main.py` with no `Cache-Control` header, so browsers fall back to heuristic caching and can keep showing an old copy of the page itself (not just its linked JS/CSS, which already had `?v=` cache-busting) until a hard refresh — this exact behavior was reproduced live in a test tab during the previous fix. Added a shared `NO_CACHE_HTML_HEADERS = {"Cache-Control": "no-cache"}` and passed it to all six `FileResponse` calls (`main.py`); `no-cache` (not `no-store`) still lets the browser reuse a cached copy after a fast conditional revalidation against the `ETag`/`Last-Modified` `FileResponse` already sends, so this is not a "reload every asset every time" cost — verified live via `curl -D-` that `/detail` now returns `cache-control: no-cache`. This removes the whole class of "edited an HTML page and the browser won't show it" bug going forward for all six pages, not just the one this session happened to hit.
  - **Found 3 more stale PASS/REVIEW/FAIL strings the original relabel missed**, all in the deterministic (non-GPT-authored) hard-filter reason text shown verbatim in the record detail page's "Filter rationale" panel: `main.py`'s `calculate_latest_full_scout_filter()`, `src/app.js`'s client-side mirror of the same derivation, and `src/detail.js`'s own copy all built the MONITOR-range sentence as `"Total score {total} is REVIEW range 9-13"` — changed all three to `"...is MONITOR range 9-13"`. `hard_filter.status` itself is untouched (`"REVIEW"` is still the correct stored value) — only this one human-readable reason sentence, generated fresh on every rubric recalculation, was affected. While comparing the three copies, also fixed an unrelated pre-existing typo in `src/detail.js`'s PASS-reason sentence (`"TAR {targetScore} >= 2"`, should read `>= 3` to match the actual gate and the other two copies exactly).
  - **Fixed the static/fallback 판단근거 content baked into `detail.html`** (`#criteriaDrawerBody`'s initial markup, shown only until the async fetch-from-dashboard sync completes or as a fallback if that fetch fails): the `full`-scope PASS/REVIEW/FAIL prose, the `aria-label="PASS REVIEW FAIL criteria"`, and the REVIEW/FAIL article headings all still used the old labels; relabeled all of them to PASS/MONITOR/T·Down (`detail.html`). Confirmed the static `focus`-scope (OI Partnership) fallback section already says "v1.7", matching the current live version, so no change needed there.
  - **Confirmed clean, no change needed**: `index.html`'s own criteria-guide markup (zero REVIEW/FAIL matches left after the earlier relabel pass); `triage_detail.html`'s 판단근거 (Fast Triage uses SELECT/REJECT/INSUFFICIENT, a vocabulary this relabel never touched); `scripts/export_pipeline_wiki.py`'s only REVIEW-like hit is the unrelated `HAS_REVIEW` graph-edge-type constant (a Team Review comment link, not Filter 2 status).
  - **Two remaining places intentionally still show the raw `PASS`/`REVIEW`/`FAIL` values, left as-is** since they read the canonical schema value by design rather than a UI label, matching how `json/pipeline-records.json`/`json/schema.md` are documented: the Excel export (`exportPipelineTable()` in `src/app.js` writes `row.filter2` — the raw status — into the "Filter 2" column) and the Wiki/Scorecard export (`scripts/export_pipeline_wiki.py:794` writes `hard_filter.status` verbatim into each note's "Hard Filter" table row). Flagging both here in case the intent is actually to show MONITOR/T·Down in those human-facing exports too — that would be a deliberate follow-up decision, not an oversight fix.
  - Bumped `app.js`/`detail.js` cache-busting query strings (`index.html`, `detail.html`).
- Diagnosed and fixed why Tab 0 (진척 현황)'s Summary Dashboard sat on "0 0 0 0" for a long time on load: profiled `GET /api/candidate-queue/progress` (cProfile against the live function) and found it takes **~9.5s** in production, versus ~0.15-0.2s for `/api/records` and `/api/dashboard-summary`. Root cause: `get_candidate_queue_progress()` loops `for group in groups: for entry in queue: find_matching_identity_group(...)`, and `find_matching_identity_group()` recomputes `asset_aliases_from_text()`/`company_aliases_from_text()` (regex-heavy Unicode normalization) from the entry's raw text on **every (group, entry) pair** — 218,492 calls in the profiled run — even though a queue entry's alias set never changes across groups. The candidate queue (`json/candidate-queue.json`) has grown to **1,349 entries** (mostly distinct real Listing candidates, not duplicates — only 8 asset/company pairs repeat), so this is a genuine O(groups × queue) blowup, not a one-off fluke.
  - Fix: split the matching predicate out of `find_matching_identity_group()` into a new `identity_aliases_match_group(asset_aliases, company_aliases, group)` that takes precomputed alias sets instead of raw strings (`main.py`); `find_matching_identity_group()` now just computes aliases once and calls it in a loop, preserving its existing public behavior/signature for its other call sites. `get_candidate_queue_progress()` now precomputes each queue entry's alias sets **once** into a `queue_aliases` dict before the group loop, turning O(groups × queue) alias recomputation into O(queue) — the per-group inner loop is now cheap set intersections only. Verified: cProfile went from 21.7s/218,492 `find_matching_identity_group` calls down to 2.4s with that function no longer appearing in the top 25 by cumulative time; the live endpoint (repeated `curl` timing) went from ~9.5s to a consistent **~0.65-0.8s**.
  - Also found and fixed a second, compounding bug while investigating: the Step 0 Summary Dashboard's loading-skeleton CSS (`.step0-summary-dashboard.is-loading`, dimmed stat values + a "loading" note — this already existed and looks reasonable) was wired to `loadRecords()`'s `setStep0SummaryLoading()` calls, but that panel's numbers are entirely fed by `loadStep0Progress()` (a separate fetch) — and `loadRecords()` resolves in ~0.2s regardless, so the skeleton cleared almost immediately while the *actually* slow fetch was still running, leaving the stat strip showing plain `0 0 0 0` with no loading indication for the rest of the wait. Moved `setStep0SummaryLoading(true/false)` into `loadStep0Progress()` itself (only on the true first load, not on a quiet `renderOnlyWhenChanged` background refresh, so it doesn't flash a real number back to "loading"), and removed the now-mismatched calls from `loadRecords()` (`src/app.js`).
  - Applied the same "paint the fast summary the moment it's ready, don't make it wait on the slower full list" principle inside `loadRecords()` itself (Tabs 1–3's Summary Dashboard): previously `await`ed `loadCategorySynonyms()` then `Promise.all([fetch(API_URL), refreshDashboardSummary()])`, only calling `render()` (which repaints metrics + charts + the whole pipeline table together) once *both* finished. Now `refreshDashboardSummary()` calls `renderMetrics()` + `renderCharts()` itself as soon as it resolves, independent of the full records fetch; `render()` still runs afterward once records arrive, for the table and everything else. `/api/records` and `/api/dashboard-summary` are both already fast today (confirmed via `activeTabSummary()`/`renderWorkflowPriorityList()` reading entirely from the backend-provided summary object, not from `state.rows`, so this is safe to call before records exist), so this doesn't fix an existing complaint on those tabs — it's the same fix applied preventatively, matching the general "show numbers first, let the table settle in behind them" pattern that was requested, in case the records list gets slow as it grows (`src/app.js`).
  - Changed the default page size for the Pipeline Table (Tabs 1–3) from 10 to 50 rows, per request (`DEFAULT_PAGE_SIZE` in `src/app.js`) — a pure preference change, since with ~28 current records this doesn't move any performance needle by itself; anyone with an existing `localStorage` page-size preference keeps it unaffected.
  - Not changed, flagged for awareness: `json/candidate-queue.json` holding 1,349 largely-distinct entries (2.2MB file) is a real, large backlog rather than test junk, so no data was touched — but it does mean this endpoint's cost will keep scaling with however large that backlog grows; if it grows another 10x, the O(groups × queue) set-intersection sweep itself (not just the alias computation this fix removed) may eventually need attention too (e.g. periodic cleanup of already-matched queue entries per `DELETE /api/candidate-queue/{queue_id}`, or indexing groups by alias instead of a linear scan).
- Split the Step 0 stat-strip numbers off from the full progress table fetch so the dot/count-up animation can start before the (much larger) table finishes, per follow-up feedback that the animation was still waiting on the whole table load even after the ~9.5s→~0.7s fix above. Added `GET /api/candidate-queue/stats` (`main.py`): same groups/queue matching loop as `get_candidate_queue_progress()` (reusing the now-cheap `identity_aliases_match_group` set-intersection matching), but skips all the per-row Listing-details/metadata/comment-feed construction that endpoint does for every one of its ~1,300+ rows — it never touches `candidate_queue_entry_metadata()`/`pipeline_human_comment_feed()`/`normalize_listing_details()`, which is where most of the *remaining* per-request cost was going. Measured consistently **~0.2-0.25s**, versus ~0.3-0.8s for the full endpoint (both vary with concurrent dev-server load, but stats reliably resolves first).
  - `loadStep0Progress()` now fires this stats-only fetch in parallel the moment a true first load starts (not on a quiet background `renderOnlyWhenChanged` refresh), and calls `renderStep0StatStrip()` as soon as it resolves — guarded so a late response is a no-op if the full progress fetch already finished first (`requestId` + `state.step0Loaded` check), so the real, filter-aware numbers always win over the approximate early ones (`src/app.js`).
  - `renderStep0StatStrip()` normally recomputes its counts from `state.step0Rows` filtered by whatever search/status/stage filters are active (a "current Tab·Filter 기준" number) — but that row list doesn't exist yet on first paint. It now falls back to the stats-only endpoint's unfiltered `state.step0Stats`/`state.step0RecentStats` totals whenever `state.step0Loaded` is still `false`, and switches back to the filtered-row computation the instant the full table arrives — so the two numbers only ever differ in the brief window before the table loads, and only if a filter happens to already be active that fast (`src/app.js`).
  - Verified via direct endpoint timing (`curl`, since headless-browser `requestAnimationFrame` throttling made in-page animation timing an unreliable signal in this environment — the fetch/render chain itself was confirmed to fire correctly via `fetch` response inspection) that `/api/candidate-queue/stats` consistently returns the exact same `stats`/`recent_15_days` values as the full endpoint, ahead of it.
- Per further feedback, even the fast stats-only fetch above still means the very first paint of any page load waits on a network round trip — added a localStorage cache (`skbp.dashboard.step0StatsCache.v1`) of the last-seen stat totals so the count-up can start with **zero network wait** using last session's numbers, correcting itself once a real fetch (stats-only, then the full table) comes back. `activateStep0Panel()` reads this cache synchronously on the true first load of a page/session and renders immediately if present; `loadStep0Progress()` writes it every time either endpoint returns fresh totals, so it always reflects the last real data seen, not the render-time snapshot only (`src/app.js`).
  - Found and fixed why this didn't visibly do anything at first: `.step0-summary-dashboard.is-loading .step0-stat-value strong { color: transparent; ...skeleton pulse... }` — the loading-skeleton CSS made the stat numbers' text color transparent for the *entire* duration `.is-loading` was set, and that class was only cleared once the full progress table finished (by design, since it also dims the workflow-card-canvas graph, which does need the full table). So the numbers were already being counted up in the DOM the whole time — just invisibly. Split the stat-value skeleton onto its own `.is-stats-loading` class/toggle (`setStep0StatsLoading()`, `src/styles.css` + `src/app.js`), cleared independently and as soon as *any* stats are on hand (cache, the fast endpoint, or the full table) — `.is-loading` itself keeps gating only the loading-note badge and the graph-canvas dimming, tied to the full fetch as before.
  - Per "nobody actually clicks a dot to see which drug it is, that's not the point" feedback: the workflow dot-cloud under each stat card (`renderStep0WorkflowMap()`) was *also* waiting on the full table, since every dot's tooltip needs a real asset/company name from a row. Added `renderStep0WorkflowMapPlaceholder(stats)`, which draws the same dot cloud sized correctly from just the stat counts, using empty placeholder rows (the existing per-dot code already falls back to a generic "Unnamed pipeline" label for a row with no asset/company, so no changes needed there) — wired into the same cache-hit and fast-stats-fetch paths as the numbers, and swapped for the real, correctly-labeled cloud the moment the full table arrives (`src/app.js`).
  - Verified live: on a repeat visit (cache present), `getComputedStyle(...).color` on the stat-value text was already a real color (not the transparent skeleton) and the dot cloud already had the full dot count within ~1s — both well before `.is-loading` (and the table) cleared several seconds later.
- Two follow-up fixes to the count-up animation itself, from watching it run: (1) it was visibly resetting to 0 and replaying a second time once the full table finished loading, even though the total hadn't changed — traced to `renderStep0StatStrip()`'s replay-skip check comparing `{stats, recent}` together, where `recent` is computed two different ways (the backend's `recent_15_days` aggregate before rows load, `step0FilteredStageStats()`'s own per-row rescan after) and routinely disagrees by a little even when the 4 headline `stats` numbers are byte-identical — comparing only `stats` fixed the false mismatch. (2) Slowed the count-up itself down (1120ms → 2600ms per card, and the matching "▲ N 증가" badge counter 1280ms → 2600ms) so the number is still visibly climbing for a while instead of finishing almost instantly and then sitting idle waiting on the network, per request to make it feel less rushed while data is still loading (`src/app.js`).
- Found the actual cause of "the number finishes, the dots all disappear, then reappear from scratch once the table loads": both `renderStep0WorkflowMap()` and `renderStep0WorkflowMapPlaceholder()` unconditionally tear down and rebuild the *entire* dot cloud on every call (`destroyStep0WorkflowGraph()` + fresh `innerHTML`), and every dot's CSS entrance animation starts at `opacity: 0` for its own staggered entry delay (up to ~2s for the last stage) before fading in — so any re-render, not just the cache→fast-stats→full-table sequence's later calls, briefly wipes the whole cloud and replays the entrance from zero, i.e. exactly the disappear-then-refade the report described. Added the same "skip if nothing actually changed" guard the stat strip got, but keyed on **per-stage dot counts** rather than deep row equality (`step0WorkflowStageCounts()`/`step0WorkflowMapLastCountsKey`) — placeholder rows and real rows are never `===` equal even when the count is identical, since real rows carry actual asset/company names, so a count-only comparison is what actually lets "placeholder count == real count" skip the rebuild. Both dot-cloud renderers and `renderStep0StatStrip()`'s replay-skip now also get force-reset (`step0WorkflowMapLastCountsKey = null` / `step0StatStripLastRenderKey = null`) on every Tab 0 re-entry after the first load, preserving the original "replay the entrance animation when coming back to this tab" behavior — the skip is only meant to suppress *redundant* replays within one load sequence, not the deliberate one on revisit (`src/app.js`).
  - Also added the "slow only the first time, fast after" rule to the number: a real correction (the rare case a later fetch disagrees with what's already shown) now animates in 500ms instead of replaying the full 2600ms leisurely climb — only the very first render of a load sequence gets the slow duration (`isFirstRender` = `step0StatStripLastRenderKey === null`, `src/app.js`).
  - Verified live: sampled dot count every 200ms across a full load (cache → placeholder → real table) and found zero drops in dot count anywhere in the sequence — the cloud is drawn once and never rebuilt while the numbers climb and settle beside it.
- Made the placeholder dot cloud visibly read as "still loading" instead of just sitting still once its one-shot entrance finishes: added a `.step0-workflow-dot.is-placeholder` CSS modifier (dimmer, desaturated, and its existing settle-wobble keyframe set to `infinite` instead of `1` iteration) applied to every dot drawn by `renderStep0WorkflowMapPlaceholder()`. `renderStep0WorkflowMap()`'s existing "skip rebuild when counts match" path now calls a new `step0WorkflowMapFinalizeDots()` instead of doing nothing — it strips `.is-placeholder` from the existing dots (no rebuild, no re-entrance), which lets each one's settle animation finish its current cycle and hold at rest while opacity/filter transition up to the sharp, final look. Net effect: the whole cloud gently wobbles in place, dim, for as long as the real table is loading, then visibly "comes into focus and settles" the moment it's ready — a clearer loading signal than a cloud that just goes static early and gives no indication anything is still happening (`src/styles.css`, `src/app.js`).
  - Verified live: while `.is-loading` was still set, a sampled dot's `getComputedStyle(...).animationIterationCount` read `"1, infinite"` (entrance once, settle looping) with reduced opacity; the instant the full table's fetch resolved (`.is-loading` cleared), the same dot read `"1, 1"` at full opacity — confirming the transition fires exactly at task completion, not on a fixed timer.
