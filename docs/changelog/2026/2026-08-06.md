## Changes

- Rebuilt all 33 records from Git source `19aa4b9` into `dashboard_hybrid_v1`: retained original Markdown, scores, TAB1 diligence, TAB3 state, score-hover fields, competitors/similar pipelines, and canonicalized repeated evidence into `validation.source_registry` + `source_ids`.
- Updated Compact v2 prompts, ingestion/save validation, JSON schema/docs, detail source-ID resolution, and both exporters for the hybrid contract while retaining legacy verbose, Compact v1, and earlier minimal-record read compatibility; non-string display-array items are now rejected at save time.
- Added dataset A/B, full graph parity, competitor-export content, malformed-field fallback, and raw/expanded Compact v1/v2 mixed-render tests; all 259 tests and JavaScript/Python syntax checks pass.
- Fixed TAB2 import compatibility found in the ILM01 and HY209 reports: legacy Compact v1 now reconstructs a missing `input`, Compact v2 ignores unsupported keys after preserving usable source references, source-ID table values and legacy competitor/fact aliases normalize safely, and `A. US TAP` / `B. US Unrisked` / `C. US Obtainable` no longer trigger a false warning; both supplied reports pass UI and FastAPI save validation and all 263 tests pass.
- Unified Full Scout Marketability around `D. Global Obtainable Peak Sales = C. US Obtainable Peak Sales × 1.5` across GPT instruction 2, TAB2 guidance, backend rubric, compact schema/storage, detail/hover displays, and ingestion normalization; backend backfill is limited to explicitly US, million-USD C values so ambiguous historical records and scores remain unchanged. Relevant syntax/schema checks and 149 regression tests pass.
- Added `config/rubric-release.json` as the canonical active Rubric Release manifest for workflow/schema versions, rubric paths, storage contract, and deterministic calculation constants; backend constants now load from it and release consistency tests guard the GPT guidance, UI labels, rubric documents, and Marketability multiplier against drift.
- Hardened all home-filter categories for legacy and new records: route/formulation-qualified Modality and clause-aware Stage values normalize consistently, Country aliases and legacy Theme/Cluster labels close into the dashboard taxonomy, and legacy blank main indications resolve only from an explicit lead clause or exactly one canonical candidate; multiple candidates and explicit `Unknown` remain unguessed. Both GPT prompts use the complete indication vocabulary and source-priority rule, Compact v2 rejects omitted/blank `main_indication`, and the Fast Triage Markdown table exposes Modality for source/JSON comparison.
- Added `Protein Homeostasis` as the third evidence-mapped R&D Theme across Fast Triage v3.3, Full Scout/판단근거 v3.4, the release manifest, schema, filters, charts, detail guidance, and the shared category dictionary; mapping requires direct proteostasis target/MoA evidence and uses `Unknown` Cluster until a sub-cluster taxonomy is approved.
- Added a manual, structural-only LLM second-pass reparse for the paste-JSON flow: a new `AI 2차 파싱` button calls `POST /api/records/llm-reparse`, which sends the pasted Markdown, best-effort JSON, and current validation issues to OpenRouter (`OPENROUTER_MODEL`) and returns JSON repaired strictly from the Markdown (missing values become `null`/documented defaults; `total_score`, `hard_filter`/`triage` status, and version fields are left untouched). Corrected field paths are stamped on save as `source_report.llm_reparse_fields`, and `apply_llm_reparse_disclaimer` then appends a small `> 이 정보가 부정확할 수 있습니다.` note to the end of that record's `source_report.raw_markdown`, surfacing automatically wherever the GPT report is rendered (detail page, report modal, exports). Updated `json/drug-valuation.schema.json` and `json/schema.md` for the new field; smoke-tested the endpoint against a live OpenRouter call and the disclaimer/allow-list logic directly.
- Made the `/api/records/llm-reparse` second-pass reparse self-improving: it now also asks the model to distill a generalized Korean caution sentence about the underlying simple-parsing/authoring mistake (JSON syntax, omitted required objects/keys, untranscribed Markdown facts) when one is identifiable, and persists it via new `append_instruction_warning`/`load_instruction_warnings` helpers into `config/instruction_warnings.json`, deduped and capped at 40 entries per workflow mode (triage/full). Added `GET /api/instruction-warnings` to serve the accumulated notes; `copyPromptToClipboard` in `src/app.js` now fetches them and appends them as a "반복 방지 주의사항" section to the end of both copied GPT instruction texts (지침 1 Fast Triage, 지침 2 Full Scout), and `runAiReparse` surfaces a preview message when a new note is added. Documented the mechanism in `CLAUDE.md`. Verified with `ast.parse`/`import main`, `node --check`, isolated Python checks of dedupe/cap/persist behavior against a temp file, and a live end-to-end OpenRouter smoke test confirming `new_warning` generation and storage.
- Auto-detects TAB1 (Fast Triage) vs TAB2 (Full Scout) from the pasted content itself so people stop pasting into the wrong tab and hitting spurious parsing errors: added `detectPastedInputMode` (reads the parsed top-level JSON shape — array vs single object — falling back to `detectInputRecordMode`'s field-level signals for the ambiguous single-item-array case) and `autoRouteDataUploadTab` in `src/app.js`, wired into the existing `gptResponseInput` `input` listener. When the pasted format disagrees with the currently active tab, it calls the existing `setTableMode()` to flip the whole dashboard to the matching tab, re-seeds that tab's draft with the just-pasted text (restoring the other tab's prior draft rather than letting it get overwritten), and shows a one-line "자동 전환했습니다" notice in the validation panel. No change to `main.py`; frontend-only. Verified with `node --check` and manual trace of the draft-swap logic (`renderDataUploadGuide`'s existing per-mode draft save/load); browser extension was unavailable this session for a live click-through.
- Switched `AI 2차 파싱` (llm-reparse) to a targeted patch when possible instead of always regenerating the whole record: added `resolve_llm_reparse_base_records` (best-effort parse of the pasted JSON via the existing `extract_first_json_value`) and `apply_dot_path_patch` in `main.py`. When the pasted JSON already parses (just missing/blank fields), `build_llm_reparse_prompt` now asks the model for a minimal `{"patch": {"0": {"dot.path": value, ...}}}` response instead of the full `{"records": [...]}` envelope, and `parse_llm_reparse_answer` applies each patched path onto a deep copy of the original parsed record — so every field the model doesn't mention is guaranteed byte-identical to the original, not just "probably unchanged." Falls back to the previous full-reconstruction envelope when the pasted JSON is too structurally broken to parse at all (e.g. unbalanced braces). `VALIDATION_ISSUES` (the dashboard's error/warning list) continues to be included verbatim in both prompt variants so the model fixes only what was flagged. Live-verified both paths: a valid-but-incomplete JSON payload produced a 2-field patch (`company_profile`, `structured_table.moa`) leaving `scoring.total_score` etc. untouched, and a genuinely unparseable payload (missing closing brace) correctly fell back to full reconstruction.
- Wired the AI 2차 파싱 button to the new `/api/records/llm-reparse/stream` SSE endpoint so raw LLM tokens render live in the validation panel as they arrive; the pasted-response textarea is disabled for the duration and re-enabled once the stream completes or errors. Added a lightweight field-level diff view (`renderAiReparseDiffPanel` in `src/app.js`): for each corrected dot-path, shows old value → new value as a compact card list (no code-editor/line-diff library), comparing the pre-reparse parsed record (`split.records`) against the returned patched/reconstructed record. Added `stream_openrouter_llm_reparse` (streaming variant of `call_openrouter_llm_reparse`) and the `/api/records/llm-reparse/stream` route in `main.py`, reusing the same `sse_event`/SSE-parsing pattern as `/api/chat/stream`. Added `ai-reparsing` status-pill state, `.input-validation-stream` (live token box), and `.ai-reparse-diff-panel/-row` CSS in `src/styles.css`. Verified with `node --check`, `py_compile`, and a live curl SSE call confirming token-by-token `delta` events followed by a correctly patched `done` payload.
- Pinned the `AI 2차 파싱` (llm-reparse) flow to its own primary model, `z-ai/glm-5.2`, independent of the chat assistant's `OPENROUTER_MODEL`: added `LLM_REPARSE_DEFAULT_MODEL` and `openrouter_reparse_models_to_try()` (reads `OPENROUTER_REPARSE_MODEL`/`OPENROUTER_REPARSE_FALLBACK_MODELS`, falling back to `z-ai/glm-5.2` + the shared free-model fallback list) in `main.py`, and switched `call_openrouter_llm_reparse` to use it instead of the shared `openrouter_models_to_try()`. Verified with `ast.parse`, a direct Python check that the reparse model list now starts with `z-ai/glm-5.2` while the chat model list is unchanged, and a live end-to-end call to `/api/records/llm-reparse` returning 200.
- Made the `AI 2차 파싱` button's LLM call clearly visible instead of appearing to hang: `runAiReparse` in `src/app.js` now swaps the button label to "AI 재파싱 중...", sets a new `ai-reparsing` status-pill state ("AI 재파싱 중 · LLM 호출 진행 중...", spinner icon, `aria-busy`), and shows an immediate "OpenRouter에 요청했습니다 · 최대 1~2분" banner in the validation panel while the request is in flight; on success it still re-runs `previewPastedReportParsing()` to re-validate and render the corrected input immediately, and on failure the OpenRouter/HTTP error text is now shown as a distinct "AI 재파싱 실패: ..." row instead of a generic message, with the button re-enabled for retry. Also fixed a pre-existing bug where the `finally` block force-enabled the button after every run, overriding the post-revalidation disabled state. Added matching `ai-reparsing` CSS states (light/dark) in `src/styles.css`. Root-caused a user-reported 502 as a transient OpenRouter response failure (not a broken route or stale server) by reproducing a successful live call against the running dev server; no backend change was needed for that. Verified with `node --check` and a direct curl call to `/api/records/llm-reparse` returning a valid 200 response.
