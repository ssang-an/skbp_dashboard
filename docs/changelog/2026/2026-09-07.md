# 2026-09-07

## Changes

- Shortlisting Project 설정 모달의 "구성원 추가" 후보 목록 체크박스가 브라우저 기본 크기 그대로 렌더링되어 12px 텍스트 대비 지나치게 커 보이던 문제 수정: `.shortlisting-member-picker-row input[type="checkbox"]`에 명시적 크기(14x14px)와 `accent-color`, `flex: none`을 지정(`src/styles.css`, `index.html` `styles.css` 버전 쿼리스트링 갱신).
- Tab0 "Step 0 진척 현황" pipeline table의 Evaluation Progress 서브헤더 버튼 라벨이 길어서 잘려 보이던 문제 수정: "Simple Research"/"Advanced Research"/"Custom Review"를 "Simple"/"Advanced"/"Custom"으로 축약(`index.html`, 툴팁·데이터 속성은 원문 유지).
- 상세 페이지의 "Custom" 패널(구 "Filter 3")을 멀티-Project Shortlisting에 맞게 전면 개편. 지금까지 `detail.js`는 어떤 Shortlisting Project에 트래킹돼 있든 무조건 OIC 기본 Project(`oic_default`)의 하드코딩된 Filter 3/DD/Action date UI만 보여줬는데, 이제 레코드가 실제로 트래킹된 Project(들)을 `GET /api/shortlisting/projects` + `meta.focus_management`/`meta.shortlisting_projects.<project_id>`로 판별해 그에 맞는 화면을 보여준다. 좌측 "Total Score" 라벨을 "Advanced Score"로 변경(값·수정 불가 여부는 그대로). 우측 헤더는 좌측 "Advanced"/"Filter 2" 레이아웃과 통일감 있게 "Custom" 제목 아래 한 줄에 Project명(또는 2개 이상 트래킹 시 드롭다운 스위처)과 상시 노출 "Custom Score"(0-9, 더블클릭 인라인 수정)를 나란히 배치했다. Hide 상태에서도 OIC는 Filter 3 분류·Action Date 두 항목이, 커스텀 Project는 `metric_columns` 중 처음 2개 지표가 계속 보이고(`.custom-metric-extra`/`.oi-classification-summary`·Owner·Plan만 Hide 시 숨김), Show를 누르면 나머지 세부 항목이 이어서 펼쳐진다. 커스텀 Project는 `metric_columns`를 타입별 편집 컨트롤(boolean/list/number/date/text, `app.js`의 `shortlistingMetricEditControl`과 동일한 컨트롤 구성)로 렌더링해 그 자리에서 값을 수정할 수 있다. 저장은 기존 `PATCH /api/records/{id}/focus-management`(OIC) 또는 `PATCH /api/records/{id}/shortlisting-projects/{project_id}`(커스텀 Project) 엔드포인트를 그대로 재사용하며 백엔드 변경은 없음(`detail.html`, `src/detail.js`, `src/styles.css`). 후속 보정: Show로 펼쳤을 때 Filter 2 쪽 reason textarea가 커지면서 Custom 칼럼이 같은 높이로 stretch되고, 그 여유 공간이 내부 auto 행들에 고르게 분배되어 "CUSTOM" 헤더가 아래로 밀려 보이던 버그를 `.review-info-column`에 `align-content: start`를 추가해 수정(collapsed 상태에서만 이 값이 적용되고 있던 누락을 보완). 관련 회귀 테스트 `tests/test_dashboard_ia.py`의 `detail.js`/`styles.css` 버전 문자열 assertion 4건과 `renderCollaborationPanel`→`renderCustomProjectPanel`로 이동한 OI Partnership origin 텍스트 assertion 1건을 갱신. 브라우저에서 OIC 단독 트래킹/커스텀 Project 단독 트래킹/3개 Project 동시 트래킹(OIC+test+test2) 레코드 각각에 대해 Show/Hide 토글(헤더 고정 확인 포함), 지표 값 수정, Custom Score 수정, Project 스위처 전환까지 end-to-end 검증, 콘솔 에러 없음.
- Full Scout 상세 페이지 "TEAM REVIEW WORKSPACE" 헤더의 즐겨찾기(별) 버튼 오른쪽 숫자 배지(`#detailCommentCount`)를 팀 댓글 개수 대신 레코드에 업로드된 **총 첨부파일 수**(Partner Materials + DD Report + Contact History 첨부 전체, `record.meta.attachments.length`)로 변경하고 hover 툴팁("총 업로드된 파일 수: N개")을 추가: `help-tooltip` 클래스 + `data-tooltip` 속성으로 기존 공용 툴팁 스타일(`::after`)을 재사용했고, 개수 갱신 로직을 `renderCommentThread`(댓글 수 기준)에서 `renderAttachments`(전체 첨부파일 수 기준)로 이동(`detail.html`, `src/detail.js`, `detail.js` 버전 쿼리스트링 `20260907-file-count-badge-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 3건 동기화). 아울러 "Custom" 카드(Filter 2 오른쪽)의 멀티-Project 스위처(`pickCustomProject`/`renderCustomProjectPanel`)가 프로젝트 전환 시 `detailOiPartnershipRow` 내부(Custom Score·Filter 3/지표 영역)만 다시 그리고 첨부파일·GPT 원문·Filter 2·댓글 등 레코드 공용 영역은 건드리지 않음을 코드 확인(`src/detail.js` L1867-1927) — 즉 어떤 Shortlisting Project를 보든 Custom 카드만 내용이 바뀌고 나머지 상세 페이지는 항상 동일하게 동기화된 상태임을 확인, 코드 변경 없음.
- 상세 페이지 "Custom" 카드 4건 개선. (1) `#detailOiPartnershipRow .review-info-primary-label`(Action Date 라벨) 색상이 나머지 카드 요소("Custom" 헤딩, "Filter 3" 라벨 — 둘 다 `var(--accent)` 티얼그린)와 다르게 `var(--fluent-purple)`로 남아있던 것을 `var(--accent)`로 통일. (2) Action Date/담당자/F·U 계획 입력 영역을 `detailOicFilter3Body`(OIC 전용, `isDefault`일 때만 표시) 내부에서 꺼내 새 `#detailActionDateBody` 블록으로 분리하고 `detailCustomProjectBody`의 항상-표시 자식으로 이동: 이제 Custom Score만 있고 Filter 3 분류가 없는 커스텀 Shortlisting Project를 보고 있어도 Action Date 섹션이 OIC 때와 동일하게 항상 노출된다(`renderCustomProjectPanel`의 표시/숨김 로직은 변경 없음 — 원래도 이 필드들은 project 무관 `meta.focus_management` 값이라 별도 토글이 필요 없었음). (3) `#detailOiPartnershipRow`에 `grid-template-rows: auto auto minmax(0, 1fr)` + `max-height: 300px` + `overflow: hidden`을, `.custom-project-body`에 `overflow-y: auto` + `min-height: 0`을 추가해 커스텀 지표 개수가 많아 카드가 Filter 2 쪽 높이를 넘길 때 카드 전체가 늘어나는 대신 Custom 카드 내부(Filter 3/지표 영역 + Action Date)에만 세로 스크롤이 생기도록 함. (4) 프로젝트 전환 버튼(`.custom-project-switch-button`, "Open Innovation Center" 등)이 트래킹 Project가 하나뿐일 때(`is-static`) 배경·테두리가 사라져 납작한 텍스트처럼 보이던 것을, Criteria scores의 `.score-chip`과 같은 배경이 채워진 rounded-rectangle 형태(`border-radius: 999px`→`6px`, 배경 `var(--surface-soft)` 상시 적용, `is-static`에서도 배경·테두리·화살표 유지)로 키우고 명확한 클릭 대상으로 보이게 함(`src/styles.css`, `detail.html`; `styles.css` 버전 쿼리스트링을 `20260907-custom-card-action-date-2`로 갱신, `tests/test_dashboard_ia.py`의 해당 버전 문자열 assertion 2건 동기화). 브라우저에서 OIC 단독/3-Project 동시 트래킹 레코드로 프로젝트 전환·지표 15개 강제 주입 후 스크롤 발생·카드 미확장을 확인(주입은 클라이언트 DOM만 조작, 저장 API 미호출 — 재로드 후 원복, `json/pipeline-records.json` 변경 없음 `git status`로 확인). `pytest tests/test_dashboard_ia.py`를 수정 전/후로 비교해 신규 실패 0건(기존에도 무관한 사유로 실패 중이던 45건은 그대로, 버전 문자열 assertion 불일치로 새로 발생한 2건은 해결).
- Home Dashboard "Custom" 탭(OIC 포함 모든 Shortlisting Project) pipeline table에서 "CUSTOM REVIEW" 그룹 헤더 안에 있던 "Action date" 서브컬럼을 그룹 밖으로 꺼내 "Total Score" 컬럼 바로 오른쪽에 독립 컬럼으로 재배치. 새 컬럼은 "Total Score"와 동일하게 rowspan=2·줄바꿈 2행 헤더("Custom<br />Review", hover 시 "Custom Review · Action Date" 타이틀)로 표시되고 값(action date 입력)은 그대로 `actionDateCell` 사용. `renderFocusTable`의 `<colgroup>`/헤더 행/바디 행 순서를 함께 옮기고, `shortlistingGroupColspan`을 `+2`→`+1`로 조정(Action date가 그룹에서 빠졌으므로), `FOCUS_TABLE_COLUMN_KEYS`·`focusTableColumnKeys()`(OIC와 커스텀 Project 공용 함수)의 컬럼 순서도 동일하게 동기화해 신규 생성 Project를 포함한 모든 Custom Review 화면에 동일하게 적용되도록 함. 새 컬럼 `<th>`는 점수 컬럼이 아니므로(`score-subtotal-col`/`score-grandtotal-col` tint 미적용) `.focus-action-date-col button`에 `.score-grandtotal-col button`과 동일한 텍스트 중앙정렬·줄간격만 부여해 Total Score와 시각적으로 짝을 이루면서도 날짜 셀 자체는 기존 배경을 유지(`src/app.js`, `src/styles.css`, `index.html`의 `styles.css`/`app.js` 버전 쿼리스트링을 `20260907-custom-review-action-date-col-2`로 갱신). 브라우저에서 OIC 및 커스텀 Project("test") 양쪽으로 전환하며 헤더 colspan(OIC 6 / test 2)·바디 셀 순서(Custom Score → Total Score → Custom Review(Action Date) → 관리)를 DOM 속성으로 직접 확인, 컬럼 폭은 기존 `columnWidth('focusDueDate')` 값을 그대로 재사용하므로 순서만 바뀌고 개별 폭 값은 변경 없어 OIC 테이블의 컬럼 간격이 기존과 동일하게 균등 유지됨을 확인. `pytest tests/test_dashboard_ia.py` 재실행 결과 신규 실패 없음(기존 무관 실패 45건과 동일 목록).
- 위에서 새로 독립시킨 Action Date 컬럼 헤더 라벨을 "Custom (줄바꿈) Review"에서 "Action (줄바꿈) Date"로 되돌림(내용은 그대로 action date 입력, 위치도 Total Score 오른쪽 그대로) — 컬럼이 실제로 담고 있는 값(action date)과 라벨이 다르면 혼동을 줄 수 있다는 피드백 반영. `renderFocusTable`의 헤더 버튼 텍스트를 `Custom<br />Review`→`Action<br />Date`, `data-sort-label`을 `Custom Review`→`Action Date`로 변경하고 불필요해진 안내용 `title` 속성은 제거(Total Score 헤더와 동일하게 별도 title 없이 라벨만으로 의미가 통하도록 통일). 이 컬럼은 OIC 전용 분기 밖에서 모든 Shortlisting Project가 공유하는 동일 코드 경로(`renderFocusTable`)로 렌더링되므로, 프로젝트별 세부 지표(`metric_columns`)는 각자 다르게 설정되더라도 Action Date·Custom Score·Total Score 세 공통 컬럼의 라벨·위치는 기존 Project와 신규 생성 Project 모두 동일하게 적용됨(`src/app.js`, `index.html`의 `styles.css`/`app.js` 버전 쿼리스트링을 `20260907-action-date-col-label-1`로 갱신). 브라우저에서 헤더 텍스트가 "ActionDate"로 렌더링됨을 DOM으로 확인. `pytest tests/test_dashboard_ia.py` 재실행 결과 pipeline table 관련 테스트에는 신규 실패 없음(다른 세션이 `detail.html`/`detail.js` 캐시버스팅 태그를 재차 갱신하며 발생한, 이번 변경과 무관한 detail 관련 테스트 실패 2건은 out of scope로 남겨둠).
- 상세 페이지 "Custom" 카드 후속 보정 4건. (1) **높이 고정 + 스크롤**: Show로 펼쳤을 때 Custom 카드 높이가 왼쪽 Filter 2 카드와 어긋나던 문제를, 고정 `max-height` 추정값 대신 실측 동기화로 교체 — `syncCustomCardHeight()`가 `#detailFilter2Row.offsetHeight`를 읽어 `#detailOiPartnershipRow`에 그대로 `style.height`로 적용하고(Collapsed/Expanded 전환, 레코드 로드, 프로젝트 전환 시 호출 + `#detailFilter2Row`에 `ResizeObserver`를 걸어 그 외 리사이즈에도 반응), `.custom-project-body`의 `overflow-y:auto`는 유지해 두 카드가 항상 같은 높이로 움직이고 기준이 길어지면 Custom 카드 내부에서만 스크롤되도록 함(하드코딩 `max-height:300px` 제거). (2) **Collapsed 상태 필수 필드 노출**: `.review-info-stack.is-collapsed` 규칙에서 `#detailActionPlan`(F/U 계획 입력)을 숨김 목록에서 제외해, Show를 누르지 않아도 Custom Score·Action Date·F/U 계획 입력까지는 보이도록 함(담당자 입력만 계속 숨김 — Hide 상태의 "꼭 필요한 부분만" 기준 유지). (3) **줄 간격 통일**: Action Date 라벨/입력행/F/U 계획 입력이 gap 없는 block-flow로 붙어있어 들쭉날쭉해 보이던 것을, 새 `.action-date-body { display:grid; gap:6px; }`로 감싸 형제 블록 `.oic-filter3-body`와 동일한 6px 리듬을 갖도록 통일. (4) **Header에서 선택한 Shortlisting Project를 상세 페이지 기본값으로 동기화**: 대시보드가 쓰는 `localStorage['skbp.dashboard.activeShortlistingProjectId.v1']`를 `detail.js`의 `loadRecord()`에서 그대로 읽어 `selectedCustomProjectId` 초기값으로 사용 — 이제 대시보드 Header에서 "test" Project를 보고 있다가 레코드 상세로 들어가면 Custom 카드도 OIC가 아니라 "test"가 기본 선택된 상태로 열림(레코드가 해당 Project를 트래킹하지 않으면 기존처럼 OIC/첫 트래킹 Project로 폴백)(`src/detail.js`, `src/styles.css`, `detail.html`; `styles.css`/`detail.js` 버전 쿼리스트링을 `20260907-custom-card-height-sync-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 5건 동기화). 디버깅 중 `requestAnimationFrame` 기반 동기화가 자동화 브라우저 탭(백그라운드/숨김 상태)에서 전혀 실행되지 않는 것을 발견해 — 레이아웃을 강제하는 `offsetHeight` 읽기 특성상 rAF 지연이 불필요하므로 — 동기 호출로 교체(실제 사용자의 포그라운드 탭에서는 원래도 정상 동작했을 가능성이 높지만, 탭 가시성에 의존하지 않는 더 견고한 방식). 브라우저에서 OIC/커스텀 Project("test") 양쪽 Collapsed·Expanded 상태의 실제 높이(`offsetHeight`)가 Filter 2와 매 픽셀 일치함을 확인하고, 지표 15개 강제 주입으로 Custom 카드가 늘어나지 않고 내부만 스크롤됨을 재확인(주입은 클라이언트 DOM만 조작, 저장 API 미호출). `localStorage`에 커스텀 Project id를 심어 재로드했을 때 Custom 카드가 그 Project로 기본 선택됨을 확인. `pytest tests/test_dashboard_ia.py`를 수정 전/후 실패 목록으로 diff한 결과 신규 실패 0건(기존에도 무관한 사유로 실패 중이던 45건은 diff 없이 그대로).
- Home Dashboard의 "Project 설정" 모달(`#shortlistingMetricModal`) UI 정리 3건. (1) 타이틀("Project 설정 · {Project명}") 오른쪽에 `.criteria-refresh-pill` 스타일을 재사용한 "✎ 이름 수정" pill 버튼(`#shortlistingProjectRenameButton`)을 추가: 클릭하면 타이틀이 인라인 `<input>`(`#shortlistingProjectRenameInput`)으로 바뀌고 Enter/blur 저장·Escape 취소(상세 페이지 Custom Score 인라인 수정과 동일한 패턴), 저장은 기존 `PATCH /api/shortlisting/projects/{project_id}`(`name` 필드, 이미 owner 권한 검증·80자 제한 지원)를 그대로 재사용해 백엔드 변경 없음. (2) "구성원 추가"/"지표 추가" 버튼이 서로 다른 사각형 스타일(`identity-modal-submit`)이던 것을 새 공용 pill 클래스 `.shortlisting-action-pill`(둥근 accent-color 버튼 + "+" 아이콘)로 통일하고, 같은 행의 select/input들의 `min-height`를 34px로 맞춰 pill과 나란히 정렬되게 함. (3) 커스텀 Shortlisting Project의 지표 값 편집 컨트롤(`select.evidence-edit.metric-value-edit`, `input.focus-due-input.metric-value-edit`)이 색상 지정 없이 `font-weight:900`만 적용되어 Pipeline Table·상세 페이지 양쪽에서 다른 셀보다 과도하게 진한 검정으로 보이던 문제를, `var(--muted)`(hover/focus 시 `var(--text)`)로 톤을 낮춰 다른 컬럼 값과 통일(`index.html`, `src/app.js`, `src/styles.css`; `index.html`의 `styles.css`/`app.js`, `detail.html`의 `styles.css`/`detail.js` 버전 쿼리스트링을 `20260907-criteria-drawer-scope-1`로 갱신 — 상세 내역은 다음 항목 참고 — `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 동기화). 브라우저에서 "Project 설정 · test" 모달을 열어 이름 수정→저장→"Project 이름을 ... 변경했습니다" 상태 메시지 확인 후 원래 이름("test")으로 복원, 구성원/지표 추가 pill 배치 확인. `pytest tests/test_dashboard_ia.py` 결과 신규 실패 0건(기존 무관 실패 45건과 목록 동일, diff 없음). (버전 쿼리스트링은 이후 커밋에서 `20260907-classification-management-1`로 한 번 더 갱신됨 — 아래 항목 참고.)
- 판단근거(criteria drawer)가 Custom Review 탭에서 항상 OIC 전용 규칙(Filter 3 등) 또는 Tab 2 규칙으로만 표시되던 것을, 각 커스텀 Shortlisting Project가 스스로 설정한 분류 기준을 표시하도록 개편(요청 반영: "그 외 custom project에서는 tab2 판단근거로 대체하지 않고, 그 project마다 입력한 내용이 표시되게"). (1) **분류 관리 신설**: Project 설정 모달의 "지표 관리" 오른쪽에 "분류 관리" 탭을 추가(`#shortlistingSettingsClassificationsTab/Pane`). OIC의 투자/Value Up/공동연구처럼 "지표들을 종합해 내리는 최종 분류"를 프로젝트별로 자유롭게 정의하는 용도로, 순번·분류·설명 3열 표와 인라인 추가 행(분류 이름·설명 입력 + "분류 추가" pill, `.shortlisting-action-pill` 재사용)으로 구성했고 최대 12개까지 등록 가능. 백엔드에 `classification_columns: [{id, label, description, created_by_name, created_at}]` 필드와 `POST`/`DELETE /api/shortlisting/projects/{project_id}/classifications[/{classification_id}]`(owner 전용, `oic_default` 차단, 지표 관리와 동일한 검증/캡 패턴)를 신규 추가(`main.py`, `SHORTLISTING_CLASSIFICATION_CAP = 12`), `json/schema.md`의 Multi-Project Shortlisting 절에 데이터 형태와 권한 규칙을 문서화. (2) **판단근거 동적 렌더링**: 대시보드(`updateCriteriaDrawerScope`/`renderCriteriaDrawerCustomProjectContent`, `src/app.js`)와 상세 페이지(`src/detail.js`, 동일 함수명) 모두에서 Custom Review 탭·카드가 OIC가 아닌 프로젝트를 보고 있으면, 정적 Tab1/2/3 섹션 대신 그 프로젝트의 `classification_columns`를 즉석에서 "{Project명} — 분류 기준" 헤딩 + 순번이 매겨진 카드 목록으로 렌더링(등록된 분류가 없으면 "Project 설정 → 분류 관리에서 추가할 수 있습니다" 안내). 스코프 라벨은 "TAB 3 · {PROJECT} · 분류 기준"으로, 버전 배지는 해당 없음이라 숨김 처리. 언어 토글(한/영) 시에도 `updateCriteriaDrawerScope()`가 재호출되며 이 동적 패널을 다시 채워 넣어 정적 마크업 교체로 인한 유실을 방지. OIC의 하드코딩된 Filter 3 판단근거(v1.7)와 Tab 1/2 판단근거는 기존 그대로 정적 렌더링 유지, 변경 없음(`index.html`, `src/app.js`, `src/detail.js`, `src/styles.css`; `index.html`/`detail.html`의 `styles.css`/`app.js`/`detail.js` 버전 쿼리스트링을 `20260907-classification-management-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 동기화). 브라우저에서 "test" Project에 분류("적극 검토" + 설명) 1건을 등록 → 판단근거 열어 스코프 라벨·본문에 즉시 반영됨을 확인 → OIC로 전환해 기존 v1.7 판단근거(영문 토글 포함)가 그대로 나옴을 확인 → 테스트 데이터 정리(분류 삭제, JSON 직접 롤백). `pytest tests/test_dashboard_ia.py`를 수정 전/후 실패 목록으로 diff한 결과 신규 실패 0건(기존 무관 실패 45건과 diff 없이 동일).
- 판단근거(criteria drawer) 노출 범위를 Custom Review 탭이 **현재 표시 중인 Shortlisting Project**에 따라 분기하도록 수정. 기존에는 Home Dashboard의 판단근거 버튼이 `activeTableMode()==='focus'`이기만 하면 항상 OIC 전용 분류 규칙(Filter 3/DD 등)을 보여줬고, 상세 페이지도 URL의 `tab=focus` 여부와 레코드의 OIC `is_tracked` 값만으로 판단해 "다른 Shortlisting Project를 보고 있어도 레코드가 OIC에도 트래킹돼 있으면 OIC 규칙이 뜨는" 오류가 있었음. 이제 대시보드는 `state.activeShortlistingProjectId === 'oic_default'`일 때만 focus(OIC) 콘텐츠를 보여주고 그 외 커스텀 Project에서는 Tab 2(Advanced Research) 판단근거로 대체(`criteriaDrawerModeForActiveTab()` 신설, `src/app.js`); 상세 페이지의 Custom 카드도 URL `tab` 파라미터 대신 카드가 실제로 표시 중인 `selectedCustomProjectId`가 OIC인지로 판단하도록 `updateCriteriaDrawerScope()`를 수정(`src/detail.js`)해 두 화면의 동작을 일치시킴. 백엔드/HTML 변경 없음. `pytest tests/test_dashboard_ia.py` 결과 신규 실패 0건(`function_body(JS, "updateCriteriaDrawerScope")` 관련 assertion 포함, 기존 무관 실패 45건과 목록 동일).
- 상세 페이지 Filter 2 영역의 "원문 기반 마지막 재평가 · Advanced Research 기준 v{ver}" 라벨이 너무 길어 보이던 문제 수정: "Advanced Research 기준"을 "Advanced"로 축약해 "원문 기반 마지막 재평가 · Advanced v{ver}"로 표시(`title` 툴팁도 동일하게 축약). `renderCollaborationPanel`(`#detailDecisionOrigin` 갱신부, `src/detail.js`)의 텍스트 리터럴 2곳만 수정 — 이 요소는 대시보드 "Custom" 탭과 "Advanced" 탭 양쪽에서 진입해도 동일한 `detail.html` 한 페이지·같은 코드로 렌더링되므로 두 진입 경로 모두에 자동 적용됨. 별도 브랜치가 없는 일반 문구(기본 "Score 기준 v{ver}" 표기, 로딩 중 안내 문장 등)는 대상이 아니라 변경하지 않음. `detail.html`의 `detail.js` 버전 쿼리스트링을 `20260907-advanced-label-shorten-1`로 갱신. 해당 문자열을 참조하는 테스트 없음을 확인(`grep`), `pytest tests/test_dashboard_ia.py` 결과 신규 실패 없음.
- 상세 페이지 "Custom" 카드 회귀 수정 + Less(축소) 상태 개편. **(1) 높이 고정 회귀 원복**: 직전 항목에서 추가한 `syncCustomCardHeight()`(Filter 2 높이를 읽어 Custom 카드에 강제 적용)와 관련 CSS(`#detailOiPartnershipRow`의 `grid-template-rows`/`overflow:hidden`, `.custom-project-body`의 `overflow-y:auto`)를 전부 제거 — Show/Less를 눌러도 Custom 카드 높이가 Filter 2에 고정돼 버려 두 상태가 똑같아 보이고 빈 공간만 남던 문제였음. 원래대로 각 카드가 자기 콘텐츠 기준으로 접히고 펼쳐지도록 복원(`src/detail.js`, `src/styles.css`). **(2) 커스텀 Project Less 상태 개편**: OIC(하드코딩 Filter 3)는 기존 그대로 두고, 그 외 커스텀 Shortlisting Project 전용 렌더링(`renderCustomMetricTable`)을 지표 전체를 나열하는 편집 행 목록에서 — Filter 2의 "Criteria scores"와 동일한 `.score-chip`/`.score-chip-link`/`.filter-score-panel` 스타일을 그대로 재사용한 "Criteria Scores" 라벨 + rounded-rectangle pill 요약(지표 label + 현재 값, boolean은 Pass/Fail로 표시)으로 교체. pill을 클릭하면 그 자리에서 숨겨져 있던 기존 편집 컨트롤(`metricValueEditControlMarkup`, select/input)이 나타나 값 수정 가능(저장은 기존 `PATCH /api/records/{id}/shortlisting-projects/{project_id}` 그대로 재사용, 저장 성공 시 전체 재렌더링되며 pill 표시로 자동 복귀; 값 변경 없이 포커스만 벗어나면 `focusout` 핸들러가 150ms 뒤 pill로 되돌림, Escape로 취소 가능), hover 시 Filter 2 chip과 동일한 accent 강조(`.score-chip-link:hover`)가 적용됨. 그 프로젝트에 `classification_columns`(다른 세션에서 신설한 "분류 관리" 데이터)가 등록돼 있으면 pill 목록 위에 `label`/`description`을 그대로 노출하는 `.custom-classification-summary` 블록을 먼저 보여주고, 없으면 생략하고 pill 요약만 표시. 기존 2개 미리보기 + `.custom-metric-extra`(Hide 시 숨김) 방식은 pill이 이미 충분히 컴팩트해 폐지 — 이제 Show/Less 여부와 무관하게 모든 지표가 pill로 항상 보이고, Hide 상태의 차이는 담당자 입력·Filter 2 reason textarea 등 기존 공통 로직에만 남음(`src/detail.js`의 `customMetricRowMarkup`/`CUSTOM_METRIC_PREVIEW_COUNT`를 `formatMetricPillValue`/`customMetricPillRowMarkup`/`customProjectClassificationSummaryMarkup`으로 교체, `saveDetailMetricValue`의 라벨 조회를 `.custom-metric-label`→`.score-chip-label`로 갱신, `src/styles.css`의 `.custom-metric-row`를 인라인 pill 래퍼로 단순화하고 `.custom-classification-summary`/`.custom-classification-item` 신설, `.custom-metric-extra` 관련 규칙 전부 제거). `detail.html`의 `styles.css`/`detail.js` 버전 쿼리스트링을 `20260907-custom-card-pill-summary-1`로 갱신, `tests/test_dashboard_ia.py`의 관련 버전 문자열 assertion 5건 동기화. 브라우저에서 Shaperon(HY209/NuCerin, "test" Project 트래킹) 레코드로 Show/Less 시 두 카드 높이가 다시 독립적으로 변함을 확인, pill 클릭→편집 컨트롤 전환→boolean 값 저장→pill이 "Pass"로 갱신되는 전체 흐름을 실제 API 호출로 검증(같은 프로젝트에 임시 분류 1건을 API로 추가해 `.custom-classification-summary` 렌더링 확인 후 API로 삭제해 원복), OIC(Threebrooks Therapeutics)는 Filter 3 드롭다운/노트가 기존과 동일하게 렌더링됨을 확인. `pytest tests/test_dashboard_ia.py` 수정 전/후 비교 결과 신규 실패 0건(기존 무관 실패 45건과 diff 없음). **주의**: 검증 과정에서 Shaperon 레코드의 "test" Project 지표 "자료등록여부" 값이 Pass로 남았음 — boolean/list/number 지표는 한 번 값을 설정하면 빈 값으로 되돌리는 걸 백엔드가 차단하는 기존 정책(오늘 변경과 무관) 때문에 테스트 중 원복하지 못했으니 필요하면 수동으로 다른 값으로 바꿔주세요.
