# 2026-08-19

## Changes

- Reordered Tab0's GPT copy-instructions pill to copy icon → GPT → label and lightened its slate-blue treatment for clearer, quieter alignment with the adjacent Excel export action.

- Corrected Fast Triage v3.3 `main_line_summary` score validation to inspect only criterion-labelled TR/MoA/Data score statements, so scientific numeric evidence no longer causes false score conflicts; grouped actionable per-criterion validation diagnostics and strengthened the Compact v2 prompt guidance.

- Simplified the similar-pipeline reupload modal: removed the redundant heading, clarified the per-item overwrite then apply-selected flow, and rendered both action names as matching icon pills in the guide copy.

- Brought Fast Triage detail review closer to Full Scout: admins can override its three criterion scores without altering GPT source scores, review changes are tracked, each criterion supports on-demand audited note cards, and the decision summary has an admin-only Final comment. Removed end-user `triage_only` and Flags display while retaining Flags for server-side hard-blocker evaluation.

- Reworded the Full Scout GPT report-save blocking modal to show only a concise wait-for-save-completion message, removing the redundant navigation restriction notice.

- Normalized GPT original-report presentation artifacts: future uploads and direct API saves remove non-resolvable `contentReference`/`oaicite` markers and convert HTML `<br>` tags to Markdown line breaks; existing Full Scout/Fast Triage reports are cleaned at display and copy time without bulk-rewriting stored report data. GPT instructions now require readable Markdown with actual URL references only.

- Made Tab0's pending-candidate header checkbox clear its partial (minus) selection on the next click, and added drag-across-row checkbox selection up to the existing 20-candidate limit.

- Updated Home Dashboard manual score markers to retain each score's established green, amber, or red fill and text, with only a darker same-tone border.

- Replaced the Team Review human-score red border with a darker border derived from each score's existing green, amber, red, or neutral tone.

- Kept Team Review score-pill tone colors intact for human edits, using only the subtle red border as the manual-change marker.

- Reworked pipeline reupload review into a wide, scrollable side-by-side comparison modal with exact/similar labels, company-match context, per-candidate overwrite or upload exclusion, and a bulk continue-upload option.

- Aligned Team Review manual score pills with the Home Dashboard's single subtle red border treatment.

- Hardened Team Review manual-score markers by rendering the red criterion-pill treatment directly for every active human score override, including Target changes.

- Increased the Tab0 Fast Triage GPT instruction-copy candidate limit from 10 to 20, including the selection counter, bulk selection, warnings, and guide copy.
- Added an explicit active manual-score marker to Team Review criterion chips and force-applied the red border/outline treatment so human overrides remain unmistakable.
- Prevented clipping of Tab0's Asset table header and restyled its GPT instruction-copy action as a compact pill with a copy icon and retained GPT badge.
- Synced active human criterion-score overrides across the Tab2 dashboard and Team Review Workspace, deriving the displayed total from overridden criteria while preserving GPT source scores; active overrides now receive a cache-refreshed red score-chip outline.
- Simplified Tab0 filters by removing the redundant Company dropdown; the wider Asset/Company search remains alongside the multi-stage filter and reset control.
- Vertically centered the Tab1 Full Scout waiting-candidate stage bars so their content center aligns with the adjacent donut charts.
- Strengthened the Team Review Criteria scores manual-edit treatment with a persistent two-pixel red outline, history fallback detection, and refreshed static asset versions.
- Reordered Shortlisting Partner Material pills to NCDP, CDP, ADMET, IR, and DD Report.
- Made Shortlisting Partner Material pills independently light for every recognized category in one filename (for example, `IR` and `NCDP`).
- Aligned Team Review AI qualitative-history labels to `정성평가 [항목] AI 답변 생성 완료/삭제` and placed the saved-reason edit action at the far right of its summary row.
- Expanded Shortlisting Partner Materials quick-upload pills to IR, CDP, NCDP, ADMET, and DD Report; category selection supports multiple files and appends a category suffix when a filename has no recognizable material label.
- Condensed deleted AI qualitative-review history to an `AI generated answer deleted` label while retaining detailed deletion values for human comments.
- Changed saved Team Review score-change reasons to a compact read-only note, reopening the input only when an admin selects edit.
- Fixed Team Review criterion-score override styling to reliably load the current stylesheet and visibly retain the dashboard-red manual-score border.
- Simplified the attachment-upload modal status copy to report processing only, without a redundant navigation warning.
- Added the cancellable processing modal to Full Scout partner-material file uploads, including upload abort handling.
- Added editable, auditable reason notes beneath manually changed Team Review scores and condensed AI qualitative-generation history entries to a completion label.
- Renamed the dashboard-facing Target Relevance label to Target Area Relevance across the Tab1/Tab2 judgment guides, Tab3/detail score displays, tooltips, and exports without changing rubric semantics or versions.
- Changed the Full Scout report table-of-contents label to Target Area Relevance while preserving the original report heading and criterion identifier.
- Added a compact, cancellable processing modal for dashboard refresh, re-evaluation, classification, data save/import, and AI reparse requests while keeping the AI chat workflow unobstructed.
- Fixed Tab2 rubric refresh so a manual criterion/total-score override is reset to the stored official GPT score even when the rubric is already current or AI finds no score changes; reset events remain auditable.

- Fixed Filter 3 ADMET completion scoring so canonical Study–Status parsing takes precedence over blank or stale document-model counts; bumped the criteria manifest and UI guidance to v1.4 and refreshed the VTA-27 score as 16/25.
- Mirrored the dashboard's human-edited score border treatment on the matching Team Review Workspace criterion chips.
- Changed Tab3 F/U Action to show only dated actions and sort them by the nearest due date, with the latest update as a tie-breaker.
- Added Tab0 workflow-level recent-upload tracking, showing each workflow's last-15-day increase beside its total.
- Fixed the server-side Tab3 F/U Action source so undated Shortlisting records cannot appear in the due-date list.
- Clarified Tab0's recent-upload metric with a matching red `▲ +N` legend beside the 15-day label.
- Replaced Tab1/2 pipeline filter selects with accessible multi-select menus, allowing combined Stage, Main indication, and other filter values.
- Corrected Tab2 Priority Pipeline client and fallback sorting to rank total score descending before using the latest investigation as a tie-breaker.
- Simplified additional-column settings by hiding raw JSON paths and presenting Fast Triage columns from its own concise record contract.
- Restyled Tab0's candidate-list import summary (parsed/added/skipped/duplicate counts) to reuse the Tab1/Tab2 input-validation badge-and-row format instead of plain text.
- Fixed Tab0's header "총 N건 로드됨" count, which previously kept the stale count from whichever pipeline tab was active before switching to 진척 현황; it now reflects the 진척 현황 Table's own row count.
