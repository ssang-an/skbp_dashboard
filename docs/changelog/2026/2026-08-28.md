# 2026-08-28

## Changes

- Widened the Filter 1 column so the full `INSUFFICIENT` state remains visible. Replaced Shortlisting In-vivo/In-vitro browser-native hints with the shared rounded tooltip, using concise O/X/N/A evidence messages. DD `O` now opens the record’s Full Scout detail page with Due Diligence expanded and scrolled into view (`src/app.js`, `src/detail.js`, `src/styles.css`, `index.html`, `detail.html`, `tests/test_dashboard_ia.py`).
- Moved the Fast Triage detail attribution/footer out of the fixed viewport overlay and into the document flow after the page content, so it appears only at the bottom of the scroll (`src/styles.css`, `triage_detail.html`, `tests/test_dashboard_ia.py`).
- Made Full Scout and Fast Triage detail rubric refreshes show the same blocking “latest criteria updating” progress modal used by the Pipeline Table, then dismiss it only after the refresh request completes. Records returned by the refresh endpoint now reload on the Full Scout detail page for every successful result (`src/detail.js`, `src/triage-detail.js`, `detail.html`, `triage_detail.html`).
- Refined Shortlisting evidence presentation: moved DD directly after Filter 3, rendered its attachment-derived O/X state as a Total Score-style circle, and added clear hover explanations for In-vivo/In-vitro `O`, `X`, and `N/A` (`src/app.js`, `src/styles.css`, `index.html`, `tests/test_dashboard_ia.py`).

- Added a compact, tooltip-only Fast Triage Score rubric refresh button beside Final Comment and Contact History actions. The button reuses the existing authenticated rubric-refresh endpoint and is limited to administrators (`triage_detail.html`, `src/triage-detail.js`, `src/styles.css`, `tests/test_dashboard_ia.py`).
- Added Shortlisting header filters for ADMET (`>=`, `<=`, legacy `=<`, or exact score) and In-vivo/In-vitro/DD (`전체`/`O`/`X`). Filters retain OR behavior within a field and AND behavior across fields; active filters also update the summary count. Added a read-only DD column immediately before Action date, driven by one or more Team Review Due Diligence attachments (`src/app.js`, `src/styles.css`, `tests/test_dashboard_ia.py`).

- Reworded the Pipeline Table's score-filter popover helper text (`src/app.js`) from "TR·MoA·Data 간 조건은 AND로 적용됩니다." to "TR·MoA·Data 등 기준 간 조건은 AND로 적용됩니다." to reflect that the same shared popover also governs Full Scout's Comp/Plat/Exp/Market score filters, not only TR/MoA/Data. Single shared render function, so the change is visible on both Tab 1 (Fast Triage) and Tab 2 (Full Scout). Verified with `node --check src/app.js`.
- Pipeline Table row-position UX, requested for both Fast Triage and Full Scout (`src/app.js`, `src/styles.css`):
  - **Return-to-row on back navigation:** clicking a row now records `{recordId, mode}` to `sessionStorage` (`skbp.pipeline.return-focus.v1`) before navigating to `/triage-detail` or `/detail`. On the next full page load, `restorePendingPipelineReturnFocus()` (called at the end of `loadRecords()`) consumes that entry, jumps the Pipeline Table to whichever page currently contains that record (by index in `getVisibleRows()`, not a stored page number, so it's correct even if sort order changed), and scrolls the row into view. Previously the "Fast Triage 목록"/"Full Scout" back link on the detail pages always reset to page 1. The detail pages themselves needed no changes since their back links already do a plain `/?tab=...` navigation.
  - **Highlight after rubric re-score:** clicking the Pipeline Table's 재평가 (rubric refresh) icon can move a row to a new sort position once the new score comes back, making it hard to relocate. `recalculateLatestRubric()` now calls the same row-locating helper after `render()`, adding a `.pipeline-row-return-highlight` class (new `@keyframes pipeline-row-return-highlight` in `src/styles.css`, a `color-mix(var(--accent), var(--readable-surface))` fade using the existing row-hover color convention, theme-aware) for 3 seconds (`PIPELINE_ROW_HIGHLIGHT_MS`), removed via `setTimeout`.
  - Shared helper `scrollAndHighlightPipelineRow(recordId)` backs both features. Bumped `index.html`'s `styles.css`/`app.js` cache-busting query strings. Verified via headless browser: simulated a "return" by writing the sessionStorage entry for a page-2 record then reloading — page restored to "2 / 17" (not reset to "1 / 17"), the row was present and carried the highlight class, and the sessionStorage entry was cleared after use. `node --check src/app.js` passed. The rubric re-score path reuses the identical, already-verified helper, so it was not separately exercised against the live (LLM-backed) refresh-rubric endpoint.
- **metadata-consistency-fix (instruction; no rubric/instruction/display version change — Fast Triage and Full Scout, per `docs/rubric-release-maintenance.md`'s new "비-scoring 메타데이터 canonical 값 정합성 보완" exception, added in this same change):** confirmed and fixed a gap the user suspected — GPT instruction 1 (Fast Triage) had no Theme/Cluster taxonomy guidance for `json_summary.theme`/`cluster` (a dashboard-grouping metadata field that scores nothing and feeds no hard_filter/status logic), while instruction 2 (Full Scout) still had the full `Allowed Theme values:`/`Allowed clusters:` list. Git archaeology (`git log -S`) traced this to `07252ec4` (2026-08-06, the commit that added the "Protein Homeostasis" Theme): it consolidated each instruction's separate inline "Theme/Cluster mirror to Others/Unknown" sentence into one shared `SHARED_CANONICAL_THEME_RULE`, which dropped that mirroring instruction and — since the `Allowed clusters:` enumeration had only ever existed in Full Scout's template, never Fast Triage's — left Fast Triage's `cluster` field with no controlled vocabulary at all. The frontend's `canonicalCluster()` doesn't validate against `main.py`'s `THEMES`/`CLUSTERS` dicts either, so an off-taxonomy GPT answer would have silently saved as free text instead of grouping correctly. Fix (`src/app.js`): restored the "Theme is Others → cluster is Others; Theme is Unknown → cluster is Unknown" sentence into `SHARED_CANONICAL_THEME_RULE` (both instructions), and factored Full Scout's existing `Allowed Theme values:`/`Allowed clusters:` block into a new shared `SHARED_CANONICAL_CLUSTER_RULE` constant referenced by both `buildTriageInstructionPromptLegacy()` and `buildGptInstructionPromptLegacy()` — Full Scout's rendered prompt is byte-for-byte unchanged aside from the restored sentence; Fast Triage now gets the same taxonomy Full Scout always had. Added `tests/test_rubric_v32_v33.py::test_fast_triage_and_full_scout_share_the_cluster_taxonomy` to guard against the two instructions drifting apart again. Verified: the new test plus the existing `StaticInstructionAndSchemaTests` class (7 tests) and the full `test_rubric_release_manifest`/`test_rubric_ai_refresh`/`test_rubric_v32_v33` suite (76 tests) pass; `node --check src/app.js`; and via headless browser, actually clicked both "GPT 지침 1"/"지침 2" copy buttons on the live dashboard and read the real clipboard contents, confirming both rendered prompts now contain the Theme rule, the Others/Unknown mirror sentence, and the full Allowed Theme/Cluster list (including `Ion Channel` and `교세포 향상성`). Also documented the new no-version-bump exception category itself in `docs/rubric-release-maintenance.md` (change-type table row + exceptions section, now "세 가지" instead of "두 가지" + the changelog label list), since no existing category covered a canonical-value gap in a purely non-scoring metadata field — per the user's explicit request to identify and fill that documentation gap.
- Corrected a Fast Triage scoring error on `Humanwell_CXJM-66_fast_triage_20260623` (`json/pipeline-records.json`) found by the user: `structured_table.indication` is "Acute pain; surgical anesthesia and postoperative pain management," which `v3_4_triage.md` explicitly scores as TR 2 ("acute/postoperative/non-neuropathic pain"), not TR 3 (reserved for neuropathic pain, one of the six SKBP priority indications) — the prior AI rubric-refresh review on 2026-08-27 had verdict "no_change" and missed this. Also lowered Data Maturity 3→2: the only cited evidence (CTR20250872 Phase 1 registration, NMPA trial approval) confirms stage progression but is not itself a quantitative evidence domain, short of the "2+ complementary domains" bar for Data 3. Re-verified independently two ways before applying: (1) a one-off dry-run OpenRouter call reusing the real `build_rubric_refresh_prompt` against `z-ai/glm-5.2` (not currently wired into any endpoint — `.env`'s `OPENROUTER_MODEL` is `deepseek/deepseek-v4-flash`) reached the same TR conclusion in its reasoning trace before hitting the existing `OPENROUTER_MAX_TOKENS=1600` cap (a reasoning model needs a much larger budget than this endpoint currently allows, so pointing it at this endpoint as-is would just silently fail with "empty response"); (2) an independent rubric-only re-read reached the same conclusion. Applied via `main.record_successful_rubric_review()` (new `rubric_refresh_history` entry, `result: "updated"`, `actor_ip: "manual-correction:claude-code"`) followed by `main.save_records()`, so `scoring.total_score` (8→6) and `hard_filter.status`/`triage.status` were recomputed by the existing `synchronize_server_derived_scoring_fields` save-boundary logic rather than hand-set — status stays REJECT both before and after, since `triage.active_asset` is `null` and v3.4's Final Status table gates SELECT on `active_asset = true` regardless of TR/MoA/Data. Also rewrote `hard_filter.reason`/`triage.why`, which previously read like a SELECT justification ("scores are all >=2") and never mentioned the actual REJECT driver (unconfirmed active-asset status) — now both name it explicitly, and `triage.missing_evidence_needed_for_full_scout` gained an explicit "confirm current activity status" item. `save_records()`'s existing controlled-vocabulary canonicalization also normalized this record's `structured_table.modality_platform` ("SM" → "Small molecule") as an incidental side effect of going through the real save path rather than a hand-edit. Because the sync logic runs over every record on save, it also auto-corrected one unrelated pre-existing inconsistency it found along the way: `MetiMedi Pharmaceuticals Co Ltd_METI-101` had its Target Relevance manually corrected 0→1 via the in-dashboard AI chat on 2026-08-27 (`update_score`), but the derived `hard_filter.status`/`triage.status`/`scoring.total_score` were never resynced at that time — this save corrected them (INSUFFICIENT→REJECT, total 3→4), matching the current TR=1/active_asset=true state; no criterion score was changed by this pass for that record. Confirmed via `git diff` that no other record in the 12,000+-line file was touched. Flagged separately to the user: the dashboard's judgment-guide cards (`index.html`'s Fast Triage SELECT/REJECT status cards) and the Fast Triage detail page's decision-hero reason line don't currently mention the `active_asset` gate at all, which is what made this record's REJECT status look inconsistent with its scores at a glance — proposed as a follow-up, not yet implemented.
