# 2026-08-22

## Changes

- Made Tab 0 direct workflow-dot motion more dynamic with deterministic per-dot stagger, curved paths, and a small landing rebound while preserving the bounded end state.

- Replaced Tab 0's d3-force animation with bounded direct node-coordinate motion after a force-layout regression clustered dots at the left; each stage now expands from a left entry area to its full-card irregular targets and stops reliably.

- Changed Tab 0 workflow-dot motion into a left-to-right expansion: dots begin in a bounded left-side entry region, then travel to their irregular rectangular anchors during the short force animation.

- Reintroduced a short, stage-sequenced d3-force repulsion animation for Tab 0 workflow dots, anchored to their rectangular jitter positions and capped at 26 iterations to avoid persistent motion.

- Enabled direct drag-and-drop positioning for individual Tab 0 workflow dots while retaining the bounded static layout and lightweight stage entrance sequence.

- Added a lightweight one-time Tab 0 workflow-cloud entrance sequence: Listing appears first, then Fast Triage, Full Scout, and Shortlisting at 300ms intervals without restarting node physics.

- Replaced Tab 0's regular workflow-node grid with bounded, deterministic jitter positions so each stage remains rectangular and still while no longer looking like a table.

- Replaced Tab 0 Summary Dashboard's force-directed workflow clouds with fixed G6 grid layouts that fill each rectangular stage card uniformly, removing continuous physics animation for large Listing sets.

- Reduced Tab 0 G6 force-layout work from 180 to 36 iterations and accelerated convergence, so node clouds settle after a brief initial motion instead of causing sustained rendering load.

- Rebalanced Tab 0 node density by making Listing compact (minimal repulsion/collision) and making Shortlisting prominent (18px nodes with wider force spacing).

- Restored Tab 0 workflow graphs to each card's full rectangular canvas, removing the centered-square constraint. Shortlisting now uses reduced many-body repulsion and collision strength so dense nodes may overlap rather than spread or clip outside the card.

- Constrained each Tab 0 G6 workflow canvas to the largest centered square within its card, so force-directed nodes maintain compact circular clusters instead of stretching across the card width.

- Tightened Tab 0 Listing-to-Summary spacing to match the workflow dashboard rhythm. Its Summary Dashboard toggle now hides only the four G6 node canvases, preserving all four stage count/filter buttons.

- Matched each Tab 0 Summary Dashboard workflow card to the shared 332px visualization-card height used by Fast Triage, Full Scout, and Shortlisting. Its metric header uses 64px and its G6 canvas fills the remaining bounded space.

- Integrated the Tab 0 G6 workflow node clouds directly into their Listing, Fast Triage, Full Scout, and Shortlisting cards. Each canvas is height-bounded and independently force-centered; Listing now uses compact 4px nodes so large populations remain visible inside the card.

- Replaced the connected Tab 0 Workflow Map with four independent G6 force node clouds aligned below the Listing, Fast Triage, Full Scout, and Shortlisting summary cards. Each cloud shows its own completed-stage population without internal duplicate headings, counts, or inter-stage links.

- Reworked the Tab 0 G6 Workflow Map into a physical force-directed graph: every Pipeline is represented as a Listing-to-current-stage node chain, with collision avoidance, connecting edges, draggable force interaction, progressive stage sizes, and no duplicate stage/count labels inside the graph.

- Replaced Tab 0's DOM dot grid with a filter-aware G6 Canvas Workflow Map: pipeline nodes are assigned to their highest completed workflow stage, use progressively larger circles from Listing through Shortlisting, retain stage counts/colors, and show Pipeline context on hover.

- Simplified the Tab 0 Workflow Map into a dynamic stage-by-stage dot graph: removed node labels, result copy, and navigation controls so each filtered Pipeline is represented only by a compact circular node, with details retained in hover/accessibility text.

- Added a filter-aware Tab 0 Workflow Map below the progress cards: every current result appears once in its highest stage (Listing → Fast Triage → Full Scout → Shortlisting), with compact named nodes, destination counts, record navigation, Listing-to-table lookup, bounded animation, and reduced-motion support.

- Removed the redundant Tab 0 `진척 현황 요약` header and recent-15-day explanatory legend from inside `Summary Dashboard`; the per-card red increase indicators remain.

- Explicitly ordered the Tab 0 flex children so the Listing guide always renders above `Summary Dashboard`; this avoids the shared workflow cascade placing the dashboard first.

- Restored the Tab 0 Listing-management information row (including its info icon) above the collapsible `Summary Dashboard`, correcting the intended visual order.

- Removed the redundant Tab 0 Listing guidance row and made its `Summary Dashboard` use the same persistent show/hide interaction, accessibility state, and chevron pattern as the other workflow tabs; only the progress cards collapse.

- Reordered the Tab 0 hierarchy so `Summary Dashboard` and its progress cards appear first, followed by the concise Listing-management guidance row.

- Unified Tab 0 with the workflow dashboard hierarchy: added a concise Listing-management info row and a connected `Summary Dashboard` section above the existing progress cards, while preserving the current counts and card-filter behavior. Added the next Workflow Map specification so its future node view is explicitly driven by the active Tab 0 filter result rather than global totals.

- Upgraded the generated `skbp_pipeline_wiki` into a workflow-aware graph: Listing entries now join Fast Triage, Full Scout, and actual Tab 3 Shortlisting through conservative pipeline identity matching; new workflow/review/stage/OI-classification notes, dashboards, graph node attributes, and independent per-record scorecards keep scientific, operational, and recommendation views distinct.

- Optimized the Tab 0 Listing input grid for one-screen desktop entry: replaced the wide fixed minimum with proportional columns, let headers wrap instead of clipping, and made the Comment `multi-line` hint a compact blue text accent; only narrow mobile widths retain safe horizontal scrolling.

- Relabelled bulk Tab 0 spreadsheet comments as `일괄 Excel 업로드: Tab 0 · Listing Comment · Team Review · 날짜`; legacy `Tab 0 Team Review` records render identically, while administrator Listing Comment Posts keep their named attribution.

- Separated Website from Tab 0 Evaluation Progress into its own rightmost column, unified it with the shared external-link icon, added the same open-only Website action to Shortlisting, and compacted default table/action widths to avoid unnecessary horizontal scrolling while retaining Tab 0-only Website editing.

- Unified Tab 0 Listing filters with Tab 1/2: added multi-select Country, Modality, Theme, Cluster, Main indication, Stage, and progress dropdowns; progress choices synchronize with the summary cards, the API provides researched Theme/Cluster values, and summary-card help text no longer clips.

- Unified Tab 0/1/2 table presentation: Target now uses the shared emphasis rule, Evaluation Progress matches score-group styling, all triage score groups are labelled `Fast Triage`, and Website controls show a consistent external-link pill (including a clear unregistered state in Tab 1/2 and a check pill in Tab 0).

- Simplified the Tab 0 Listing grid: restyled Clear as a matching secondary pill, removed per-row delete controls, and reject blank, `-`, `X`, or `×` Asset placeholders on both client and server before import.

- Refined Tab 0 operational metadata behavior: Listing Comment now identifies `Tab 0 Team Review`, duplicate Listing rows replace conflicts only when the new row is richer, and the shared Pipeline Website URL opens on click, updates on administrator double-click, and does not create edit-history noise.

- Added a Tab 0 Website field and external-link pill: imports retain one valid HTTP(S) URL, surface it at the table’s right edge, include it in CSV/search/Fast Triage identification context, and preserve it across matching researched records as non-evidence metadata; duplicate Listing imports now fill blanks and only replace conflicts when the incoming row is more complete.

- Expanded Tab 0 semantic Excel-header mapping: HQ Location maps to Country; Priority, Reason for Priority, and Next Step concatenate into Comment; Meeting/Contact History concatenate into Contact without discarding a same-row value.

- Unified Tab 0 Listing progress-table typography, row values, and check/minus pills with Tab 1/2; pending Listing rows now support administrator inline edits with subtle manual markers, while researched rows direct edits to the authoritative Fast Triage or Full Scout Dashboard.

- Added safe semantic Excel-header mapping for Tab 0 Listing paste, supporting reordered and descriptive columns such as Company geography and Company name while skipping ambiguous or duplicate headers.

- Aligned Tab 0 progress-table metadata with the Tab 1/2 dashboard vocabulary: canonical country flags, modality, multi-indication display, stage, source tooltips, and CSV exports.

- Standardized Tab 0 Contact history display: O, dates, and notes show a check, while X, hyphen-only, and blank values show a non-interactive no-history dash.

- Sized Tab 0 Listing input columns explicitly so Company and Asset Required badges remain inside their own headers without overlapping adjacent columns.

- Restored multi-indication dashboard handling: canonical indications now remain source ordered, the first confirmed one is displayed when no lead is declared, and indication filtering continues to use OR semantics.

- Highlighted the required Company and Asset fields in Tab 0's Listing grid with restrained Required badges while preserving the existing validation contract.

- Fixed the dashboard Pipeline Upload shortcut so Tab 0 opens and scrolls to its own candidate-list upload input, while Tab 1 and Tab 2 keep their existing result-upload targets; added sticky-header scroll compensation and cache invalidation.
- Unified Tab 1 reassessment and Full Scout prompt-copy row icons with the SVG utility-button system used by the Tab 2 refresh control, including matching hover and in-progress feedback.
- Added persistent Tab 0 Listing Comment/Contact metadata with four-column Excel paste support, safe identity-based promotion to Fast Triage/Full Scout, workflow check/minus indicators, inline editing, search/export coverage, and read-only detail visibility without affecting GPT reports or scores.
- Extended the Tab 2 Shortlisting favorite into an audit-logged three-state cycle: untracked, yellow priority review, and strong-gray Stationary monitoring while remaining shortlisted.
- Reworked Tab 0 Listing into an Excel-style grid with Company/Asset required fields, added Listing-only country/modality/target/indication/stage context, grouped the progress columns under Evaluation Progress, unified Comment/Contact check-minus pills, and added independently verified context to selected Fast Triage prompt copies.
- Made pending Tab 0 entries display Listing context and researched entries display official fields with field-level Full Scout precedence over Fast Triage; also connected the Listing/Fast Triage/Full Scout/Shortlisting summary cards to synchronized progress-table filters with explanatory hover text.
- Fixed Tab 0 Excel Listing imports so quoted multiline Comment cells remain in one row, and made Comment an auto-resizing multiline input cell.
- Hardened Tab 0 Excel paste handling for full-table and column-range imports: skip Korean/English headers, preserve internal blank rows, block non-empty overflow and malformed quotes, and report the resulting row/column mapping before save.
- Made Tab 0 Comment an operational feed: repeated spreadsheet Comment headers remain newline-concatenated, and the Comment popover now also shows human-authored Tab 1/2 Final Comments and qualitative-review notes with source and timestamp while excluding AI answers and GPT evidence.
- Placed Tab 2 Qualitative Review behind the existing Show/Hide control and added an always-visible, typography-matched Comments heading for OIT Review, Contact History, and visitor notes.
- Refined Tab 0 Listing UI: aligned progress-table column sizing and header typography with the research tables, wrapped long Company values in-cell, moved the clear control and persistent import outcome above the input grid, surfaced duplicate-enrichment outcomes, and append-deduplicated reimported Listing Comments.

- Connected cross-workflow operational comments and Website metadata: Listing comments now appear as `Tab 0` posts on matching researched records; human Fast Triage Final/criterion notes promote idempotently into Full Scout Team Review with source labels while AI content stays excluded; administrator deletions remain suppressed across reuploads. Added shared Website link/edit controls to Tab 0 and Tab 1/2 reassessment/management actions.

- Added source-attributed Listing Comment Posts: bulk Tab 0 imports retain `Tab 0 Team Review` as author, direct Listing posts and edits record the signed-in administrator, and the same author carries into cross-workflow comments. Listing imports and metadata changes are now administrator-only.

- Made Website URL management Tab 0-only: Tab 1/2 Website buttons now only open the shared Listing URL, sit at the far right of their action groups, and no longer offer double-click edits. Narrowed the main Stage column and sized multi-icon action groups explicitly to reduce horizontal scrolling.

- Made Tab 0 Comment feed provenance readable: bulk spreadsheet comments display as `Tab 0 · Team Comment`, administrator-authored or edited comments as `Tab 0 · Listing Comment`, and all ISO timestamps are rendered as local Korean date/time instead of raw UTC strings.

- Added a short, deterministic 220ms settle jitter to Tab 0 workflow dots before they stop, while preserving their fixed final positions.

- Lengthened the Tab 0 workflow-dot landing motion to 760ms with a subtle directional overshoot and slower, per-dot damped settle movement.

- Extended the Tab 0 workflow-dot settle animation to 1.5 seconds at the requested pacing.
