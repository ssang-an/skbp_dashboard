# 2026-08-20

## Changes

- Bumped the dashboard asset version so browsers fetch the Tab 0 multi-search JavaScript and styles immediately.
- Added Tab 0 multi-search tokens: add company or asset searches with `+`, remove each pill with `−`, and filter the candidate table by any selected term.
- Deleting the last direct Partner Materials upload now turns off its NCDP, CDP, ADMET, IR, or DD pill while preserving explicit legacy manual overrides.
- Added regression coverage confirming every direct Partner Materials pill (NCDP, CDP, ADMET, IR, and DD) uses the selected-category filename-suffix rule.
- Fixed direct DD pill uploads to append `_DD` before the extension whenever the chosen DD category is absent from the original filename, even if the filename already contains IR, NCDP, CDP, or ADMET labels.
- Bumped dashboard CSS and JavaScript asset versions so open browsers fetch the repaired Tab1/2 multi-select filter UI instead of a cached pre-fix bundle.
- Kept Shortlisting Filter 3 rationale and Partner Materials pills visible in the initially collapsed Team Review Workspace, directly above the Show control.
- Fixed Tab1/2 multi-select filter menus being clipped by the control panel, allowing their circular multi-choice options to open above the pipeline table again.
- Streamlined the Shortlisting Filter 3 detail control: removed duplicate auto-reclassification from the dropdown, placed its compact manual classification selector beside the Filter 3 header, and expanded the rationale input to two visible lines.
