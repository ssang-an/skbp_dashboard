# 2026-08-20

## Changes

- Replaced the AI second-pass parsing modal’s redundant navigation restriction with an upload-preparation wait message.
- Added Tab 1 administrator inline edits for Company, Asset, and Main indication, with bold manual-fact markers for these fields and Stage plus synchronized detail-page audit labels.
- Expanded overwrite candidate matching into code, named-product, and descriptive-asset paths with company-scoped descriptive review and structured-code conflict protection.
- Excluded distinct numeric code assets (for example AR1001/AR1002) from overwrite candidates while retaining formatting and leading-zero aliases such as AR-1001/AR01001.
- Made the Tab 1/2 Pipeline Upload shortcut reliably reveal and focus the active input area, and clear only successfully saved Fast Triage/Full Scout source text while retaining the save result.
- Reworded incoming-duplicate upload review copy around user-facing “same Pipeline candidates” rather than internal record IDs.
- Added an incoming-record duplicate review step for Fast Triage/Full Scout uploads: reviewers select one normalized-ID collision before saving while direct duplicate API saves remain blocked.
- Restyled the Fast Triage 3-point-screening Total as a clearer score pill with an emphasized total out of 9.
- Added spacing between a saved Fast Triage final comment and its edit pill.
- Added rubric-version-aware history labels when re-evaluation clears manual scores: records now distinguish restoration to the existing rubric from reset under a newer rubric version.
- Corrected the Fast Triage score-pill score text that was being reduced by the shared header-span rule, keeping the pre-edit score clearly visible.
- Moved Fast Triage final comments above the right-side change history, added author-admin-only deletion controls for final and criterion comments, and retained each add/delete action in the audit history.
- Increased Fast Triage detail criterion score-pill typography so the score and its 3-point maximum are clearly readable without changing score colors or inline editing.
- Unified Fast Triage score editing: click the detail score pill or edit TR, MoA, and Data in the Tab 1 table using the same human-override styling and history flow as Full Scout.
- Restored the missing upload-guide event boundary that had prevented dashboard initialization, tab switching, and record loading until an unrelated guide click.
- Replaced Fast Triage detail’s separate admin-score dropdowns with admin-only inline score-pill editing while retaining the existing manual-review history and Dashboard score overrides.
- Restored Tab 1/2 multi-select filter menus by registering the click handler for every control row and raising the open menu layer above the pipeline table.
- Bumped the dashboard asset version so browsers fetch the Tab 0 multi-search JavaScript and styles immediately.
- Added Tab 0 multi-search tokens: add company or asset searches with `+`, remove each pill with `−`, and filter the candidate table by any selected term.
- Deleting the last direct Partner Materials upload now turns off its NCDP, CDP, ADMET, IR, or DD pill while preserving explicit legacy manual overrides.
- Added regression coverage confirming every direct Partner Materials pill (NCDP, CDP, ADMET, IR, and DD) uses the selected-category filename-suffix rule.
- Fixed direct DD pill uploads to append `_DD` before the extension whenever the chosen DD category is absent from the original filename, even if the filename already contains IR, NCDP, CDP, or ADMET labels.
- Bumped dashboard CSS and JavaScript asset versions so open browsers fetch the repaired Tab1/2 multi-select filter UI instead of a cached pre-fix bundle.
- Kept Shortlisting Filter 3 rationale and Partner Materials pills visible in the initially collapsed Team Review Workspace, directly above the Show control.
- Fixed Tab1/2 multi-select filter menus being clipped by the control panel, allowing their circular multi-choice options to open above the pipeline table again.
- Streamlined the Shortlisting Filter 3 detail control: removed duplicate auto-reclassification from the dropdown, placed its compact manual classification selector beside the Filter 3 header, and expanded the rationale input to two visible lines.
